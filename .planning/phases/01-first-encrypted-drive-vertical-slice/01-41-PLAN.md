---
phase: 01-first-encrypted-drive-vertical-slice
plan: 41
type: execute
wave: 29
depends_on:
  - 01-40
files_modified:
  - scripts/verify-phase1-security.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
autonomous: true
gap_closure: true
requirements:
  - CRY-04
  - TST-08
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Signed-off verification authenticates every historical attestation CMS under the approved trust contract, or verifies an authenticated archival envelope that explicitly commits to every historical CMS byte sequence and signer context, including superseded payload histories."
    - "A byte flip in any superseded CMS signature, history removal/reordering, junction, symlink, or path-swap escape fails closed with stable diagnostics before external bytes are accepted."
    - "Invoke-CustomRootChainValidation restores each caller environment variable to its prior value (or removes only variables it introduced), including exception paths."
    - "The D-48 archival digest mismatch and legacy null-subject compatibility observation are surfaced as explicit fail-closed diagnostics or a versioned, tested compatibility rule; neither is silently treated as solved by this plan."
  artifacts:
    - path: scripts/verify-phase1-security.ps1
      provides: "Historical-signature, final-handle containment, and composable chain-helper verification"
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Historical CMS, reparse/path-swap, and environment-restoration regressions"
  key_links:
    - from: attestation history and superseded_payload_history
      to: signed-off trust validation
      via: "ordered per-record CMS/envelope authentication and signer policy checks"
    - from: manifest reference
      to: hashed bytes
      via: "single opened handle final path resolution and repository-boundary proof"
    - from: chain helper temporary environment
      to: caller process
      via: "snapshot/restore in finally for present and absent variables"
---

<objective>
Close CR-06/SEC-06 and CR-07/SEC-07, and preferably WR-02, by hardening signed-off history authentication, Windows final-handle containment, and caller-environment restoration.

Purpose: A valid latest signature or lexical path is insufficient when historical bytes or Windows reparse targets can be altered; verifier helpers must also be composable for nested runs.
Output: Fail-closed verifier and behavioral coverage for historical CMS tamper, supersession, reparse/path-swap escape, and environment restoration, with D-48 operational observations explicitly bounded.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-40-PLAN.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-39-SUMMARY.md
@scripts/verify-phase1-security.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
</context>

## Source Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Trustworthy Phase 1 evidence and fail-closed verification | Tasks 1-3 preserve prior functional work and close only the verification blockers. |
| REQ | CRY-04, TST-08 | Frontmatter and all three task verifications cover secret-safe evidence and adversarial validation. |
| VERIFICATION | CR-06/SEC-06, CR-07/SEC-07, WR-02; D-48 digest/null-subject observations | Tasks 1-3 explicitly implement or bound each item; no deferred gap is silently omitted. |
| CONTEXT | D-14, D-22, D-33-D-50 | Tasks enforce fail-closed integrity, machine-role/provenance rules, append-only evidence, and sanitized diagnostics. |

<tasks>

<task type="auto">
  <name>Task 1: Authenticate complete attestation and superseded CMS history</name>
  <files>scripts/verify-phase1-security.ps1</files>
  <read_first>scripts/verify-phase1-security.ps1 (Test-SignedAttestation, Test-HistoryEnvelope, Test-SupersededPayloadHistory, Test-Record); scripts/add-security-closure-review.ps1 (history envelope shape); .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md (CR-06/SEC-06 and D-48 observations)</read_first>
  <action>Before modifying any verifier function or symbol, run GitNexus impact({target:"the symbol",direction:"upstream"}) and record the blast radius; warn and stop for HIGH or CRITICAL risk. In RequireSignedOff mode, validate every historical attestation's CMS bytes and signer/trust context, not only the latest, or retain the authenticated archival-envelope design only if the envelope signature, ordered attestation digests, CMS-byte digests, signer policy, and superseded histories are all checked against the same external trust contract. Apply the same rule to each superseded_payload_history entry and reject missing/malformed historical signatures, digest mismatch, reorder, removal, or envelope signer-context drift with stable diagnostics. Ensure the D-48 archival digest mismatch remains a distinct fail-closed diagnostic until the artifact is re-signed or an explicit compatibility migration is implemented; do not accept a legacy null-subject record without a versioned, tested rule. Before committing, run GitNexus detect_changes() and confirm only expected symbols/flows changed.</action>
  <acceptance_criteria>Signed-off verification cryptographically authenticates the complete current and superseded history, detects a tampered non-latest CMS even when attestation digests are recomputed, and never reports valid on a D-48 digest mismatch or unbounded legacy subject shape.</acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Verifier</automated>
  </verify>
  <done>CR-06/SEC-06 history authenticity is enforced across all signed records and supersessions with stable fail-closed diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce final-handle repository containment and path-swap resistance</name>
  <files>scripts/verify-phase1-security.ps1</files>
  <read_first>scripts/verify-phase1-security.ps1 (Resolve-RepositoryReference and Open-RepositoryReference); scripts/evidence/Phase1.Security.Tests.ps1 (reference and reparse fixtures); .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md (D-14, D-37 through D-49)</read_first>
  <action>Before modifying any containment function or symbol, run GitNexus impact({target:"the symbol",direction:"upstream"}) and record the blast radius; warn and stop for HIGH or CRITICAL risk. Resolve each manifest-controlled reference lexically beneath the repository, open it once with a handle, obtain its final Windows path using GetFinalPathNameByHandleW, normalize device/UNC prefixes, and require final containment under the canonical repository root before hashing from that same accepted handle. Reject any directory junction, symbolic link, reparse component, or path-swap that resolves outside the repository with stable reference_reparse_escape (or a documented equivalent), and ensure all handles/streams are disposed on success and failure. Preserve existing rooted, traversal, prefix-collision, and mixed-separator diagnostics. Before committing, run GitNexus detect_changes() and confirm only expected symbols/flows changed.</action>
  <acceptance_criteria>External targets reached through junctions, symlinks, or post-open path swaps are rejected before hashing; hashes are computed only from a handle whose final target is repository-contained; canonical in-repository files continue to validate.</acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Verifier</automated>
  </verify>
  <done>CR-07/SEC-07 reparse and handle-stability escapes fail closed with deterministic diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Restore caller environment variables after custom-root validation</name>
  <files>scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/verify-phase1-security.ps1 (Invoke-CustomRootChainValidation); scripts/evidence/Phase1.Security.Tests.ps1 (Invoke-Verify helpers); .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md (WR-02)</read_first>
  <action>Before modifying Invoke-CustomRootChainValidation or related test symbols, run GitNexus impact({target:"the symbol",direction:"upstream"}) and record the blast radius; warn and stop for HIGH or CRITICAL risk. Snapshot whether PHASE1_CHAIN_CERT and PHASE1_CHAIN_ROOTS existed and their exact values before assigning temporary child-process paths. In finally, restore each original value when present and remove only variables introduced by this invocation when absent; perform cleanup even when certificate export, pwsh startup, chain build, or parsing fails. Add a focused regression that seeds both variables with sentinel values, invokes a successful and a failing chain-validation path, and asserts exact restoration plus removal of newly introduced variables. Keep temporary certificates/roots outside the repository and delete them in all paths. Before committing, run GitNexus detect_changes() and confirm only expected symbols/flows changed.</action>
  <acceptance_criteria>Nested verifier invocation leaves caller environment byte-for-byte unchanged on success and failure; absent variables remain absent; temporary trust files are removed; no secret values are logged.</acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Verifier</automated>
  </verify>
  <done>WR-02 is closed by composable environment restoration and deterministic cleanup tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signed closure -> verifier | Historical CMS/envelope bytes and signer metadata are attacker-modifiable inputs. |
| Manifest path -> filesystem handle | Windows reparse points and races can redirect a lexical repository path. |
| Verifier helper -> caller process | Temporary trust environment must not overwrite caller state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-41-01 | Tampering | historical CMS/envelope verification | critical | mitigate | Authenticate every historical record or a complete signed envelope committing to each CMS byte sequence, including superseded histories. |
| T-01-41-02 | Elevation of privilege | repository reference hashing | critical | mitigate | Hash only from a handle whose final Windows path is proven beneath the canonical repository root. |
| T-01-41-03 | Availability/Integrity | path-swap and reparse handling | high | mitigate | Reject reparse escapes and close handles deterministically on all paths. |
| T-01-41-04 | Tampering | caller environment restoration | medium | mitigate | Snapshot and restore temporary chain variables in finally; regression-test success and failure. |
| T-01-41-SC | Tampering | PowerShell/CMS trust inputs | high | mitigate | No package installation is required; retain the package-legitimacy gate if execution introduces any dependency. |
</threat_model>

<verification>
Run the verifier-focused behavioral suite on the explicitly named Windows test machine, then run the full security suite and canonical FinalGate with external trust inputs. Preserve prior failed intervals and append new evidence only after all diagnostics are stable. A D-48 archival digest mismatch or legacy null-subject incompatibility must remain visible as blocked/fail-closed until an authenticated additive ceremony or explicitly tested compatibility rule resolves it.
</verification>

<success_criteria>
- Every current and superseded historical signature/envelope is authenticated and tamper regressions fail closed.
- Junction, symlink, and path-swap references cannot cause hashing outside the repository.
- Caller environment variables are restored exactly on all chain-helper paths.
- Existing canonical evidence and append-only history remain untouched except for explicitly authenticated additive updates.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-41-SUMMARY.md` when done.
</output>
