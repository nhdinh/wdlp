---
phase: 01-first-encrypted-drive-vertical-slice
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - config/lab.phase1.example.yaml
  - evidence/phase1/schema/evidence-manifest.schema.json
  - evidence/phase1/requirement-matrix.yaml
  - evidence/phase1/README.md
  - scripts/evidence/Phase1.Evidence.psm1
  - scripts/verify-phase1-evidence.ps1
autonomous: false
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, SRV-01, SRV-03, SRV-11, SRV-12, CRY-01, CRY-02, CRY-04, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, DRV-01, DRV-02, DRV-03, DRV-04, DRV-06, DRV-07, DRV-09, TST-01, TST-02, TST-03, TST-05, TST-08]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-25, every Phase 1 check is classified as portable automation, focused Hyper-V, signed visual checklist, or final phase-exit review, and a lower tier cannot satisfy a higher-tier boundary."
    - "Per D-20 through D-24 and D-27, every result names hungdinh-lt, LAB-DC01, LAB-DC02, or LAB-CLIENT01 with its allowed role; wrong-machine evidence is non-passing."
    - "Per D-28 through D-32, the requirement matrix distinguishes real PostgreSQL, dual-DC AD, Kerberos WinRM, PKI/mTLS, DPAPI, SCM, WTS, WinFsp, Office, and restart evidence from SQLite, mocks, fakes, callback emulation, and other permitted component fixtures."
    - "Per D-33 through D-36, Plans 01-13 through 01-16 each have an exact machine-specific privilege manifest with baseline, apply, verify, remove/rollback, persistence, reboot, version, integrity, idempotence, and role-allowlist fields; approval covers only the listed changes."
    - "Per D-37 and D-44, every attempt uses a machine-validated versioned manifest with an immutable globally unique evidence ID, status, UTC, commit/build, target machine/role, procedure, operator/automation identity, environment fingerprint, and hashed raw-artifact references."
    - "Per D-38, D-39, D-46, and D-47, visual attestations are explicit authenticated-domain-identity checklist records, committed artifacts are allowlisted and sanitized, and environment fingerprints are complete without secrets or unrelated volatile state."
    - "Per D-40, D-41, D-43, D-45, and D-49, stale inputs, prior failures, missing/hash-mismatched raw artifacts, excessive clock skew, and procedure deviations remain visible and non-passing until a valid new attempt supersedes them."
    - "Per D-42, D-48, and D-50, every Phase 1 requirement and success criterion maps to current evidence and verification tiers, an independent verifier records the final review, and raw-artifact retention/deletion/hold state is auditable."
  artifacts:
    - path: "evidence/phase1/schema/evidence-manifest.schema.json"
      provides: "Versioned fail-closed evidence, visual-attestation, deviation, supersession, retention, and independent-review contract"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Requirement/success-criterion/decision indexed verification-tier and current-evidence matrix"
    - path: "scripts/evidence/Phase1.Evidence.psm1"
      provides: "Portable manifest creation, validation, redaction, hashing, staleness, publication, and privilege-manifest helpers"
    - path: "config/lab.phase1.example.yaml"
      provides: "Secret-free four-machine role allowlist plus exact per-plan privilege manifests"
    - path: "scripts/verify-phase1-evidence.ps1"
      provides: "Fast schema, provenance, coverage, substitution-boundary, time, artifact, and redaction gate"
  key_links:
    - from: "scripts/evidence/Phase1.Evidence.psm1"
      to: "evidence/phase1/schema/evidence-manifest.schema.json"
      via: "all writers validate before publishing and reject unknown or secret-bearing fields"
      pattern: "New-Phase1Evidence|Test-Phase1Evidence|Publish-Phase1Evidence"
    - from: "evidence/phase1/requirement-matrix.yaml"
      to: "scripts/verify-phase1-evidence.ps1"
      via: "the verifier resolves every requirement, success criterion, D-01 through D-50 decision, verification tier, evidence ID, and current status"
      pattern: "requirements|success_criteria|decisions|current_evidence_id"
    - from: "config/lab.phase1.example.yaml"
      to: "01-13-PLAN.md through 01-16-PLAN.md"
      via: "each privileged plan consumes only its approved manifest section and fails on unlisted mutation"
      pattern: "privilege_manifests"
  flagged_assumptions:
    - { id: "EDGE-TST-01-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-01; policy matching, priority, conflict, defaults, empty input, and deterministic repetition remain named portable evidence." }
    - { id: "EDGE-TST-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-02; signed-bundle valid, tamper, key, schema, audience, hash, replay, partial, and concurrent cases remain named evidence." }
    - { id: "EDGE-TST-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-03; nonce, AAD, corruption, recovery, disk-full, and non-vacuous plaintext scans remain named evidence." }
  prohibitions:
    - "flagged-unverified: MUST NOT promote portable, substitute, wrong-machine, stale, deviated, inaccessible, hash-mismatched, unsigned, or unreviewed evidence to a passing infrastructure or phase-exit result."
    - "flagged-unverified: MUST NOT place credentials, private keys, tokens, protected plaintext, raw hardware serials, unnecessary personal data, or sensitive command arguments in committed manifests or raw-artifact archives."
    - "flagged-unverified: MUST NOT overwrite a failed attempt, silently discard a deviation, or delete held failure/security evidence under the ordinary retention workflow."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Create the Phase 1 verification and privilege-control tracer before any new lab mutation, then prove one portable result from execution through publication and matrix indexing.

Purpose: Make D-25 through D-50 executable contracts that every server, endpoint, WinFsp, Office, restart, and phase-exit check must satisfy.
Output: Versioned evidence schema, publication/verification tooling, full requirement matrix, four-machine role contract, per-plan privilege manifests, and explicit approval records.

Decision traceability: D-25, D-26, D-27, D-28, D-29, D-30, D-31, D-32, D-33, D-34, D-35, D-36, D-37, D-38, D-39, D-40, D-41, D-42, D-43, D-44, D-45, D-46, D-47, D-48, D-49, and D-50 are implemented by the schema, matrix, publication policy, privilege manifests, visual-attestation contract, and independent exit review below.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-01-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-03-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-09-SUMMARY.md

<interfaces>
Preserve the existing requirement IDs, seven ROADMAP success criteria, historical SUMMARY evidence, Cargo dependency allowlist, and `dlp-store/aes256gcm-4m/v1` contract. New evidence writers expose `New-Phase1Evidence`, `Test-Phase1Evidence`, `Publish-Phase1Evidence`, `Test-Phase1PrivilegeManifest`, and `Resolve-Phase1EvidenceStatus`; callers pass identifiers and artifact paths, never secret values or protected payload bytes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one portable check through the complete evidence contract</name>
  <reversibility rating="costly">The evidence schema and matrix become the shared contract consumed by every remaining Phase 1 plan and audit; changes require coordinated producer and verifier updates.</reversibility>
  <files>evidence/phase1/schema/evidence-manifest.schema.json, evidence/phase1/requirement-matrix.yaml, scripts/evidence/Phase1.Evidence.psm1, scripts/verify-phase1-evidence.ps1, evidence/phase1/README.md</files>
  <read_first>
    - Every path in `<files>` if already present, preserving user-authored data
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-25 through D-50
    - `.planning/ROADMAP.md` Phase 1 goal, requirement list, and all seven success criteria
    - `.planning/REQUIREMENTS.md` all Phase 1 requirement texts and Definition of Done
    - Historical SUMMARY frontmatter and `COVERAGE.md` machine/substitute boundaries
  </read_first>
  <behavior>
    - Test 1: a passing portable TST-01 check produces a unique validated manifest, hashes a real raw output, passes allowlist/redaction checks, and updates exactly its requirement-matrix tier/status entry.
    - Test 2: missing required fields, reused evidence ID, unsupported schema, secret-bearing fields, absent raw artifact, hash mismatch, excessive clock skew, and wrong machine/role all fail publication.
    - Test 3: a failed attempt remains immutable; a rerun receives a new ID, links the prior attempt and remediation commit, and marks only the matrix pointer superseded.
    - Test 4: relevant binary/source/config/infrastructure/procedure/baseline changes stale the affected row while unrelated rows remain current.
  </behavior>
  <action>Implement the manifest and PowerShell tooling per D-37 through D-47 with version, immutable evidence ID, requirement/check ID, pass/fail/blocked status, UTC, observed clock offset, commit/build ID, machine and role, procedure version, automation/operator identity, environment fingerprint, expected/actual stable result, deviation state, prior attempt, remediation commit, superseded entry, raw-artifact URI/hash/accessibility, self-contained designation, retention deadline/hold, and redaction-scan result. Encode the four D-25 tiers and D-27 machine-role acceptance rules. Populate the D-42 matrix with every Phase 1 requirement, all seven success criteria, D-01 through D-50, required automated/Hyper-V/visual/exit tiers, and empty current evidence IDs. Implement D-40 dependency-aware staleness, D-41 immutable attempt history, D-43 raw-artifact invalidation, D-45 clock-skew blocking, D-46 allowlisted serialization/scanning, D-47 stable environment fingerprints, D-49 deviation non-pass, and D-50 retention/hold metadata. Run one existing deterministic policy test on hungdinh-lt, capture its actual output in ignored raw storage, publish a sanitized self-contained or hashed-reference manifest, and update the TST-01 portable row. Do not fabricate infrastructure or prohibition-check results.</action>
  <acceptance_criteria>
    - "On hungdinh-lt, one real TST-01 portable test result passes schema validation, redaction, hash verification, clock policy, machine-role policy, and requirement-matrix linkage."
    - "Invalid schema, duplicate ID, missing/hash-mismatched raw artifact, wrong machine, stale dependency, excessive skew, secret marker, and unreviewed deviation fixtures are non-passing."
    - "A failure plus successful rerun preserves both attempts and links prior ID, remediation commit, superseded matrix entry, and current ID without overwriting history."
    - "The matrix contains all 30 Phase 1 requirements, seven success criteria, and D-01 through D-50 with explicit verification tiers and no synthetic passing IDs."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: cargo test --locked -p dlp-policy; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario PortableTracer</automated>
  </verify>
  <done>One genuine portable result travels from execution to immutable sanitized publication and requirement indexing, while every fail-closed provenance fixture is rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define binding machine roles, substitute boundaries, visual review, and exact privilege manifests</name>
  <files>config/lab.phase1.example.yaml, evidence/phase1/requirement-matrix.yaml, evidence/phase1/README.md, scripts/evidence/Phase1.Evidence.psm1, scripts/verify-phase1-evidence.ps1</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-20 through D-36 and D-38/D-48
    - Replacement plans `01-13-PLAN.md` through `01-16-PLAN.md` for every proposed elevated change and check
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md` external capabilities and execution roles
  </read_first>
  <behavior>
    - Test 1: each planned elevated action resolves to one named machine, exact change, reason, baseline, expected state, apply/verify/remove commands, cleanup-on-failure, persistence, reboot, pinned version/hash, and idempotence contract.
    - Test 2: any unlisted privileged action or role violation fails before mutation; rerun after success or partial failure converges safely and remove restores the captured baseline except declared persistent dependencies.
    - Test 3: SQLite/mocks/fakes and the LAB-CLIENT01 virtual-disk fixture are accepted only for their explicit component/workflow scopes and cannot fill PostgreSQL, AD, WinRM, service, DPAPI, mTLS, WinFsp, session, Office, or recovery tiers.
    - Test 4: visual checklist and independent-review records require authenticated identities, UTC, machine, build, expected/actual result, deviations, matrix digest, and artifact-integrity result.
  </behavior>
  <action>In `config/lab.phase1.example.yaml`, encode D-20 through D-24 machine roles and one separate D-33 manifest section for each of Plans 01-13 through 01-16. List only the exact elevated mutations already planned: verified hungdinh-lt endpoint-residue removal; LAB-DC01 PostgreSQL/Compose/server and development PKI/provisioning state; LAB-CLIENT01 public trust, service identity/ACL, DPAPI data, official WinFsp runtime, DLP service, user-session pipe/mount, restart/reboot and fault harness state; and hungdinh-lt Hyper-V control of LAB-CLIENT01 only. For each, implement D-34 baseline/cleanup and persistent-state declaration, D-35 idempotent apply/verify/remove plus version/integrity checks, and D-36 strict role allowlist. Encode D-29 through D-32 substitute boundaries in both config and matrix. Define D-26/D-38 signed visual checklist rows only for LAB-CLIENT01 drive presence/absence, Explorer, Word, Excel, mount-failure recovery, and restart recovery; all machine-verifiable facts stay automated. Define D-48 independent review and D-49 deviation disposition fields. Make the validator reject manifest drift after approval and require a fresh approval digest for any new change.</action>
  <acceptance_criteria>
    - "Plans 01-13 through 01-16 each have a distinct complete privilege-manifest section whose digest changes on any added/changed privileged action."
    - "hungdinh-lt's allowlist contains source/build/LLVM/Rust/Hyper-V orchestration only and rejects endpoint runtime, trust, hosts, credential, mount, and domain/network mutations."
    - "Every substitute has an explicit allowed scope and every binding infrastructure/runtime tier rejects substitute evidence."
    - "Visual and independent-review schemas require authenticated identity and cannot convert missing machine evidence or a procedure deviation to pass."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario ContractsAndPrivileges</automated>
  </verify>
  <done>The four machine roles, infrastructure-substitute rules, visual/independent review records, and exact per-plan privileged changes are machine validated before any lab mutation.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Approve the exact Phase 1 privilege-manifest digests</name>
  <files>config/lab.phase1.example.yaml</files>
  <read_first>
    - `config/lab.phase1.example.yaml` complete role allowlist and Plans 01-13 through 01-16 privilege sections
    - Task 2 automated validation output and calculated digest for each plan section
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-33 through D-36
  </read_first>
  <action>Present each plan section separately with machine, exact mutation, reason, baseline, expected final state, apply/verify/remove behavior, persistence, reboot possibility, version/hash, and cleanup-on-success/failure. Record approval using the authenticated operator identity, UTC, and the exact manifest digest. Approval authorizes only those digests; any executor-discovered privileged change or digest change must stop for fresh approval.</action>
  <acceptance_criteria>
    - "The operator can approve or reject each plan section independently after seeing every privileged change and rollback consequence."
    - "An approval record binds authenticated identity, UTC, plan ID, and exact digest and cannot authorize a changed or additional mutation."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt after approval: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario PrivilegeApprovals</automated>
  </verify>
  <decision>Approve the exact per-plan privilege manifests required for the remaining Phase 1 lab execution.</decision>
  <options>
    <option id="approve-listed-digests">
      <name>Approve listed digests</name>
      <pros>Allows only the displayed, role-correct, reversible or declared-persistent lab mutations.</pros>
      <cons>Execution pauses again if any manifest digest changes or a new privileged action is discovered.</cons>
    </option>
    <option id="reject-for-revision">
      <name>Reject for revision</name>
      <pros>Prevents lab mutation until scope, cleanup, persistence, or reboot details are corrected.</pros>
      <cons>The dependent runtime plans remain blocked.</cons>
    </option>
  </options>
  <resume-signal>Select `approve-listed-digests` or `reject-for-revision` and identify any rejected plan section.</resume-signal>
  <done>Each remaining privileged plan has either an exact digest-bound approval or an explicit rejection that blocks execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Check process/raw output → committed manifest | Untrusted output may be incomplete, forged, secret-bearing, stale, or detached from its source artifact. |
| Manifest/matrix → phase acceptance | Wrong-machine, substitute, superseded, deviated, or inaccessible evidence could falsely satisfy a requirement. |
| Approved privilege manifest → lab mutation | A new or changed elevated action could escape the user's exact approval or run on the wrong machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-17-01 | Spoofing/Repudiation | evidence identity/provenance | high | mitigate | Schema-required evidence ID, authenticated operator/automation identity, machine role, procedure/build/environment fingerprints, and immutable hashes. |
| T-01-17-02 | Tampering | matrix and attempt history | high | mitigate | Schema validation, immutable attempts, prior/superseded links, dependency-aware staleness, raw-artifact hash/access checks, and sealed matrix digest. |
| T-01-17-03 | Information disclosure | publication/archive | high | mitigate | Field allowlist, source redaction, forbidden-marker scanning, controlled raw storage, and no secret-bearing commands or committed payloads. |
| T-01-17-04 | Elevation/Tampering | privileged lab changes | high | mitigate | Per-plan exact manifests, digest-bound approval, role guard, baseline, idempotent apply/verify/remove, cleanup, pinned versions/hashes, and fresh approval on drift. |
| T-01-17-05 | Spoofing | infrastructure substitutes | high | mitigate | Typed verification tiers and explicit substitute scopes prevent component fixtures from filling infrastructure/runtime rows. |
| T-01-17-06 | Repudiation | visual/independent review | medium | mitigate | Authenticated identity, UTC, target/build, expected/actual result, deviations, matrix digest, and artifact-integrity attestation. |

## ASVS L1 Gate

Every high-severity threat is blocking. Publication, approval, or dependent execution stops on invalid provenance, wrong machine, substitute promotion, secret leakage, inaccessible/hash-mismatched artifacts, manifest drift, or unapproved elevated work.
</threat_model>

<verification>
On hungdinh-lt, run the portable tracer and all malformed/stale/supersession/redaction/substitute/privilege contract fixtures. No Hyper-V mutation occurs in this plan. Validate exact digest-bound approvals before Plans 01-13 through 01-16 become runnable.
</verification>

<success_criteria>
- One real portable result is published and indexed through the full D-37 through D-50 contract.
- Every requirement, success criterion, D-01 through D-50 decision, tier, substitute boundary, and current evidence slot exists in the matrix.
- Every later privileged plan has an exact role-correct manifest and approval digest.
- All high-severity evidence and privilege threats are mitigated by passing automated gates.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New path | `evidence/phase1/schema/evidence-manifest.schema.json` | Versioned provenance and attestation schema |
| New path | `evidence/phase1/requirement-matrix.yaml` | Requirement/success/decision indexed tier and evidence matrix |
| New path | `scripts/evidence/Phase1.Evidence.psm1` | Evidence, staleness, publication, and privilege-manifest functions |
| New path | `scripts/verify-phase1-evidence.ps1` | Focused evidence and privilege contract verifier |
| New path | `config/lab.phase1.example.yaml` | Four-machine role allowlist and per-plan privilege manifests |
| New path | `evidence/phase1/README.md` | Raw storage, retention, hold, redaction, review, and rerun procedure |
| New symbols | `New-Phase1Evidence`, `Test-Phase1Evidence`, `Publish-Phase1Evidence`, `Test-Phase1PrivilegeManifest`, `Resolve-Phase1EvidenceStatus` | Shared producer and verifier contract |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md` when done.
</output>
