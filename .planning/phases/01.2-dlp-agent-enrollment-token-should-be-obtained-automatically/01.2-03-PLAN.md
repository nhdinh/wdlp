---
phase: 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically
plan: 03
type: execute
wave: 3
depends_on: [01.2-01, 01.2-02]
files_modified:
  - tests/windows/Invoke-AgentServiceSmoke.ps1
  - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-ordinary-authority-evidence.json
  - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-replacement-authority-evidence.json
autonomous: false
requirements: [SRV-13, SRV-14, TST-05, AGT-01]
gap_closure: true
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Invoke-AuthorityQuery passes the exact argv sequence -tA, -v, ON_ERROR_STOP=1, -c, one SQL argument containing whitespace/quotes/parentheses, then the final DLP_DATABASE_URL positional argument; a deterministic fake psql rejects any split or reconstructed SQL (SRV-13, SRV-14)."
    - "Authority queries capture stdout and stderr independently and fail closed on nonzero exit, any warning/error stderr, zero rows, multiple rows, or a row that does not match the requested active-serial, four-field snapshot, or credential-status shape (SRV-14, TST-05)."
    - "Authority snapshot equality uses the validated original PostgreSQL text fields byte-for-byte, without timestamp parsing, rounding, normalization, or tie-breaking before comparison (SRV-14)."
    - "OrdinaryMissingCredentialDoesNotRevoke proves active_serial, token_digest, token_consumed_at, and token_expires_at are unchanged and the predecessor remains active before its finally path restores LAB-CLIENT01 to Automatic/Running, Active policy, and zero token entries (TST-05, AGT-01, D-01, D-03, D-04, D-05, D-09 through D-12)."
    - "ReplacementRevocation proves a different successor is active, exactly the predecessor is revoked, both token locations are clean, Active policy evidence is present, and the service/data/cache remain present (SRV-13, SRV-14, TST-05, AGT-01, D-06 through D-12)."
    - "Live authority mutation runs only after an exclusive LAB-DC01/LAB-CLIENT01 window is confirmed, runs the scenarios sequentially, and a deterministic post-mutation replacement failure proves fresh finally recovery, dual cleanup, a healthy endpoint, preserved artifacts, unchanged published pass evidence, and a nonzero redacted original result (SRV-13, SRV-14)."
  artifacts:
    - path: tests/windows/Invoke-AgentServiceSmoke.ps1
      provides: "ArgumentList-preserving URL-last PostgreSQL adapter, exact row-shape validation, deterministic fake-psql scenario, fail-closed evidence writer, and failure-injected finally-guarded live authority scenarios"
      contains: "AuthorityQueryAdapter, AuthorityEvidenceContract, Invoke-AuthorityQuery, Write-AuthorityTransitionEvidence, AfterReplacementMutation"
    - path: .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-ordinary-authority-evidence.json
      provides: "Secret-free proof of ordinary authority non-mutation followed by operational recovery"
      contains: "phase-01.2-authority-evidence/v1"
    - path: .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-replacement-authority-evidence.json
      provides: "Secret-free proof of predecessor revocation, successor activation, cleanup, policy activation, and preservation"
      contains: "phase-01.2-authority-evidence/v1"
  key_links:
    - from: tests/windows/Invoke-AgentServiceSmoke.ps1::Get-ActiveCredentialSerial
      to: tests/windows/Invoke-AgentServiceSmoke.ps1::Invoke-AuthorityQuery
      via: "ActiveSerial row-shape contract"
      pattern: "Invoke-AuthorityQuery.*ActiveSerial"
    - from: tests/windows/Invoke-AgentServiceSmoke.ps1::Get-EnrollmentAuthoritySnapshot
      to: PostgreSQL enrollment_authority
      via: "ProcessStartInfo.ArgumentList preserves one whitespace/quote/parenthesis-bearing SQL argv item immediately after -c and before the final database URL"
      pattern: "ArgumentList.*-c.*DLP_DATABASE_URL"
    - from: tests/windows/Invoke-AgentServiceSmoke.ps1::OrdinaryMissingCredentialDoesNotRevoke
      to: tests/windows/Invoke-AgentServiceSmoke.ps1::Invoke-LiveEnrollmentRecovery
      via: "finally recovery plus post-recovery health, policy, and cleanup assertions"
      pattern: "finally.*Invoke-LiveEnrollmentRecovery"
    - from: tests/windows/Invoke-AgentServiceSmoke.ps1::ReplacementRevocation
      to: 01.2-03-replacement-authority-evidence.json
      via: "exact property allowlist and atomic publication only after validated live assertions/recovery; injected post-mutation failure preserves the prior pass artifact"
      pattern: "Write-AuthorityTransitionEvidence"
  flagged_assumptions:
    - requirement: TST-05
      status: unresolved
      flagged: true
      statement: "The specless edge probe classified TST-05 as unclassified; this plan assumes the remaining Phase 01.2 integration gap is exactly the two live authority-transition scenarios named by 01.2-VERIFICATION.md, while the already-verified enrollment-through-first-policy path remains preserved."
    - requirement: AGT-01
      status: unresolved
      flagged: true
      statement: "The specless edge probe classified AGT-01 as unclassified; this plan assumes Automatic/Running plus no interactive-session dependency and preserved service/data/cache are the relevant agent acceptance signals for this gap."
  prohibitions:
    - requirement_id: TST-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT use the deterministic fake-psql result as a substitute for live LAB-DC01 authority-transition evidence."
    - requirement_id: AGT-01
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT leave the shared LAB-CLIENT01 endpoint stopped, credential-less, or without its preserved data/cache after a live verification interruption or failure."
---

<objective>
Repair the disconnected PostgreSQL authority-query adapter and produce trustworthy deterministic plus live evidence for the two behavior-unverified Phase 01.2 truths.

Purpose: Close the exact blocker in 01.2-VERIFICATION.md without reimplementing the already-completed automatic enrollment, cleanup, diagnostics, documentation, Rust authority, or policy-cache scope.
Output: One corrected PowerShell smoke artifact, deterministic adapter/evidence contract scenarios, and two secret-free live authority-transition evidence records.
</objective>

## Artifacts this phase produces

- Modified function: `Invoke-AuthorityQuery` in `tests/windows/Invoke-AgentServiceSmoke.ps1`, with executable injection for deterministic coverage, URL-last option ordering, independent stream capture, exact-one-row enforcement, and requested shape validation.
- Modified wrappers: `Get-ActiveCredentialSerial`, `Get-EnrollmentAuthoritySnapshot`, and `Get-CredentialAuthorityStatus`, each selecting its exact row contract.
- New function: `Write-AuthorityTransitionEvidence`, which writes only an allowlisted redacted schema and uses atomic same-directory publication.
- New script parameters/symbols: `EvidencePath`, optional for existing callers and required by this plan's live evidence runs; `FailureInjection`, optional and restricted to the named replacement recovery test seam.
- New test cases/scenarios: `AuthorityQueryAdapter`, including clean one-row, zero-row, multiple-row, malformed-row, warning-on-stderr-with-zero-exit, nonzero-exit, exact SQL-argv, and argument-order cases; `AuthorityEvidenceContract`, including unknown-key rejection, exact schemas, protected-value exclusion, publication gating, and staging cleanup.
- New files: `01.2-03-ordinary-authority-evidence.json` and `01.2-03-replacement-authority-evidence.json` in the Phase 01.2 directory.
- New Rust crates/modules/types, migrations/schema objects, runtime APIs, operator documentation, and package dependencies: none.

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-CONTEXT.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-RESEARCH.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-PATTERNS.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-VERIFICATION.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-01-SUMMARY.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-02-SUMMARY.md
@tests/windows/Invoke-AgentServiceSmoke.ps1
@tests/lab/EnrollmentTokenOrchestration.Tests.ps1

<interfaces>
Existing smoke interfaces to preserve:
- `Invoke-AuthorityQuery([string]$Sql)` currently returns trimmed merged command output and is called only by the three authority wrappers in this script.
- `Get-ActiveCredentialSerial()` selects `encode(active_serial, 'hex')` for `LAB-CLIENT01.lab.local`.
- `Get-EnrollmentAuthoritySnapshot()` selects exactly active_serial, token_digest, token_consumed_at, and token_expires_at as a pipe-delimited row.
- `Get-CredentialAuthorityStatus([string]$Serial)` selects one `active|revoked` status after validating a lowercase hexadecimal serial.
- `Invoke-LiveEnrollmentRecovery -ExpectFailure` requires the ordinary path to fail with ForceReenrollment/Apply guidance; `-Force` performs recovery and already asserts active policy version/state.
- `Assert-EnrollmentTokenRemoved` checks both `C:\dlp\agent\agent.env` and the DlpWindowsService SCM Environment.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and repair the PostgreSQL authority adapter end to end</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-VERIFICATION.md
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-PATTERNS.md
    - tests/windows/Invoke-AgentServiceSmoke.ps1:1
    - tests/lab/EnrollmentTokenOrchestration.Tests.ps1:117
  </read_first>
  <files>tests/windows/Invoke-AgentServiceSmoke.ps1</files>
  <behavior>
    - RED 1: Fake psql requires exact argv equality to `-tA`, `-v`, `ON_ERROR_STOP=1`, `-c`, one sentinel SQL argument containing whitespace, quotes, and parentheses, then the final sentinel database URL; it rejects split, dropped, reordered, or reconstructed SQL.
    - RED 2: Clean stdout containing exactly one requested-shape row is accepted while whitespace-only/zero rows, two rows, and malformed active-serial/snapshot/status rows are rejected with distinct stable codes.
    - RED 3: Warning text written to stderr is rejected even when the fake process exits zero; a nonzero process exit is independently rejected.
    - RED 4: The native process is disposed, fake argument/fixture files are deleted, and temporary PATH/environment mutations are restored in finally on every fixture outcome.
  </behavior>
  <action>Before editing, capture `git status --short` and the current changed-path list as the preservation baseline. Run GitNexus upstream impact separately for every existing function this task will modify: `Invoke-AuthorityQuery`, `Get-ActiveCredentialSerial`, `Get-EnrollmentAuthoritySnapshot`, and `Get-CredentialAuthorityStatus`, plus the `tests/windows/Invoke-AgentServiceSmoke.ps1` file target, using `node .gitnexus/run.cjs impact TARGET --direction upstream --repo wdlp --include-tests`. Report each target's direct callers, affected processes, and returned risk to the user before changing it. A target-not-found result is UNKNOWN, not low risk; for every unresolved PowerShell function, record UNKNOWN and produce a local script-scoped inventory of its definition, direct call sites, enclosing scenario/switch regions, and test-process entry points. If any resolved target reports HIGH or CRITICAL, warn and stop for user review before editing.

Follow RED/GREEN/REFACTOR in the same file. First add `AuthorityQueryAdapter` to the scenario ValidateSet and implement its fake-psql fixtures so the broken URL-first adapter fails. Use a sentinel SQL statement containing whitespace, embedded quotes, and parentheses. The fake executable must compare its received argv array element-for-element against exactly `-tA`, `-v`, `ON_ERROR_STOP=1`, `-c`, that unchanged sentinel SQL string as one element, and the final sentinel database URL; it must exit nonzero for tokenized, split, reconstructed, dropped, duplicated, or reordered SQL even when the text could be joined back to the sentinel.

Then change `Invoke-AuthorityQuery` to accept an explicit expected shape and optional injected executable/database URL for the deterministic scenario while keeping live callers on resolved `psql` and `$env:DLP_DATABASE_URL`. Construct `System.Diagnostics.ProcessStartInfo` with `UseShellExecute = $false`, redirected stdout/stderr, and add the six values individually through `ProcessStartInfo.ArgumentList` in the exact order proven by the fake; do not use `Start-Process -ArgumentList` or a flattened argument string because Windows quoting can split SQL. Start the process directly, read both streams independently, wait for exit, reject nonzero exit before semantic parsing, reject any non-whitespace stderr even at exit zero, normalize stdout into non-empty lines, require exactly one line, and dispose the process/clean any fake fixture files in finally. Never include the URL, SQL, stderr contents, or returned row in exceptions/evidence.

Validate the requested row before returning it: ActiveSerial is non-empty lowercase even-length hexadecimal; AuthoritySnapshot has exactly four pipe-delimited fields, non-empty lowercase even-length hexadecimal serial and digest, optional parseable consumed timestamp, and non-empty parseable expiry timestamp; CredentialStatus is exactly `active` or `revoked`. Preserve the original validated snapshot field text for ordinal before/after equality so timestamp precision is not normalized. Update the three wrappers to request these shapes. The fake command must record received arguments in a temporary file and support clean, zero-row, multi-row, malformed, stderr-warning, and nonzero-exit modes without either lab VM or PostgreSQL. Preserve D-09 through D-12 by emitting stable codes only.</action>
  <acceptance_criteria>
    - `AuthorityQueryAdapter` fails against the original URL-first implementation, then passes after the repaired adapter is present.
    - The fake process proves exact argv equality and that the whitespace/quote/parenthesis-bearing SQL is one unchanged element immediately after `-c` and before the final database URL; a static source match or joinable split SQL cannot pass.
    - Exit status, stderr, row count, and row shape are independent fail-closed gates with no secret-bearing diagnostic content.
    - GitNexus impact is reported as returned, including UNKNOWN if lookup still fails; compare-scope change detection is recorded before commit, and the executor-introduced delta beyond the preserved dirty-worktree baseline contains only the expected smoke artifact.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario AuthorityQueryAdapter; if ($LASTEXITCODE -ne 0) { throw 'AuthorityQueryAdapter failed' }; pwsh -NoProfile -File tests/lab/EnrollmentTokenOrchestration.Tests.ps1 -TestCase CoreFlow; if ($LASTEXITCODE -ne 0) { throw 'CoreFlow failed' }; node .gitnexus/run.cjs detect_changes --scope compare --base-ref master --repo wdlp; if ($LASTEXITCODE -ne 0) { throw 'GitNexus compare-scope change detection failed' }</automated>
  </verify>
  <done>The local tracer executes a fake psql end to end, proves URL-last SQL invocation, and rejects every disconnected/warning/malformed result mode before any live authority mutation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Reserve the shared lab mutation window</name>
  <read_first>
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-CONTEXT.md
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-VERIFICATION.md
    - tests/windows/Invoke-AgentServiceSmoke.ps1:57
    - tests/windows/Invoke-AgentServiceSmoke.ps1:439
  </read_first>
  <files>None; coordination checkpoint only</files>
  <action>Run the non-mutating automated preflight below without printing credential or database values, present only VM reachability/port/tool readiness booleans, and pause. Ask the operator to confirm that no other job or person is using LAB-DC01 or LAB-CLIENT01 and that the two live scenarios may rotate/revoke the current lab credential sequentially. Do not start either mutating scenario until the exact resume signal is received.</action>
  <instructions>
    1. Run the non-mutating preflight and show only its readiness result.
    2. Ask the operator to stop or defer any competing LAB-DC01/LAB-CLIENT01 work.
    3. Explain that the live run sequentially removes/replaces endpoint credentials and revokes predecessor serials while preserving service/data/cache.
    4. Wait for the exact resume signal; do not infer consent from auto-advance mode.
  </instructions>
  <acceptance_criteria>
    - LAB-DC01 and LAB-CLIENT01 are running/reachable, LAB-DC01:8443 is reachable, psql is available, and required database/VM credential inputs are present without being printed.
    - The operator confirms an exclusive lab window and accepts that the ordinary scenario's finally recovery and the replacement scenario will mint successor credentials and revoke predecessors.
    - No live mutation starts in parallel with another lab workflow.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; foreach ($name in @('LAB-DC01','LAB-CLIENT01')) { $vm = Get-VM -Name $name -ErrorAction Stop; if ($vm.State -ne 'Running') { throw "lab VM is not running: $name" } }; if (-not (Test-NetConnection -ComputerName LAB-DC01 -Port 8443 -InformationLevel Quiet)) { throw 'LAB-DC01:8443 is unreachable' }; if (-not (Test-Connection -ComputerName LAB-CLIENT01 -Count 1 -Quiet)) { throw 'LAB-CLIENT01 is unreachable' }; if ($null -eq (Get-Command psql -ErrorAction SilentlyContinue)) { throw 'psql is unavailable' }; if ([string]::IsNullOrWhiteSpace($env:DLP_DATABASE_URL)) { throw 'DLP_DATABASE_URL is unavailable' }; if ([string]::IsNullOrWhiteSpace($env:DLP_VM_ADMIN_USER) -or [string]::IsNullOrWhiteSpace($env:DLP_VM_ADMIN_PASSWORD)) { throw 'VM credential inputs are unavailable' }; 'exclusive-lab-preflight=ready'</automated>
    <human-check>Confirm no other operator or automation is using LAB-DC01/LAB-CLIENT01 and authorize the sequential credential mutation window.</human-check>
  </verify>
  <verification>The preflight prints `exclusive-lab-preflight=ready`, and the operator supplies the exact resume signal after confirming exclusive use.</verification>
  <resume-signal>exclusive-lab-window-confirmed</resume-signal>
  <done>The executor holds an explicitly confirmed exclusive window for the two sequential live authority scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rerun ordinary refusal and replacement revocation with redacted recovery evidence</name>
  <precondition>Task 2's exclusive lab window is still active; LAB-DC01/LAB-CLIENT01 remain reachable; DLP_DATABASE_URL and VM credentials remain available without disclosure.</precondition>
  <reversibility rating="costly">The live scenarios rotate device credentials and revoke predecessors; recovery requires another fresh Apply-gated enrollment even though service/data/cache are preserved.</reversibility>
  <read_first>
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-VERIFICATION.md
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-PATTERNS.md
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-01-SUMMARY.md
    - tests/windows/Invoke-AgentServiceSmoke.ps1:202
    - tests/windows/Invoke-AgentServiceSmoke.ps1:239
    - tests/windows/Invoke-AgentServiceSmoke.ps1:439
  </read_first>
  <files>tests/windows/Invoke-AgentServiceSmoke.ps1, .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-ordinary-authority-evidence.json, .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-replacement-authority-evidence.json</files>
  <behavior>
    - Test 1: Ordinary missing-credential refusal compares the validated active serial and all four validated snapshot fields ordinally before recovery, keeps the predecessor active, then always force-recovers operational endpoint state in finally (D-01, D-03, D-04, D-05).
    - Test 2: Replacement revocation observes a distinct successor, predecessor `revoked`, successor `active`, both token counts zero, Active policy, Automatic/Running service, and preserved service/data/cache (D-06, D-07, D-08).
    - Test 3: A deterministic `AfterReplacementMutation` injection fails after live replacement mutation, then proves finally attempted fresh force recovery plus cleanup of both token locations, the endpoint is Automatic/Running with Active policy, service/data/cache remain present, the prior pass artifact is byte-identical, and the original run still exits nonzero with only fixed redacted codes (D-01 through D-04, D-09 through D-12).
    - Test 4: The writer rejects any property outside the scenario's exact allowlist, requires UTC `observed_at_utc` plus all named SHA-256 fingerprints, cleans same-directory staging on success and rejection, and never replaces the destination before all pass/recovery gates succeed (D-09 through D-12).
    - Test 5: Each published pass file has exactly its declared property-name set and contains no raw database URL, SQL, native streams, token, authority row, private key, password, certificate, credential, or DPAPI blob values (D-09 through D-12).
  </behavior>
  <action>Before editing Task 3 regions, run GitNexus upstream impact separately for every existing symbol/region this task will modify: `Invoke-LiveEnrollmentRecovery` if the failure seam touches it, `OrdinaryMissingCredentialDoesNotRevoke`, and `ReplacementRevocation`, plus the `tests/windows/Invoke-AgentServiceSmoke.ps1` file target, using `node .gitnexus/run.cjs impact TARGET --direction upstream --repo wdlp --include-tests`. Report direct callers, affected processes, and risk for each resolved target. Scenario switch labels that GitNexus cannot resolve remain UNKNOWN; for each unresolved target, record its exact switch/ValidateSet location, the local functions it calls, its direct dispatch entry point, and the test processes that invoke the script. Stop and warn before edits if any resolved target is HIGH or CRITICAL. `Write-AuthorityTransitionEvidence`, the writer-contract scenario, and the injection parameter are new symbols and must be identified as new rather than assigned a fabricated impact result.

Add optional `-EvidencePath` handling, a deterministic `AuthorityEvidenceContract` scenario, and `Write-AuthorityTransitionEvidence` without changing existing scenario invocation behavior. The writer takes an explicit scenario kind and an ordered object, rejects duplicate or unknown properties and any missing required property before serialization, validates `observed_at_utc` as round-trip UTC ending in `Z`, validates every fingerprint as lowercase 64-hex SHA-256, serializes UTF-8 JSON to a uniquely named same-directory staging file, and atomically replaces/moves the destination only after the caller supplies a completed pass/recovery gate. It cleans staging in finally on successful write, schema rejection, serialization failure, and publication failure. `AuthorityEvidenceContract` injects an unknown field and secret sentinels, requires stable rejection, proves no destination publication or overwrite, then writes valid ordinary/replacement fixtures and proves exact schemas and no staging residue without either lab VM.

Use exactly these ordinary pass properties: `schema`, `scenario`, `observed_at_utc`, `status`, `active_serial_before_fingerprint`, `active_serial_after_fingerprint`, `authority_snapshot_before_fingerprint`, `authority_snapshot_after_fingerprint`, `active_serial_unchanged`, `authority_snapshot_unchanged`, `predecessor_status_after_refusal`, `recovery_service_status`, `recovery_service_start_mode`, `recovery_active_policy_state`, `recovery_agent_env_token_count`, and `recovery_scm_token_count`. Use exactly these replacement pass properties: `schema`, `scenario`, `observed_at_utc`, `status`, `predecessor_serial_fingerprint`, `successor_serial_fingerprint`, `serial_changed`, `predecessor_status`, `successor_status`, `service_preserved`, `data_preserved`, `cache_preserved`, `service_status`, `service_start_mode`, `active_policy_state`, `agent_env_token_count`, and `scm_token_count`. Hash validated authority snapshot/serial strings with SHA-256 before constructing either object; never persist raw values or native stdout/stderr.

Strengthen `OrdinaryMissingCredentialDoesNotRevoke` around its existing mutation: capture one validated active serial and the validated four-field snapshot; stop the service/remove `device.dpapi`; require the ordinary ServiceInstall to refuse with force/apply remediation; capture the second validated values; require active serial and every original snapshot field to be ordinally identical and the predecessor status to remain `active`. Keep recovery in finally and then prove the restored endpoint is installed, Automatic/Running, has non-empty active policy version with state Active, and has zero token entries in both `agent.env` and SCM Environment. The recovery is operational restoration with a fresh successor, not restoration of the deleted private credential. Publish `01.2-03-ordinary-authority-evidence.json` only from these validated outcomes.

Run `ReplacementRevocation` only after the ordinary scenario has completed recovery. Capture the current predecessor and service/data/cache pre-state, perform `-ForceReenrollment -Apply` through the existing helper, and require a distinct validated successor, predecessor `revoked`, successor `active`, Automatic/Running, Active policy, zero token entries, and preserved service/data/cache. Add an optional test-only `-FailureInjection AfterReplacementMutation` seam that triggers immediately after the replacement mutation is known to have occurred and before successor/post-state assertions or evidence publication. Its finally guard must always mark recovery attempted, perform a fresh Apply-gated force recovery, clean both token locations, and then reassert Automatic/Running, Active policy, service/data/cache presence, and zero token counts. Preserve the primary injected failure after successful recovery so the original process exits nonzero; aggregate a recovery failure without replacing the primary cause. In injection mode suppress native/raw output and emit only fixed redacted codes for injection, recovery attempted, recovery success/failure, and dual-cleanup success/failure. The injection run must not create, replace, or truncate the existing replacement pass artifact. Publish `01.2-03-replacement-authority-evidence.json` only on the normal validated pass path. Execute ordinary, normal replacement, and injected replacement sequentially and never background or parallelize them.

After both evidence files pass their allowlist/schema assertions, run GitNexus `detect_changes({scope: "compare", base_ref: "master"})` via `node .gitnexus/run.cjs detect_changes --scope compare --base-ref master --repo wdlp` before committing. Compare the final worktree against Task 1's preservation baseline and confirm the executor introduced only the expected smoke function/scenario changes and two evidence artifacts; broader pre-existing compare-to-master changes remain user-owned and must not be modified or attributed to this plan. Do not modify the already-verified runtime, Rust, documentation, migrations, completed plans, or unrelated dirty-worktree files.</action>
  <acceptance_criteria>
    - `OrdinaryMissingCredentialDoesNotRevoke` exits zero only after the four authority fields and active serial are proven unchanged before recovery, the predecessor is still active, and finally recovery leaves a healthy Active-policy endpoint with both token locations clean.
    - `ReplacementRevocation` exits zero only after exact predecessor/successor authority state, dual cleanup, Active policy, Automatic/Running, and service/data/cache preservation are proven.
    - Each evidence record has exactly its scenario allowlist, a round-trip UTC observation ending in `Z`, every required 64-hex fingerprint, no unknown/raw/secret-bearing property or value, no staging residue, and publication only after pass/recovery gates.
    - The deterministic post-mutation replacement failure attempts fresh recovery and both cleanup locations, leaves a healthy Active-policy endpoint with service/data/cache present, preserves the prior pass evidence byte-for-byte, emits fixed redacted codes only, and remains nonzero after successful recovery; live scenarios never overlap.
    - Final GitNexus compare-scope change detection runs before commit and its actual risk/affected processes are reported without downgrading UNKNOWN/HIGH/CRITICAL results.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
function Assert-ExactProperties($Object, [string[]]$Expected, [string]$Name) {
  $actual = @($Object.PSObject.Properties.Name | Sort-Object)
  $wanted = @($Expected | Sort-Object)
  if (@(Compare-Object $actual $wanted).Count -ne 0) { throw "$Name property allowlist failed" }
}
function Assert-UtcAndFingerprints($Object, [string[]]$Fingerprints, [string]$Name) {
  $parsed = [DateTimeOffset]::MinValue
  $observed = [string]$Object.observed_at_utc
  if (-not $observed.EndsWith('Z') -or -not [DateTimeOffset]::TryParseExact($observed, 'o', [Globalization.CultureInfo]::InvariantCulture, [Globalization.DateTimeStyles]::RoundtripKind, [ref]$parsed)) { throw "$Name UTC observation failed" }
  foreach ($property in $Fingerprints) { if ([string]$Object.$property -cnotmatch '^[0-9a-f]{64}$') { throw "$Name fingerprint failed: $property" } }
}
$phase = '.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically'
$ordinary = Join-Path $phase '01.2-03-ordinary-authority-evidence.json'
$replacement = Join-Path $phase '01.2-03-replacement-authority-evidence.json'
$contractPath = Join-Path ([IO.Path]::GetTempPath()) ('authority-evidence-contract-' + [guid]::NewGuid().ToString('N') + '.json')
try {
  pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario AuthorityEvidenceContract -EvidencePath $contractPath
  if ($LASTEXITCODE -ne 0) { throw 'authority evidence writer contract failed' }
  pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario OrdinaryMissingCredentialDoesNotRevoke -EvidencePath $ordinary
  if ($LASTEXITCODE -ne 0) { throw 'ordinary authority scenario failed' }
  pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario ReplacementRevocation -EvidencePath $replacement
  if ($LASTEXITCODE -ne 0) { throw 'replacement authority scenario failed' }
  $ordinaryExpected = @('schema','scenario','observed_at_utc','status','active_serial_before_fingerprint','active_serial_after_fingerprint','authority_snapshot_before_fingerprint','authority_snapshot_after_fingerprint','active_serial_unchanged','authority_snapshot_unchanged','predecessor_status_after_refusal','recovery_service_status','recovery_service_start_mode','recovery_active_policy_state','recovery_agent_env_token_count','recovery_scm_token_count')
  $replacementExpected = @('schema','scenario','observed_at_utc','status','predecessor_serial_fingerprint','successor_serial_fingerprint','serial_changed','predecessor_status','successor_status','service_preserved','data_preserved','cache_preserved','service_status','service_start_mode','active_policy_state','agent_env_token_count','scm_token_count')
  $oRaw = Get-Content -LiteralPath $ordinary -Raw
  $rRaw = Get-Content -LiteralPath $replacement -Raw
  $o = $oRaw | ConvertFrom-Json
  $r = $rRaw | ConvertFrom-Json
  Assert-ExactProperties $o $ordinaryExpected 'ordinary'
  Assert-ExactProperties $r $replacementExpected 'replacement'
  Assert-UtcAndFingerprints $o @('active_serial_before_fingerprint','active_serial_after_fingerprint','authority_snapshot_before_fingerprint','authority_snapshot_after_fingerprint') 'ordinary'
  Assert-UtcAndFingerprints $r @('predecessor_serial_fingerprint','successor_serial_fingerprint') 'replacement'
  if ($o.schema -ne 'phase-01.2-authority-evidence/v1' -or $o.scenario -ne 'OrdinaryMissingCredentialDoesNotRevoke' -or $o.status -ne 'pass' -or -not $o.active_serial_unchanged -or -not $o.authority_snapshot_unchanged -or $o.predecessor_status_after_refusal -ne 'active' -or $o.recovery_service_status -ne 'Running' -or $o.recovery_service_start_mode -ne 'Auto' -or $o.recovery_active_policy_state -ne 'Active' -or $o.recovery_agent_env_token_count -ne 0 -or $o.recovery_scm_token_count -ne 0) { throw 'ordinary evidence contract failed' }
  if ($r.schema -ne 'phase-01.2-authority-evidence/v1' -or $r.scenario -ne 'ReplacementRevocation' -or $r.status -ne 'pass' -or -not $r.serial_changed -or $r.predecessor_status -ne 'revoked' -or $r.successor_status -ne 'active' -or -not $r.service_preserved -or -not $r.data_preserved -or -not $r.cache_preserved -or $r.service_status -ne 'Running' -or $r.service_start_mode -ne 'Auto' -or $r.active_policy_state -ne 'Active' -or $r.agent_env_token_count -ne 0 -or $r.scm_token_count -ne 0) { throw 'replacement evidence contract failed' }
  $protectedKeyPattern = '"(database_url|sql|stdout|stderr|raw_authority_row|enrollment_token|private_key|password|certificate|credential|dpapi_blob)"\s*:'
  if ($oRaw -match $protectedKeyPattern -or $rRaw -match $protectedKeyPattern) { throw 'protected evidence property detected' }
  $staging = @(Get-ChildItem -LiteralPath $phase -File | Where-Object Name -Match '^\.01\.2-03-(ordinary|replacement)-authority-evidence\.json\..*\.tmp$')
  if ($staging.Count -ne 0) { throw 'evidence staging residue detected' }
  $replacementHashBefore = (Get-FileHash -LiteralPath $replacement -Algorithm SHA256).Hash
  $failureOutput = @(& pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario ReplacementRevocation -FailureInjection AfterReplacementMutation -EvidencePath $replacement 2>&1)
  $failureExit = $LASTEXITCODE
  if ($failureExit -eq 0) { throw 'injected replacement failure exited zero' }
  $requiredCodes = @('replacement_post_mutation_failure_injected','replacement_recovery_attempted','replacement_recovery_succeeded','replacement_dual_cleanup_succeeded')
  foreach ($code in $requiredCodes) { if (-not ($failureOutput -contains $code)) { throw "missing stable recovery code: $code" } }
  if (@($failureOutput | Where-Object { $_ -notin $requiredCodes }).Count -ne 0) { throw 'injected replacement emitted non-allowlisted output' }
  if ((Get-FileHash -LiteralPath $replacement -Algorithm SHA256).Hash -ne $replacementHashBefore) { throw 'injected failure changed published pass evidence' }
  if (@(Get-ChildItem -LiteralPath $phase -File | Where-Object Name -Match '^\.01\.2-03-replacement-authority-evidence\.json\..*\.tmp$').Count -ne 0) { throw 'failure injection left staging residue' }
  node .gitnexus/run.cjs detect_changes --scope compare --base-ref master --repo wdlp
  if ($LASTEXITCODE -ne 0) { throw 'GitNexus compare-scope change detection failed' }
} finally {
  Remove-Item -LiteralPath $contractPath -Force -ErrorAction SilentlyContinue
  Get-ChildItem -LiteralPath ([IO.Path]::GetDirectoryName($contractPath)) -File -ErrorAction SilentlyContinue | Where-Object Name -Like (([IO.Path]::GetFileName($contractPath)) + '.*.tmp') | Remove-Item -Force -ErrorAction SilentlyContinue
}</automated>
  </verify>
  <done>Both live authority invariants are proven by real PostgreSQL rows; exact fail-closed evidence schemas reject unknown/raw fields; and a forced post-mutation replacement failure recovers LAB-CLIENT01 to healthy/clean state while preserving the prior pass artifact and original nonzero result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke process -> native psql | Internal SQL/options and protected DLP_DATABASE_URL cross a native-process argument boundary. |
| psql stdout/stderr -> authority assertions | Untrusted process streams become authority evidence only after exit, stderr, row-count, and row-shape validation. |
| hungdinh-lt -> LAB-DC01 PostgreSQL | The orchestrator reads authoritative enrollment and credential state without exposing the connection string. |
| hungdinh-lt -> LAB-CLIENT01 | Live scenarios stop the service, remove/replace DPAPI credentials, and must restore operational state in finally. |
| Live lab state -> planning evidence | Raw authority/credential material is reduced to allowlisted booleans, statuses, counts, and SHA-256 fingerprints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.2-03-01 | Tampering | psql command/argument ordering | high | mitigate | Task 1 uses ProcessStartInfo.ArgumentList and a fake process that requires exact six-element argv equality, including one unchanged whitespace/quote/parenthesis-bearing SQL element after -c and the final URL. |
| T-01.2-03-02 | Information disclosure | DLP_DATABASE_URL, SQL, temp streams, evidence | high | mitigate | Independent streams and stable codes are paired with exact per-scenario evidence property sets, unknown-field rejection, required UTC/fingerprints, protected-key/value exclusion, staging cleanup, and pass/recovery-gated atomic publication (D-09 through D-12). |
| T-01.2-03-03 | Spoofing | stderr warning masquerading as a row | high | mitigate | Reject any non-whitespace stderr even at exit zero before stdout parsing; deterministic warning fixture proves the gate. |
| T-01.2-03-04 | Tampering | authority row count/shape | high | mitigate | Require exactly one non-empty row and wrapper-specific hexadecimal/timestamp/status contracts before comparison or evidence. |
| T-01.2-03-05 | Denial of service | shared live-lab authority mutation | high | mitigate | Blocking-human exclusive window and sequential scenarios plus deterministic post-replacement-mutation failure injection proving fresh finally recovery, dual cleanup, healthy Active-policy endpoint, preserved service/data/cache/pass artifact, and original nonzero result. |
| T-01.2-03-06 | Repudiation | live evidence publication | medium | mitigate | Versioned allowlisted JSON records scenario/time/pass claims and SHA-256 fingerprints, atomically published only after assertions/recovery. |
| T-01.2-03-07 | Elevation of privilege | live provisioning boundary | high | mitigate | Reuse existing LAB-DC01 trusted provisioning and Apply-gated force path; no admin material moves to LAB-CLIENT01 (D-06, D-07, D-12). |
| T-01.2-03-SC | Tampering | package supply chain | high | mitigate | No npm/pip/cargo install tasks and no new dependency; use existing PowerShell, psql, and repository helpers only. |

ASVS Level 1 enforcement is fail-closed at HIGH: if any HIGH mitigation above cannot be demonstrated by the named automated gate or recovery evidence, stop execution and do not commit or publish passing evidence.
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Automatic trusted-provisioning install without manual token copy | 01.2-01, 01.2-03 | COVERED | Existing implementation is preserved; Plan 03 repairs its remaining authority evidence. |
| REQ | SRV-13 | Automatic short-lived token handoff and service install | 01.2-01, 01.2-03 | COVERED | Plan 03 proves live ordinary/replacement authority behavior without changing the handoff. |
| REQ | SRV-14 | LAB-DC01 boundary, validated handoff, cleanup, redaction | 01.2-01, 01.2-02, 01.2-03 | COVERED | Plan 03 adds fail-closed native-query validation and redacted live evidence. |
| REQ | TST-05 | Enrollment integration through first policy activation | 01.2-01, 01.2-03 | COVERED | Plan 03 closes the two disconnected live authority transitions; unclassified probe assumption remains flagged. |
| REQ | AGT-01 | Automatic Windows service without interactive session | 01.2-01, 01.2-03 | COVERED | Live recovery/replacement require Automatic/Running and preserved endpoint state; unclassified probe assumption remains flagged. |
| RESEARCH | - | Keep administrator mTLS material on LAB-DC01 and reuse trusted provisioning | 01.2-01, 01.2-03 | COVERED | No runtime/protocol boundary change. |
| RESEARCH | - | Token cleanup, preserved credential/cache directories, redacted output | 01.2-01, 01.2-03 | COVERED | Both live scenarios reassert cleanup/preservation; evidence is allowlisted. |
| RESEARCH | - | No new packages or Rust/protocol changes | 01.2-03 | COVERED | Explicit artifact and supply-chain constraints. |
| CONTEXT | D-01 | Failed install/start cleans token and retries fresh | 01.2-01, 01.2-03 | COVERED | Existing behavior preserved; live finally recovery uses fresh enrollment. |
| CONTEXT | D-02 | Cleanup failure is hard and names both locations | 01.2-01 | COVERED | Already verified; deliberately not re-executed by the gap plan. |
| CONTEXT | D-03 | Preserve partial service/binaries; remove token only | 01.2-01, 01.2-03 | COVERED | Live plan preserves service/data/cache. |
| CONTEXT | D-04 | Retry always provisions a fresh token | 01.2-01, 01.2-03 | COVERED | Finally recovery uses the existing fresh force path. |
| CONTEXT | D-05 | Existing credential skips normal provisioning | 01.2-01, 01.2-03 | COVERED | Ordinary missing-credential refusal is proven without changing normal reuse behavior. |
| CONTEXT | D-06 | ForceReenrollment is explicit | 01.2-01, 01.2-03 | COVERED | Replacement evidence exercises only the explicit path. |
| CONTEXT | D-07 | Force mutation requires Apply | 01.2-01, 01.2-03 | COVERED | Live replacement/recovery use the existing Apply-gated helper. |
| CONTEXT | D-08 | Forced replacement preserves service/data/cache | 01.2-01, 01.2-03 | COVERED | Required live evidence fields and assertions. |
| CONTEXT | D-09 | Normal output is sparse | 01.2-01, 01.2-03 | COVERED | Stable codes and allowlisted evidence only. |
| CONTEXT | D-10 | Errors use stable code and safe hint | 01.2-01, 01.2-03 | COVERED | Adapter/recovery failures use distinct stable codes without raw streams. |
| CONTEXT | D-11 | Detailed diagnostics are explicit and redacted | 01.2-01, 01.2-03 | COVERED | No detailed native stream content is persisted by the gap plan. |
| CONTEXT | D-12 | Protected values are always redacted | 01.2-01, 01.2-02, 01.2-03 | COVERED | Evidence schema contains only claims/counts/statuses/fingerprints. |

Prohibition recall kept two bespoke items under `must_haves.prohibitions`. Database URL, token, and credential secrecy are canonical security/data-protection controls, so they are handled by D-12 plus the threat model rather than minted again as a prohibition.

<verification>
1. Run `AuthorityQueryAdapter` locally and require exact six-element fake-process argv equality with the whitespace/quote/parenthesis-bearing SQL preserved as one item, plus fail-closed exit/stderr/row-count/shape cases.
2. Run the static `CoreFlow` gate so the new scenario and adapter contract cannot be silently removed.
3. After the blocking exclusive-lab checkpoint, run `OrdinaryMissingCredentialDoesNotRevoke` and `ReplacementRevocation` sequentially against LAB-DC01/LAB-CLIENT01 with their explicit evidence paths.
4. Run the deterministic writer contract; parse both pass files; require exact property-name sets, UTC observations, all fingerprints, protected-field/value absence, no staging residue, and publication only after pass/recovery.
5. Force `AfterReplacementMutation`, require fresh recovery and both cleanup locations, prove the endpoint healthy with service/data/cache and prior pass evidence preserved, and require fixed redacted output plus a nonzero original result.
6. Run `node .gitnexus/run.cjs detect_changes --scope compare --base-ref master --repo wdlp` before each code/evidence commit and report the actual blast radius/risk; UNKNOWN is never described as low.
</verification>

<success_criteria>
- The psql adapter preserves exact argv boundaries, including one unchanged SQL argument immediately after `-c` and before the database URL, and warning text cannot become a row.
- Zero/multiple/malformed authority results and nonzero/stderr process outcomes fail closed with stable redacted codes.
- Ordinary refusal proves byte-for-byte authority non-mutation before finally recovery restores a healthy endpoint.
- Replacement proves predecessor revoked, successor active, token cleanup, Active policy, and service/data/cache preservation.
- Both redacted evidence artifacts have exact fail-closed schemas with UTC/fingerprints and no raw/protected fields; unknown fields, staging residue, and premature publication are rejected.
- A deterministic post-mutation replacement failure proves recovery/cleanup and endpoint preservation while the prior pass artifact stays unchanged and the original run remains nonzero with fixed redacted output.
- Existing completed Phase 01.2 implementation/docs/Rust scope and unrelated dirty-worktree changes remain untouched.
</success_criteria>

<output>
Create `.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-SUMMARY.md` when execution completes, including the RED/GREEN/REFACTOR record, GitNexus UNKNOWN/available impact and final compare scope, checkpoint confirmation, live evidence paths/hashes, recovery outcome, and no secret values.
</output>
