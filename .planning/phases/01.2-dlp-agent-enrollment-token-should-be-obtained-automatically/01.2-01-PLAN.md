---
phase: 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lab/Invoke-Client01Runtime.ps1
  - tests/windows/Invoke-AgentServiceSmoke.ps1
  - tests/lab/EnrollmentTokenOrchestration.Tests.ps1
  - .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
  - .planning/docs/ENV-VARS.md
  - .planning/docs/LAB-SETUP-GUIDE.md
  - scripts/lab/README.md
autonomous: true
requirements: [SRV-13, SRV-14, TST-05, AGT-01]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TrustedProvisioning installs and starts DlpWindowsService without manual token copying (SRV-13, AGT-01; D-05)."
    - "Normal reruns reuse device.dpapi; only explicit -ForceReenrollment -Apply replaces credentials, with preview non-destructive and data/cache preserved (D-05, D-06, D-07, D-08)."
    - "Provisioning/install failures remove token state from both endpoint locations, preserve partial artifacts, and require a fresh token on retry (D-01, D-02, D-03, D-04)."
    - "Normal output is high-level and coded; opt-in diagnostics are redacted and exclude tokens, keys, passwords, full certificates, and credential blobs (D-09, D-10, D-11, D-12)."
    - "The existing trusted TLS path and integration smoke test prove enrollment through first policy activation without moving admin mTLS material to LAB-CLIENT01 (SRV-14, TST-05)."
  artifacts:
    - path: scripts/lab/Invoke-Client01Runtime.ps1
      provides: "Automatic token acquisition, credential-aware force gate, failure cleanup, stable redacted diagnostics, service startup"
      contains: "EnrollmentTokenProvider, ForceReenrollment, Diagnostic, Remove-Client01EnrollmentToken"
    - path: tests/windows/Invoke-AgentServiceSmoke.ps1
      provides: "Initial/replacement live enrollment and cleanup assertions"
      contains: "InitialEnrollmentCredential, ReplacementRevocation, Assert-EnrollmentTokenRemoved"
    - path: tests/lab/EnrollmentTokenOrchestration.Tests.ps1
      provides: "Static regression tests for gates, redaction, cleanup, and docs"
      contains: "enrollment_token_invalid, ForceReenrollment, Diagnostic"
    - path: .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
      provides: "Automatic, force, recovery, and diagnostic operator workflow"
      contains: "-EnrollmentTokenProvider TrustedProvisioning"
    - path: .planning/docs/ENV-VARS.md
      provides: "Token optionality, persistence, redaction, and manual fallback"
      contains: "DLP_AGENT_ENROLLMENT_TOKEN"
  key_links:
    - from: scripts/lab/Invoke-Client01Runtime.ps1
      to: scripts/lab/Invoke-TrustedProvisioning.ps1
      via: "LAB-DC01-only provisioning returns one validated token in memory"
      pattern: "Invoke-TrustedProvisioning.ps1.*enrollment_token"
    - from: scripts/lab/Invoke-Client01Runtime.ps1
      to: "LAB-CLIENT01 agent.env and SCM Environment"
      via: "Token is written for startup then removed from both locations"
      pattern: "DLP_AGENT_ENROLLMENT_TOKEN"
    - from: "LAB-CLIENT01 service"
      to: "C:\\dlp\\agent\\data\\credentials\\device.dpapi"
      via: "Enrollment creates durable credential used by normal reruns"
      pattern: "device.dpapi"
---

<objective>
Replan the endpoint deployment flow so trusted provisioning supplies a short-lived enrollment token automatically, with explicit force-reenrollment semantics, fail-closed cleanup, and operator-safe diagnostics.

Purpose: Remove manual token handoff without weakening the LAB-DC01 trust boundary or leaving reusable enrollment material behind. Close the decision-set guarantees that the obsolete plan did not make executable: fresh-token retries, hard cleanup failure, preserved partial installs, Apply-gated force replacement, sparse output, and redacted opt-in diagnostics.

Output: Production-quality PowerShell orchestration, live/static regression coverage, and synchronized operator documentation.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-CONTEXT.md
@.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-RESEARCH.md
@scripts/lab/Invoke-Client01Runtime.ps1
@scripts/lab/Invoke-TrustedProvisioning.ps1
@scripts/lab/Invoke-Dc01Server.ps1
@tests/windows/Invoke-AgentServiceSmoke.ps1
@.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
@.planning/docs/ENV-VARS.md
@.planning/docs/LAB-SETUP-GUIDE.md
@scripts/lab/README.md
</context>

<interfaces>
`Invoke-Client01Runtime.ps1` already exposes `-Scenario`, `-EnrollmentTokenProvider`, `-RetainEnrollmentToken`, `-Apply`, and `-Credential`; its seams are `Invoke-Client01TrustedProvisioning`, `Install-Client01RuntimeSecrets`, `Install-Client01Service`, `Test-Client01CredentialPresent`, `Assert-EnrollmentTokenValid`, `Remove-Client01EnrollmentToken`, `Invoke-Client01ServiceInstall`, and `Invoke-Client01Tracer`.
`Invoke-AgentServiceSmoke.ps1` already provides `Invoke-LiveEnrollmentRecovery`, `Assert-EnrollmentTokenRemoved`, and `InitialEnrollmentCredential`/`ReplacementRevocation` scenarios.
</interfaces>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Wire the automatic enrollment tracer and explicit force gate</name>
  <files>scripts/lab/Invoke-Client01Runtime.ps1, tests/windows/Invoke-AgentServiceSmoke.ps1</files>
  <behavior>
    - Test 1: TrustedProvisioning with no usable device.dpapi obtains a LAB-DC01 token, validates it before endpoint writes, persists it for startup, and starts the automatic service.
    - Test 2: An existing usable credential skips provisioning and does not consume a one-time token (D-05).
    - Test 3: -ForceReenrollment without -Apply is preview-only; with -Apply it replaces enrollment credentials/token state while preserving service, data, and cache directories (D-06, D-07, D-08).
  </behavior>
  <action>Extend the entrypoint with `-ForceReenrollment`, enforce the Apply mutation boundary, and retain Manual as the default/offline provider. Probe the protected credential before TrustedProvisioning; skip normal provisioning when usable unless force is explicit. For first enrollment or force, invoke the existing trusted procedure on LAB-DC01 with its fingerprint and admin-mTLS boundary, capture a fresh token in memory, validate before any LAB-CLIENT01 write, and pass it to the existing install path. Ensure binary replacement never deletes credentials or cache. Add live assertions for initial enrollment, automatic startup, force replacement, and directory preservation. Cite D-05 through D-08 in guarded code/tests.</action>
  <verify><automated>pwsh -NoProfile -File tests/lab/EnrollmentTokenOrchestration.Tests.ps1 -TestCase CoreFlow; pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario InitialEnrollmentCredential</automated></verify>
  <done>The TrustedProvisioning-to-service-to-DPAPI path works end-to-end; normal reruns skip provisioning, force replacement is Apply-gated, and service/data/cache survive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make token failure recovery hard-fail safe and diagnostics redacted</name>
  <files>scripts/lab/Invoke-Client01Runtime.ps1, tests/windows/Invoke-AgentServiceSmoke.ps1, tests/lab/EnrollmentTokenOrchestration.Tests.ps1</files>
  <behavior>
    - Test 1: Provisioning followed by install/start failure removes token lines from both agent.env and SCM Environment; inability to clean either path is a stable hard failure naming both paths (D-01, D-02).
    - Test 2: Partial service/binary artifacts remain, token state is the only removed state, and the next retry obtains a fresh token (D-03, D-04).
    - Test 3: Default output is sparse and coded; an explicit diagnostic switch emits only redacted status, lengths, paths, fingerprints, and bounded error metadata (D-09 through D-12).
  </behavior>
  <action>Wrap the newly handed-off token lifecycle in failure cleanup that runs when provisioning succeeded but installation/startup does not. Clean both `C:\dlp\agent\agent.env` and `HKLM:\SYSTEM\CurrentControlSet\Services\DlpWindowsService\Environment`; if either cleanup fails, stop with a stable code and concise remediation, while retaining partial services/binaries. Clear local token state and always mint afresh on retry. Add an explicit opt-in diagnostic switch, route normal errors through stable codes and safe hints, and redact token/private-key/password/full-certificate/credential-blob contents from console, transcripts, evidence, exceptions, and remote payloads. Replace unconditional detailed dumps that can expose environment contents. Cite D-01 through D-04 and D-09 through D-12 in guarded code/tests.</action>
  <verify><automated>pwsh -NoProfile -File tests/lab/EnrollmentTokenOrchestration.Tests.ps1 -TestCase FailureAndRedaction; pwsh -NoProfile -File tests/windows/Invoke-AgentServiceSmoke.ps1 -Scenario ReplacementRevocation</automated></verify>
  <done>Failed installs leave diagnosable partial artifacts but no token state; cleanup failure is fatal/actionable, retries are fresh, and both output modes satisfy redaction.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize operator docs and decision/requirement acceptance tests</name>
  <files>tests/lab/EnrollmentTokenOrchestration.Tests.ps1, .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md, .planning/docs/ENV-VARS.md, .planning/docs/LAB-SETUP-GUIDE.md, scripts/lab/README.md</files>
  <action>Document TrustedProvisioning as the normal path, Manual as explicit offline fallback, `-ForceReenrollment -Apply` as the only replacement path, and the diagnostic switch as opt-in/redacted. Explain fresh-token retries, both cleanup remediation paths, partial-artifact/data/cache preservation, and the rule that admin mTLS material stays on LAB-DC01. Remove normal-path manual token-copy instructions while retaining manual/offline semantics. Extend static tests to assert D-01..D-12 coverage and SRV-13, SRV-14, TST-05, AGT-01 references; examples must not contain secret contents.</action>
  <verify><automated>pwsh -NoProfile -File tests/lab/EnrollmentTokenOrchestration.Tests.ps1 -TestCase DocumentationAndDecisionCoverage; pwsh -NoProfile -File tests/lab/EnvironmentGuidance.Tests.ps1</automated></verify>
  <done>All canonical docs and automated acceptance checks describe every supported flow and locked decision without unsafe secret examples.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| hungdinh-lt -> LAB-DC01 | Orchestrator invokes trusted provisioning; admin mTLS stays on LAB-DC01. |
| LAB-DC01 -> hungdinh-lt -> LAB-CLIENT01 | Short-lived token crosses return/injection boundaries in memory only as long as needed for enrollment. |
| LAB-CLIENT01 SCM -> service | SCM supplies environment; service converts token to DPAPI credential and later starts without stale token. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.2-01 | Information disclosure | Token/log/evidence flow | high | mitigate | In-memory token, redacted opt-in diagnostics, no host artifacts (D-09, D-11, D-12). |
| T-01.2-02 | Tampering | Token validation | medium | mitigate | Validate length/charset before endpoint writes; stable `enrollment_token_invalid`. |
| T-01.2-03 | Elevation | Provisioning boundary | critical | mitigate | Existing LAB-DC01 procedure and fingerprint/admin-mTLS checks; no admin material on endpoint. |
| T-01.2-04 | DoS | Reinstall path | medium | mitigate | Preserve partial service/binaries and data/cache; clean token state only (D-03). |
| T-01.2-05 | Spoofing/replay | Retry/credential handling | high | mitigate | Existing-credential skip, explicit Apply-gated force, fresh retry token (D-04 through D-07). |
| T-01.2-06 | DoS | Cleanup | high | mitigate | Clean both locations; cleanup failure is fatal and actionable (D-01, D-02). |
| T-01.2-SC | Tampering | Package installs | high | mitigate | No package-manager installs; use existing scripts/binaries only. |
</threat_model>

<verification>
Run static orchestration/decision/redaction/document tests; run the TrustedProvisioning tracer on a clean enrollment state and verify Running, device.dpapi, and no token in either endpoint location; rerun with an existing credential; exercise force preview and Apply; inject install/start failure and verify cleanup, preserved partial artifacts, fatal cleanup errors, and fresh retry; inspect normal/opt-in diagnostics for stable redacted output.
</verification>

<success_criteria>
Automatic enrollment works without manual token copy; existing credentials skip provisioning; only `-ForceReenrollment -Apply` replaces credentials; both endpoint token locations are cleaned on success/failure; partial artifacts and data/cache survive; cleanup failure is hard and actionable; diagnostics are sparse/redacted; tests cover SRV-13, SRV-14, TST-05, AGT-01 and D-01..D-12.
</success_criteria>

<output>
Create `.planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-01-SUMMARY.md` when execution completes.
</output>
