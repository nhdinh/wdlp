# Phase 01.3: Development-Only Device Log Debugging Service - Context

**Gathered:** 2026-08-14
**Status:** Ready for planning

<domain>
## Phase Boundary

Deliver a development-only Windows service that runs independently on a DLP endpoint and exposes explicitly requested log-file content over HTTP for easier lab debugging. It is not part of the production DLP ecosystem: it does not enroll, receive policy, report health, affect DLP component availability, aggregate logs centrally, or ship in production packaging.

</domain>

<decisions>
## Implementation Decisions

### Process Ownership and Lifecycle
- **D-01:** Implement the debugger as a separate Windows service, not inside `DlpWindowsService` or a per-user companion.
- **D-02:** The service is development-only and remains outside the DLP ecosystem. It must not participate in enrollment, management-server registration, policy, health reporting, or production deployment.
- **D-03:** Developers install, start, stop, and remove it manually with standard Windows commands. No repository-managed installer or automatic lab provisioning is required.
- **D-04:** Failure or absence of the debugging service has no effect on any DLP component.
- **D-05:** Keep the implementation in a dedicated Cargo workspace crate and exclude it from production packaging.

### HTTP Access Boundary
- **D-06:** Listen on all network interfaces to support remote access from other lab machines.
- **D-07:** Authorize remote clients with a configurable source-IP allowlist; identity-based authentication is not required for this development tool.
- **D-08:** Store trusted client IPs in a local configuration file.
- **D-09:** If the configuration is missing, invalid, or has no trusted clients, continue in localhost-only mode and reject remote requests.

### File Authorization and Reading
- **D-10:** The client requests a specific absolute log-file path. The service does not discover or aggregate logs automatically.
- **D-11:** Permit a request only when the resolved file is a direct child of a folder allowlisted in the local configuration. Nested descendants are not implicitly authorized.
- **D-12:** Read the source file and return its content directly. Do not cache, copy, archive, or otherwise retain requested log data.
- **D-13:** Return a bounded tail snapshot for large or actively written files. The maximum response size is configurable and remains a hard safety cap.

### HTTP API Contract
- **D-14:** Use `GET /logs?path=<absolute-path>&tail=<line-count>`.
- **D-15:** On success, return only plain log text. Do not wrap it in JSON or attach diagnostic metadata headers.
- **D-16:** If the byte cap prevents satisfying the requested line count, return the newest complete lines that fit without adding a truncation marker.
- **D-17:** Invalid and unsuccessful requests use standard HTTP statuses with short plain-text error codes, such as `invalid_tail`, `untrusted_client`, or `file_not_found`. Do not expose filesystem or operating-system error details.

### Agent's Discretion
- Configuration file format and default location.
- Default HTTP port and the configurable maximum response size.
- Exact HTTP status mapping and short error-code vocabulary, provided it follows D-17.
- Crate and Windows service names.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Product and Phase Scope
- `.planning/ROADMAP.md` — Defines Phase 01.3 as the urgent device-side debugging capability and its dependency on Phase 1.
- `.planning/PROJECT.md` — Defines the Windows endpoint architecture, Rust workspace, and security-sensitive logging principles.
- `.planning/REQUIREMENTS.md` — Definition of done requires diagnosable failures and prohibits secrets or protected content in logs and test artifacts.

### Existing Phase Decisions
- `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` — Defines the existing endpoint service lifecycle and production boundaries that this debug-only service must not disrupt.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `crates/dlp-server/src/lib.rs`: Existing Tokio/Axum listener and router patterns can guide the small HTTP service without coupling it to the server crate.
- `crates/dlp-windows-service/src/service.rs`: Existing Windows SCM registration, status transitions, shutdown handling, and primitive early-start logging provide patterns for a separate service.
- `crates/dlp-windows-service/src/main.rs`: Existing Windows service executable startup and crypto-provider initialization show workspace conventions for Windows binaries.

### Established Patterns
- Rust Cargo workspace with platform-specific Windows service integration.
- Tokio async runtime and Axum HTTP serving are already used in the repository.
- Early service diagnostics currently append to `C:\dlp\agent\logs\dlp-windows-service.log` before runtime configuration is available.
- Diagnostic output must be redacted and must never disclose credentials, protected content, or sensitive filesystem details.

### Integration Points
- Add a dedicated development-only crate to the Cargo workspace.
- Reuse Windows SCM lifecycle patterns while keeping runtime and failure handling isolated from `DlpWindowsService`.
- Reuse repository HTTP conventions where suitable, but expose only the narrow file-tail endpoint defined above.

</code_context>

<specifics>
## Specific Ideas

- The tool exists only for the development period and should be manually activated when a developer needs endpoint diagnostics.
- Remote lab access is intentional, with the local configuration file serving both network trust and filesystem-boundary configuration.
- Successful output should be directly usable as raw log text by simple clients.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 01.3-development-only-device-log-debugging-service*
*Context gathered: 2026-08-14*
