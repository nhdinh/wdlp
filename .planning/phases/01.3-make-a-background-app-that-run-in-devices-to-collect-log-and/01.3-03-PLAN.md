---
phase: 01.3-make-a-background-app-that-run-in-devices-to-collect-log-and
plan: 03
type: execute
wave: 3
depends_on:
  - 01.3-02
files_modified:
  - crates/dlp-log-debug-service/config.example.json
  - tests/windows/Test-DlpLogDebugRunbookSyntax.ps1
  - .planning/docs/DLP-LOG-DEBUG-SERVICE.md
  - .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
  - .planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-03-BLOCKED.md
autonomous: false
requirements:
  - TBD
user_setup:
  - name: LAB-CLIENT01 administrator session
    why: "D-03 requires developers to create/remove the service, protect ProgramData, and manage the firewall with standard elevated Windows commands."
    source: "Use an authorized lab administrator credential; do not commit or paste it into evidence. If LAB-CLIENT01 is unreachable, follow the blocked-evidence branch instead of claiming runtime verification."
  - name: Two lab probe machines
    why: "One trusted and one untrusted source are required to prove both the application allowlist and firewall RemoteAddress boundary."
    source: "Use LAB-DC01 as the trusted probe and LAB-DC02 as the untrusted probe after resolving their current IP addresses."

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A developer has syntax-checked standard Windows commands to build, configure, ACL-protect, manually install, start, stop, remove, and clean up the debugger without repository-managed provisioning (D-02, D-03, D-05)."
    - "The documented firewall rule is scoped simultaneously to the debugger executable, configured TCP port, and trusted remote addresses; any two-probe widening is labeled as a temporary verification window and is narrowed immediately (D-06, D-07, D-08)."
    - "A trusted lab source retrieves exact raw tail text, an application-untrusted source receives 403, and a firewall-untrusted source cannot reach the listener (D-07, D-14, D-15, D-17)."
    - "Missing or invalid configuration leaves the service Running but rejects remote requests and authorizes no folders (D-08, D-09)."
    - "The debugger is absent from production packaging/deployment and its full lifecycle preserves the exact DlpWindowsService baseline, including an explicit NotInstalled baseline that never becomes an availability claim (D-02, D-04, D-05)."
    - "If LAB-CLIENT01, LAB-DC01, or LAB-DC02 is unreachable, execution records a blocked-evidence handoff and leaves runtime predicates unverified rather than reporting the phase's Windows behavior as passed (D-03, D-04)."
  prohibitions:
    - "Documentation must not introduce a repository-managed installer, automatic lab provisioning, production deployment step, enrollment step, management registration, or DLP health dependency."
    - "Examples, commands, checklists, and summaries must not contain real credentials, private addresses presented as universal values, requested log content beyond synthetic fixtures, protected data, or raw service errors."
    - "The firewall must not use Any for RemoteAddress, and the config directory/log folders must not be writable by ordinary users."
  artifacts:
    - path: "crates/dlp-log-debug-service/config.example.json"
      provides: "Synthetic complete configuration schema example"
      contains: "version, trusted_client_ips, allowed_folders, port, max_response_bytes"
    - path: ".planning/docs/DLP-LOG-DEBUG-SERVICE.md"
      provides: "Development-only manual lifecycle, ACL, firewall, verification, troubleshooting, and cleanup runbook"
      contains: "DlpLogDebugService, sc.exe, icacls, New-NetFirewallRule, sc.exe delete"
    - path: ".planning/docs/HYPERV-DLP-STARTUP-GUIDE.md"
      provides: "Discoverable link to the independent debugger runbook"
      contains: "DLP-LOG-DEBUG-SERVICE.md"
    - path: "tests/windows/Test-DlpLogDebugRunbookSyntax.ps1"
      provides: "Non-mutating AST parser gate for every PowerShell code block in the debugger runbook"
      contains: "System.Management.Automation.Language.Parser, ParseInput"
    - path: ".planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-03-BLOCKED.md"
      provides: "Conditional, non-secret lab-unavailability handoff that records no runtime passes"
      contains: "status: blocked, runtime_verification: not_run"
  key_links:
    - from: "crates/dlp-log-debug-service/config.example.json"
      to: "crates/dlp-log-debug-service/src/config.rs"
      via: "example uses the exact strict field names and value types accepted by FileConfig"
      pattern: "trusted_client_ips.*allowed_folders.*max_response_bytes"
    - from: ".planning/docs/DLP-LOG-DEBUG-SERVICE.md"
      to: "crates/dlp-log-debug-service/src/service.rs"
      via: "manual sc.exe commands use SERVICE_NAME, executable path, ProgramData config path, port, and cleanup contract"
      pattern: "DlpLogDebugService"
    - from: ".planning/docs/DLP-LOG-DEBUG-SERVICE.md"
      to: "crates/dlp-log-debug-service/src/http.rs"
      via: "Invoke-WebRequest examples call the exact GET /logs path/tail contract and expected fixed errors"
      pattern: "/logs.*path=.*tail="
    - from: "tests/windows/Test-DlpLogDebugRunbookSyntax.ps1"
      to: ".planning/docs/DLP-LOG-DEBUG-SERVICE.md"
      via: "the non-mutating gate extracts and parses every fenced PowerShell block before the checkpoint"
      pattern: "ParseInput.*powershell"
    - from: ".planning/STATE.md"
      to: ".planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-03-BLOCKED.md"
      via: "current lab reachability blockers trigger an explicit not-run handoff instead of runtime-pass evidence"
      pattern: "runtime_verification: not_run"
---

<objective>
Make the independent debugger safely operable and prove its Windows lab behavior without adding managed installation or production deployment, exactly preserving D-02, D-03, D-04, D-05, D-06, D-07, and D-08.

Purpose: The code is useful only when a developer can activate it with the correct ACL and firewall boundaries, verify both accepted and rejected network paths, and remove it cleanly without touching DLP runtime availability.

Output: A synthetic config example, a complete manual developer runbook, a startup-guide link, and a blocking LAB-CLIENT01 lifecycle/security verification.

Requirement probe: visible skip because ROADMAP.md assigns no Phase 01.3 requirement IDs; `requirements: [TBD]` is retained and verification is derived from the locked phase decisions.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-CONTEXT.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-RESEARCH.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-PATTERNS.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-REVIEWS.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/COVERAGE.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-02-SUMMARY.md
@.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
@crates/dlp-log-debug-service/src/config.rs
@crates/dlp-log-debug-service/src/http.rs
@crates/dlp-log-debug-service/src/service.rs

<interfaces>
Configuration schema:
- `version`: integer `1`
- `trusted_client_ips`: array of exact IP address strings
- `allowed_folders`: array of absolute existing folder strings; only direct children are authorized
- `port`: nonzero TCP port, default `9191`
- `max_response_bytes`: positive hard cap, default `262144`

Runtime contracts:
- Service name: `DlpLogDebugService`
- Default config: `C:\ProgramData\DlpLogDebugService\config.json`
- Endpoint: `GET /logs?path=<absolute-path>&tail=<positive-line-count>`
- Success: 200 and source text only
- Primary errors: `invalid_tail`, `invalid_path`, `untrusted_client`, `forbidden_path`, `file_not_found`, `read_failed`
</interfaces>
</context>

## Artifacts this phase produces

| Kind | File / symbol / contract | Owner |
|---|---|---|
| Workspace/package | `Cargo.toml`, `Cargo.lock`, `crates/dlp-log-debug-service/Cargo.toml`; package `[lints.rust] unsafe_code = "allow"` is documented solely for the Windows binary macro boundary | Plan 01 |
| Crate/platform roots | `src/lib.rs` with `#![forbid(unsafe_code)]`; `src/main.rs` with `cfg(windows)` dispatcher and `cfg(not(windows))` `windows_only` behavior | Plan 01/02 |
| Configuration | `config.rs`: `FileConfig`, `RuntimeConfig`, `AccessMode`, `ConfigError`, `load_runtime_config`; fields `version`, `trusted_client_ips`, `allowed_folders`, `port`, `max_response_bytes` | Plan 01 |
| Filesystem authorization | `paths.rs`: `AuthorizedFolders`, `PathAuthorizationError`, `authorize_requested_file` | Plan 01 |
| Tail snapshot | `tail.rs`: `TailReadError`, `read_bounded_tail` | Plan 01 |
| HTTP | `http.rs`: `AppState`, `LogQuery`, `HttpError`, `build_router`, `serve_http`; endpoint `GET /logs?path=<absolute-path>&tail=<line-count>` | Plan 02 |
| Portable lifecycle contract | `lib.rs`: `ServiceError`, `ServiceError::stable_code`, `service_exit_code`; `tests/service_contract.rs` | Plan 02 |
| Windows service | `service.rs` and `main.rs`: `SERVICE_NAME`, `DEFAULT_CONFIG_PATH`, `DEFAULT_PORT`, `DEFAULT_MAX_RESPONSE_BYTES`, `run_scm_service`, `service_log` | Plan 02 |
| Portable HTTP contract tests | `tests/endpoint_contract.rs` | Plan 01/02 |
| Configuration example | `config.example.json` with the five configuration fields | Plan 03 |
| Runbook syntax test | `tests/windows/Test-DlpLogDebugRunbookSyntax.ps1` | Plan 03 |
| Operator documentation | `.planning/docs/DLP-LOG-DEBUG-SERVICE.md` and a link from `.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md` | Plan 03 |
| Conditional blocked handoff | `01.3-03-BLOCKED.md`, created only when the three required lab machines are unreachable and never represented as runtime-pass evidence | Plan 03 |
| Flags | None; the SCM service uses the documented default configuration path and file-owned runtime settings | Phase contract |

## Review feedback disposition

| Review concern | Severity | Executable disposition |
|---|---|---|
| Blocking lab checkpoint may be unreachable | HIGH | Task 2 performs a no-mutation reachability preflight and, if blocked, writes `01.3-03-BLOCKED.md`, reports runtime verification as not run, preserves the checkpoint, and returns a blocked handoff. |
| Runbook commands need automated syntax validation | MEDIUM | Task 1 adds a versioned non-mutating AST parser for every PowerShell block before the human checkpoint. |
| Temporary firewall widening is security-sensitive | MEDIUM | Task 1 labels the two-probe rule as a short verification window; Task 2 narrows it immediately after application-level denial is proven. |
| DlpWindowsService may not be installed at baseline | MEDIUM | Tasks 1-2 normalize the baseline to Installed/status/start-mode or NotInstalled and compare like-for-like without claiming availability for an absent service. |

<tasks>

<task type="auto">
  <name>Task 1: Publish the synthetic config and manual ACL/firewall/SCM runbook</name>
  <read_first>
    - crates/dlp-log-debug-service/src/config.rs
    - crates/dlp-log-debug-service/src/http.rs
    - crates/dlp-log-debug-service/src/service.rs
    - .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
    - .planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-CONTEXT.md
    - .planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-RESEARCH.md
  </read_first>
  <reversibility rating="costly">The documented service/config/firewall coordinates must change together; undo remains a manual service recreation and file replacement rather than a data migration.</reversibility>
  <files>crates/dlp-log-debug-service/config.example.json, tests/windows/Test-DlpLogDebugRunbookSyntax.ps1, .planning/docs/DLP-LOG-DEBUG-SERVICE.md, .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md</files>
  <action>
    Create `config.example.json` using every exact strict field with synthetic documentation ranges only: version 1, `192.0.2.10` as an example trusted IP, `C:\path\to\logs` as an example folder, port 9191, and cap 262144. State in the runbook that these placeholders cannot be copied unchanged and that an empty trusted list deliberately selects localhost-only mode. Include no secrets, live machine addresses, or actual protected paths.

    Create `.planning/docs/DLP-LOG-DEBUG-SERVICE.md` as the canonical development-only runbook. Document the D-02/D-05 boundary first: independent crate and service, no enrollment/policy/health/management registration, no DLP availability dependency, no production packaging, and no deployment through `Invoke-Client01Runtime.ps1`. Provide exact manual commands for an administrator to build the release binary, create `C:\ProgramData\DlpLogDebugService`, copy/edit the config, remove inherited write access, and grant modification only to SYSTEM and Administrators with `icacls`. Require every allowed log folder to be non-user-writable except for the trusted log writer, and explain the canonicalize/open race assumption.

    Per D-03, document standard `sc.exe create` with demand start, `sc.exe start`, `sc.exe stop`, `sc.exe delete`, process/service queries, and cleanup; do not add an installer or repository automation. Document `New-NetFirewallRule` and removal with exact Program, TCP LocalPort, Direction Inbound, Action Allow, and resolved trusted RemoteAddress values. Explicitly reject an unrestricted RemoteAddress rule. Label the step that temporarily permits LAB-DC01 and LAB-DC02 as a time-bounded verification window: open it only after the synthetic fixture and application allowlist are ready, use it only to prove application-level rejection, narrow it to LAB-DC01 immediately afterward, and remove it during cleanup. Explain that the service binds all interfaces per D-06 but application IP authorization remains mandatory per D-07/D-08, HTTP log text is unencrypted, deployment is restricted to an isolated trusted lab, and the service should be stopped/removed when debugging ends.

    Before every privilege-manifest or mutation section, add a fenced `powershell` function named `Test-DlpLogDebugLabPreflight` with parameters `ClientComputerName`, `TrustedProbeComputerName`, and `UntrustedProbeComputerName`, invoked exactly as `Test-DlpLogDebugLabPreflight -ClientComputerName LAB-CLIENT01 -TrustedProbeComputerName LAB-DC01 -UntrustedProbeComputerName LAB-DC02`. The function must remain read-only: for each alias run `Resolve-DnsName -Name <alias> -Type A -DnsOnly -ErrorAction Stop` and require at least one returned `IPAddress` that parses with `[System.Net.IPAddress]::Parse`; run `Test-WSMan -ComputerName <alias> -Authentication Negotiate -ErrorAction Stop` and require a non-null response. For LAB-CLIENT01 additionally run `Invoke-Command -ComputerName LAB-CLIENT01 -Authentication Negotiate -ErrorAction Stop` with a script block that constructs `WindowsPrincipal` from `WindowsIdentity::GetCurrent()` and returns `IsInRole(Administrator)`; it passes only when the returned value is exactly `$true`. For LAB-DC01 and LAB-DC02 additionally run `Invoke-Command` with the same computer/authentication/error parameters to return `$env:COMPUTERNAME`; each passes only when the result case-insensitively equals its requested alias. Define an all-pass result as all three machine results passing every required check and exit 0; any missing/invalid DNS result, WSMan failure/null response, remote-command failure, non-administrator client result, or probe-name mismatch fails that machine and exits nonzero. Emit only machine alias, boolean pass/fail, and the stable mapped reason code: `PREFLIGHT_LAB_CLIENT01_ADMIN_UNREACHABLE`, `PREFLIGHT_LAB_DC01_PROBE_UNREACHABLE`, or `PREFLIGHT_LAB_DC02_PROBE_UNREACHABLE`; never emit raw exception, address, identity, or credential data. State that these codes map one-to-one to LAB-CLIENT01, LAB-DC01, and LAB-DC02 respectively and that a failure of any one prohibits every SCM, ACL, firewall, config, fixture, or endpoint mutation.

    Addresses review concern: make the runbook commands syntax-checkable and make an absent product service an explicit baseline. Put every `icacls`, `New-NetFirewallRule`, `Set-NetFirewallRule`, `Remove-NetFirewallRule`, `sc.exe`, and percent-encoded request command in fenced `powershell` blocks. Add request examples that use `[uri]::EscapeDataString` for the absolute `path`, pass positive `tail`, and show exact raw 200 output plus fixed errors without raw detail. Include a copy/paste `Test-DlpLogDebugServiceContract` verification function that measures and emits only the boolean fields consumed by Task 2: `ServiceRunning`, `AllInterfaceListener`, `TrustedRawTail`, `ApplicationReject`, `FirewallReject`, `FallbackClosed`, and `DlpServiceUnchanged`; it must return a nonzero exit when any field is false and must not print real paths or response content. Capture the product-service baseline with `Get-CimInstance Win32_Service` as either `Installed|<State>|<StartMode>` or the exact sentinel `NotInstalled`; compare the same normalized form after cleanup. When both sides are `NotInstalled`, report only that the debugger did not create/change the service, not that DLP availability was runtime-verified. Cover direct-child/nested path behavior, hard cap/complete lines, missing-invalid-empty configuration fallback, application allowlist, firewall source scope, graceful stop, cleanup, and before/after product-service baseline. Add a concise troubleshooting table using only stable codes. Link this runbook once from the Hyper-V guide Related Docs and troubleshooting area without merging the debugger into the endpoint deployment flow.

    Create `tests/windows/Test-DlpLogDebugRunbookSyntax.ps1` as a non-mutating documentation test. It accepts `-RunbookPath`, requires at least one fenced `powershell` block, extracts every such block, calls `System.Management.Automation.Language.Parser::ParseInput` for each, reports only block number and parser message on failure, and exits nonzero on any syntax error. It must never execute a parsed block, contact a lab machine, create a service, or change a firewall/ACL. This turns syntax validity into automated evidence while preserving D-03's manual lifecycle boundary.
  </action>
  <verify>
    <automated>rtk proxy C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/windows/Test-DlpLogDebugRunbookSyntax.ps1 -RunbookPath .planning/docs/DLP-LOG-DEBUG-SERVICE.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $cfg = Get-Content -Raw 'crates/dlp-log-debug-service/config.example.json' | ConvertFrom-Json; if ($cfg.version -ne 1 -or $cfg.port -ne 9191 -or $cfg.max_response_bytes -ne 262144) { throw 'config_example_contract_failed' }; $doc = Get-Content -Raw '.planning/docs/DLP-LOG-DEBUG-SERVICE.md'; @('sc.exe create','sc.exe start','sc.exe stop','sc.exe delete','icacls','New-NetFirewallRule','Set-NetFirewallRule','RemoteAddress','EscapeDataString','temporary verification window','NotInstalled','GET /logs','untrusted_client','LocalhostOnly','Test-DlpLogDebugLabPreflight','Resolve-DnsName','Test-WSMan','Invoke-Command','PREFLIGHT_LAB_CLIENT01_ADMIN_UNREACHABLE','PREFLIGHT_LAB_DC01_PROBE_UNREACHABLE','PREFLIGHT_LAB_DC02_PROBE_UNREACHABLE') | ForEach-Object { if (-not $doc.Contains($_)) { throw "runbook_missing:$($_)" } }; if (-not (Select-String -Quiet -Path '.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md' -SimpleMatch 'DLP-LOG-DEBUG-SERVICE.md')) { throw 'startup_guide_link_missing' }</automated>
  </verify>
  <acceptance_criteria>
    - The JSON example parses and exactly matches the implemented schema/defaults while containing only synthetic values.
    - The runbook covers build, protected config, protected log folders, demand-start SCM lifecycle, program/port/address-scoped firewall, requests, fallback behavior, stable troubleshooting, and complete cleanup.
    - Every PowerShell code block passes the non-mutating AST parser gate; the gate fails when any block has a syntax error and never executes the block.
    - The two-probe firewall widening is visibly labeled temporary, forbids an unrestricted address scope, and is narrowed immediately after the application rejection check.
    - The runbook provides the exact read-only `Test-DlpLogDebugLabPreflight` invocation, deterministic DNS/WSMan/remote-command pass criteria, the three one-to-one machine reason codes, and a nonzero all-pass gate before any privileged or mutating command.
    - DlpWindowsService baseline capture handles both installed and NotInstalled states and makes no availability claim for the NotInstalled case.
    - The runbook explicitly excludes installer/provisioning/production/DLP integration and warns that HTTP response content is limited to an isolated trusted lab.
    - The existing startup guide adds only discoverability and does not insert debugger steps into DlpWindowsService deployment.
  </acceptance_criteria>
  <done>A developer can safely operate the debugger using only the documented standard Windows commands, exact config schema, and layered lab-only network/filesystem boundaries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the reversible manual LAB-CLIENT01 lifecycle and both network authorization layers</name>
  <read_first>
    - .planning/docs/DLP-LOG-DEBUG-SERVICE.md
    - crates/dlp-log-debug-service/config.example.json
    - crates/dlp-log-debug-service/src/http.rs
    - crates/dlp-log-debug-service/src/service.rs
    - .planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-CONTEXT.md
    - .planning/STATE.md
  </read_first>
  <reversibility rating="reversible">The test service, firewall rule, ProgramData fixture, and synthetic log are explicitly removed at the end; no reboot, schema migration, trust-store change, or DLP configuration change is involved.</reversibility>
  <files>.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-03-BLOCKED.md (conditional blocked handoff only; successful runtime results belong in the plan summary)</files>
  <action>
    Addresses review concern: distinguish unavailable-lab evidence from completed runtime verification. As the first checkpoint operation, before presenting the privilege manifest or making any change, invoke the runbook's named command exactly as `Test-DlpLogDebugLabPreflight -ClientComputerName LAB-CLIENT01 -TrustedProbeComputerName LAB-DC01 -UntrustedProbeComputerName LAB-DC02`. Require exit 0 plus passing LAB-CLIENT01 DNS, WSMan, and administrator-token checks and passing LAB-DC01/LAB-DC02 DNS, WSMan, and remote-name checks. Only that complete result unlocks the privilege manifest and every later SCM/ACL/firewall/config/fixture operation. Any nonzero exit or failed machine result maps deterministically to `PREFLIGHT_LAB_CLIENT01_ADMIN_UNREACHABLE`, `PREFLIGHT_LAB_DC01_PROBE_UNREACHABLE`, or `PREFLIGHT_LAB_DC02_PROBE_UNREACHABLE`; record every failed machine's mapped code. If any required machine is unavailable, make no SCM/ACL/firewall/config mutation, do not run the runtime contract, do not create `01.3-03-SUMMARY.md`, and do not set any runtime predicate to True. Instead create or update `01.3-03-BLOCKED.md` with `status: blocked`, `runtime_verification: not_run`, UTC timestamp, non-secret machine aliases, the mapped code for each failed preflight, the passing source/runbook verification command names, and the exact remaining manual steps; include no credential, address, identity, real requested path/content, or raw remoting/OS error. Return a blocked checkpoint handoff so execution can resume when the lab is reachable. A blocked handoff is not task completion and must not be described as phase runtime verification.

    Only after the named preflight exits 0 with all three machine results passing, perform the runbook manually on LAB-CLIENT01 per D-03 using a disposable synthetic log folder and file. Before elevation, present this exact privilege manifest for approval: create demand-start `DlpLogDebugService`; create/ACL `C:\ProgramData\DlpLogDebugService` and the synthetic log folder; create one inbound firewall rule scoped to the debugger executable, TCP 9191, and the named probe addresses for the temporary application-rejection window; expected state is one manually running debugger with the product-service baseline unchanged; rollback is stop/delete debugger, remove its firewall rule, and delete only debugger fixtures; no reboot is expected. Do not use repository-managed provisioning. Normalize the DlpWindowsService baseline to `Installed|<State>|<StartMode>` or `NotInstalled`, repeat the same query after cleanup, and require byte-for-byte equality. If it is NotInstalled both times, record that fact and only conclude that the debugger did not install/change it.
  </action>
  <what-built>The independent development-only debugger, protected JSON configuration, exact source-IP and canonical-folder checks, bounded raw-text endpoint, and manual operations runbook.</what-built>
  <how-to-verify>
    0. Run `Test-DlpLogDebugLabPreflight -ClientComputerName LAB-CLIENT01 -TrustedProbeComputerName LAB-DC01 -UntrustedProbeComputerName LAB-DC02` exactly as documented. Require exit 0 and all three machine results to pass before displaying the privilege manifest or running step 1. If any result fails, record its mapped `PREFLIGHT_LAB_*` code, follow the blocked-evidence action above, make no endpoint mutation, leave this checkpoint unapproved, and stop here.
    1. Capture DlpWindowsService as `Installed|<State>|<StartMode>` or `NotInstalled`, resolve the current LAB-DC01/LAB-DC02 IPs, create a three-line synthetic newline-terminated log directly under the test folder, and configure only LAB-DC01 in `trusted_client_ips`. Open the program/port-scoped firewall to both probe IPs only as the explicitly labeled temporary verification window; never use Any.
    2. Use the documented `sc.exe` commands to create and start `DlpLogDebugService`. Confirm `Get-Service DlpLogDebugService` is Running and `Get-NetTCPConnection -LocalPort 9191 -State Listen` shows an all-interface listener.
    3. From LAB-DC01, issue the encoded `GET /logs?...&tail=2`; require status 200, `text/plain`, and exactly the last two complete synthetic lines with no JSON, marker, or custom diagnostic header.
    4. From LAB-DC02 while the firewall still permits its address, issue the same request; require 403 with body exactly `untrusted_client`. A nonexistent requested path must still return `untrusted_client`, proving peer authorization precedes filesystem inspection.
    5. Immediately narrow the firewall RemoteAddress to LAB-DC01 only. Repeat from LAB-DC02 and require the connection to be blocked before the application; repeat from LAB-DC01 and require the original 200 response. Do not continue with the widened two-probe rule.
    6. Request a nested file and an oversized/unterminated synthetic file from LAB-DC01; require `forbidden_path` for the nested target and only newest complete lines within 262144 response bytes for the authorized large target.
    7. Replace the config with malformed JSON, restart only `DlpLogDebugService`, and confirm it remains Running while remote requests return `untrusted_client` and loopback cannot read the prior folder. Restore valid config afterward.
    8. Stop and delete only `DlpLogDebugService`, remove its firewall rule and debugger fixtures, and confirm the process/listener/config fixture are gone. Recapture the normalized DlpWindowsService baseline and require exact equality with step 1. When both values are NotInstalled, record `DlpServiceBaseline=NotInstalled` and do not claim that DLP runtime availability was exercised; only record that no DLP service or artifact was created or changed per D-04.
  </how-to-verify>
  <verify>
    <automated>First run `Test-DlpLogDebugLabPreflight -ClientComputerName LAB-CLIENT01 -TrustedProbeComputerName LAB-DC01 -UntrustedProbeComputerName LAB-DC02`; it must exit 0 with all three machine results passing before any mutating block is invoked. Then, on LAB-CLIENT01, run the runbook's `Test-DlpLogDebugServiceContract` verification block after steps 1-7; it must report `ServiceRunning=True`, `AllInterfaceListener=True`, `TrustedRawTail=True`, `ApplicationReject=True`, `FirewallReject=True`, `FallbackClosed=True`, `DlpServiceUnchanged=True`, and exit 0 without printing any requested path/content beyond the synthetic expected lines.</automated>
    <human-check>Confirm the exact privilege manifest before changes, observe the eight checks above, then confirm cleanup removed only debugger-owned artifacts.</human-check>
  </verify>
  <acceptance_criteria>
    - Trusted application and firewall paths succeed; application-untrusted and firewall-untrusted paths fail at their intended layer.
    - Missing/invalid configuration keeps the debugger Running but grants no remote or folder access.
    - Start, stop, graceful shutdown, delete, firewall cleanup, and fixture cleanup all complete without reboot.
    - DlpWindowsService state and all DLP enrollment/policy/health/configuration artifacts are unchanged.
    - An unavailable lab produces `01.3-03-BLOCKED.md`, no endpoint mutation, no summary, no approved checkpoint, and no claimed runtime pass; successful completion still requires every lab check.
    - Task 2 invokes the named read-only preflight first, advances only on exit 0 with all three deterministic machine checks passing, and records the stable LAB-CLIENT01/LAB-DC01/LAB-DC02 reason-code mapping on failure.
    - The installed baseline compares state/start mode exactly; the NotInstalled baseline compares the sentinel exactly and is not rewritten as an availability result.
    - The summary records only pass/fail codes and synthetic fixture hashes; it contains no credentials, real requested path, raw log content, or OS error detail.
  </acceptance_criteria>
  <done>Manual Windows verification proves the complete development-only lifecycle, layered remote access boundary, fail-closed fallback, cleanup, and zero DLP availability impact.</done>
  <resume-signal>Type "approved" only after all checks and cleanup pass; otherwise return "blocked" with the recorded stable reason code and leave runtime verification pending.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| administrator -> ProgramData config | A privileged local file controls remote IP and filesystem authorization. |
| lab network -> Windows Firewall -> service | Raw log text is exposed over HTTP only to deliberately scoped trusted lab sources. |
| manual elevated lifecycle -> endpoint | SCM, ACL, firewall, and fixture mutations must be exact, reversible, and isolated from DLP runtime state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.3-10 | Tampering / Elevation of Privilege | ProgramData config and allowed folders | high | mitigate | Runbook removes inherited access, grants modification only to SYSTEM/Administrators, requires trusted-writer-only log folders, and verifies ACLs before start. |
| T-01.3-11 | Spoofing / Information Disclosure | Windows Firewall rule | high | mitigate | Scope simultaneously to exact executable, TCP port, and resolved trusted RemoteAddress values; label the two-probe scope as temporary, open it only for application denial proof, narrow immediately, and verify firewall rejection afterward. |
| T-01.3-12 | Information Disclosure | HTTP lab traffic and evidence | high | mitigate | Isolated trusted lab only, demand-start service, immediate cleanup, synthetic fixtures, no secrets/protected content/raw errors, and no persistent response copies. |
| T-01.3-13 | Tampering | Manual elevated changes | medium | mitigate | Exact pre-approved privilege manifest, baseline DlpWindowsService state, reversible standard commands, named ownership, and post-test removal verification. |
| T-01.3-14 | Repudiation | Manual runtime verification | low | accept | A blocking human approval plus summary pass/fail codes is sufficient for this development-only tool; unavailable infrastructure instead creates a timestamped blocked handoff with `runtime_verification: not_run`, and no production audit integration is created. |
| T-01.3-SC | Tampering | Package installation | high | mitigate | This plan installs no package and uses the already locked release binary; any unexpected package request blocks execution pending a new legitimacy audit. |
</threat_model>

<verification>
1. Parse the config example and check every implemented field/default.
2. AST-parse every fenced PowerShell block, then validate the runbook includes exact manual SCM, ACL, firewall, request, fallback, baseline, and cleanup commands and contains no production provisioning path.
3. Run the three-machine read-only reachability preflight. If unavailable, emit only the blocked/not-run handoff and preserve the checkpoint; if available, complete the blocking LAB-CLIENT01 checklist from a trusted and untrusted lab source.
4. On a successful runtime pass, confirm all debugger-owned runtime artifacts are removed and the normalized DlpWindowsService installed/NotInstalled baseline is unchanged.
</verification>

<success_criteria>
- The manual runbook is sufficient to operate the debugger without an installer or automatic provisioning.
- ProgramData, allowed folders, and the firewall are explicitly protected at their real Windows boundaries.
- Trusted raw-tail access and both application/firewall denial paths are proven on LAB-CLIENT01.
- Invalid configuration is proven running and fail-closed.
- Cleanup is complete and DLP components remain unaffected; an unavailable lab remains explicitly blocked and does not satisfy these runtime criteria.
</success_criteria>

<output>
Create `.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-03-SUMMARY.md` only after the checkpoint is approved. If the reachability preflight blocks execution, create/update `01.3-03-BLOCKED.md`, return the blocked checkpoint handoff, and do not create the summary.
</output>
