---
phase: 01-first-encrypted-drive-vertical-slice
plan: 34
type: execute
wave: 22
depends_on:
  - 01-33
files_modified:
  - scripts/add-security-closure-review.ps1
  - scripts/verify-phase1-security.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
autonomous: false
gap_closure: true
requirements:
  - CRY-04
user_setup:
  - service: phase1-reviewer-pki
    why: "Signed-off validation requires controlled external reviewer trust and live revocation data."
    env_vars:
      - name: PHASE1_TRUSTED_ROOT_PATH
        source: "Set to evidence-private/phase1/reviewer-root.cer on hungdinh-lt."
      - name: PHASE1_REVIEWER_POLICY_PATH
        source: "Set to evidence-private/phase1/reviewer-policy.json on hungdinh-lt."
    dashboard_config:
      - task: "On LAB-DC01, sign in as LAB\\dlp-reviewer, confirm CRL or OCSP reachability, and confirm the approved CurrentUser signing certificate remains present with private key; derive SignerThumbprint from that store rather than accepting a copied value."
        location: "LAB-DC01 trusted provisioning station (D-22), authenticated LAB\\dlp-reviewer session"
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The exact complete payload and predecessor a reviewer affirms are unchanged when the signature is created and published."
    - "Signed-off validation accepts only a policy-authorized certificate with digital-signature key usage, the required EKU, a trusted chain, and a successful fail-closed revocation check."
    - "Manifest-controlled implementation and artifact references cannot resolve outside the repository."
    - "The security suite executes a valid-trust forgery, two concurrent publishers, an injected pre-replace crash, and a forced FinalGate security failure."
  artifacts:
    - path: scripts/add-security-closure-review.ps1
      provides: "Consent-to-signing continuity across the publication lock"
    - path: scripts/verify-phase1-security.ps1
      provides: "Certificate-purpose, revocation, and repository-containment enforcement"
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Executable adversarial regression coverage for all four verification gaps"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      provides: "Append-only record of the hardened closure result"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
      provides: "Append-only resolution evidence for CR-01 through CR-04"
  key_links:
    - from: scripts/add-security-closure-review.ps1
      to: evidence/phase1/security-closure.yaml
      via: "Approved canonical payload and predecessor digests must match the locked re-read before signing"
    - from: scripts/verify-phase1-security.ps1
      to: reviewer policy and trusted root inputs
      via: "Policy purpose, key usage, chain, and revocation are all mandatory in signed-off mode"
    - from: scripts/evidence/Phase1.Security.Tests.ps1
      to: scripts/verify-phase1.ps1
      via: "A spawned FinalGate process must propagate an actual failing security subgate"
---

<objective>
Close the four remaining Phase 1 trust-boundary gaps with fail-closed implementation and executable adversarial proof.

Purpose: Make the Phase 1 completion evidence trustworthy under concurrent mutation, certificate misuse or revocation, path traversal, publication interruption, and FinalGate failure, per D-32, D-37, D-40, D-41, D-43, D-48, and D-49.
Output: Hardened review capture and verification scripts, behavioral security regressions, and append-only closure records.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-UAT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-31-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-33-SUMMARY.md
@scripts/add-security-closure-review.ps1
@scripts/verify-phase1-security.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
@scripts/verify-phase1.ps1
</context>

## Artifacts this phase produces

- Modified function `Payload` and publication loop in `scripts/add-security-closure-review.ps1`: immutable approved payload/predecessor digest snapshots and locked drift rejection.
- Modified functions `Read-TrustInputs`, `Test-SignedAttestation`, and `Test-Record` in `scripts/verify-phase1-security.ps1`: certificate-purpose/revocation enforcement and repository-contained reference resolution.
- Modified helpers and behavioral cases in `scripts/evidence/Phase1.Security.Tests.ps1`: valid-trust forgery, consent drift, concurrent publication, crash preservation, certificate policy/revocation, path traversal, and FinalGate propagation.
- Append-only sections in `01-SECURITY.md` and `01-REVIEW.md`; no new production path or exported application symbol is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind reviewer consent to the locked payload and prove publication behavior</name>
  <files>scripts/add-security-closure-review.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/add-security-closure-review.ps1; scripts/evidence/Phase1.Security.Tests.ps1; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md</read_first>
  <behavior>
    - A protected-field or latest-predecessor change after affirmation but before the locked re-read exits nonzero with `publication_conflict:&lt;threat&gt;` and leaves the manifest byte-identical.
    - Two spawned publishers targeting the same copied manifest serialize through the named mutex; a stale publisher is rejected, one valid attestation is published, and the JSON remains parseable with an intact predecessor chain.
    - `CrashBeforeReplace` exits nonzero and preserves the original manifest bytes while removing temporary publication files.
  </behavior>
  <action>
    Before changing `Payload`, `Get-PublicationMutexName`, or the publication loop, run GitNexus upstream impact analysis for every indexed symbol; report direct callers, affected execution flows, and risk. If any result is HIGH or CRITICAL, warn the user and stop before editing until the warning is acknowledged. Add failing behavioral cases first. During preview, retain the canonical payload digest and latest predecessor digest for every affirmed threat. After acquiring the existing canonical-path mutex and re-reading the manifest, recompute both values and compare them with the approved snapshots before creating any signature. Reject drift with the stable per-threat publication-conflict diagnostic and perform no write. Keep exact `YES` review, CurrentUser private-key isolation, flush-through temporary generation, atomic replacement, and cleanup contracts from Plans 01-31 through 01-33. Replace source-presence publication assertions with spawned-process cases that use deterministic stdin, the existing barrier hook, bounded waits, unique temporary paths, captured exit/output, and cleanup in `finally`; exercise real two-publisher and injected-crash behavior without mutating checked-in evidence, per D-32/D-41/D-49. Before committing this task, run GitNexus `detect_changes()` and review the affected symbols and execution flows against `Payload`, `Get-PublicationMutexName`, the publication loop, and publication-focused test helpers; investigate unexpected scope before committing.
  </action>
  <acceptance_criteria>
    - The signed payload digest and predecessor are exactly the values displayed and affirmed.
    - Concurrent drift cannot publish an attestation and all rejected/crashed attempts preserve valid prior bytes.
    - The tests execute separate PowerShell publisher processes and assert outcomes and manifest content, not script text.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Publication</automated></verify>
  <done>SEC-01 and the publication portions of SEC-04 fail closed under real concurrent mutation and interruption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce reviewer certificate authorization, revocation, and repository containment</name>
  <files>scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1; evidence/phase1/security-closure.yaml; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md</read_first>
  <precondition>On hungdinh-lt, `evidence-private/phase1/reviewer-root.cer` and `evidence-private/phase1/reviewer-policy.json` exist and hash successfully, and the issuing PKI revocation endpoint is reachable; stop for the declared user setup if any check fails.</precondition>
  <behavior>
    - A cryptographically valid signer without digital-signature key usage or the policy-required EKU is rejected with a distinct stable diagnostic.
    - A revoked certificate and an indeterminate/offline revocation result are both rejected; only a demonstrably good revocation result can pass signed-off validation.
    - Rooted, parent-traversal, prefix-collision, and separator/case-normalization reference paths are rejected before existence or hashing outside the repository.
    - A recomputed attestation forgery tested with otherwise valid trust inputs reaches CMS validation and reports `signature_invalid`.
  </behavior>
  <action>
    Before changing `Read-TrustInputs`, `Test-SignedAttestation`, `Test-Record`, or helpers, run GitNexus upstream impact analysis for every indexed symbol and report the blast radius. Warn and stop before editing for HIGH or CRITICAL results. Extend signed-off trust-input validation to require the policy-declared EKU OID and digital-signature key usage, validate the signer certificate extensions, and build the chain with revocation enabled for the full non-root chain. Treat revoked, offline, unknown, or otherwise indeterminate revocation as failure with separate stable diagnostics; retain external-root anchoring, signer identity, review-time, procedure, and machine-role checks. Add a single path resolver that rejects rooted child values, computes the full candidate path, normalizes the repository root with a trailing directory separator, performs Windows-appropriate containment comparison, and returns a path only when it is strictly under the repository root. Use it for every implementation and artifact reference before existence and digest checks, per D-37/D-39/D-43. Build deterministic ephemeral PKI/trust-policy fixtures for negative certificate-purpose and revocation cases, and use the canonical valid external trust inputs for the recomputed-forgery oracle. Behavioral tests must assert the expected diagnostic rather than only a nonzero exit. Before committing this task, run GitNexus `detect_changes()` and review affected symbols and flows against `Read-TrustInputs`, `Test-SignedAttestation`, `Test-Record`, the containment helper, and verifier-focused tests; investigate unexpected scope before committing.
  </action>
  <acceptance_criteria>
    - Signed-off validation cannot accept a certificate with the wrong purpose, missing digital-signature usage, revoked status, or unavailable revocation evidence.
    - No manifest-controlled reference can cause a read or hash outside the normalized repository root.
    - The forgery regression uses valid trust inputs and proves CMS signature rejection.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Verifier</automated></verify>
  <done>SEC-02, SEC-03, and the forgery portion of SEC-04 are covered by fail-closed validation and behavioral tests.</done>
</task>

<task type="auto">
  <name>Task 3: Prove FinalGate propagation and append the closure audit</name>
  <files>scripts/evidence/Phase1.Security.Tests.ps1; .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md</files>
  <read_first>scripts/evidence/Phase1.Security.Tests.ps1; scripts/verify-phase1.ps1; scripts/verify-phase1-security.ps1; .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md</read_first>
  <action>
    Run GitNexus impact analysis before changing any indexed test helper and apply the same HIGH/CRITICAL warning gate. Preflight the two exact user-setup files and live revocation reachability before changing evidence; do not substitute generated trust for signed-off validation. Replace the FinalGate source assertion with a spawned canonical `scripts/verify-phase1.ps1` regression that holds all non-security inputs valid while supplying a deterministic failing security fixture/subgate, asserts a nonzero FinalGate exit and the security diagnostic, then runs the successful path and asserts its manifest digest and reviewer-policy identity equal the security subgate result. Run the complete security suite and signed-off verifier on `hungdinh-lt` against exported public-only inputs `evidence-private/phase1/reviewer-root.cer` and `evidence-private/phase1/reviewer-policy.json`. If any existing attestation fails solely because the hardened EKU, key-usage, chain, or revocation contract is newly enforced, record the exact affected IDs from verifier diagnostics and stop for a separate signing ceremony on the D-22 trusted provisioning station: sign in interactively to `LAB-DC01` as `LAB\\dlp-reviewer`; set `$signerThumbprint = (Get-ChildItem Cert:\\CurrentUser\\My | Where-Object { $_.HasPrivateKey -and (($_.Thumbprint -replace '\\s','') -eq 'E5AC839BE9C7F8800941B81E73A2AB3EF07C5CF7') } | Select-Object -ExpandProperty Thumbprint -First 1)` and fail if it is empty; set `$affectedThreatIds` to the explicit verifier-reported subset of `@('T-01-15-01','T-01-15-02','T-01-15-03','T-01-15-04','T-01-15-06','T-01-16-01','T-01-16-02','T-01-16-03','T-01-16-04','T-01-16-06','T-01-18-SC','T-01-20-01','T-01-20-02','T-01-20-05','T-01-21-01','T-01-21-02','T-01-21-03','T-01-21-04','T-01-21-05')`; then execute `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/add-security-closure-review.ps1 -ClosurePath evidence/phase1/security-closure.yaml -ThreatId $affectedThreatIds -ReviewerPolicyPath evidence-private/phase1/reviewer-policy.json -SignerThumbprint $signerThumbprint`. Require exact `YES` affirmation for each affected payload, preserve superseded signatures, and link the remediation commit. Do not execute signing on `hungdinh-lt`; do not copy, export, or import the reviewer private key there, and do not add/import reviewer PKI trust-store entries there, per D-20/D-22/D-36. Export only the public root certificate and public reviewer-policy file for verification on `hungdinh-lt`, then rerun signed-off verification before FinalGate. Never weaken policy or reuse a signature over a changed payload to avoid re-signing. Run canonical Phase 1 FinalGate on `hungdinh-lt` with the same literal public-input paths. Append—not replace—dated evidence to `01-SECURITY.md` and resolutions for CR-01 through CR-04 to `01-REVIEW.md`, including exact commands, diagnostic-specific regression names, current manifest/policy identities, counts, any conditional re-signing interval, and the remediation commit, while preserving failed and superseded intervals per D-40/D-41/D-48/D-50. Before committing this task, run GitNexus `detect_changes()` and confirm only the expected test helpers, audit documents, and security/FinalGate execution flows are affected; investigate unexpected scope before committing.
  </action>
  <acceptance_criteria>
    - A behavioral regression proves canonical FinalGate fails solely because its security subgate fails.
    - The successful signed-off verifier and FinalGate report identical current manifest and policy identities.
    - The security and review records preserve history and cite evidence for all four resolved blockers.
    - GitNexus change detection reports only expected affected symbols and flows before commit.
  </acceptance_criteria>
  <verify><automated>if (!(Test-Path -LiteralPath 'evidence-private/phase1/reviewer-root.cer') -or !(Test-Path -LiteralPath 'evidence-private/phase1/reviewer-policy.json')) { throw 'phase1_reviewer_trust_setup_missing' }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -TrustedRootPath evidence-private/phase1/reviewer-root.cer -ReviewerPolicyPath evidence-private/phase1/reviewer-policy.json -RequireSignedOff; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -TrustedRootPath evidence-private/phase1/reviewer-root.cer -ReviewerPolicyPath evidence-private/phase1/reviewer-policy.json</automated></verify>
  <done>SEC-04 is behaviorally proven and the append-only audit truthfully records closure of CR-01 through CR-04.</done>
</task>

</tasks>

<threat_model>
This plan enforces the OWASP ASVS Level 1 baseline. Applicable controls are V2 certificate-backed reviewer authentication, V5 canonicalization and path containment, V7 fail-closed signature/chain/revocation validation and safe diagnostics, and V14 evidence-publication integrity. No high-severity threat may remain accepted, untested, or open: each high item below blocks closure until its mitigation's behavioral regression passes, signed-off verification succeeds with demonstrably good revocation status, and GitNexus change detection confirms expected scope.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Human affirmation -> locked manifest -> CMS signature | Untrusted concurrent changes must not alter the payload or predecessor after review. |
| External reviewer policy/PKI -> signed-off status | Certificate purpose and current revocation state determine whether a signer remains authorized. |
| Closure manifest -> repository filesystem | Manifest-controlled paths must never select files outside the repository. |
| Security subgate -> canonical FinalGate | FinalGate must propagate a real subgate failure and bind success to identical trust identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-34-01 | Tampering | review capture publication | high | mitigate | Compare affirmed canonical payload and predecessor digests under the publication mutex before signing or writing. |
| T-01-34-02 | Spoofing | reviewer certificate | high | mitigate | Enforce policy EKU, digital-signature usage, external-root chain, and fail-closed full-chain revocation. |
| T-01-34-03 | Information Disclosure | artifact reference resolver | high | mitigate | Reject rooted/traversing inputs and enforce normalized repository containment before filesystem access. |
| T-01-34-04 | Repudiation | regression and closure evidence | high | mitigate | Execute attacks in child processes, assert stable diagnostics/content invariants, and append exact evidence without rewriting history. |
| T-01-34-05 | Tampering | security subgate propagation | high | mitigate | Execute a deterministic failing subgate through canonical FinalGate and assert nonzero propagation. |
</threat_model>

## Source coverage audit

| Source | ID | Gap/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Trustworthy encrypted-drive completion evidence | 01-34 | COVERED | Functional drive truths remain verified; this plan restores the blocked phase-trust truth. |
| REQ | CRY-04 | Closure/integrity remains trustworthy | 01-34 | COVERED | All four verification blockers affect CRY-04. |
| REQ | remaining Phase 01 IDs | Previously verified functional scope | 01-01 through 01-33 | COVERED | No functional regression or reopened requirement is reported. |
| RESEARCH | — | Signed evidence is fail-closed and auditable | 01-34 | COVERED | Consent, trust, containment, and behavioral proof are hardened. |
| CONTEXT | D-32/D-37/D-39/D-40/D-41/D-43/D-48/D-49/D-50 | Real fixtures, structured evidence, containment, staleness, append-only audit, independent closure, and retention | 01-34 | COVERED | Task actions cite and implement the applicable locked decisions. |

<verification>
- The complete `Phase1.Security.Tests.ps1` suite executes every claimed attack/invariant and passes.
- Signed-off security verification passes only with the approved external trust inputs and a currently authorized, non-revoked signer.
- Canonical FinalGate fails under an injected security-subgate failure and passes with the same manifest/policy identities as the successful subgate.
- `gitnexus detect_changes()` identifies only the planned security scripts, audit documents, symbols, and flows before commit.
- ASVS Level 1 applicable controls are evidenced, and every high-severity threat is mitigated and verified; any open or unverifiable high-severity item blocks closure.
</verification>

<success_criteria>
- All four actionable gaps in `01-VERIFICATION.md` are closed without rewriting executed plans or accepted UAT evidence.
- Phase trust cannot be forged by consent drift, certificate misuse/revocation blindness, path traversal, false-positive tests, or FinalGate bypass.
- Historical failed and superseded evidence remains append-only and auditable.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-34-SUMMARY.md` when done.
</output>
