---
phase: 01.3-make-a-background-app-that-run-in-devices-to-collect-log-and
plan: 02
type: execute
wave: 2
depends_on:
  - 01.3-01
files_modified:
  - crates/dlp-log-debug-service/src/lib.rs
  - crates/dlp-log-debug-service/src/http.rs
  - crates/dlp-log-debug-service/src/service.rs
  - crates/dlp-log-debug-service/src/main.rs
  - crates/dlp-log-debug-service/tests/endpoint_contract.rs
autonomous: true
requirements:
  - TBD

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A trusted remote lab IP can request an explicitly authorized log while a non-loopback IP absent from the active allowlist receives 403 `untrusted_client` before any path or file operation."
    - "The listener accepts traffic on all IPv4 interfaces at the configured port while using only the accepted TCP peer address as the client identity."
    - "Every valid response is raw plain text and every unsuccessful request is a standard HTTP status with one short stable text code and no filesystem or OS detail."
    - "Windows SCM can start, stop, and shut down the independent debugger gracefully, and invalid configuration leaves it running in localhost-only mode."
    - "Debugger failure, stop, absence, or bind failure does not call, stop, configure, or change any DLP component."
  prohibitions:
    - "The HTTP boundary must not trust X-Forwarded-For, Forwarded, certificate headers, cookies, bearer credentials, or DLP device credentials."
    - "Responses and service diagnostics must not include requested paths, allowed-folder lists, log content, raw I/O errors, or diagnostic metadata headers."
    - "The SCM service must not register for enrollment, policy, health, session companion, management-server, or production deployment behavior."
  artifacts:
    - path: "crates/dlp-log-debug-service/src/http.rs"
      provides: "Peer-authorized Axum router and stable raw-text response contract"
      contains: "AppState, LogQuery, HttpError, build_router, serve_http"
    - path: "crates/dlp-log-debug-service/src/service.rs"
      provides: "Independent Windows SCM state machine, listener ownership, and graceful cancellation"
      contains: "SERVICE_NAME, ServiceError, run_scm_service, service_log"
    - path: "crates/dlp-log-debug-service/src/main.rs"
      provides: "Thin Windows service dispatcher entry"
      contains: "run_scm_service"
    - path: "crates/dlp-log-debug-service/tests/endpoint_contract.rs"
      provides: "HTTP peer, query, status, body, and redaction contract tests"
      contains: "untrusted_client, invalid_tail, file_not_found"
  key_links:
    - from: "crates/dlp-log-debug-service/src/service.rs"
      to: "crates/dlp-log-debug-service/src/http.rs"
      via: "pre-bound all-interface TcpListener passed to serve_http with a shutdown signal"
      pattern: "TcpListener.*serve_http"
    - from: "crates/dlp-log-debug-service/src/http.rs"
      to: "crates/dlp-log-debug-service/src/paths.rs"
      via: "peer authorization and query validation precede authorize_requested_file"
      pattern: "authorize_peer.*authorize_requested_file"
    - from: "crates/dlp-log-debug-service/src/http.rs"
      to: "crates/dlp-log-debug-service/src/tail.rs"
      via: "authorized canonical path and validated tail count feed read_bounded_tail"
      pattern: "read_bounded_tail"
---

<objective>
Expand the proven local tracer into the complete remote HTTP boundary and independent Windows SCM lifecycle per D-01, D-04, D-06, D-07, D-09, and D-14 through D-17.

Purpose: Make the development debugger remotely useful in the lab while closing the peer-identity, error-redaction, listener lifecycle, and DLP-isolation failure modes.

Output: A peer-aware Axum module, full endpoint status/body contract, an independent gracefully stoppable SCM service, and expanded contract tests.

Requirement probe: visible skip because ROADMAP.md assigns no Phase 01.3 requirement IDs; `requirements: [TBD]` is retained and all acceptance predicates are source-derived.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-CONTEXT.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-RESEARCH.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-PATTERNS.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/COVERAGE.md
@.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-01-SUMMARY.md
@crates/dlp-windows-service/src/main.rs
@crates/dlp-windows-service/src/service.rs
@crates/dlp-server/src/routes.rs

<interfaces>
Existing SCM pattern from `dlp-windows-service`:
- `define_windows_service!(ffi_service_main, service_main)`
- `service_control_handler::register(service_name, handler)` handles `Stop` and `Shutdown`.
- Service states progress `StartPending -> Running -> Stopped`; `service_dispatcher::start` owns process dispatch.

Existing Axum pattern from `dlp-server`:
- `Router::new().route(path, get(handler)).with_state(state)`
- `ConnectInfo(connection)` supplies transport-owned connection metadata; request headers are not identity.

Plan 01 contracts consumed here:
- `RuntimeConfig { access_mode, authorized_folders, port, max_response_bytes }`
- `AccessMode::{RemoteAllowlist, LocalhostOnly}`
- `authorize_requested_file(...)`
- `read_bounded_tail(...)`

New interfaces created here:
- `AppState { access_mode, authorized_folders, max_response_bytes }`
- `LogQuery { path, tail }`
- `HttpError::{InvalidTail, InvalidPath, UntrustedClient, ForbiddenPath, FileNotFound, ReadFailed, RouteNotFound, MethodNotAllowed}`
- `build_router(state) -> Router`
- `serve_http(listener, state, shutdown) -> Result<(), ServiceError>`
- `run_scm_service() -> Result<(), windows_service::Error>`
</interfaces>
</context>

## Artifacts this phase produces

| Kind | File / symbol / contract | Owner |
|---|---|---|
| Workspace | `Cargo.toml`, `Cargo.lock`, `crates/dlp-log-debug-service/Cargo.toml` | Plan 01 |
| Configuration | `config.rs`: `FileConfig`, `RuntimeConfig`, `AccessMode`, `ConfigError`, `load_runtime_config`; fields `version`, `trusted_client_ips`, `allowed_folders`, `port`, `max_response_bytes` | Plan 01 |
| Filesystem authorization | `paths.rs`: `AuthorizedFolders`, `PathAuthorizationError`, `authorize_requested_file` | Plan 01 |
| Tail snapshot | `tail.rs`: `TailReadError`, `read_bounded_tail` | Plan 01 |
| HTTP | `http.rs`: `AppState`, `LogQuery`, `HttpError`, `build_router`, `serve_http`; endpoint `GET /logs?path=<absolute-path>&tail=<line-count>` | Plan 02 |
| Windows service | `service.rs` and `main.rs`: `SERVICE_NAME`, `DEFAULT_CONFIG_PATH`, `DEFAULT_PORT`, `DEFAULT_MAX_RESPONSE_BYTES`, `ServiceError`, `run_scm_service`, `service_log` | Plan 02 |
| Configuration example | `config.example.json` with the five configuration fields | Plan 03 |
| Operator documentation | `.planning/docs/DLP-LOG-DEBUG-SERVICE.md` and a link from `.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md` | Plan 03 |
| Flags | None; the SCM service uses the documented default configuration path and file-owned runtime settings | Phase contract |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authorize TCP peers and complete the raw-text HTTP contract</name>
  <read_first>
    - crates/dlp-log-debug-service/src/lib.rs
    - crates/dlp-log-debug-service/src/config.rs
    - crates/dlp-log-debug-service/src/paths.rs
    - crates/dlp-log-debug-service/src/tail.rs
    - crates/dlp-log-debug-service/tests/endpoint_contract.rs
    - crates/dlp-server/src/routes.rs
    - .planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-CONTEXT.md
  </read_first>
  <reversibility rating="costly">The status/code map and raw-text endpoint shape are client-visible lab contracts; undo requires coordinated handler, tests, examples, and runbook changes.</reversibility>
  <files>crates/dlp-log-debug-service/src/http.rs, crates/dlp-log-debug-service/src/lib.rs, crates/dlp-log-debug-service/tests/endpoint_contract.rs</files>
  <behavior>
    - Peer cases: every loopback address is accepted; a remote exact allowlist match is accepted only in `RemoteAllowlist`; every other remote peer is rejected as 403 `untrusted_client` before query/path processing.
    - Identity cases: conflicting `X-Forwarded-For`, `Forwarded`, or certificate-style headers never change the transport peer decision.
    - Query cases: missing/empty/non-absolute `path`, missing/non-numeric/zero/overflow `tail`, unknown route, and wrong method map to fixed 400/404/405 text codes.
    - File cases: missing/canonicalization failure maps to 404 `file_not_found`; existing unauthorized target maps to 403 `forbidden_path`; read/encoding failure maps to 500 `read_failed` without underlying detail.
    - Success case: 200 contains only the selected source text with `text/plain; charset=utf-8` and no JSON or custom diagnostic headers per D-15 and D-16.
  </behavior>
  <action>
    Extract the listener/controller boundary into `http.rs` and keep `lib.rs` as explicit module exports. Build exactly `GET /logs` per D-14, with typed state and query handling that allows custom rejection mapping. Obtain the caller only from Axum `ConnectInfo<SocketAddr>` created by the accepted TCP connection. Authorize the peer before parsing or canonicalizing the requested path: loopback is always eligible; remote peers require `AccessMode::RemoteAllowlist` and exact `IpAddr` membership per D-07 and D-09. Ignore all forwarding, certificate, cookie, bearer, and DLP credential inputs.

    Implement one centralized `HttpError -> (StatusCode, &'static str)` map: `invalid_tail` and `invalid_path` at 400, `untrusted_client` and `forbidden_path` at 403, `file_not_found` and `route_not_found` at 404, `method_not_allowed` at 405, and `read_failed` at 500 per D-17. Do not format source errors. A successful response is the source `String` only; allow standard transport/content headers but add no path, truncation, cap, line-count, or diagnostic metadata header per D-15 and D-16.

    Expand `endpoint_contract.rs` before implementation with the peer, header-spoofing, query, status/body, route/method, and error-redaction matrix. Exercise the actual listener for loopback routing and the public peer-authorization/handler seam with explicit non-loopback `SocketAddr` values for deterministic remote cases; add no unaudited test dependency. Assert authorization ordering with an unreadable or nonexistent path so an untrusted peer cannot use errors as a filesystem oracle.
  </action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-log-debug-service --test endpoint_contract</automated>
  </verify>
  <acceptance_criteria>
    - A deterministic non-loopback trusted peer succeeds only in remote mode, and an absent peer receives exactly 403 `untrusted_client` before file inspection.
    - Spoofed forwarding/certificate headers cannot turn an untrusted transport peer into a trusted one.
    - All invalid and unsuccessful requests in the behavior matrix have the documented standard status and one stable plain-text code with no raw detail.
    - A successful body is byte-for-byte source text selected by Plan 01, with no JSON envelope, marker, or custom diagnostic metadata header.
  </acceptance_criteria>
  <done>The complete D-07 and D-14 through D-17 HTTP surface is deterministic, peer-derived, redacted, and covered without adding an external authentication or DLP identity dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the all-interface listener as an isolated gracefully stoppable Windows service</name>
  <read_first>
    - crates/dlp-log-debug-service/src/http.rs
    - crates/dlp-log-debug-service/src/config.rs
    - crates/dlp-log-debug-service/src/main.rs
    - crates/dlp-windows-service/src/main.rs
    - crates/dlp-windows-service/src/service.rs
    - .planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-PATTERNS.md
  </read_first>
  <reversibility rating="costly">Renaming `DlpLogDebugService` or relocating its ProgramData config/log paths requires service recreation plus coordinated firewall, config, test, and documentation changes.</reversibility>
  <files>crates/dlp-log-debug-service/src/service.rs, crates/dlp-log-debug-service/src/main.rs, crates/dlp-log-debug-service/src/lib.rs</files>
  <behavior>
    - Startup state: SCM reports StartPending, loads valid or fallback config, binds `0.0.0.0:<port>`, then reports Running only after the socket is owned.
    - Fallback state: a missing/invalid/empty-trust config still reaches Running on default port 9191 with `LocalhostOnly` and no authorized folders per D-09.
    - Stop state: Stop or Shutdown signals Axum graceful shutdown, awaits listener completion, shuts down the Tokio runtime, then reports Stopped.
    - Failure state: runtime or bind failure reports Stopped with nonzero service exit and a stable lifecycle code; it never calls or changes DlpWindowsService.
    - Logging state: lifecycle diagnostics contain stable codes only and never peer IPs, request paths, log text, allowed folders, or raw OS errors.
  </behavior>
  <action>
    Move the SCM and runtime adapter to `service.rs` using the existing `windows-service` pattern without importing the existing DLP service. Define `SERVICE_NAME = "DlpLogDebugService"`, default config path `C:\ProgramData\DlpLogDebugService\config.json`, default port `9191`, and default cap `262144`. Register only Stop and Shutdown controls. Report StartPending while creating the Tokio runtime, loading a valid or fail-closed config, and binding `0.0.0.0:<selected-port>` per D-01, D-06, and D-09. Report Running only after bind succeeds.

    Bridge SCM controls to Axum graceful shutdown with a Tokio synchronization primitive, await the serve future, then report Stopped. Bind/runtime/serve failures must end only this service with a stable `ServiceError` code and a nonzero SCM exit; they must not query, signal, share a runtime with, or otherwise affect DlpWindowsService per D-04. Keep `main.rs` as dispatcher-only glue. The crate remains development-only and is not added to agent/server composition, enrollment, health, policy, session handling, Docker, installer, or lab provisioning per D-02 and D-05.

    Add testable pure status/error mapping and an async graceful-shutdown listener test before completing the Windows adapter. Implement `service_log` only if needed for startup diagnosis, writing stable lifecycle codes to this service's own ProgramData log; never include formatted source errors, remote addresses, request data, paths, folder configuration, or returned content. Keep workspace unsafe-code prohibition intact.
  </action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-log-debug-service service_ &amp;&amp; rtk cargo check --locked -p dlp-log-debug-service --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - Windows compilation includes a `DlpLogDebugService` dispatcher/control handler distinct from `DlpWindowsService` and accepts Stop/Shutdown.
    - The service owns one all-interface listener, reaches Running after bind, and exits only after graceful listener shutdown.
    - Missing, malformed, and empty-trust configuration reach Running in fail-closed localhost-only mode rather than opening remote/file access or terminating.
    - The new crate has no product-crate dependency and no source, script, package, or runtime registration change to a DLP component.
    - Lifecycle logs expose stable codes only and never reveal path/content/raw-error values.
  </acceptance_criteria>
  <done>The complete debugger runs as its own all-interface Windows SCM service, stops gracefully, fails independently, and preserves every D-01, D-02, D-04, D-05, D-06, and D-09 isolation boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| remote TCP peer -> HTTP handler | An unauthenticated lab network source requests privileged local log content. |
| HTTP failure -> client | Internal path, file, and OS failures could disclose endpoint details through status/body/headers. |
| Windows SCM -> Tokio/Axum runtime | Synchronous service controls must stop an asynchronous network listener without hanging or leaking availability into DLP services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.3-05 | Spoofing | `http.rs` peer authorization | high | mitigate | Use accepted TCP `SocketAddr` only, ignore forwarding/auth headers, exact-match `IpAddr`, and combine with Plan 03 program/port/RemoteAddress firewall scope. |
| T-01.3-06 | Information Disclosure | raw HTTP log response over the lab network | high | mitigate | Restrict to exact application allowlist plus Windows Firewall on an isolated trusted lab, expose only while manually debugging, and stop/remove after use; identity authentication remains excluded by D-07. |
| T-01.3-07 | Information Disclosure | HTTP errors and service diagnostics | high | mitigate | Central fixed code map, no raw source formatting, no path/folder/content logging, and explicit redaction tests. |
| T-01.3-08 | Denial of Service | SCM stop and Axum listener | medium | mitigate | Stop/Shutdown cancellation, graceful serve drain, awaited task completion, and nonzero fail-fast bind/runtime exit. |
| T-01.3-09 | Elevation of Privilege | debugger/DLP process boundary | high | mitigate | Dedicated crate/process/service name with no product-crate dependency, IPC, credentials, enrollment, management registration, or shared runtime. |
| T-01.3-SC | Tampering | Cargo packages | high | mitigate | No package family is added in this plan; build remains locked to the Plan 01 audited manifest and Cargo.lock. |
</threat_model>

<verification>
1. `rtk cargo test --locked -p dlp-log-debug-service` passes all tracer, endpoint, service, path, tail, and config tests.
2. `rtk cargo clippy --locked -p dlp-log-debug-service --all-targets -- -D warnings` passes.
3. `rtk cargo tree --locked -p dlp-log-debug-service --depth 1` confirms no DLP product-crate dependency.
4. `rtk cargo test --locked -p dlp-log-debug-service dependency_isolation_contract` proves the manifest and production module graph contain no DLP product dependency or integration seam.
</verification>

<success_criteria>
- Trusted and untrusted remote peers receive the exact authorized/denied behavior from transport-owned peer identity.
- The endpoint implements only the locked GET route and raw-text success/fixed-error contract.
- Windows SCM lifecycle binds all interfaces, reports accurate state, and stops the listener gracefully.
- Invalid configuration remains fail-closed and available for loopback diagnosis.
- Failure or absence of the debugger has no code or runtime effect on any DLP component.
</success_criteria>

<output>
Create `.planning/phases/01.3-make-a-background-app-that-run-in-devices-to-collect-log-and/01.3-02-SUMMARY.md` when done.
</output>
