---
phase: 02-policy-enforcement-and-user-feedback
plan: 03
type: tdd
wave: 3
depends_on:
  - 02-02
files_modified:
  - Cargo.lock
  - crates/dlp-protocol/src/lib.rs
  - crates/dlp-crypto/src/lib.rs
  - crates/dlp-agent-core/Cargo.toml
  - crates/dlp-agent-core/src/config_cache.rs
  - crates/dlp-agent-core/tests/policy_activation.rs
  - crates/dlp-server/src/repository.rs
  - crates/dlp-server/src/routes.rs
  - crates/dlp-server/tests/policy_distribution.rs
  - crates/dlp-windows-service/src/pipe.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/session.rs
  - crates/dlp-windows-service/tests/session_lifecycle.rs
  - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs
autonomous: true
requirements:
  - SRV-06
  - SRV-07
  - POL-07
  - CRY-05
  - AGT-10
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An authenticated device poll materializes its selected immutable policy into a canonical signed bundle; the endpoint verifies schema, audience, digest, key ID, signature, monotonic bundle version, detector ceilings, and unsupported actions before atomically replacing current/LKG (SRV-06, SRV-07)."
    - "An invalid, older, partially written, over-budget, require_justification, wrong-audience, or unknown-key bundle leaves both the current evaluator snapshot and LKG usable after process/service/machine restart (AGT-10 unclassified; POL-07)."
    - "A trusted old key alone can authorize a transition introducing a unique new key ID; unknown/self-introduced keys are rejected and equal/repeated transitions are idempotent without rewriting selected pointers (CRY-05 empty/idempotency)."
    - "During overlap both explicitly listed key IDs verify until the exact minimum of transition not-after bundle version and not-after time; equality at the bound is documented and tested, and one step beyond is rejected without rounding (CRY-05 adjacency/ordering)."
    - "Concurrent activation of valid versions selects the greatest authorized bundle and never cross-links payload, signature, content hash, compiled evaluator, or keyring generation (CRY-05 concurrency; AGT-10 restart)."
    - "The production Windows service delivers the verified current/LKG policy snapshot through the authenticated actor bootstrap, pushes later versioned updates through the retained control channel, and the real dlp-drive-host atomically replaces the evaluator used by its mounted DlpFileSystemContext; a missing, stale, malformed, or unacknowledged snapshot leaves the prior evaluator active or the mount fail-closed (D-04, D-09, D-10, D-12)."
    - "[FLAGGED ASSUMPTION A-ROTATE] Key retirement is governed by both a monotonic bundle-version bound and an epoch-seconds not-after bound; endpoints that miss the overlap require re-enrollment as ADR-005 specifies."
  artifacts:
    - path: "crates/dlp-protocol/src/lib.rs"
      provides: "Canonical ConfigurationBundleV2 and signed key-transition wire contracts"
      exports: ["ConfigurationBundleV2", "SigningKeyTransitionV1", "DeploymentStatusV1"]
    - path: "crates/dlp-agent-core/src/config_cache.rs"
      provides: "Verify/materialize/activate transaction with current/LKG and bounded verification keyring"
    - path: "crates/dlp-agent-core/tests/policy_activation.rs"
      provides: "Activation, key-rotation, concurrency, rollback, and restart contract tests"
    - path: "crates/dlp-server/tests/policy_distribution.rs"
      provides: "Admin publish/assign -> device poll -> endpoint activation -> evaluator decision integration"
    - path: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      provides: "Production actor bootstrap/control consumer that installs and hot-swaps the service-delivered evaluator snapshot"
  key_links:
    - from: "crates/dlp-server/src/routes.rs"
      to: "crates/dlp-protocol/src/lib.rs"
      via: "selected policy is encoded canonically and signed only after transactional materialization"
      pattern: "ConfigurationBundleV2|SignedConfiguration"
    - from: "crates/dlp-agent-core/src/config_cache.rs"
      to: "crates/dlp-policy/src/lib.rs"
      via: "verified payload is compiled under endpoint ceilings before current pointer swap"
      pattern: "compile|materialize.*activate"
    - from: "crates/dlp-agent-core/src/config_cache.rs"
      to: "crates/dlp-crypto/src/lib.rs"
      via: "already-trusted key verifies bounded key transition before keyring mutation"
      pattern: "key_transition|key_id|verify"
    - from: "crates/dlp-windows-service/src/service.rs"
      to: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      via: "current/LKG ActivePolicySnapshotV1 travels through StorageBootstrap and acknowledged PolicySnapshotUpdateV1 frames on the authenticated actor channel"
      pattern: "StorageBootstrap|PolicySnapshotUpdate|policy_version|policy_digest"
    - from: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      to: "crates/dlp-windows-drive/src/filesystem.rs"
      via: "validated snapshot materializes and atomically replaces the evaluator handle shared by the mounted DlpFileSystemContext"
      pattern: "DlpFileSystemContext|active.*policy|evaluator"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Configuration/key rotation must not silently strand an authorized endpoint without preserving its last known good policy or producing a stable recovery signal."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization’s DLP boundary.

<objective>
Carry selected immutable policies through canonical signed distribution, bounded endpoint materialization, key rotation, and atomic current/LKG activation.

Purpose: Server policy authority has no enforcement effect until an authenticated endpoint can safely activate the exact assigned evaluator snapshot and recover it across restarts.
Output: Bundle V2/key-transition protocols, server materialization, bounded keyring activation, restart recovery, and full publish-to-evaluate integration tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-RESEARCH.md
@.planning/docs/adrs/ADR-005-policy-signing.md
@crates/dlp-protocol/src/lib.rs
@crates/dlp-crypto/src/lib.rs
@crates/dlp-agent-core/src/config_cache.rs
@crates/dlp-server/src/repository.rs
@crates/dlp-server/src/routes.rs
@crates/dlp-windows-service/src/pipe.rs
@crates/dlp-windows-service/src/service.rs
@crates/dlp-windows-service/src/session.rs
@crates/dlp-windows-drive/src/bin/dlp-drive-host.rs

<interfaces>
Preserve `ConfigurationEnvelopeV1` compatibility and the `ConfigurationCache::stage_verify_activate` content-addressed current/LKG transaction. Plan 02 supplies immutable selected policy/version and per-device cursor; Plan 01 supplies canonical policy compilation and ceilings. The live production mount path is `run_scm_service -> SessionMonitor::session_logon -> StorageBootstrap -> dlp-drive-host -> DlpFileSystemContext`; extend that path and its retained authenticated control channel rather than relying on an injected filesystem port.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Activate one selected policy bundle without weakening current/LKG</name>
  <reversibility rating="costly">The versioned signed payload and activation transaction are shared persisted/wire contracts; incompatible changes require explicit schema migration.</reversibility>
  <read_first>
    - 02-CONTEXT.md — D-04 next-poll activation and D-09/D-12 endpoint validation.
    - 02-RESEARCH.md — desired assignment to monotonic bundle and activation pitfalls.
    - ADR-005-policy-signing.md — canonical signing scope, key IDs, rotation authorization.
    - crates/dlp-protocol/src/lib.rs — existing envelope canonical bytes and bounds.
    - crates/dlp-agent-core/src/config_cache.rs — current content-addressed stage/verify/current/LKG flow.
    - crates/dlp-agent-core/Cargo.toml — current crate-local exact-pin convention for materializing CompiledPolicyV2.
    - crates/dlp-server/src/repository.rs and routes.rs — Plan 02 selected policy/cursor contracts.
  </read_first>
  <files>Cargo.lock, crates/dlp-protocol/src/lib.rs, crates/dlp-agent-core/Cargo.toml, crates/dlp-agent-core/src/config_cache.rs, crates/dlp-agent-core/tests/policy_activation.rs, crates/dlp-server/src/repository.rs, crates/dlp-server/src/routes.rs</files>
  <behavior>
    - RED: publish plus explicit assignment, authenticated poll, canonical signature, endpoint verification/materialization, and atomic activation produce the assigned evaluator decision.
    - RED: publish without assignment leaves the endpoint selection unchanged per D-04.
    - RED: invalid/older/partial/over-budget/unsupported bundles preserve current and LKG and return stable nonsecret errors.
    - RED: restart loads exactly the selected compiled-policy identity before polling and cleans incomplete staging without selecting it.
  </behavior>
  <action>Write failing activation and server-materialization tests first. Add `dlp-policy = { path = "../dlp-policy", version = "=0.1.0" }` to `crates/dlp-agent-core/Cargo.toml` and refresh `Cargo.lock` using the repository's crate-local exact-pin convention. Define a bounded `ConfigurationBundleV2` that includes every SRV-06 field plus device audience, policy digest/version, signing key ID, and canonical detector definitions; reject unknown/trailing fields. In the Plan 02 row-locked device transaction, sign and persist immutable bytes only when desired content differs. On endpoint, verify envelope/schema/audience/version/digest/key before materializing `CompiledPolicyV2` under Plan 01 hard ceilings; only after successful materialization perform the existing atomic current/LKG pointer transition. D-09 invalid actions and resource failures must preserve the selected evaluator. Load the compiled/current identity during restart before the next poll. Use exact-symbol GitNexus upstream impact and the HIGH/CRITICAL/UNKNOWN handling from Plan 01 before edits, and complete nonpartial detect-changes before each TDD commit.</action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-agent-core --test policy_activation activation_tracer --quiet &amp;&amp; rtk cargo test --locked -p dlp-server --test policy_lifecycle policy_bundle_contract --quiet</automated>
  </verify>
  <acceptance_criteria>One explicitly assigned policy travels from PostgreSQL selection through canonical signing and endpoint verification/materialization into an atomic current/LKG evaluator; publication alone and every invalid bundle leave current/LKG unchanged across restart.</acceptance_criteria>
  <done>The assigned immutable server policy becomes a verified materialized endpoint evaluator on the next successful poll, with safe rollback and restart recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver current/LKG into the real mounted drive host and hot-update it</name>
  <read_first>
    - 02-CONTEXT.md — D-04 next-poll activation, D-09/D-12 validation, and D-10 audit-only process/ETW signals.
    - crates/dlp-agent-core/src/config_cache.rs — Task 1 materialized current/LKG policy identity and snapshot source.
    - crates/dlp-windows-service/src/service.rs — ServiceContext cache ownership and run_scm_service construction of SessionMonitor.
    - crates/dlp-windows-service/src/session.rs and pipe.rs — authenticated StorageBootstrap and retained actor control channel.
    - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs — production context construction and currently ignored control messages.
  </read_first>
  <files>crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/session.rs, crates/dlp-windows-service/tests/session_lifecycle.rs, crates/dlp-windows-drive/src/bin/dlp-drive-host.rs</files>
  <behavior>
    - RED: the built production service/drive-host path mounts only after receiving its verified current or LKG snapshot and acknowledges the installed policy version/digest.
    - RED: a later higher-version activation updates the evaluator used by the already-mounted DlpFileSystemContext without remount.
    - RED: missing startup policy, malformed/stale/wrong-digest update, materialization failure, and acknowledgement timeout never install unverified state; startup fails closed or the prior evaluator remains active.
    - RED: process/ETW variations do not change read/export or create/write/import decisions for the same policy/file input.
  </behavior>
  <action>Define bounded versioned `ActivePolicySnapshotV1`, `PolicySnapshotUpdateV1`, and acknowledgement frames carrying policy version, canonical document/materialization input, digest, and detector ceilings—not process/ETW authorization data. Extend `StorageBootstrap`, `PipeBootstrap`/`AuthenticatedPipe`, `SessionMonitor`, and `run_scm_service` so the authenticated service sends current-or-LKG before mount readiness and pushes each later activated snapshot over the retained actor channel. Replace `dlp-drive-host`'s discarded control frames with strict decode/version/digest/order checks; materialize the snapshot and atomically swap the evaluator handle shared by the mounted `DlpFileSystemContext`, acknowledge exact version/digest, reject stale frames, and retain the prior evaluator on every failure. If startup has no verified current/LKG snapshot or the initial acknowledgement times out, do not expose the mount. Preserve D-10: process/ETW signals remain event enrichment only. Test the built service and `dlp-drive-host` process path on Windows, not only injected `ConfigurationCache` or filesystem ports. Apply exact-symbol impact and complete detect-changes gates before edits/commits.</action>
  <verify>
    <automated>rtk cargo build --locked -p dlp-windows-drive --bin dlp-drive-host &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test session_lifecycle real_drive_host_policy_snapshot --quiet</automated>
  </verify>
  <acceptance_criteria>The authenticated production service bootstraps and hot-updates the built drive host's mounted evaluator with exact acknowledgement; every missing/stale/malformed/failing path retains prior authority or prevents mount exposure.</acceptance_criteria>
  <done>The real mounted drive host, not only an injected evaluator port, enforces the service's verified current/LKG snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add old-key-authorized rotation and complete distribution E2E</name>
  <read_first>
    - ADR-005-policy-signing.md — old-key-authorized transition and missed-overlap recovery.
    - 02-RESEARCH.md — Pattern 5 and A-ROTATE uncertainty.
    - crates/dlp-crypto/src/lib.rs — current single-key verification boundary.
    - crates/dlp-agent-core/src/config_cache.rs — Task 1 atomic activation and persisted selector.
    - tests/e2e/server_enrollment.rs — existing signed-distribution test conventions.
  </read_first>
  <files>crates/dlp-crypto/src/lib.rs, crates/dlp-agent-core/src/config_cache.rs, crates/dlp-agent-core/tests/policy_activation.rs, crates/dlp-protocol/src/lib.rs, crates/dlp-server/tests/policy_distribution.rs</files>
  <behavior>
    - RED: current trusted key signs a transition to a unique new key; both IDs verify inside overlap and the new key verifies after old-key retirement.
    - RED: unknown/self-signed key introduction, duplicate ID/different bytes, empty keyring, expired transition, replay, and one-step-beyond version/time bounds are rejected.
    - RED: equal/repeated valid transition is idempotent; concurrent activations select the greatest bundle without mixing key/payload/signature generations.
    - RED: full admin publish/assign -> device poll -> activate -> evaluate flow reports desired, issued, then activated version; invalid/older assignment retains LKG and reports stable failure.
  </behavior>
  <action>Write rotation and full-distribution failures first. Place the distribution integration at `crates/dlp-server/tests/policy_distribution.rs` so Cargo discovers the `policy_distribution` target automatically without a manifest registration. Replace the single verifier with a bounded, versioned keyring whose mutation is authorized only by a signature from an already trusted key. Implement A-ROTATE explicitly: transition contains a monotonic not-after bundle version and epoch-seconds not-after; old-key acceptance requires both bounds not exceeded, equality is accepted, and either exceeded rejects. Duplicate IDs must have identical public bytes or fail; empty/unknown/self-introduction fails. Persist keyring transition atomically with the configuration selection so restart cannot cross-link generations. Exercise the real Plan 02 server path and Plan 01 evaluator in `policy_distribution.rs`, including deployment status. Apply exact impact and complete detect-changes gates before edits/commits.</action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-agent-core --test policy_activation --quiet &amp;&amp; rtk cargo test --locked -p dlp-server --test policy_distribution --quiet &amp;&amp; rtk cargo clippy --locked -p dlp-protocol -p dlp-crypto -p dlp-agent-core --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>Rotation is authorized only by established trust, overlap edges and empty/replay/concurrency cases are deterministic, restart preserves one coherent key/config/evaluator generation, and the real distribution E2E reports the activated assigned version.</acceptance_criteria>
  <done>CRY-05 rotation and SRV-06/SRV-07 distribution operate without trust-on-first-use or loss of last-known-good enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PostgreSQL selected policy -> signing provider | Transactional desired state becomes immutable signed bytes. |
| Network bundle -> endpoint cache/compiler | Untrusted bytes must pass identity, signature, replay, schema, digest, and resource checks before activation. |
| Trusted keyring -> key transition | New verification authority may be introduced only by established authority. |
| Windows service -> per-user drive host | Verified current/LKG policy state crosses authenticated bootstrap/control IPC and must be acknowledged before it can authorize mounted callbacks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03-01 | Tampering | Signed configuration activation | high | mitigate | Verify canonical signature, digest, audience, schema, version, action support, and ceilings before atomic pointer swap. |
| T-02-03-02 | Spoofing | Signing-key transition | high | mitigate | Require an already trusted key to sign a bounded unique-ID transition; reject self-introduction and conflicting IDs. |
| T-02-03-03 | Tampering | Concurrent activation/restart | high | mitigate | Atomically bind keyring, bundle, compiled-policy digest, current, and LKG generation; test concurrency and reopen. |
| T-02-03-04 | Denial of Service | Endpoint materialization | medium | mitigate | Compile only after bounded decode and enforce Plan 01 immutable endpoint ceilings before allocation/pointer swap. |
| T-02-03-05 | Tampering | Service-to-drive-host policy snapshot | high | mitigate | Version/digest/order-check bootstrap and update frames, authenticate the actor channel, atomically swap only after materialization, require exact acknowledgement, and retain the prior evaluator on failure. |
| T-02-03-SC | Tampering | Package supply chain | low | accept | Add only the already-audited in-workspace `dlp-policy` crate through an exact local path/version entry in dlp-agent-core; no external package is introduced. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Assigned policy reaches the encrypted drive safely | 02-03 | COVERED | Full publish/assign/poll/activate/evaluate flow. |
| REQ | SRV-06, SRV-07, POL-07, CRY-05, AGT-10 | Signed bundle, distribution/status, unsupported action, rotation, restart | 02-03 | COVERED | Both tasks and edge truths cover all. |
| RESEARCH | Bundle V2, atomic activation, old-key transition, migration inventory | 02-03 | COVERED | A-ROTATE is surfaced and implemented explicitly. |
| CONTEXT | D-04, D-09, D-12 | Next-poll effect and validation-before-activation | 02-03 | COVERED | Publish alone no-ops; invalid resources preserve LKG. |
| CONTEXT | Deferred | Offline operation/event upload | — | EXCLUDED | Only Phase 2 activation/status is planned. |
</source_audit>

<verification>
1. Run focused activation tracer, full activation/rotation suite, and distribution E2E.
2. Verify current/LKG and keyring reopen after invalid/partial/concurrent operations.
3. Run clippy for protocol, crypto, and agent crates.
4. Require exact impact reports and complete detect-changes before commits.
</verification>

<success_criteria>
- Selected policy activates atomically only after complete verification/materialization.
- Invalid and stale inputs preserve current/LKG across restart.
- Key rotation is old-key-authorized, bounded, deterministic, and replay-safe.
- Server status reflects desired, issued, activated, and failed versions.
</success_criteria>

## Artifacts this phase produces

- Modified `crates/dlp-protocol/src/lib.rs`: `ConfigurationBundleV2`, `SigningKeyTransitionV1`, `DeploymentStatusV1` canonical contracts.
- Modified `crates/dlp-crypto/src/lib.rs`: bounded trusted verification keyring and transition verification.
- Modified `crates/dlp-agent-core/src/config_cache.rs`: verified policy materialization plus atomic key/config/evaluator current/LKG activation.
- Modified `crates/dlp-agent-core/Cargo.toml` and `Cargo.lock`: exact crate-local `dlp-policy` dependency used to materialize `CompiledPolicyV2`.
- New `crates/dlp-agent-core/tests/policy_activation.rs`: activation, edge, rotation, concurrency, and restart suite.
- Modified server repository/routes: selected-bundle materialization and status updates.
- New `crates/dlp-server/tests/policy_distribution.rs`: publish-to-evaluator distribution proof discovered as the `dlp-server` integration-test target.
- Modified Windows service bootstrap/control files and `dlp-drive-host.rs`: current/LKG snapshot delivery, acknowledged hot update, and real mounted-context evaluator installation.
- Expanded `crates/dlp-windows-service/tests/session_lifecycle.rs`: built service-to-drive-host policy bootstrap/update/failure proof.

<output>
Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-03-SUMMARY.md` with TDD commits, signed-byte/key-transition contracts, restart/concurrency evidence, and GitNexus reports.
</output>
