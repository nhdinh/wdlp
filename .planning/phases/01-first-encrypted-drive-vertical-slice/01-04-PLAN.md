---
phase: 01-first-encrypted-drive-vertical-slice
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-03]
files_modified:
  - crates/dlp-crypto/src/lib.rs
  - crates/dlp-crypto/src/aead.rs
  - crates/dlp-crypto/src/key.rs
  - crates/dlp-storage/src/lib.rs
  - crates/dlp-storage/src/format.rs
  - crates/dlp-storage/src/path.rs
  - crates/dlp-storage/src/store.rs
  - crates/dlp-storage/tests/roundtrip.rs
  - crates/dlp-storage/tests/operations.rs
autonomous: true
requirements: [CRY-01, CRY-04, DRV-01, DRV-03, DRV-04, DRV-06, TST-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-12, `flush`/final `close` returns success only after ciphertext chunks, encrypted metadata, authenticated manifest, and commit record satisfy the durable commit protocol."
    - "Per D-15, disk-full or backing I/O failure returns typed storage errors, never publishes the staging generation, and leaves the previous committed file readable."
    - "Creating/opening the same SID/path is idempotent where Windows semantics require, while concurrent stores and handles remain isolated by captured SID/store/file/generation identity (spec-less DRV-01 idempotency/concurrency probes)."
    - "SID/path equality is exact after canonical normalization; null/empty user identity is rejected; case-collision and directory ordering are deterministic; concurrent requests cannot switch store identity (spec-less DRV-03 adjacency/empty/ordering/concurrency probes)."
    - "Concurrent encrypted writes use distinct record identities/nonces and serialize commit publication per file without global cross-user blocking (spec-less DRV-04/DRV-06 concurrency probes)."
  artifacts:
    - path: "crates/dlp-storage/src/format.rs"
      provides: "Versioned encrypted-record, manifest, and commit formats"
      exports: ["EncryptedRecordV1", "EncryptedManifestV1", "CommitRecordV1"]
    - path: "crates/dlp-storage/src/path.rs"
      provides: "Bounded canonical virtual path parser"
      exports: ["VirtualPath", "PathError"]
    - path: "crates/dlp-storage/src/store.rs"
      provides: "Per-SID encrypted filesystem operations and durable generation commits"
      exports: ["EncryptedStore", "FileHandle", "CommitOutcome"]
  key_links:
    - from: "crates/dlp-storage/src/store.rs"
      to: "crates/dlp-crypto/src/aead.rs"
      via: "every chunk/manifest/commit encrypts with fresh nonce and identity AAD"
      pattern: "RecordCipher"
    - from: "crates/dlp-storage/src/path.rs"
      to: "crates/dlp-storage/src/store.rs"
      via: "host paths are derived from captured StoreId/FileId, never virtual path concatenation"
      pattern: "VirtualPath"
  flagged_assumptions:
    - { id: "PROBE-TST-03", status: unresolved, statement: "The deterministic edge probe could not classify TST-03; key, nonce, AAD, corruption, and plaintext-search cases must all execute." }
  prohibitions: []
---

<objective>
Implement the approved durable Phase 1 encrypted filesystem core: versioned 4 MiB AEAD chunks, encrypted sensitive metadata, SID-safe virtual paths, normal file/directory operations, durable generation publication, and disk-full preservation.

Purpose: Make storage semantics strong enough that WinFsp callbacks can acknowledge Windows flush/close only after D-12 through D-15 are true.
Output: Portable crypto/storage format, path, operation, and durability modules with boundary/concurrency tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/docs/adrs/ADR-006-key-hierarchy.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-01-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-03-SUMMARY.md

<interfaces>
Preserve `EncryptedStore`, `StoreKey`, `StoreKeyProvider`, `ProtectedFileSystem`, typed IDs, and structured errors from 01-01. Add versioned on-disk types without exposing their host paths. `StoreKeyProvider::load_for_sid` returns a secrecy-wrapped per-user DEK; tests use an in-memory provider and Plan 01-08 supplies the DPAPI-backed Windows provider.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Durable authenticated create/write/flush/read for one file generation</name>
  <precondition>`01-03-SUMMARY.md` records `approve-aes-4m` and exact format ID `dlp-store/aes256gcm-4m/v1` before this task creates any persisted encrypted data.</precondition>
  <files>crates/dlp-crypto/src/lib.rs, crates/dlp-crypto/src/aead.rs, crates/dlp-crypto/src/key.rs, crates/dlp-storage/src/lib.rs, crates/dlp-storage/src/format.rs, crates/dlp-storage/src/store.rs, crates/dlp-storage/tests/roundtrip.rs</files>
  <read_first>
    - Every path in `<files>` plus the `EncryptedStore` and crypto contracts from 01-01
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-11 through D-15 and D-18
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md` Generation-based encrypted commit, AEAD pitfall, A1 chunk-size assumption
    - `.planning/docs/adrs/ADR-006-key-hierarchy.md`
  </read_first>
  <behavior>
    - Test 1: empty, 4 MiB - 1, 4 MiB, 4 MiB + 1, multi-chunk, and sparse-offset writes roundtrip only after flush.
    - Test 2: every record under one DEK has a distinct persisted 96-bit nonce; an injected duplicate is rejected before publication.
    - Test 3: changing store/file/generation/chunk/length/version AAD causes `IntegrityFailure` and returns no bytes.
    - Test 4: successful flush proves chunk files, encrypted manifest, commit record, and directory entries were flushed before the commit became selected.
  </behavior>
  <action>Version the Phase 1 store as 4 MiB logical chunks. Define fixed-field `EncryptedRecordV1`, `EncryptedManifestV1`, and `CommitRecordV1` with magic/version/key ID/nonce/ciphertext/tag and authenticated `{format_version, store_id, file_id, generation, record_kind, chunk_index, plaintext_length}` AAD. Generate each AES-256-GCM nonce from the OS CSPRNG, persist it, maintain a per-generation duplicate detector, and treat any duplicate as a fatal staging error. Keep DEKs in secrecy/zeroize containers and never derive them from a password. `EncryptedStore` writes a new unreferenced generation, flushes all records and encrypted manifest, writes/flushed an authenticated commit record, then atomically replaces the selected-commit pointer and flushes its directory; only then may flush/close report success per D-12. Read authenticates the commit, manifest, and selected chunks before copying bytes to the caller. Use a versioned writer/reader seam so the one-way persisted format has an explicit migration boundary.</action>
  <acceptance_criteria>
    - Exact D-18 boundary tests pass and record count/lengths match the 4 MiB format.
    - No test uses a fixed production nonce; duplicate injection fails before selected commit changes.
    - Tampering any AAD identity field or authentication tag returns zero plaintext.
    - A successful flush trace proves durability operations precede atomic publication.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dlp-storage --test roundtrip; cargo test -p dlp-crypto aead key; cargo clippy -p dlp-crypto -p dlp-storage --all-targets -- -D warnings</automated>
  </verify>
  <done>One file generation is durably published and read through authenticated 4 MiB records, with nonce uniqueness, identity AAD, and no success before the commit boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SID-safe path and complete file/directory operation model</name>
  <files>crates/dlp-storage/src/path.rs, crates/dlp-storage/src/store.rs, crates/dlp-storage/src/format.rs, crates/dlp-storage/tests/operations.rs</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `.planning/REQUIREMENTS.md` DRV-01, DRV-03, DRV-04, DRV-06
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-11/D-12/D-15/D-17
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md` integrated WinFsp operation rows
  </read_first>
  <behavior>
    - Test 1: create/open/read/write/truncate/flush/close/rename/replace/move/delete/create-directory/read-directory behave deterministically with Windows-compatible case-insensitive lookup and stable display name.
    - Test 2: absolute/UNC/device paths, `..`, alternate separators/streams, reserved names, embedded NUL, overlong components/path, and encoded aliases cannot escape or collide with another store.
    - Test 3: null/empty SID is rejected; equal normalized SID selects the same store; different SIDs never share host paths/keys/handles.
    - Test 4: concurrent handles obey share/delete-pending rules and serialize publication per file while unrelated files/users progress independently.
  </behavior>
  <action>Implement `VirtualPath` as a component parser over Windows-style names with explicit maximum path/component/count limits, canonical case-folded lookup keys, stable original display names, and rejection of root escapes, host/device/UNC prefixes, alternate data streams, NUL, reserved device names, and unsupported reparse semantics. Derive host paths only from opaque `StoreId`, `FileId`, and generation IDs; never concatenate a virtual path or caller user ID into the backing path. Bind each `EncryptedStore` to one normalized captured `UserSid` and one key provider result. Extend encrypted directory metadata and file-handle state to cover every D-17 operation plus open/share/delete-pending/cache/explicit-flush/final-close behavior; make rename/replace and directory mutation use the same staged authenticated commit protocol. Map disk-full/write failures to typed `NoSpace`/`IoFailure` without publishing staging.</action>
  <acceptance_criteria>
    - All D-17 operations pass on portable storage with case/share/delete semantics documented by tests.
    - The traversal/collision corpus cannot create or read a host path outside the selected SID store.
    - Two SIDs using identical virtual paths have different store IDs, keys, host records, and handle registries.
    - Disk-full and write-failure injection preserve the prior commit and return a typed error suitable for Windows mapping.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dlp-storage --test operations; cargo test -p dlp-storage path store</automated>
  </verify>
  <done>The portable operation model covers the Phase 1 Windows semantics over isolated SID-bound stores and cannot traverse, alias, or publish a failed update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Virtual filesystem inputs → `VirtualPath`/store | Names, lengths, offsets, flags, and concurrency are attacker-controlled. |
| Store key → AEAD record writer | Nonce/key reuse or unauthenticated metadata breaks confidentiality/integrity. |
| Backing filesystem → recovery/read | Every local file, directory entry, length, and timestamp is untrusted/tamperable. |
| Buffered write → durable commit | Crash/disk-full may separate data, metadata, and publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-21 | Elevation of privilege | `VirtualPath`/store selection | high | mitigate | Parse/bound components, reject traversal/device/UNC/ADS/reparse forms, derive host path from captured SID/store/file IDs only. |
| T-01-22 | Tampering | untrusted backing records | high | mitigate | Bound and authenticate commit/manifest/chunks/AAD before decode; never trust host metadata/order/names. |
| T-01-23 | Tampering/Information disclosure | AEAD nonce/AAD | high | mitigate | OS-random persisted 96-bit nonce, per-generation duplicate rejection, version/store/file/generation/kind/index/length AAD. |
| T-01-24 | Tampering/Denial of service | commit/recovery | high | mitigate | Unreferenced staging, ordered flushes, authenticated commit, atomic pointer, exhaustive fault injection, prior-commit preservation. |
| T-01-25 | Information disclosure | plaintext/key artifacts | high | mitigate | Encrypt content/sensitive metadata, secrecy/zeroize keys, zero plaintext temp files, recursive non-vacuous marker scan. |
| T-01-26 | Repudiation | integrity diagnostics/evidence | medium | mitigate | Preserve encrypted evidence unchanged and log only stable code, opaque record ID, digest, correlation ID. |
| T-01-27 | Denial of service | file/directory operations | medium | mitigate | Bound paths/components/records/handles, per-file locking, typed no-space errors, no partial publication. |
| T-01-SC | Tampering | Cargo packages | high | mitigate | Use only audited or explicitly approved exact Cargo.lock packages and run slopcheck before accepting the crypto/storage dependency graph. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Implementation evidence | Executable evidence |
|---|---|---|---|
| V5 Validation | Untrusted paths, record lengths, versions, offsets, and identities are bounded before allocation/use | `VirtualPath`, versioned fixed-field record parsers | `cargo test -p dlp-storage --test operations` |
| V6 Cryptography | Approved AEAD uses unique nonces and identity-complete AAD | `RecordCipher`, `RecordAad`, duplicate detector | roundtrip boundary/tamper/nonce tests |
| V8 Data Protection | Content, sensitive metadata, and DEKs are never persisted or logged in plaintext | encrypted manifest/records plus secrecy/zeroize | recursive marker/key scan in TST-03 |
| V12 Files and Resources | Path traversal, cross-store aliasing, failed publication, and no-space preserve isolation/prior commit | opaque store/file paths and staged generation protocol | path corpus and fault-injection tests |

**Completion gate:** Any unresolved critical/high threat, failed ASVS evidence command, nonce/AAD gap, or missing prior-commit evidence blocks this plan's completion and summary.
</threat_model>

<verification>
Run crypto/storage unit and integration suites for all chunk boundaries, paths, operations, concurrent handles/users, every durability fault point, corruption types, clean restart, disk-full, and recursive plaintext/key marker scans. Inspect the format version/migration seam because it is the plan's persisted one-way door.
</verification>

<success_criteria>
- AES-GCM content and sensitive metadata records have unique nonces and complete identity AAD.
- SID/path handling cannot escape or select another user's store.
- Normal file/directory operations share one durable generation protocol.
- Crashes and disk-full preserve the last fully committed generation.
- Disk-full and backing I/O failure do not publish partial state or damage the prior commit.
</success_criteria>

## Artifact Inventory

| Disposition | File | Key symbols / purpose |
|---|---|---|
| Modified | `crates/dlp-crypto/src/lib.rs` | exports approved record cipher/key interfaces |
| New | `crates/dlp-crypto/src/aead.rs` | `RecordCipher`, `RecordAad`, authenticated encrypt/decrypt |
| New | `crates/dlp-crypto/src/key.rs` | secrecy-wrapped `StoreKey`, `KeyId` |
| Modified | `crates/dlp-storage/src/lib.rs` | exports final format/path/store APIs and errors |
| New | `crates/dlp-storage/src/format.rs` | `EncryptedRecordV1`, `EncryptedManifestV1`, `CommitRecordV1`, migration seam |
| New | `crates/dlp-storage/src/path.rs` | `VirtualPath`, normalization and bounds |
| New | `crates/dlp-storage/src/store.rs` | `EncryptedStore`, `FileHandle`, durable publication/share state |
| New | `crates/dlp-storage/tests/roundtrip.rs` | chunk/AAD/nonce/durability boundary evidence |
| New | `crates/dlp-storage/tests/operations.rs` | path/operation/concurrency/disk-full evidence |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-04-SUMMARY.md` when done.
</output>
