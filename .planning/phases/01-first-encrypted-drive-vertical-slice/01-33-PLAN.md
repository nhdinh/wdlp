---
phase: 01-first-encrypted-drive-vertical-slice
plan: 33
type: execute
wave: 21
depends_on:
  - 01-32
files_modified:
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
autonomous: true
gap_closure: true
requirements:
  - CRY-04
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phase 1 returns to complete only after recomputed-forgery, mandatory-confirmation, and two-writer regressions pass with trusted signed attestations."
    - "The security register, review resolution, signed-off verifier, and FinalGate cite the same current manifest and trust policy."
  artifacts:
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      provides: "Append-only authenticated closure re-sign-off"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
      provides: "Append-only resolution of the three re-verification findings"
    - path: scripts/verify-phase1.ps1
      provides: "Canonical FinalGate that mandates the authenticated security subgate and external trust inputs"
  key_links:
    - from: scripts/evidence/Phase1.Security.Tests.ps1
      to: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      via: "Completion requires all new adversarial regressions and signed-off validation"
    - from: scripts/verify-phase1-security.ps1
      to: scripts/verify-phase1.ps1
      via: "Canonical FinalGate consumes the hardened security subgate"
---

<objective>
Reconcile Phase 1 completion only after the hardened trust boundary, fresh signed reviews, and canonical FinalGate agree.

Purpose: Close SEC-01 through SEC-03 truthfully while retaining every prior failed and superseded audit interval.
Output: Updated security/review records bound to the hardened manifest and passing gates.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-31-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-32-SUMMARY.md
@evidence/phase1/security-closure.yaml
@scripts/verify-phase1.ps1
@scripts/verify-phase1-security.ps1
</context>

## Artifacts this phase produces

- `01-SECURITY.md`: append-only completion entry citing authenticated signatures and all new regressions.
- `01-REVIEW.md`: append-only resolution of the latest three verification findings.
- `scripts/verify-phase1.ps1`: implementation from Plan 01-31 that FinalGate exercises with the identical external trust paths and manifest/policy identity.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the hardened security boundary and update the register</name>
  <precondition>The Plan 01-32 signed-off verifier succeeds for all 19 current payloads using the approved external trust inputs</precondition>
  <files>.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md</files>
  <read_first>scripts/evidence/Phase1.Security.Tests.ps1; scripts/verify-phase1-security.ps1; evidence/phase1/security-closure.yaml; .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md</read_first>
  <action>
    Run the entire Phase 1 security suite and signed-off verifier with `-TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH` and `-ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH`; neither value may be omitted or obtained from the closure manifest. Require explicit passing coverage for recomputed forgery, manifest-contained trust substitution, each trust-policy dimension, complete-payload confirmation/refusal, non-interactive signed input if supported, dry-run/WhatIf non-mutation, deterministic two-writer interleaving, crash-before-replace preservation, and canonical FinalGate failure when the security subgate fails. Only after all checks and all 19 trusted signatures pass, append a new `01-SECURITY.md` audit entry that records the canonical manifest digest, trusted-root/reviewer-policy identity, signer thumbprint(s), review UTC range, procedure version, test counts, and signed-off result; restore complete/zero-open status without overwriting the Aug 24 failed interval or earlier resolutions, per D-40/D-41/D-48.
  </action>
  <acceptance_criteria>
    - The security register cites the exact manifest and external trust policy that passed.
    - Complete/zero-open is impossible while any current signature or adversarial regression fails.
    - Prior failed and superseded history remains inspectable.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH -RequireSignedOff</automated></verify>
  <done>SEC-01 through SEC-03 are backed by trusted signatures, mandatory exact-payload review, and lossless publication tests.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the latest findings and pass canonical FinalGate</name>
  <files>.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md</files>
  <read_first>.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; scripts/verify-phase1.ps1; scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1</read_first>
  <action>
    Run canonical Phase 1 FinalGate with the established named machine-role contract, current evidence, `-TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH`, and `-ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH`. Confirm FinalGate invokes `scripts/verify-phase1-security.ps1` with those exact paths and `-RequireSignedOff`, exits nonzero when that subgate fails, and reports the same manifest digest and reviewer-policy identity returned by the subgate. Require the automated regression that forces the security subgate to fail while all other FinalGate inputs remain valid. Append a resolution section to `01-REVIEW.md` for the three Aug 24 re-verification findings, citing Plans 01-31 through 01-33, exact regression names/results, current manifest digest, external trust-policy identity, authenticated signer provenance, signed-off result, and FinalGate counts. Preserve every original finding and prior resolution. If any gate fails or identities differ, keep the security state open and record the exact failure rather than claiming completion.
  </action>
  <acceptance_criteria>
    - The three current findings have append-only evidence-backed resolutions.
    - FinalGate consumes the hardened signed-off subgate and passes against current named-machine evidence.
    - A regression proves FinalGate exits nonzero when the hardened security subgate fails, and the successful path reports identical manifest/policy identity.
    - No functional encrypted-drive artifact or accepted UAT record is rewritten.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH</automated></verify>
  <done>The canonical Phase 1 gate passes and the latest verification findings are truthfully resolved without erasing audit history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hardened manifest -> security status | Completion must bind to the exact currently verified signed manifest and external policy. |
| Security subgate -> canonical FinalGate | FinalGate must not bypass or substitute the trusted signed-off validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-33-01 | Tampering | completion status | high | mitigate | Bind append-only status entry to manifest digest, external policy identity, signer thumbprints, and passing regression results. |
| T-01-33-02 | Repudiation | finding resolution | high | mitigate | Preserve original findings and append exact commands, provenance, results, and plan references. |
| T-01-33-03 | Spoofing | FinalGate environment | high | mitigate | Use established named machine-role guards and current evidence bundle. |
</threat_model>

<verification>
- Security regressions, trusted signed-off verifier, and canonical FinalGate all pass in sequence.
- SECURITY and REVIEW cite the same current manifest digest and trust-policy identity.
</verification>

<success_criteria>
- All actionable VERIFICATION gaps are closed by implementation, real signed review, and final reconciliation.
- Phase completion no longer depends on recomputable approval fields, optional confirmation, or a lossy publication race.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-33-SUMMARY.md` when done.
</output>
