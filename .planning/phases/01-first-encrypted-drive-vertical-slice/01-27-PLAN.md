---
phase: 01-first-encrypted-drive-vertical-slice
plan: 27
type: execute
wave: 15
depends_on:
  - 01-26
files_modified:
  - crates/dlp-windows-drive/tests/mounted_smoke.rs
  - evidence/phase1/security-closure.yaml
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
autonomous: true
gap_closure: true
requirements:
  - CRY-04
  - TST-08
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TST-08 has a committed real-WinFsp regression assertion proving a newly created directory exposes a non-zero last-write timestamp when the runtime is available."
    - "CRY-04's T-01-15-03 closure record describes and hashes the current credential implementation after a fresh security review; a digest refresh without review is not accepted."
    - "Phase 01 reports complete with zero open blocking threats only while the tamper suite, signed-off security verifier, and FinalGate all pass against current artifacts."
  artifacts:
    - path: crates/dlp-windows-drive/tests/mounted_smoke.rs
      provides: "Real mounted-drive timestamp regression coverage promised by Plan 01-26"
    - path: evidence/phase1/security-closure.yaml
      provides: "Resealed T-01-15-03 implementation and artifact digests for the reviewed credential implementation"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      provides: "Truthful security audit trail and sign-off state matching current verifier output"
  key_links:
    - from: crates/dlp-windows-drive/tests/mounted_smoke.rs
      to: crates/dlp-windows-drive/src/filesystem.rs
      via: "A real mounted directory is created through Win32 and its fs::metadata().modified() value exercises FileInfo.last_write_time"
    - from: evidence/phase1/security-closure.yaml
      to: crates/dlp-windows-service/src/credential.rs
      via: "T-01-15-03 implementation_refs and artifact_refs contain the post-review SHA-256 of the current file"
    - from: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      to: scripts/verify-phase1-security.ps1
      via: "Signed-off status is restored only after the current closure manifest passes -RequireSignedOff"
---

<objective>
Close every actionable item in the latest Phase 01 verification report: add the missing real-mount timestamp regression assertion, re-audit and reseal the stale T-01-15-03 credential closure, and make the security register agree with the machine-checkable gates.

Purpose: Preserve the proven encrypted-drive behavior while restoring a current, fail-closed security sign-off for CRY-04 and the promised WinFsp regression coverage for TST-08.
Output: Updated mounted-drive test coverage, a reviewed and resealed T-01-15-03 closure record, a consistent Phase 01 security register, and passing security/FinalGate verification.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-25-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-26-SUMMARY.md
@evidence/phase1/security-closure.yaml
@crates/dlp-windows-service/src/credential.rs
@crates/dlp-windows-drive/tests/mounted_smoke.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the mounted directory timestamp fix with a real WinFsp assertion</name>
  <files>crates/dlp-windows-drive/tests/mounted_smoke.rs</files>
  <read_first>
    - `crates/dlp-windows-drive/tests/mounted_smoke.rs` existing runtime-skip helper and `sid_bound_context_mounts_roundtrips_denies_corruption_and_unmounts` flow
    - `crates/dlp-windows-drive/src/filesystem.rs` `file_info_for` timestamp mapping implemented by Plan 01-26
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md` mounted-smoke warning
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-26-SUMMARY.md` promised timestamp behavior and runtime constraints
  </read_first>
  <behavior>
    - Test 1: after a directory is created through the real mounted drive, its last-write timestamp is present and later than the Windows FILETIME epoch.
    - Test 2: when WinFsp is unavailable, the existing graceful runtime skip remains the only skip path and the test suite succeeds without fabricating a timestamp result.
  </behavior>
  <action>
    Before editing, run GitNexus upstream impact for the exact mounted-smoke test symbol; the current file-level lookup returned UNKNOWN because the index did not resolve the test path, so treat blast radius as unknown and limit the edit to the existing real-mount test. After `fs::create_dir(&directory)` succeeds in `sid_bound_context_mounts_roundtrips_denies_corruption_and_unmounts`, read `fs::metadata(&directory)`, require `modified()` to succeed, and assert the returned time is strictly after `std::time::UNIX_EPOCH`. Keep the assertion inside the mounted-runtime path so it executes on LAB-CLIENT01 and does not turn the documented no-WinFsp skip into a false failure. Do not replace the real filesystem query with direct `EntryMetadata` access because the gap specifically requires coverage through WinFsp/Win32.
  </action>
  <acceptance_criteria>
    - `mounted_smoke.rs` queries metadata for the directory created through the mounted drive and asserts a non-zero/recent last-write timestamp.
    - The assertion is reached only after `try_mount_volume` returns a live volume; no synthetic fallback value is used.
    - Existing corruption, disk-full, restart, and graceful-runtime-skip behaviors remain intact.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked -p dlp-windows-drive --test mounted_smoke</automated>
    <automated>cargo clippy --locked -p dlp-windows-drive --test mounted_smoke -- -D warnings</automated>
  </verify>
  <done>The committed real-mount test fails if WinFsp again returns a zero directory last-write timestamp and still runs safely on hosts without WinFsp.</done>
</task>

<task type="auto">
  <name>Task 2: Re-audit the current credential implementation and reseal T-01-15-03</name>
  <files>evidence/phase1/security-closure.yaml</files>
  <read_first>
    - `crates/dlp-windows-service/src/credential.rs` complete current credential storage, DPAPI, ACL, atomic-write, and redaction implementation
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-PLAN.md` T-01-15-03 mitigation contract
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md` implemented mitigation and verification evidence
    - `evidence/phase1/security-closure.yaml` complete T-01-15-03 record, including every occurrence of the stale credential digest
    - `scripts/verify-phase1-security.ps1` implementation/artifact hash and machine-role gates
    - `scripts/evidence/Phase1.Security.Tests.ps1` canonical-file immutability and tamper tests
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md` stale-digest blocker
  </read_first>
  <action>
    Re-audit T-01-15-03 against the full current `credential.rs`, explicitly confirming that the random DEK remains machine-DPAPI wrapped, the credential/blob remains UI-forbidden and service-owned with the required DACL, persistence remains atomic, plaintext key material is not logged or serialized, and zeroization/redaction behavior still matches the recorded mitigation. Compare the post-sign-off change against the implementation represented by Plan 01-15 and record any security-semantic change as a hard failure requiring source remediation; do not reseal merely because a new hash can be computed. If and only if the mitigation remains fully implemented, compute SHA-256 from the current file bytes and replace every stale `credential.rs` digest within the T-01-15-03 record in both `implementation_refs` and `artifact_refs`, leaving the immutable 19-ID target set, evidence attempt IDs, machine roles, reviewer identity, and other threat records unchanged. Run the focused verifier for T-01-15-03 before proceeding.
  </action>
  <acceptance_criteria>
    - The audit explicitly checks every T-01-15-03 mitigation property against current code before any digest is accepted.
    - Every T-01-15-03 reference to `credential.rs` contains the same SHA-256 computed from current bytes.
    - No other closure target, evidence attempt, role, disposition, or threat record changes.
    - The focused verifier accepts T-01-15-03 and still rejects a temporary mismatched-hash mutation through the existing Pester suite.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -ThreatId T-01-15-03</automated>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1</automated>
  </verify>
  <done>T-01-15-03 is bound to the reviewed current credential implementation, and stale or tampered credential hashes remain fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3: Restore truthful security sign-off and run all Phase 01 gates</name>
  <files>.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md</files>
  <read_first>
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md` frontmatter, T-01-15-03 row, audit trail, and sign-off
    - `evidence/phase1/security-closure.yaml` resealed manifest from Task 2
    - `scripts/verify-phase1-security.ps1` signed-off register checks
    - `scripts/evidence/Phase1.Security.Tests.ps1` canonical security-register integrity checks
    - `scripts/verify-phase1-evidence.ps1` Phase 1 FinalGate contract
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md` SEC-01 and SEC-03 failed truths
  </read_first>
  <action>
    Run the Pester tamper suite and the security verifier in pre-sign-off mode against the resealed manifest. Update `01-SECURITY.md` only after those commands pass: preserve `status: complete`, `threats_open: 0`, and the closed T-01-15-03 row, but append a new dated audit-trail/sign-off entry describing the detected stale digest, the completed current-code re-audit, the new manifest digest, the focused T-01-15-03 result, and the reviewer identity. Do not overwrite or rewrite the prior sign-off history. Then run the Pester suite again, the signed-off verifier with `-SecurityPath` and `-RequireSignedOff`, the mounted-smoke test, and Phase 1 FinalGate. If any command fails, leave the register in an explicitly non-complete/open state rather than publishing a contradictory complete/zero-open claim.
  </action>
  <acceptance_criteria>
    - `01-SECURITY.md` contains an append-only re-audit record for the credential digest change and identifies the current manifest digest/reviewer.
    - Complete/zero-open frontmatter is present only with passing pre-sign-off, tamper, signed-off, and FinalGate results.
    - The signed-off verifier validates all 19 closure records against current file bytes.
    - Phase 1 FinalGate still reports all requirements and success criteria passing.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md -RequireSignedOff; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --locked -p dlp-windows-drive --test mounted_smoke; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario FinalGate; exit $LASTEXITCODE</automated>
  </verify>
  <done>The security register, closure manifest, mounted regression test, and FinalGate agree that Phase 01 is complete with no stale implementation-bound threat evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current credential source -> signed closure manifest | A mutable implementation digest crosses into an immutable security assertion; digest equality alone does not prove mitigation equivalence. |
| Win32 mounted-drive metadata -> Rust test assertion | Runtime-supplied timestamps cross the WinFsp callback boundary and must be validated without synthetic fallback values. |
| Closure manifest/register -> phase completion state | Machine-checkable evidence controls whether the project may claim zero open blocking threats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-27-01 | Tampering | `security-closure.yaml` T-01-15-03 record | high | mitigate | Require semantic re-audit before replacing both credential digests; focused verifier and Pester hash-mismatch mutation must pass. |
| T-01-27-02 | Repudiation | `01-SECURITY.md` re-sign-off | high | mitigate | Append dated audit history with current manifest digest, gate results, and reviewer identity; retain prior history. |
| T-01-27-03 | Information Disclosure | credential review and security artifacts | high | mitigate | Record only mitigation conclusions and hashes; retain existing forbidden-marker/redaction scans and never publish credential/blob/key contents. |
| T-01-27-04 | Tampering | mounted timestamp regression | medium | mitigate | Query metadata through the real mounted path and assert the OS-returned modification time after directory creation. |
</threat_model>

<verification>
- Run `cargo test --locked -p dlp-windows-drive --test mounted_smoke` and the focused clippy command.
- Run the focused T-01-15-03 verifier after resealing.
- Run `scripts/evidence/Phase1.Security.Tests.ps1` before and after updating the security register.
- Run the signed-off security verifier against the closure manifest and `01-SECURITY.md`.
- Run `scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario FinalGate` last.
</verification>

<success_criteria>
- The mounted-smoke test contains and executes a real mounted-directory timestamp assertion when WinFsp is available.
- The current `credential.rs` implementation has been semantically re-audited and every T-01-15-03 digest matches its current bytes.
- The tamper suite, focused verifier, signed-off verifier, mounted-smoke test, and FinalGate all pass.
- `01-SECURITY.md` reports complete/zero-open only with those passing results and retains an append-only re-audit trail.
- No unrelated production source, evidence attempts, closure targets, or threat dispositions change.
</success_criteria>

## Artifacts this phase produces

- `crates/dlp-windows-drive/tests/mounted_smoke.rs` — explicit real-WinFsp directory last-write timestamp regression assertion.
- `evidence/phase1/security-closure.yaml` — current reviewed SHA-256 binding for `credential.rs` in T-01-15-03 only.
- `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md` — append-only credential re-audit/sign-off entry consistent with passing gates.

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-27-SUMMARY.md` when done.
</output>
