---
phase: 01-first-encrypted-drive-vertical-slice
plan: 07
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - crates/dlp-server/src/lib.rs
  - crates/dlp-server/src/main.rs
  - crates/dlp-server/src/routes.rs
  - crates/dlp-server/src/health.rs
  - crates/dlp-server/src/tls.rs
  - crates/dlp-server/src/repository.rs
  - crates/dlp-protocol/src/lib.rs
  - config/server.env.example
  - tests/e2e/server_enrollment.rs
autonomous: true
requirements: [SRV-01, SRV-11, SRV-12, CRY-02, AGT-07, TST-05]
user_setup:
  - service: phase1-server-pki
    why: "Authenticated administrator and agent routes require distinct TLS trust roots and a server identity."
    env_vars:
      - { name: DLP_SERVER_CERT_PEM, source: "Certificate matching the management-server address" }
      - { name: DLP_SERVER_KEY_PEM, source: "Mounted server TLS private-key secret" }
      - { name: DLP_ADMIN_CA_CERT_PEM, source: "Administrator client-certificate trust anchor" }
      - { name: DLP_DEVICE_CA_CERT_PEM, source: "Device client-certificate trust anchor from 01-06" }
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Administrator routes accept only the configured administrator client CA; post-enrollment agent routes require a valid device certificate whose URI SAN/serial maps to one active non-revoked device before handler execution."
    - "The active device receives immutable canonical signed configuration with schema/bundle/key/hash/audience fields and cannot select a replayed/lower version."
    - "Health persists agent version, drive state, active policy version, and redacted stable errors only for the authenticated device."
    - "Liveness remains process-only and concurrent-safe; readiness returns 200 only after PostgreSQL, expected migration ledger, signer, issuer, TLS roots, and both required AD configurations are usable without mutating state."
    - "All integrated HTTP capability rows have route/middleware tests and all reasoned OPT-OUT surfaces remain absent."
  artifacts:
    - path: "crates/dlp-server/src/routes.rs"
      provides: "Authenticated `/api/v1` administrator, enrollment, configuration, and health routes"
      exports: ["api_v1_router"]
    - path: "crates/dlp-server/src/tls.rs"
      provides: "Server TLS plus distinct admin/device mTLS identity extraction"
      exports: ["AuthenticatedAdmin", "AuthenticatedDevice"]
    - path: "crates/dlp-server/src/health.rs"
      provides: "Process liveness and dependency-aware readiness"
      exports: ["liveness", "readiness", "ReadinessReport"]
  key_links:
    - from: "crates/dlp-server/src/tls.rs"
      to: "crates/dlp-server/src/repository.rs"
      via: "certificate SAN/serial resolves active non-revoked device before route handler"
      pattern: "credential_serial|AuthenticatedDevice"
    - from: "crates/dlp-server/src/routes.rs"
      to: "crates/dlp-crypto/src/lib.rs"
      via: "selected immutable configuration bytes are signed before response"
      pattern: "SignedConfigurationV1|ConfigurationSigner"
  flagged_assumptions: []
  prohibitions: []
---

<objective>
Complete the authenticated server surface by adding distinct admin/device mTLS routing, immutable signed configuration, health persistence, and dependency-aware liveness/readiness.

Purpose: Give the Windows agent a production-authenticated configuration/health API after enrollment authority exists, without mixing those concerns into the authority migration plan.
Output: TLS middleware, `/api/v1` route modules, signed bundle selection, health/readiness logic, and full HTTP capability tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/docs/adrs/ADR-002-api-transport.md
@.planning/docs/adrs/ADR-005-policy-signing.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-05-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-06-SUMMARY.md

<interfaces>
Use `EnrollmentService`, active credential repository, and issued certificate identity from 01-06; final versioned DTO/signature/activation contracts from 01-01/01-05. `AuthenticatedDevice` is created only after rustls chain verification plus application lookup of URI SAN and serial.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Device mTLS fetches one signed configuration and posts health</name>
  <precondition>Server/admin/device certificate files are readable from configured mounted-secret paths and migrations through `202608070002_enrollment_authority.sql` are applied.</precondition>
  <files>crates/dlp-server/src/lib.rs, crates/dlp-server/src/main.rs, crates/dlp-server/src/routes.rs, crates/dlp-server/src/tls.rs, tests/e2e/server_enrollment.rs</files>
  <behavior>
    - Test 1: admin routes reject absent/wrong client CA and accept the configured admin certificate.
    - Test 2: agent routes reject absent/wrong/expired/revoked/mismatched SAN+serial certificates before handler execution.
    - Test 3: active device receives canonical signed configuration; authenticated health report persists for that device.
    - Test 4: forwarded certificate headers never substitute for TLS peer identity.
  </behavior>
  <action>Serve HTTP/1.1 and HTTP/2 over rustls/tokio-rustls with server-name validation and distinct trust branches. Enrollment uses server-authenticated TLS plus the 01-06 authority checks; administrator routes require admin CA; configuration/health require device CA and SAN+serial-to-active-device lookup before handlers. Never trust proxy certificate headers. Return immutable canonical `SignedConfigurationV1` and accept bounded versioned health JSON only for the authenticated device. Apply body/time/concurrency limits and stable correlation-ID errors without certificate/private/health-detail leakage.</action>
  <verify><automated>cargo test -p dlp-server --test server_enrollment mtls_routes -- --nocapture; cargo test -p dlp-server tls routes</automated></verify>
  <done>Distinct administrator/device mTLS boundaries protect signed configuration and health, with revoked/mismatched identities rejected before handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay-safe configuration selection and dependency-aware readiness</name>
  <files>crates/dlp-server/src/health.rs, crates/dlp-server/src/repository.rs, crates/dlp-protocol/src/lib.rs, config/server.env.example, tests/e2e/server_enrollment.rs</files>
  <behavior>
    - Test 1: signed configuration includes exact content hash/schema/bundle/key/audience fields and lower/equal replay cannot become selected.
    - Test 2: liveness stays 200 for a running process independent of dependencies and never mutates state.
    - Test 3: readiness transitions 503→200 only when database, exact migration ledger, signer, issuer, TLS, and both AD configuration checks are usable.
    - Test 4: concurrent liveness/readiness probes produce deterministic results and no token/device/bundle/health mutation.
  </behavior>
  <action>Extend `SignedConfigurationV1` with fixed 32-byte SHA-256 content digest and authenticated audience, persist immutable signed bytes, and select only the greatest valid active version. Run embedded SQLx migrations before listener bind. Implement `/health/live` as process-only and `/health/ready` as bounded read-only checks for expected migration version, PgPool, signer, issuer, TLS roots, and both explicit AD endpoint/trust configurations; do not consume tokens, query/mutate device state, or issue certificates. Cover every HTTP INTEGRATE row and assert each OPT-OUT route/transport is absent for its documented reason.</action>
  <verify><automated>cargo test -p dlp-server --test server_enrollment signed_configuration readiness coverage; cargo run -p dlp-server -- --check-config --env-file config/server.env.example</automated></verify>
  <done>Signed selection is immutable/replay-safe and readiness reports only genuine dependency/migration usability without state mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TLS peer → admin/agent route | Certificate chain, trust root, SAN, serial, expiry, and revocation determine authorization. |
| Repository signer → network configuration | Selection/signing/version errors can enable tamper or rollback. |
| Dependency probes → readiness | Probes must reflect usable state without changing authority data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-17 | Spoofing/Elevation | admin/device mTLS | high | mitigate | Distinct trust roots, rustls chain checks, SAN+serial active-device lookup, expiry/revocation enforcement. |
| T-01-18 | Tampering | signed configuration | high | mitigate | Immutable canonical bytes, hash/signature/schema/key/audience/version, greatest-active query, replay tests. |
| T-01-19 | Information disclosure | JSON/errors/health | high | mitigate | Bounded DTOs and stable correlation/code output; no cert/private/raw-error secret detail. |
| T-01-20 | Denial of service | routes/readiness | medium | mitigate | Request/timeout/pool/concurrency limits and read-only bounded probes. |
| T-01-SC | Tampering | Cargo packages | high | mitigate | Exact 01-03-approved TLS/support dependency versions, lockfile, and slopcheck evidence. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Implementation evidence | Executable evidence |
|---|---|---|---|
| V2 Authentication | Client certificates are validated and bound to explicit admin/device identities | rustls verifier plus `AuthenticatedAdmin`/`AuthenticatedDevice` | no/wrong/expired/revoked/mismatch tests |
| V3 Session Management | Device credential revocation/expiry is checked on each request | active credential repository lookup | old/revoked serial negative tests |
| V4 Access Control | Admin and agent route authorities are distinct | separate router branches/extractors | cross-role route denial tests |
| V5 Validation | JSON/body/hash/schema/version fields are bounded before use | typed extractors and DTO validation | malformed/oversize/replay tests |
| V9 Communications | TLS server identity and required client auth protect all non-liveness APIs | rustls HTTP/1.1+HTTP/2 listener | real TLS matrix tests |

**Completion gate:** Any unresolved critical/high threat, failed ASVS evidence, auth bypass, replay, or readiness mutation blocks this plan's completion and summary.
</threat_model>

<verification>
Run complete TLS/admin/device negative matrix, signed selection/replay tests, health/redaction tests, readiness transition/concurrency checks, and every COVERAGE HTTP capability assertion.
</verification>

<success_criteria>
- Admin and agent APIs authenticate at distinct TLS trust boundaries.
- Signed configuration is immutable, audience/version bound, and replay-safe.
- Health is device-bound/redacted and readiness is accurate/read-only.
- Every HTTP coverage row is tested or remains explicitly absent.
- All ASVS L1 evidence passes and no critical/high threat remains unresolved.
</success_criteria>

## Artifact Inventory

| Disposition | File | Key symbols / purpose |
|---|---|---|
| Modified | `crates/dlp-server/src/lib.rs` | compose final TLS/router/health services |
| Modified | `crates/dlp-server/src/main.rs` | migration-before-rustls listener startup |
| New | `crates/dlp-server/src/routes.rs` | `api_v1_router`, admin/enrollment/config/health handlers |
| New | `crates/dlp-server/src/health.rs` | `liveness`, `readiness`, `ReadinessReport` |
| New | `crates/dlp-server/src/tls.rs` | rustls config, `AuthenticatedAdmin`, `AuthenticatedDevice` |
| Modified | `crates/dlp-server/src/repository.rs` | immutable signed configuration and device-bound health queries |
| Modified | `crates/dlp-protocol/src/lib.rs` | configuration digest/audience and health fields |
| Modified | `config/server.env.example` | server/admin/device TLS trust configuration |
| Modified | `tests/e2e/server_enrollment.rs` | mTLS, replay, readiness, coverage, redaction tests |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-07-SUMMARY.md` when done.
</output>
