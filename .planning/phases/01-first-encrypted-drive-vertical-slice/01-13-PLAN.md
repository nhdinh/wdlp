---
phase: 01-first-encrypted-drive-vertical-slice
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - config/lab.roles.example.json
  - config/server.env.example
  - deploy/compose.yaml
  - scripts/lab/Invoke-Phase1EnvironmentReconcile.ps1
  - scripts/lab/Invoke-Dc01Server.ps1
  - tests/e2e/compose.rs
autonomous: true
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, SRV-01, SRV-11, SRV-12]
user_setup:
  - service: phase1-hyperv-lab
    why: "The executor needs the four named machines running and a preconfigured runtime-only secret provider; no credential value is stored in the repository or passed on a command line."
    dashboard_config:
      - task: "Ensure hungdinh-lt can use Hyper-V PowerShell Direct for LAB-DC01, LAB-DC02, and LAB-CLIENT01 and that LAB-DC01 can start the Compose/PostgreSQL stack."
        location: "Existing local Hyper-V lab"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-20 through D-24, hungdinh-lt contains developer tooling and Hyper-V orchestration only, LAB-DC01 contains the running management server and PostgreSQL development database, LAB-DC02 remains an independent corroborating AD authority, and LAB-CLIENT01 is the sole endpoint-runtime machine."
    - "The previously installed WinFsp runtime and any exact DLP endpoint service, mount, credential, PKI-trust, hosts-entry, or domain/network artifact are removed from hungdinh-lt after a machine-role audit, while Rust, LLVM, source trees, Hyper-V, and unrelated software remain untouched."
    - "SRV-11 is proved against PostgreSQL on LAB-DC01: every versioned SQLx migration applies before listener binding, repeat application is ledger-idempotent, checksum drift fails closed, and concurrent starters cannot diverge."
    - "SRV-12 liveness and readiness execute on LAB-DC01 and are reachable from LAB-CLIENT01 without treating any hungdinh-lt or SQLite result as server/runtime evidence."
    - "Existing portable, server, crypto, storage, and WinFsp source tests continue to pass on hungdinh-lt without rebuilding already-good implementation."
  artifacts:
    - path: "config/lab.roles.example.json"
      provides: "Secret-free authoritative machine-role contract for D-20 through D-24"
    - path: "scripts/lab/Invoke-Phase1EnvironmentReconcile.ps1"
      provides: "Guarded inventory, developer-host cleanup, and role assertion"
    - path: "scripts/lab/Invoke-Dc01Server.ps1"
      provides: "LAB-DC01 Compose/PostgreSQL migration, listener, liveness, and readiness orchestration"
    - path: "tests/e2e/compose.rs"
      provides: "PostgreSQL migration ordering, idempotency, concurrency, and readiness assertions"
  key_links:
    - from: "scripts/lab/Invoke-Phase1EnvironmentReconcile.ps1"
      to: "config/lab.roles.example.json"
      via: "every audit, cleanup, deployment, or verification step asserts the named machine role before action"
      pattern: "Assert-DlpMachineRole"
    - from: "scripts/lab/Invoke-Dc01Server.ps1"
      to: "deploy/compose.yaml"
      via: "LAB-DC01 starts PostgreSQL, runs the versioned migration job, and only then starts the server"
      pattern: "service_completed_successfully|--migrate-only"
  flagged_assumptions:
    - { id: "EDGE-WRK-01-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify WRK-01; the closed ten-crate membership and preservation audit are required evidence." }
    - { id: "EDGE-WRK-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify WRK-02; typed identifier, decision, and redacted-error tests remain explicit evidence." }
    - { id: "EDGE-WRK-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify WRK-03; versioned DTO and unsupported-version tests remain explicit evidence." }
    - { id: "EDGE-WRK-04-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify WRK-04; portable unsafe-code denial and isolated Windows FFI audits remain explicit evidence." }
  edge_resolutions:
    - { id: "EDGE-SRV-01-CONCURRENCY", status: covered, statement: "Concurrent authenticated requests on LAB-DC01 preserve route authorization and repository invariants without cross-device state." }
    - { id: "EDGE-SRV-11-IDEMPOTENCY", status: covered, statement: "Repeated SQLx migration execution on LAB-DC01 leaves one applied ledger state and no duplicate authority rows." }
    - { id: "EDGE-SRV-11-CONCURRENCY", status: covered, statement: "Concurrent LAB-DC01 migration/server starters serialize safely and never bind before the complete expected ledger." }
    - { id: "EDGE-SRV-12-CONCURRENCY", status: covered, statement: "Concurrent LAB-DC01 liveness/readiness probes are read-only, deterministic, and do not mutate tokens, devices, bundles, or health rows." }
  prohibitions:
    - "flagged-unverified: MUST NOT remove LLVM, Rust, Hyper-V, source trees, or unrelated developer software from hungdinh-lt while cleaning the exact disallowed WinFsp and DLP endpoint artifacts."
    - "flagged-unverified: MUST NOT present a hungdinh-lt or SQLite result as PostgreSQL, server-host, DPAPI, WinFsp, endpoint-service, restart, or user-session evidence."
    - "flagged-unverified: MUST NOT place credential values, tokens, private keys, protected content, or raw hardware serials in plans, scripts, command lines, logs, evidence, or commits."
---

<objective>
Move the already-built server slice into the binding lab topology, safely remove endpoint-runtime residue from the physical developer host, and prove the production PostgreSQL migration/listener/readiness path on LAB-DC01.

Purpose: Establish trustworthy machine placement before any endpoint evidence is collected and close the historical SQLite-only SRV-11 gap without rebuilding completed source.
Output: Guarded lab-role/cleanup orchestration, a LAB-DC01 PostgreSQL/server tracer, and repeatable migration/readiness tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-02-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-05-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-07-SUMMARY.md

<interfaces>
Preserve `run_migrations_from_environment`, `run_server`, `ProductionProviders`, `/health/live`, `/health/ready`, the three forward-only files under `migrations/`, and `deploy/compose.yaml`. The orchestration layer passes only machine names, scenario names, and a runtime secret-provider selector; it never transports secret values in arguments or repository files.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reconcile the four machine roles and serve one LAB-DC01 readiness path</name>
  <precondition>hungdinh-lt is the current orchestration machine; LAB-DC01, LAB-DC02, and LAB-CLIENT01 exist and are running; the runtime-only secret provider can supply server/DB/PKI material without command-line disclosure.</precondition>
  <files>config/lab.roles.example.json, deploy/compose.yaml, scripts/lab/Invoke-Phase1EnvironmentReconcile.ps1, scripts/lab/Invoke-Dc01Server.ps1, config/server.env.example</files>
  <read_first>
    - Every path in `<files>`, preserving existing Compose and environment contracts
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-20 through D-24
    - `01-02-SUMMARY.md`, `01-05-SUMMARY.md`, and `01-07-SUMMARY.md` PostgreSQL limitations and completed server interfaces
    - All three `migrations/*.sql` files and `crates/dlp-server/src/main.rs`
  </read_first>
  <action>Per D-20, D-21, D-22, D-23, and D-24, create a secret-free role manifest naming hungdinh-lt as `developer_orchestrator`, LAB-DC01 as `management_server_database_provisioning`, LAB-DC02 as `secondary_ad_authority`, and LAB-CLIENT01 as `endpoint_runtime`. Implement `Assert-DlpMachineRole` so every operation aborts before mutation when the actual computer name and expected role differ. In the reconcile script, first inventory exact WinFsp MSI registrations, DLP services/processes/mounts/directories, DLP-added certificate thumbprints, DLP-tagged hosts entries, and DLP-attributed network/domain changes on hungdinh-lt into ignored `target/phase1-reconcile`; preview the exact targets, then remove only the verified WinFsp runtime and exact DLP endpoint artifacts under `-Apply`. Do not remove or modify LLVM, Rust, Hyper-V, repositories, unrelated certificates/hosts entries/software, or base network/domain state. Implement the DC01 script so hungdinh-lt uses Hyper-V orchestration to execute Compose and SQLx migration commands inside LAB-DC01, resolves all sensitive material through the runtime secret provider inside LAB-DC01, applies migrations before listener start, and obtains `/health/live` and `/health/ready` from LAB-CLIENT01. Do not install WinFsp or any endpoint service on hungdinh-lt or LAB-DC01. Per D-20 through D-24, record the evidence-producing machine for every row.</action>
  <acceptance_criteria>
    - "On hungdinh-lt, the post-cleanup audit reports no WinFsp runtime, DLP endpoint service/process/mount, DPAPI credential, development DLP PKI trust entry, DLP hosts mapping, or DLP-attributed domain/network change, while Rust, LLVM, Hyper-V, and unrelated software match the pre-cleanup inventory."
    - "On LAB-DC01, PostgreSQL starts, all three versioned migrations complete, and the management listener binds only after the migration job succeeds."
    - "From LAB-CLIENT01, the management hostname reaches LAB-DC01 over validated TLS and returns the expected liveness/readiness states; hungdinh-lt output is orchestration metadata only."
    - "On LAB-DC02, the role guard confirms secondary-AD-only placement and makes no endpoint, provisioning, database, or server change."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/lab/Invoke-Phase1EnvironmentReconcile.ps1 -ExecutionMachine hungdinh-lt -ServerVm LAB-DC01 -SecondaryDcVm LAB-DC02 -EndpointVm LAB-CLIENT01 -Apply; then on hungdinh-lt orchestrating execution inside LAB-DC01 and a probe inside LAB-CLIENT01: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/lab/Invoke-Dc01Server.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-DC01 -ProbeMachine LAB-CLIENT01 -SecretProvider Runtime -Scenario Tracer</automated>
  </verify>
  <done>The physical host is developer-only, the four role guards pass, and one LAB-CLIENT01 probe reaches a migration-gated PostgreSQL-backed server on LAB-DC01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove PostgreSQL migration idempotency, concurrency, and readiness on LAB-DC01</name>
  <files>deploy/compose.yaml, scripts/lab/Invoke-Dc01Server.ps1, tests/e2e/compose.rs, config/server.env.example</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `migrations/202608070001_walking_skeleton.sql`, `202608070002_enrollment_authority.sql`, and `202608070003_authenticated_routes.sql`
    - `crates/dlp-server/src/{main,health,repository}.rs`
    - `.planning/docs/adrs/ADR-003-database-migrations.md`
  </read_first>
  <behavior>
    - Test 1: empty LAB-DC01 PostgreSQL applies each migration once and a repeated run is ledger-idempotent.
    - Test 2: checksum drift and a deliberately failed migration keep the LAB-DC01 server unbound/unready.
    - Test 3: concurrent migration/server starters converge on one complete ledger and one listener without duplicate authority/configuration state.
    - Test 4: concurrent liveness/readiness probes are deterministic and read-only.
  </behavior>
  <action>Add integration assertions around the real PostgreSQL SQLx ledger, schema constraints, migration-before-bind ordering, and concurrent startup. Keep `migrations-sqlite/` available only for isolated local tests explicitly named as such; no SQLite result may satisfy these assertions. Harden Compose for the existing LAB-DC01 deployment while keeping secret values outside YAML, images, environment examples, commands, logs, and commits. Extend the LAB-DC01 orchestration with `PostgresFresh`, `PostgresRepeat`, `MigrationFailure`, `ConcurrentStart`, and `ReadinessConcurrency` scenarios, each producing sanitized machine-tagged evidence. Run existing source/unit checks on hungdinh-lt and the database/listener scenarios inside LAB-DC01; run the remote TLS readiness probe inside LAB-CLIENT01.</action>
  <acceptance_criteria>
    - "On LAB-DC01, fresh and repeated SQLx runs show exactly the expected three checksummed migrations and no automatic production seed rows."
    - "On LAB-DC01, checksum drift, failed migration, and incomplete dependency state prevent listener/readiness success without corrupting the prior database."
    - "On LAB-DC01, concurrent starters converge on one migration ledger/listener and concurrent readiness probes perform no token, device, bundle, credential, or health mutation."
    - "On hungdinh-lt, `cargo test` and Compose source checks pass as source evidence only; on LAB-CLIENT01, the TLS readiness probe confirms the deployed LAB-DC01 service."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: cargo test --locked -p dlp-server -p dlpctl; then on hungdinh-lt orchestrating LAB-DC01: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/lab/Invoke-Dc01Server.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-DC01 -ProbeMachine LAB-CLIENT01 -SecretProvider Runtime -Scenario All</automated>
  </verify>
  <done>SRV-11 and SRV-12 have genuine LAB-DC01 PostgreSQL/migration/listener evidence plus a LAB-CLIENT01 reachability probe, with historical SQLite evidence correctly bounded to isolated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hungdinh-lt orchestration → named VMs | A command executed on the wrong machine could install runtime state, collect invalid evidence, or damage developer tooling. |
| LAB-DC01 server → PostgreSQL | Migration ordering, concurrent startup, and secret injection determine authoritative state. |
| LAB-CLIENT01 probe → LAB-DC01 TLS listener | The client must validate the configured server identity; orchestration output is not endpoint evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13-01 | Tampering/Denial of service | developer-host cleanup | high | mitigate | Exact computer-role assertion, inventory/dry-run, target allowlist, post-diff, and explicit LLVM/Rust/Hyper-V exclusions in `Invoke-Phase1EnvironmentReconcile.ps1`. |
| T-01-13-02 | Tampering | PostgreSQL migrations | high | mitigate | SQLx checksummed migrations, failure-before-bind, idempotency and concurrent-start tests on LAB-DC01. |
| T-01-13-03 | Information disclosure | runtime secret flow | high | mitigate | Resolve secrets only inside the runtime provider on LAB-DC01; forbid values in arguments, files, logs, evidence, and commits. |
| T-01-13-04 | Spoofing | evidence provenance | high | mitigate | Machine-role guard plus machine-tagged evidence; LAB-CLIENT01 performs the remote TLS probe and hungdinh-lt cannot satisfy runtime claims. |
| T-01-13-05 | Repudiation | cleanup/deployment evidence | medium | mitigate | Hash-sealed sanitized before/after inventories and scenario results under ignored runtime output. |

## ASVS L1 Gate

Mitigate every high-severity row before completion. The executor blocks the plan if cleanup target validation, PostgreSQL migration ordering, runtime-only secret injection, or machine provenance is absent or failing.
</threat_model>

<verification>
On hungdinh-lt, run source tests and the guarded orchestration entry points. Inside LAB-DC01, run PostgreSQL/Compose/migration/readiness scenarios. Inside LAB-CLIENT01, run the server-identity/readiness probe. Inspect the sanitized role/cleanup evidence and reject any row whose machine is absent or mismatched.
</verification>

<success_criteria>
- hungdinh-lt is clean of disallowed endpoint runtime while developer tooling is preserved.
- LAB-DC01 supplies real PostgreSQL, migrations, server liveness, and readiness.
- LAB-DC02 remains an independent secondary AD authority.
- LAB-CLIENT01 supplies the remote TLS probe and is reserved for endpoint runtime.
- All high-severity threat mitigations have passing machine-specific evidence.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New path | `config/lab.roles.example.json` | Four-machine role contract without secrets |
| New path | `scripts/lab/Invoke-Phase1EnvironmentReconcile.ps1` | Safe host inventory/cleanup and VM-role validation |
| New symbol | `Assert-DlpMachineRole` | Fails before any command runs on the wrong machine |
| New symbol | `Invoke-DeveloperHostCleanup` | Removes only verified WinFsp/DLP endpoint residue from hungdinh-lt |
| New path | `scripts/lab/Invoke-Dc01Server.ps1` | LAB-DC01 PostgreSQL/server scenario runner |
| New symbol | `Invoke-Dc01PostgresProof` | Executes fresh/repeat/failure/concurrency migration evidence |
| New path | `tests/e2e/compose.rs` | Real PostgreSQL migration/startup assertions |
| New flags | `-ExecutionMachine`, `-CallerMachine`, `-ProbeMachine`, `-SecretProvider`, `-Scenario`, `-Apply` | Explicit machine/provenance and guarded-operation selectors |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md` when done.
</output>
