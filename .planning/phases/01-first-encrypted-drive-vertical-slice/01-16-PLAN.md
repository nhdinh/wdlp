---
phase: 01-first-encrypted-drive-vertical-slice
plan: 16
type: execute
wave: 10
depends_on: [01-20]
files_modified:
  - tests/e2e/phase1_vertical_slice.rs
  - tests/e2e/compose.rs
  - tests/windows/Invoke-Phase1Matrix.ps1
  - tests/windows/fixtures/manifest.json
  - tests/windows/README.md
  - tests/windows/results/.gitkeep
  - tests/windows/results/phase1-evidence.json
  - tests/windows/results/phase1-evidence.sha256
autonomous: true
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, SRV-01, SRV-03, SRV-11, SRV-12, CRY-01, CRY-02, CRY-04, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, DRV-01, DRV-02, DRV-03, DRV-04, DRV-06, DRV-07, DRV-09, TST-01, TST-02, TST-03, TST-05, TST-08]
user_setup:
  - service: phase1-office-lab
    why: "D-16 requires Word and Excel in the LAB-CLIENT01 interactive session; evidence remains sanitized and secret-free."
    dashboard_config:
      - task: "Ensure Word and Excel are licensed and launchable for the eligible LAB-CLIENT01 user."
        location: "Existing Phase 1 Hyper-V lab"
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-25 through D-28, portable, focused Hyper-V, and signed visual tiers assigned to the production vertical slice and D-16 through D-18 matrix are all present; no mapped requirement or success criterion is waived or satisfied by a lower tier."
    - "Per D-29 through D-32, every row identifies its real provider or permitted fixture scope and rejects SQLite, mocks, fakes, callback emulation, host-side endpoint runs, and other runtime substitutes."
    - "Per D-33 through D-36, matrix-time elevated actions use the separately approved 01-16 manifest digest, captured baselines, strict role allowlists, pinned tools, idempotent apply/verify/remove, and success/failure cleanup."
    - "Per D-37 through D-47, every attempt, signed visual checklist, raw-artifact reference, staleness result, clock offset, environment fingerprint, rerun link, and redaction outcome validates through Plan 01-17."
    - "The production-provider path runs on the binding topology: provisioning and server on LAB-DC01, PostgreSQL on LAB-SERVER01, secondary AD on LAB-DC02, endpoint service/DPAPI/mTLS/WinFsp/user session on LAB-CLIENT01, and orchestration/source checks on hungdinh-lt."
    - "The real LAB-CLIENT01 drive completes enrollment, signed activation, per-user mount, encrypted copy/flush/readback, health, service restart, and Windows reboot while LAB-DC01 uses LAB-SERVER01 PostgreSQL and hungdinh-lt contains no endpoint runtime."
    - "D-16 has real LAB-CLIENT01 evidence for Explorer, PowerShell, Notepad, Microsoft Word, and Microsoft Excel with application/runtime/OS provenance."
    - "D-17 has real evidence for create, copy in/out, open, edit, save, Save As, rename, move, delete, and directory operations."
    - "D-18 has real evidence for 0 B, 1 B, 4 MiB - 1, 4 MiB, 4 MiB + 1, 100 MiB, and 1 GiB with authenticated hashes and non-vacuous backing plaintext scans."
  artifacts:
    - path: "tests/e2e/phase1_vertical_slice.rs"
      provides: "Production-provider enrollment-through-mounted-roundtrip integration test"
    - path: "tests/windows/Invoke-Phase1Matrix.ps1"
      provides: "Machine-role-aware vertical, application, operation, and size matrix"
    - path: "tests/windows/fixtures/manifest.json"
      provides: "Complete D-16, D-17, and D-18 case manifest"
    - path: "tests/windows/results/phase1-evidence.json"
      provides: "Sanitized case-level evidence with execution-machine provenance and hashes"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Authoritative current Phase 1 requirement, success-criterion, decision, tier, and checklist index"
  key_links:
    - from: "tests/e2e/phase1_vertical_slice.rs"
      to: "deploy/compose.yaml"
      via: "production PostgreSQL and migration providers on LAB-SERVER01, server provider on LAB-DC01"
      pattern: "Postgres|EnrollmentRequestV1|SignedConfigurationV1"
    - from: "tests/windows/Invoke-Phase1Matrix.ps1"
      to: "tests/windows/results/phase1-evidence.json"
      via: "every production trust, application, operation, and size row records its actual execution machine and sanitized outcome"
      pattern: "execution_machine|requirement_or_decision"
  flagged_assumptions:
    - { id: "EDGE-TST-01-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-01; matching, priority, conflict, defaults, empty input, and deterministic repetition remain named source evidence." }
    - { id: "EDGE-TST-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-02; valid, tampered, wrong-key, weak-key, truncated, schema, key-ID, audience, hash, replay, and concurrency cases remain named evidence." }
    - { id: "EDGE-TST-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-03; nonce, AAD, key, corruption, recovery, disk-full, and non-vacuous plaintext searches remain named evidence." }
    - { id: "EDGE-TST-05-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-05; PostgreSQL, AD, WinRM, PKI, TLS, enrollment, signed activation, and negative trust boundaries remain named evidence." }
    - { id: "EDGE-TST-08-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-08; real LAB-CLIENT01 OS, WinFsp, application versions, and every required application, operation, and size row remain named evidence." }
  prohibitions:
    - "flagged-unverified: MUST NOT mark a PostgreSQL, AD, WinRM, PKI, TLS, DPAPI, WinFsp, service, user-session, Office, restart, or reboot case passed without case-specific evidence from its binding machine."
    - "flagged-unverified: MUST NOT substitute SQLite, mocks, callback emulation, hungdinh-lt endpoint runs, or unsigned synthetic rows for the production-provider and LAB-CLIENT01 evidence."
    - "flagged-unverified: MUST NOT store protected content, raw paths or SIDs, raw serials, tokens, credential blobs, private keys, CA secrets, database credentials, or command-line secret values in evidence or history."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Run the production-provider encrypted-drive vertical slice and complete D-16 through D-18 application, operation, and size matrix on the binding four-machine topology.

Purpose: Prove the complete trust/data path and representative user workload before destructive D-19 failure execution and independent evidence sealing in Plan 01-21.
Output: Production E2E test, Windows/Office/size/operation harness, fixture manifest, and sanitized case evidence.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-22-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-23-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-18-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-19-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-20-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-01-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-04-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-09-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-10-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@evidence/phase1/requirement-matrix.yaml
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Treat completed server, enrollment, credential, configuration, service, session, WinFsp, encrypted-store, and recovery contracts as final. Evidence rows contain only allowlisted IDs, digests, codes, versions, timestamps, environment provenance, and artifact hashes—never protected input bytes or secrets.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the production-provider enrollment-to-restart encrypted roundtrip</name>
  <precondition>Plans 01-22, 01-23, 01-13 through 01-15, and 01-17 through 01-20 are green; the separate 01-16 privilege-manifest section has a valid digest-bound approval; all four named machines and runtime-secure providers are available.</precondition>
  <files>tests/e2e/phase1_vertical_slice.rs, tests/e2e/compose.rs, tests/windows/Invoke-Phase1Matrix.ps1, tests/windows/results/.gitkeep</files>
  <read_first>
    - Every path in `<files>` plus dependency summaries in `<context>`
    - `.planning/ROADMAP.md` Phase 1 goal and seven success criteria
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-01 through D-24
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md` every INTEGRATE and OPT-OUT capability
  </read_first>
  <behavior>
    - Test 1: LAB-DC01 applies PostgreSQL migrations, provisions through both DCs and Kerberos WinRM-over-HTTPS to LAB-CLIENT01, stores digest-only authority, and creates one single-use token through runtime-secure handling.
    - Test 2: LAB-CLIENT01 generates key/CSR, DPAPI-protects the constrained identity, uses revocation-aware mTLS, activates signed configuration, posts health, and launches one user-session drive host.
    - Test 3: a unique-marker file copied, flushed, and read through the drive has equal source/output SHA-256 and no marker in backing, cache, log, or evidence files.
    - Test 4: service restart reloads credential/current/LKG/store, remounts in the active eligible session, and reads the same committed hash; hungdinh-lt remains endpoint-clean.
  </behavior>
  <action>Create a production-provider harness that refuses fixture directory, certificate, database, TLS, DPAPI, WinFsp, or session providers. From hungdinh-lt, orchestrate commands into binding machines: LAB-DC01 runs PostgreSQL, migrations, server, and trusted provisioning; both DCs corroborate the computer; LAB-DC01 collects LAB-CLIENT01 hardware through Kerberos WinRM-over-HTTPS; LAB-CLIENT01 runs endpoint CSR, DPAPI, mTLS, signed activation, service, session host, WinFsp, encrypted write/read, and service restart. Generate the random marker only inside LAB-CLIENT01, hash before/after, and scan all backing, cache, log, and evidence paths non-vacuously. Add negative trust cases for wrong fingerprint, DC/CIM disagreement, reused or racing token, invalid CSR/profile, revoked prior serial, wrong server identity, bad signed bundle, forged host IPC, corrupt ciphertext, and wrong execution machine. Per D-33 through D-36, enforce the separate 01-16 manifest. Per D-25, D-27, D-29 through D-32, and D-37 through D-47, publish one immutable attempt per positive/negative case with tier, machine, build/procedure/environment/time, raw-artifact hashes, staleness, deviation, redaction, and prior links.</action>
  <acceptance_criteria>
    - "LAB-DC01 with LAB-DC02 corroboration completes PostgreSQL and production AD, WinRM, PKI, and TLS provisioning/enrollment without fixture providers, raw serial persistence, or server-held device private key."
    - "LAB-CLIENT01 completes DPAPI custody, device mTLS, signed activation, user-session WinFsp mount, encrypted flush/readback, health, and service-restart remount."
    - "Source/output hashes match and non-vacuous marker/secret scans are clean; every negative trust case returns no unauthorized state or bytes."
    - "hungdinh-lt remains developer/orchestration only, and no host result is counted as endpoint evidence."
    - "Every trust-chain case is a distinct schema-valid current or retained attempt; no substitute, stale, deviated, inaccessible, skewed, unsanitized, or wrong-machine result passes."
  </acceptance_criteria>
  <verify>
    <automated>
powershell -NoProfile -ExecutionPolicy Bypass -Command {
  $ErrorActionPreference = 'Stop'
  cargo test --locked --workspace
  if ($LASTEXITCODE -ne 0) { throw 'workspace source tests failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-Phase1Matrix.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -SecretProvider Runtime -Scenario VerticalSlice
  if ($LASTEXITCODE -ne 0) { throw 'four-machine vertical slice failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario VerticalSlice
  if ($LASTEXITCODE -ne 0) { throw 'VerticalSlice evidence verification failed' }
  if (-not (Test-Path 'evidence/phase1/requirement-matrix.yaml')) { throw 'required Phase 1 evidence matrix is missing' }
}
    </automated>
  </verify>
  <done>The four-machine topology completes the production enrollment, activation, mount, encrypted roundtrip, and service-restart path with negative trust boundaries and clean provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the complete LAB-CLIENT01 application, operation, and size matrix</name>
  <precondition>The Task 1 vertical slice is green and Explorer, PowerShell, Notepad, Word, and Excel are launchable for the eligible LAB-CLIENT01 user.</precondition>
  <files>tests/windows/Invoke-Phase1Matrix.ps1, tests/windows/fixtures/manifest.json, tests/windows/README.md, tests/windows/results/phase1-evidence.json, tests/windows/results/phase1-evidence.sha256</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-11 through D-18 and D-21/D-24
    - `.planning/docs/adrs/ADR-001-winfsp-framework.md`
    - `01-10-SUMMARY.md`, `01-15-SUMMARY.md`, and `01-20-SUMMARY.md`
  </read_first>
  <action>Define a manifest covering every D-16, D-17, and D-18 item: Explorer, PowerShell, Notepad, Word, Excel; create, copy in/out, open, edit, save, Save As, rename, move, delete, directory create/enumerate; 0 B, 1 B, 4 MiB - 1, 4 MiB, 4 MiB + 1, 100 MiB, and 1 GiB. Run all actions inside the eligible LAB-CLIENT01 user session. Automate PowerShell operations, Shell/Explorer observation, Word/Excel COM with alerts disabled, and deterministic Notepad interaction. Per D-26 and D-38, route only visible drive presence/absence, Explorer, Word/Excel behavior, mount-failure recovery, and restart recovery to explicit signed checklist records with authenticated domain identity, UTC, machine, build, expected behavior, and pass/fail. For every case, close/reopen, compare hashes where applicable, perform a non-vacuous backing marker scan, and record OS, WinFsp, agent, server, migration, and application provenance. Record non-applicable combinations only with specific rationale while still covering each required item. Per D-39 through D-47, commit only sanitized manifests/checklists, keep bulky or sensitive raw artifacts in controlled storage by immutable ID/hash, apply staleness/time/artifact/redaction gates, and preserve failures and reruns.</action>
  <acceptance_criteria>
    - "Evidence contains every D-16 application, D-17 operation, and D-18 size/chunk boundary at least once with no unexplained missing row."
    - "Word and Excel open, edit, save, Save As, and reopen successfully, and Explorer/Notepad visible behavior has an end-of-phase human outcome."
    - "Every successful file case has source/output hash equality after close/reopen and a clean non-vacuous encrypted-backing marker scan."
    - "No application/runtime row names hungdinh-lt as its execution machine."
    - "Every required visible row has a signed checklist record; every machine-verifiable row has automated evidence, and neither tier substitutes for the other."
  </acceptance_criteria>
  <verify>
    <automated>
powershell -NoProfile -ExecutionPolicy Bypass -Command {
  $ErrorActionPreference = 'Stop'
  powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-Phase1Matrix.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -Scenario ApplicationsOperationsSizes
  if ($LASTEXITCODE -ne 0) { throw 'application/operation/size matrix failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario MatrixEvidence
  if ($LASTEXITCODE -ne 0) { throw 'MatrixEvidence verification failed' }
  if (-not (Test-Path 'tests/windows/results/phase1-evidence.json')) { throw 'matrix evidence bundle is missing' }
  if (-not (Test-Path 'tests/windows/results/phase1-evidence.sha256')) { throw 'matrix evidence digest is missing' }
}
    </automated>
    <human-check>On LAB-CLIENT01, the authenticated domain operator reviews the visible drive plus Explorer, Word, Excel, mount-failure, and restart rows and signs the D-38 checklist records; machine-verifiable Notepad, hash, and status facts remain automated.</human-check>
  </verify>
  <done>The complete D-16 through D-18 corpus has real LAB-CLIENT01 evidence, authenticated readback hashes, human-visible application outcomes, and no readable backing marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Four-machine orchestration → evidence rows | Correct behavior on the wrong machine is invalid and can conceal unsafe deployment. |
| Production trust chain → mounted drive | Enrollment, AD, WinRM, PKI, TLS, DPAPI, signed config, session IPC, WinFsp, and store authentication must connect without fixture substitution. |
| Product/log files → sanitized evidence | Collection can leak credentials, identity data, paths, or protected content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16-01 | Spoofing/Repudiation | machine provenance | high | mitigate | Role-guarded commands and required execution machine in every attempt. |
| T-01-16-02 | Spoofing/Elevation | production trust chain | high | mitigate | No fixture providers and negative fingerprint, DC, CSR, cert, revocation, TLS, bundle, and IPC cases. |
| T-01-16-03 | Tampering/Information disclosure | encrypted-store result | high | mitigate | Hash equality, unique markers, authenticate-before-copy, corruption denial, and non-vacuous scans. |
| T-01-16-04 | Information disclosure | evidence/logs | high | mitigate | Strict sanitized schema, forbidden-field/marker scan, runtime-only secrets, and no protected payload bytes. |
| T-01-16-05 | Denial of service | large/application matrix | medium | mitigate | Bounded manifest, per-case timeout/cleanup, deterministic retry, and isolated test-file cleanup. |
| T-01-16-06 | Elevation | matrix mutations | high | mitigate | Separate digest-bound 01-16 approval, baseline, role allowlist, idempotent apply/verify/remove, cleanup, and pinned tools. |
| T-01-16-SC | Tampering | dependency/runtime provenance | high | mitigate | Verify approved Cargo.lock and WinFsp installer provenance; reject unaudited packages. |

## ASVS L1 Gate

Any unapproved mutation, wrong-machine result, fixture substitution, missing negative case, secret/plaintext marker, stale/deviated/inaccessible/skewed attempt, or invalid hash blocks the plan.
</threat_model>

<verification>
Run source tests on hungdinh-lt, server/provisioning on LAB-DC01 with LAB-DC02 corroboration, and every endpoint/application operation on LAB-CLIENT01. Validate every Plan 01-17 matrix/provenance rule before Plan 01-21 begins.
</verification>

<success_criteria>
- The production trust/data path passes on the binding topology.
- Every D-16 application, D-17 operation, and D-18 size has real LAB-CLIENT01 evidence.
- PostgreSQL backs deployment verification on LAB-DC01.
- Encrypted backing remains unreadable and machine/tier provenance is valid.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New path | `tests/e2e/phase1_vertical_slice.rs` | Production-provider four-machine vertical-slice test |
| New path | `tests/windows/Invoke-Phase1Matrix.ps1` | Vertical, application, operation, and size runner |
| New path | `tests/windows/fixtures/manifest.json` | D-16 through D-18 case manifest |
| New path | `tests/windows/README.md` | Machine prerequisites, commands, schema, and redaction rules |
| Generated path | `tests/windows/results/phase1-evidence.json` | Case outcomes, hashes, provenance, and visual records |
| Generated path | `tests/windows/results/phase1-evidence.sha256` | Intermediate sanitized evidence digest |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-16-SUMMARY.md` when done.
</output>
