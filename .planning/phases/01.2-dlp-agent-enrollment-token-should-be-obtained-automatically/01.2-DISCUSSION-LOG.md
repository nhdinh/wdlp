# Phase 01.2: dlp-agent-enrollment-token-should-be-obtained-automatically - Discussion Log

> **Audit trail only.** Decisions are captured in CONTEXT.md.

**Date:** 2026-08-28
**Phase:** 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically
**Areas discussed:** Failure recovery and token lifecycle, Existing-credential behavior, Operator-facing diagnostics

## Failure recovery and token lifecycle

| Option | Selected |
|--------|----------|
| Clean immediately; fresh provisioning on retry | ✓ |
| Retain temporarily for automatic retry | |
| Clean by default; explicit troubleshooting retention | |

**User choices:** Cleanup on failed install; cleanup failure is fatal; preserve partial installation for diagnosis; always request a fresh token on retry.

## Existing-credential behavior

| Option | Selected |
|--------|----------|
| Skip provisioning when `device.dpapi` exists | ✓ |
| Verify metadata/fingerprint before skipping | |
| Re-provision by default | |

**User choices:** Add `-ForceReenrollment`; require `-Apply`; preserve service/data/cache state and replace only credential/token state.

## Operator-facing diagnostics

| Option | Selected |
|--------|----------|
| High-level normal output only | ✓ |
| Full exception detail by default | |
| Stable code only | |

**User choices:** Stable error code plus safe remediation hint; explicit opt-in diagnostics; redact all secret material while allowing names, lengths, paths, fingerprints, and status.

## the agent's Discretion

- Exact diagnostic-switch name and project-consistent error wording.

## Deferred Ideas

None.
