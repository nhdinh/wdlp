---
phase: 02-policy-enforcement-and-user-feedback
plan: 09
type: tdd
wave: 8
depends_on: [02-05, 02-07, 02-08]
files_modified:
  - crates/dlp-windows-drive/src/filesystem.rs
  - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs
  - crates/dlp-windows-service/src/notification.rs
  - crates/dlp-windows-service/src/pipe.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/session.rs
  - crates/dlp-windows-service/src/lib.rs
  - crates/dlp-windows-service/tests/companion_grants.rs
  - crates/dlp-windows-service/tests/session_lifecycle.rs
autonomous: true
requirements: [POL-06, POL-09, DRV-05, DRV-08, UI-02, UI-03]
estimate: {tokens: 46000, raw_tokens: 46000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Every real drive-host callback sends a bounded correlated decision request and waits for the service-owned event/grant response before WinFsp returns."
    - "Timeout, disconnect, malformed/mismatched response, unavailable service, or absent grant denies; snapshot frames cannot be mistaken for decision responses."
    - "The service records every decision event before feedback; notification dedupe never changes event count per D-18/D-19."
    - "Notification projection contains base file name and the other D-17 allowlisted fields, never full path/content/matches/SID/secrets/internal identifiers."
    - "First toast always emits; exact decision-key repeats within 30 seconds update count, while different user/file/operation/rule/action/policy never group."
  artifacts:
    - {path: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs", provides: "Production correlated decision/event/grant client"}
    - {path: "crates/dlp-windows-service/src/notification.rs", provides: "Privacy projection, dedupe model, and sink trait"}
    - {path: "crates/dlp-windows-service/tests/session_lifecycle.rs", provides: "Built-host synchronous request/response integration"}
  key_links:
    - {from: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs", to: "crates/dlp-windows-service/src/service.rs", via: "tagged request IDs correlate callback decision and exact response", pattern: "DecisionEvent(Request|Response)|request_id"}
    - {from: "crates/dlp-windows-service/src/service.rs", to: "crates/dlp-windows-service/src/notification.rs", via: "event-first allowlisted projection then presentation-only grouping", pattern: "record.*notify|NotificationModel"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Presentation grouping cannot suppress, merge, or authorize enforcement events or grants."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Connect mounted callbacks to the authoritative service and create privacy-safe, event-independent notification intents.

Purpose: Injection-only ports do not prove that production callbacks wait for service authority or that feedback grouping preserves evidence.
Output: Multiplexed request/response transport, event-first routing, safe projection/dedupe, and built-process tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@crates/dlp-windows-drive/src/filesystem.rs
@crates/dlp-windows-drive/src/bin/dlp-drive-host.rs
@crates/dlp-windows-service/src/pipe.rs
@crates/dlp-windows-service/src/service.rs

<interfaces>
Plan 02-05 owns tagged snapshot frames; Plan 02-08 owns grant state and companion identity. Add distinct DecisionEventRequestV1/ResponseV1 tags and a pure NotificationModel projection without changing those authorities.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one warn callback through the built drive host and exact service response</name>
  <read_first>
    - 02-CONTEXT.md — D-14 through D-16 and D-19.
    - crates/dlp-windows-drive/src/filesystem.rs — Plan 02-07 decision/event boundary.
    - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs — Plan 02-05 tagged control dispatcher.
    - crates/dlp-windows-service/src/pipe.rs and service.rs — authenticated actor/service authority.
    - crates/dlp-windows-service/tests/session_lifecycle.rs — built-process fixture.
  </read_first>
  <files>crates/dlp-windows-drive/src/filesystem.rs, crates/dlp-windows-drive/src/bin/dlp-drive-host.rs, crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/tests/session_lifecycle.rs</files>
  <behavior>
    - Exact top-level real_drive_host_warn_grant test makes a mounted warn block until a correlated service response and allows one exact consumed grant retry.
    - Policy update and decision response tags cannot cross-dispatch; unique request IDs wake only the matching callback.
    - Timeout, disconnect, duplicate/mismatch, malformed/oversized frame, or service rejection returns access denied before effect.
    - Service-authenticated session identity overrides all host-supplied identity fields.
  </behavior>
  <action>Write the exact named built-process RED test. Define bounded tagged DecisionEventRequestV1/ResponseV1 with unique request ID, redacted event, operation/file/content/policy fingerprints, and no caller-authoritative SID/session. In drive-host, strictly dispatch snapshot updates versus responses and wake only the matching callback. Filesystem callbacks wait a short bounded interval before effect; every transport uncertainty denies. Service binds actor runtime identity, records the event, and consumes the exact Plan 02-08 grant before returning success. Apply exact-symbol GitNexus impact, HIGH/CRITICAL warning, UNKNOWN text confirmation, and complete nonpartial detect-changes before commits.</action>
  <verify><automated>rtk cargo build --locked -p dlp-windows-drive --bin dlp-drive-host &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test session_lifecycle -- --list | rtk rg "^real_drive_host_warn_grant: test$" &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test session_lifecycle real_drive_host_warn_grant -- --exact</automated></verify>
  <acceptance_criteria>The named built-process test executes and proves callback-before-effect synchronization, exact one-use service authority, and fail-closed multiplexed transport.</acceptance_criteria>
  <done>The production mounted callback cannot complete without the exact authenticated service response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record every event before privacy-safe notification grouping</name>
  <read_first>
    - 02-CONTEXT.md — D-17, D-18, D-19.
    - 02-PATTERNS.md — corrected base-file-name allowlist and event-before-dedupe pattern.
    - crates/dlp-domain/src/lib.rs — normalized enforcement event fields.
    - crates/dlp-windows-service/src/service.rs — Task 1 event entry.
  </read_first>
  <files>crates/dlp-windows-service/src/notification.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/session.rs, crates/dlp-windows-service/src/lib.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - Every allow/block/warn/allow_and_audit/inspection_failed attempt records one event before notification logic.
    - Projection includes base file name, operation, safe display name, stable reason, remediation, count, and opaque action only for warn.
    - Empty/invalid basename becomes generic protected-item; prohibited full path/content/match/SID/secret/internal ID fields are absent.
    - First item emits; repeats before 30 seconds update exact-key count; equality at 30 seconds starts a new group; events remain one per attempt under concurrency.
  </behavior>
  <action>Write event-order/privacy/dedupe tests first. Add a pure allowlisted NotificationModel and injected NotificationSink. Base name is permitted and required by D-17/UI-03; full path and all other prohibited fields never cross the projection. Use a 30-second window, 60-second hard maximum, exact decision key, monotonic update sequence, deterministic bounded state, and separate SID/session partitions. Record the event before grouping or IPC. Apply Task 1 GitNexus gates.</action>
  <verify><automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants &amp;&amp; rtk cargo clippy --locked -p dlp-windows-service -p dlp-windows-drive --all-targets -- -D warnings</automated></verify>
  <acceptance_criteria>Event count equals attempts; safe projection includes the base file name and no prohibited data; exact grouping and boundary/concurrency behavior pass.</acceptance_criteria>
  <done>The service emits privacy-safe feedback intents without weakening evidence or authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Drive host -> service | Decision and retry authorization cross authenticated process IPC. |
| Event -> notification model | Rich metadata is minimized before interactive delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-09-01 | Tampering | Multiplexed actor channel | high | mitigate | Tagged bounded frames, correlated IDs, exact response, timeout-deny. |
| T-02-09-02 | Spoofing | Decision identity | high | mitigate | Bind SID/session from authenticated actor runtime only. |
| T-02-09-03 | Information Disclosure | Notification projection | high | mitigate | D-17 allowlist with base filename and explicit prohibited fields. |
| T-02-09-04 | Repudiation | Event/dedupe ordering | medium | mitigate | Event-before-grouping and concurrent attempt-count tests. |
| T-02-09-SC | Tampering | Package supply chain | low | accept | No package install or dependency change. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Decisions create authoritative events and safe feedback | 02-09 | COVERED |
| REQ | POL-06, POL-09, DRV-05, DRV-08, UI-02, UI-03 | Effects, events, isolation, denial, authenticated safe feedback | 02-09 | COVERED |
| RESEARCH | — | Production request/response, event-first projection, dedupe | 02-09 | COVERED |
| CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19 | Fail-closed service authority and privacy/grouping | 02-09 | COVERED |
</source_audit>

<verification>Run the built-host named-test gate, notification/event suites, clippy, and complete GitNexus change detection.</verification>
<success_criteria>Real callbacks wait for exact service authority; every attempt creates an event; only D-17 fields reach grouped feedback.</success_criteria>

## Artifacts this phase produces

- Production decision/event/grant request-response transport
- Safe NotificationModel and grouping state
- Built-host and notification integration coverage

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-09-SUMMARY.md` with process evidence, event/projection matrix, TDD commits, and GitNexus results.</output>
