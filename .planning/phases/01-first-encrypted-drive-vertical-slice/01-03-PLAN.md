---
phase: 01-first-encrypted-drive-vertical-slice
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [WRK-04, CRY-01, TST-03, TST-08]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No SUS/ASSUMED Cargo package is added to a manifest or lockfile until its exact registry/repository/publisher/version/license/build-script evidence is explicitly approved."
    - "No task creates persisted encrypted user data until the human selects the Phase 1 cipher/chunk/record/commit format."
    - "Approval of AES-256-GCM with 4 MiB chunks records the exact format ID and migration consequence consumed by 01-04 and all downstream plans."
  artifacts: []
  key_links:
    - from: "01-03 package checkpoint"
      to: "01-06, 01-08, and 01-10 dependency installs"
      via: "blocking approval signal names exact packages/versions before manifest changes"
      pattern: "approved:"
    - from: "01-03 persisted-format checkpoint"
      to: "01-04 encrypted record writer"
      via: "01-04 depends_on 01-03 and records the selected format ID"
      pattern: "approve-aes-4m"
---

<objective>
Resolve the Phase 1 supply-chain and persisted-format gates before any gated install or encrypted-store write occurs.

Purpose: Put the human WinFsp/package approval and the one-way on-disk decision ahead of every dependent implementation path.
Output: Two explicit approval records in `01-03-SUMMARY.md`; no production or data files are created by this plan.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/docs/adrs/ADR-001-winfsp-framework.md
@.planning/docs/adrs/ADR-006-key-hierarchy.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve every SUS/ASSUMED Cargo dependency before installation</name>
  <read_first>
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md` Package Legitimacy Audit, Standard Stack, Assumptions Log, and sources
    - `Cargo.toml` and `Cargo.lock` if 01-01/01-02 have executed; verify the gated names are absent
  </read_first>
  <what-built>Research marked `winfsp` SUS and left phase-specific legitimacy for `ldap3`, `rcgen`, `windows`, `sha2`, `secrecy`, `zeroize`, `reqwest`, `uuid`, `thiserror`, `tracing`, `tracing-subscriber`, `tempfile`, and `wiremock` assumed or incompletely audited. Downstream plans require an explicit exact-package decision before any is committed.</what-built>
  <how-to-verify>
    1. Inspect each package's crates.io page, linked source repository, publisher/owner history, recent non-yanked release, license, dependency graph, build script, and native payload.
    2. For `winfsp`, additionally inspect `https://crates.io/crates/winfsp`, `https://github.com/SnowflakePowered/winfsp-rs`, docs.rs ownership/source links, and exact `0.13.0+winfsp-2.1` dependency/runtime relationship.
    3. Record exact approved versions and source links; reject unexplained publisher takeover, mismatched repository, unexpected build/native payload, yanked release, or unacceptable license.
    4. If `winfsp` is rejected, stop Phase 1; ADR-001's Dokany fallback requires fresh research/coverage/planning and is not substituted automatically.
  </how-to-verify>
  <acceptance_criteria>Every named SUS/ASSUMED package has an explicit approve/reject result with exact version and evidence; auto-advance can never satisfy this checkpoint.</acceptance_criteria>
  <resume-signal>Type `approved:` followed by every approved package/version, or list rejected packages with evidence.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the persisted encrypted-store format before the first write</name>
  <read_first>
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md` Assumption A1, Generation-based encrypted file commit, Standard Stack, and AEAD pitfall
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-12 through D-15 and D-18
    - `.planning/docs/adrs/ADR-006-key-hierarchy.md`
  </read_first>
  <decision>Choose the Phase 1 persisted record format before 01-04 creates stores whose later change requires a migration.</decision>
  <context>D-12 through D-15 lock durability/integrity behavior but leave cipher/chunk sizing to planning. Research recommends AES-256-GCM, 4 MiB logical chunks, random persisted 96-bit nonce per record, identity-bound AAD, staged generations, encrypted manifest, and authenticated commit/pointer publication.</context>
  <options>
    <option id="approve-aes-4m">
      <name>Approve AES-GCM 4 MiB (Recommended)</name>
      <pros>Matches the research, D-18 boundary corpus, available audited primitive, and generation recovery model.</pros>
      <cons>Changing cipher/chunk/record layout after stores exist requires an explicit migration.</cons>
    </option>
    <option id="revise-format">
      <name>Revise and replan</name>
      <pros>Allows a different cipher, chunk size, or record layout to receive security/package research before persisted bytes exist.</pros>
      <cons>Stops execution until SKELETON, storage, WinFsp, and validation plans are revised consistently.</cons>
    </option>
  </options>
  <acceptance_criteria>`approve-aes-4m` records format ID `dlp-store/aes256gcm-4m/v1` and authorizes 01-04; `revise-format` halts before any persisted encrypted data is created.</acceptance_criteria>
  <resume-signal>Select: approve-aes-4m or revise-format.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry/repository evidence → dependency graph | Typosquatting, takeover, build scripts, and native payloads can execute during build/runtime. |
| Human format choice → all persisted store readers/writers | Cipher/chunk/AAD/commit choices become migration-sensitive on-disk authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | Cargo dependencies | high | mitigate | Blocking human evidence review of every SUS/ASSUMED exact package/version before install; rejected packages halt/research. |
| T-01-07 | Tampering/Information disclosure | persisted-format decision | high | mitigate | Approve explicit AEAD/nonce/AAD/chunk/generation/commit contract before writer exists; version records and require migration for change. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Decision evidence | Executable/blocking evidence |
|---|---|---|---|
| V6 Cryptography | Cryptographic algorithm, nonce, AAD, and key-record boundaries are explicitly approved | `approve-aes-4m` records `dlp-store/aes256gcm-4m/v1` | 01-04 cannot execute without this plan's completed summary |
| V10 Malicious Code | Third-party components are provenance-checked before use | exact package/version/source approval record | lockfile absence before approval; later exact-lock checks |
| V14 Configuration | Security-critical choices are explicit and auditable | checkpoint outcomes in `01-03-SUMMARY.md` | unresolved/rejected outcome blocks dependent plans |

**Completion gate:** Any rejected/unresolved package, unresolved persisted-format choice, or unresolved critical/high threat blocks this plan and every dependent plan.
</threat_model>

<verification>
Confirm the summary contains both explicit outcomes, exact package/version evidence, the selected format ID, and no manifest/lock/store modification performed by this plan.
</verification>

<success_criteria>
- Every SUS/ASSUMED package is explicitly approved or Phase 1 halts.
- The persisted format is explicitly approved or Phase 1 halts before storage writes.
- Dependent plan ordering enforces both approvals.
- No critical/high threat remains unresolved.
</success_criteria>

## Artifact Inventory

No production, configuration, migration, test, or persisted-data file is created or modified. The executor writes only the required `01-03-SUMMARY.md` approval record after both checkpoints resolve.

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-03-SUMMARY.md` when done.
</output>
