---
phase: 01-first-encrypted-drive-vertical-slice
plan: 14
type: execute
wave: 5
depends_on: [01-13]
files_modified:
  - crates/dlp-agent-core/Cargo.toml
  - crates/dlp-agent-core/src/enrollment.rs
  - crates/dlp-agent-core/src/client.rs
  - crates/dlp-windows-service/src/credential.rs
  - tests/windows/Invoke-AgentServiceSmoke.ps1
autonomous: true
requirements: [SRV-03, CRY-04, AGT-02, AGT-03, TST-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-25 through D-27, enrollment, DPAPI, mTLS, and health completion requires focused evidence from LAB-DC01, LAB-DC02, and LAB-CLIENT01 in addition to portable tests; these machine-verifiable facts do not route to visual confirmation."
    - "Per D-30 and D-31, the development CA preserves production-shaped server/device roles, EKUs, identity binding, replacement and revocation, and the LAB-CLIENT01 virtual-system-disk fixture proves exact match plus identity-change rejection without being represented as physical-hardware attestation."
    - "Per D-01 through D-06 and D-21/D-24, LAB-CLIENT01 automatically enrolls through LAB-DC01, generates the ECDSA P-256 key locally, sends only its CSR and exact version-1 hardware observation, and uses the returned constrained device certificate for mTLS."
    - "Plans 01-22 and 01-23 own the server enrollment, route, TLS, repository, PKI, and migration implementation; Plan 01-13 has already published current approved dual-DC/Kerberos provisioning evidence and the exact unused allowlist/token record before this client plan runs."
    - "The endpoint private key, returned chain, serial, expiry, and token-until-consumed exist only in one UI-forbidden machine-DPAPI blob with a validated SYSTEM/service-SID-only ACL; hungdinh-lt and both DCs hold no endpoint credential blob."
    - "Missing, corrupt, wrong-machine, or ACL-invalid credentials cause a fresh key/CSR and complete replacement enrollment; LAB-DC01 atomically revokes the old serial before the replacement client proceeds."
    - "Per D-33 through D-47, every 01-14 privileged mutation is separately digest-approved, baseline/cleanup/idempotence checked, role-allowlisted, and published as immutable sanitized evidence with time, environment, raw-artifact, staleness, and supersession validation."
  artifacts:
    - path: "crates/dlp-agent-core/src/enrollment.rs"
      provides: "Initial and replacement enrollment coordinator"
    - path: "crates/dlp-windows-service/src/credential.rs"
      provides: "UI-forbidden machine-DPAPI credential store with service-only ACL"
    - path: "tests/windows/Invoke-AgentServiceSmoke.ps1"
      provides: "LAB-CLIENT01 enrollment, DPAPI, mTLS, revocation, and health evidence"
  key_links:
    - from: "crates/dlp-agent-core/src/enrollment.rs"
      to: "crates/dlp-windows-service/src/credential.rs"
      via: "the endpoint-local key and validated returned identity are committed to DPAPI before the device-mTLS client is constructed"
      pattern: "EnrollmentCoordinator|DpapiCredentialStore"
    - from: "crates/dlp-agent-core/src/client.rs"
      to: "crates/dlp-server/src/routes.rs"
      via: "bootstrap TLS becomes revocation-aware device mTLS after the credential commit"
      pattern: "AgentHttpClient|client_auth_cert"
  flagged_assumptions:
    - { id: "EDGE-SRV-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify SRV-03; initial, expiry, reuse, wrong-device, replacement, and race evidence stays explicit." }
    - { id: "EDGE-AGT-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-02; DPAPI flags, owner/DACL, ordinary-user denial, tamper, and replacement evidence stays explicit." }
  edge_resolutions:
    - { id: "EDGE-CRY-04-IDEMPOTENCY", status: covered, statement: "Repeated protect/load/restart operations on LAB-CLIENT01 converge on one valid DPAPI credential without emitting plaintext." }
    - { id: "EDGE-CRY-04-CONCURRENCY", status: covered, statement: "Concurrent protect/load/replace operations on LAB-CLIENT01 atomically select one validated DPAPI blob and never expose an unprotected file." }
  prohibitions:
    - "flagged-unverified: MUST NOT downgrade an enrolled LAB-CLIENT01 agent to bearer authentication, proceed after DPAPI or ACL validation failure, or use a replacement certificate before LAB-DC01 revokes the prior serial."
    - "flagged-unverified: MUST NOT describe the exact hardware allowlist as TPM-backed or hardware-rooted remote attestation; privileged-local spoofing remains an explicit residual risk."
    - "flagged-unverified: MUST NOT persist or emit raw serials, enrollment tokens, endpoint private keys, DPAPI plaintext, certificate-secret material, protected content, or sensitive paths."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Complete the LAB-CLIENT01 side of trusted initial/replacement enrollment, endpoint-local private-key custody, device mTLS, and redacted health against the already deployed server authority.

Purpose: Establish the endpoint credential boundary independently before signed-configuration caching and SCM lifecycle expansion.
Output: Completed endpoint enrollment/client/DPAPI code plus focused LAB-CLIENT01 evidence and immutable proofs of the prior LAB-DC01 server/provisioning contracts.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-06-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-07-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-22-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-23-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Consume the final `ProvisionDeviceRequestV1`, `EnrollmentRequestV1`, `EnrollmentResponseV1`, LAB-DC01 `/api/v1/enrollment`, administrator/device route partitions, PostgreSQL authority, dual-DC verifier, certificate issuer, and active-serial lookup from Plans 01-22/01-23. This plan modifies only `EnrollmentCoordinator`, `EnrollmentTransport`, `AgentHttpClient`, `DpapiCredentialStore`, and the LAB-CLIENT01 scenario harness; server enrollment/routes/TLS/repositories/migrations are immutable inputs proved by their focused tests and Plan 01-13 evidence.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: LAB-CLIENT01 automatically enrolls and posts health with a DPAPI-held device credential</name>
  <precondition>Plans 01-22, 01-23, 01-13, and 01-17 are green; 01-13 `TrustedProvisioningApproved` evidence is current and proves the exact unused LAB-CLIENT01 allowlist/token record; the exact 01-14 privilege-manifest section has a valid digest-bound approval; LAB-CLIENT01 trusts only the approved public Phase 1 root.</precondition>
  <reversibility rating="costly">Per D-02 and D-05, the fingerprint tuple and certificate/DPAPI credential contract are persisted server/endpoint identities; changing either later requires coordinated migration.</reversibility>
  <files>crates/dlp-agent-core/Cargo.toml, crates/dlp-agent-core/src/enrollment.rs, crates/dlp-agent-core/src/client.rs, crates/dlp-windows-service/src/credential.rs, tests/windows/Invoke-AgentServiceSmoke.ps1</files>
  <read_first>
    - Every path in `<files>` plus final DTOs in `crates/dlp-protocol/src/lib.rs` and `crates/dlp-windows-service/src/fingerprint.rs`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-01 through D-06 and D-21 through D-24
    - `01-06-SUMMARY.md`, `01-07-SUMMARY.md`, `01-22-SUMMARY.md`, `01-23-SUMMARY.md`, and `01-13-SUMMARY.md`
    - `01-03-SUMMARY.md` exact human-approved `reqwest@0.13.4` dependency record
    - Research resolved PKI profile, DPAPI pitfall, and canonical fingerprint contract
  </read_first>
  <behavior>
    - Test 1: first startup sends the exact version-1 fingerprint observation/digest, device/computer identity, one-time token, and endpoint-signed ECDSA P-256 CSR but no private key; LAB-DC01 returns one constrained 30-day certificate identity.
    - Test 2: LAB-CLIENT01 validates root/hostname, certificate chain/profile/SAN/serial/expiry, commits key/chain/serial/metadata/token state to one UI-forbidden machine-DPAPI blob, erases the consumed token, builds device mTLS, and posts health.
    - Test 3: the DPAPI directory/file owner and DACL allow SYSTEM and the dedicated service SID only; an ordinary LAB-CLIENT01 user cannot list, read, or decrypt the file.
    - Test 4: token reuse, changed fingerprint, invalid returned profile, wrong root/name, absent client certificate, revoked serial, and attempted bearer fallback fail before credential commit or protected-route data.
  </behavior>
  <action>Per D-01 through D-05, implement the endpoint coordinator against the final Plan 01-22 DTOs: load the one-time token from the runtime secret provider, collect the exact local version-1 observation, generate an ECDSA P-256 key and signed CSR locally, and send token/observation/computer identity/CSR without any private key. Add the exact human-approved `reqwest@0.13.4` direct dependency already locked by Plan 01-23, then implement real bootstrap HTTPS against the installed public root with ordinary hostname validation, bounded body/timeouts, and no dangerous verifier; after success validate leaf/chain/profile/URI-SAN/serial/expiry/audience before any persistence. Complete `DpapiCredentialStore` with UI-forbidden machine DPAPI, same-directory atomic write/flush/replace and directory flush, bounded decode, SYSTEM plus dedicated-service-SID owner/DACL creation and revalidation, zeroization of private/token/plain buffers, consumed-token erasure, and fail-closed load. Construct the device-mTLS client only after the DPAPI commit and require it for health/configuration calls with no bearer fallback. Preserve non-Windows adapters only as explicit unit-test injection. Per D-30/D-31, validate the lab certificate roles and exact virtual-system-disk match/change denial without claiming hardware-rooted attestation. Treat server enrollment/routes/TLS/repository/migrations as immutable Plan 01-22/01-23 outputs: invoke and prove their exact focused tests/evidence, but do not edit them here. Per D-33 through D-47, apply only 01-14 endpoint mutations and publish immutable machine-correct initial-enrollment, DPAPI ACL, mTLS, health, and negative-trust attempts.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, initial startup reaches one active LAB-DC01 device record and posts health using device mTLS without an interactive session or transmitted private key."
    - "On LAB-CLIENT01, the credential blob is machine-DPAPI protected with UI forbidden, atomically stored, owner/DACL validated, and denied to an ordinary test user; no corresponding blob exists on hungdinh-lt, LAB-DC01, or LAB-DC02."
    - "Wrong root/name, invalid returned profile, timeout, absent client certificate, revoked serial, or attempted bearer fallback fails before protected agent data is returned."
    - "Initial, exact-match, disk-identity-change, DPAPI ACL, mTLS, and health attempts validate with binding machines, environment fingerprints, artifact hashes, and no secret markers; current 01-13 evidence proves the prerequisite dual-DC/Kerberos allowlist/token record."
  </acceptance_criteria>
  <verify>
    <automated>
powershell -NoProfile -ExecutionPolicy Bypass -Command {
  $ErrorActionPreference = 'Stop'
  cargo test --locked -p dlp-agent-core -p dlp-windows-service
  if ($LASTEXITCODE -ne 0) { throw 'agent enrollment and credential source tests failed' }
  cargo test --locked -p dlp-server --test server_enrollment
  if ($LASTEXITCODE -ne 0) { throw 'immutable server enrollment contract tests failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario InitialEnrollmentCredential
  if ($LASTEXITCODE -ne 0) { throw 'LAB-CLIENT01 initial enrollment scenario failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario InitialEnrollmentCredential
  if ($LASTEXITCODE -ne 0) { throw 'initial enrollment evidence verification failed' }
  if (-not (Test-Path 'evidence/phase1/requirement-matrix.yaml')) { throw 'required Phase 1 evidence matrix is missing' }
}
    </automated>
  </verify>
  <done>LAB-CLIENT01 completes initial enrollment with endpoint-local key custody, validated TLS/mTLS, consumed-token erasure, and a redacted health report against the immutable server authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-enroll after credential failure and prove atomic prior-serial revocation</name>
  <precondition>The runtime provider can request a replacement token through the same approved LAB-DC01 procedure without exposing either token; the prior serial remains queryable only as an opaque digest/status.</precondition>
  <files>crates/dlp-agent-core/src/enrollment.rs, crates/dlp-agent-core/src/client.rs, crates/dlp-windows-service/src/credential.rs, tests/windows/Invoke-AgentServiceSmoke.ps1</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - Plans 01-22/01-23 summaries and `tests/e2e/server_enrollment.rs` replacement/route tests
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-03, D-05, D-06, and D-30/D-31
    - Plan 01-17 evidence, privilege, redaction, and supersession contracts
  </read_first>
  <behavior>
    - Test 1: missing, corrupt, wrong-machine, or owner/DACL-invalid DPAPI blob is unusable and starts a fresh key/CSR replacement flow; the invalid blob is preserved only as opaque encrypted evidence or safely replaced.
    - Test 2: replacement sends the prior serial reference plus fresh token/observation/CSR, does not use the old private key as authority, and commits the returned credential only after profile validation.
    - Test 3: the server's immutable transactional contract revokes the prior serial before replacement success; old mTLS configuration/health calls fail while new serial calls succeed.
    - Test 4: fingerprint change, dual-DC failure, token race/reuse, invalid CSR/profile, server transaction failure, or local DPAPI commit failure leaves no usable replacement and never restores bearer/old-serial access.
  </behavior>
  <action>Per D-03, D-05, and D-06, make fail-closed credential loading distinguish missing, corrupt, wrong-machine, and ACL-invalid states using stable redacted codes, generate a fresh ECDSA key/CSR, and invoke the same complete replacement enrollment with prior-serial reference and a newly approved runtime token. Do not reuse an unverifiable private key or continue network activity with an invalid blob. Validate and atomically DPAPI-commit the returned replacement before building its mTLS client. The server transaction remains owned by Plans 01-22/01-23; prove it through the immutable server test plus LAB-DC01 status checks showing prior serial revoked before the client observes success and new serial active before health. Add endpoint negative tests for fingerprint/disk change, DC failure, token race/reuse, returned profile, server rollback, DPAPI write interruption, ordinary-user denial, and old/new concurrent requests. Per D-37 through D-47, publish separate immutable attempts, link replacement reruns, retain failures, and scan credential/log/evidence paths for tokens, keys, raw serials/SIDs/paths, protected content, and CA/database secrets.</action>
  <verify>
    <automated>
powershell -NoProfile -ExecutionPolicy Bypass -Command {
  $ErrorActionPreference = 'Stop'
  cargo test --locked -p dlp-agent-core -p dlp-windows-service replacement
  if ($LASTEXITCODE -ne 0) { throw 'agent replacement and credential failure tests failed' }
  cargo test --locked -p dlp-server --test server_enrollment replacement_
  if ($LASTEXITCODE -ne 0) { throw 'immutable server replacement transaction tests failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario ReplacementRevocation
  if ($LASTEXITCODE -ne 0) { throw 'LAB-CLIENT01 replacement/revocation scenario failed' }
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario ReplacementRevocation
  if ($LASTEXITCODE -ne 0) { throw 'replacement/revocation evidence verification failed' }
  if (-not (Test-Path 'evidence/phase1/requirement-matrix.yaml')) { throw 'required Phase 1 evidence matrix is missing' }
}
    </automated>
  </verify>
  <done>Every unusable local credential triggers complete re-enrollment; the old serial is rejected before the new credential posts health, and no invalid/bearer fallback is possible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LAB-CLIENT01 hardware/service → LAB-DC01 enrollment | Local observations, token, CSR, and replacement metadata are untrusted until server authority checks succeed. |
| Endpoint-generated key + server response → DPAPI file | The private key must stay local and the returned identity/profile plus owner/DACL must validate before use. |
| Device certificate → protected agent route | Chain validity is insufficient; SAN/serial must resolve to an active device on every request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Spoofing | enrollment identity | high | mitigate | Exact version-1 observations, LAB-DC01 allowlist and dual-DC checks, single-use token, signed CSR, and transaction-backed replacement. |
| T-01-14-02 | Information disclosure/Elevation | DPAPI credential | high | mitigate | UI-forbidden machine DPAPI, SYSTEM/service-SID owner/DACL, atomic file, ACL revalidation, zeroization, and ordinary-user denial. |
| T-01-14-03 | Spoofing | TLS/mTLS client | high | mitigate | Installed public root, ordinary hostname validation, returned profile validation, active serial lookup, bounded transport, and no bearer fallback. |
| T-01-14-04 | Information disclosure | diagnostics/evidence | high | mitigate | Stable codes/digests only and machine-specific marker scans for secrets, raw serials, paths, and protected content. |
| T-01-14-05 | Elevation/Repudiation | privileged enrollment changes | high | mitigate | Separate digest-bound 01-14 manifest, role guard, baseline/apply/verify/remove, cleanup, and immutable Plan 01-17 evidence. |
| T-01-14-SC | Tampering | dependency graph | high | mitigate | Preserve the approved Cargo.lock and reject any unaudited dependency change. |

## ASVS L1 Gate

Any unapproved mutation, bearer downgrade, transmitted or persisted plaintext secret, broad credential ACL, revoked-serial access, wrong-machine result, or invalid evidence attempt blocks the plan.
</threat_model>

<verification>
Run source/unit checks on hungdinh-lt only, including the immutable Plan 01-22/01-23 server contracts. Run initial/replacement enrollment, DPAPI/ACL, mTLS, and health scenarios inside LAB-CLIENT01 against the already provisioned LAB-DC01 authority. Require the current Plan 01-13 dual-DC/Kerberos provisioning attempt and validate every focused result through Plan 01-17.
</verification>

<success_criteria>
- LAB-CLIENT01 enrolls automatically and protects its endpoint-local credential.
- Device mTLS fails closed, including revoked prior credentials.
- LAB-DC01 records redacted health and no endpoint runtime appears on hungdinh-lt.
- All high-severity threat mitigations have machine-specific passing evidence.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| Extended symbol | `EnrollmentCoordinator` | Initial and replacement state machine |
| Extended symbol | `AgentHttpClient` | Bootstrap TLS and device-mTLS transport |
| Extended symbol | `DpapiCredentialStore` | Machine-DPAPI and service-SID credential custody |
| Extended path | `tests/windows/Invoke-AgentServiceSmoke.ps1` | Enrollment, DPAPI, mTLS, and health scenarios |
| Generated evidence | `evidence/phase1/attempts/<evidence-id>.json` and matrix pointers | Immutable enrollment/credential attempts |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md` when done.
</output>
