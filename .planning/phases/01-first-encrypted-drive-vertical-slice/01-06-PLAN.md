---
phase: 01-first-encrypted-drive-vertical-slice
plan: 06
type: execute
wave: 4
depends_on: [01-05]
files_modified:
  - Cargo.lock
  - crates/dlp-server/Cargo.toml
  - crates/dlp-server/src/enrollment.rs
  - crates/dlp-server/src/ad.rs
  - crates/dlp-server/src/pki.rs
  - crates/dlp-server/src/repository.rs
  - crates/dlpctl/Cargo.toml
  - crates/dlpctl/src/main.rs
  - migrations/202608070002_enrollment_authority.sql
  - config/server.env.example
  - tests/e2e/server_enrollment.rs
autonomous: true
requirements: [SRV-01, SRV-03, SRV-11, TST-05]
user_setup:
  - service: active-directory
    why: "D-04 requires direct trusted queries to the organization's primary and secondary DCs."
    env_vars:
      - { name: DLP_AD_PRIMARY_LDAPS_URL, source: "Primary DC LDAPS endpoint" }
      - { name: DLP_AD_SECONDARY_LDAPS_URL, source: "Secondary DC LDAPS endpoint" }
      - { name: DLP_AD_BIND_DN, source: "Dedicated read-only directory account" }
      - { name: DLP_AD_BIND_PASSWORD, source: "Secret-store value for that account" }
      - { name: DLP_AD_CA_CERT_PEM, source: "Organization CA chain authenticating both DCs" }
  - service: development-device-ca
    why: "D-05 requires a device-bound mTLS certificate issuer for Phase 1."
    env_vars:
      - { name: DLP_DEVICE_CA_CERT_PEM, source: "Development device client CA certificate" }
      - { name: DLP_DEVICE_CA_KEY_PEM, source: "Mounted CA private-key secret" }
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02/D-03, only exact normalized `SMBIOS UUID + BIOS serial + physical system-disk serial` digest match is eligible; MAC address, missing/sentinel source, or any changed component is denied."
    - "Per D-04, both explicitly configured trusted LDAPS DCs independently return the same enabled computer GUID/SID/domain/DNS identity; timeout, trust failure, absence, or disagreement denies enrollment."
    - "Per D-05/D-06, successful initial/replacement enrollment issues one device-bound client certificate and transactionally revokes the prior serial before replacement is usable."
    - "Two concurrent uses of one token yield exactly one credential; token/migration application is idempotent where required and cannot diverge across server instances."
  artifacts:
    - path: "crates/dlp-server/src/enrollment.rs"
      provides: "Fail-closed enrollment transaction"
      exports: ["EnrollmentService", "EnrollmentError"]
    - path: "crates/dlp-server/src/ad.rs"
      provides: "Dual-DC LDAPS identity corroboration"
      exports: ["LdapDirectoryVerifier", "VerifiedComputerIdentity"]
    - path: "crates/dlp-server/src/pki.rs"
      provides: "Device certificate issuance"
      exports: ["RcgenDeviceCertificateIssuer", "IssuedDeviceCredential"]
    - path: "migrations/202608070002_enrollment_authority.sql"
      provides: "Allowlist/token/device/credential/revocation authority schema"
  key_links:
    - from: "crates/dlp-server/src/enrollment.rs"
      to: "crates/dlp-server/src/ad.rs"
      via: "complete two-DC agreement precedes token consumption and issuance"
      pattern: "verify_computer"
    - from: "crates/dlp-server/src/enrollment.rs"
      to: "migrations/202608070002_enrollment_authority.sql"
      via: "one transaction locks token/device and replaces credential"
      pattern: "FOR UPDATE|device_credentials"
  flagged_assumptions:
    - { id: "PROBE-SRV-03", status: unresolved, statement: "The deterministic edge probe could not classify SRV-03; expired, reused, malformed, wrong-device, and racing token cases remain explicit." }
  prohibitions:
    - id: PROH-02
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT describe the exact hardware allowlist as hardware-rooted remote attestation; administrator provisioning and privileged-local spoofing remain explicit residual risk."
    - id: PROH-03
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT persist or emit raw hardware serials, AD bind credentials, enrollment tokens, CA private keys, issued private keys, or protected file data."
---

<objective>
Replace the tracer's enrollment authority fixtures with exact fingerprint, dual-DC, transaction-backed token, and device-certificate authority required by D-02 through D-06.

Purpose: Make enrollment identity and replacement revocation authoritative before the real Windows agent starts using device mTLS.
Output: Enrollment migration, AD/PKI adapters, authority service/repository, provisioning CLI, and negative/concurrency integration evidence.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/docs/adrs/ADR-003-database-migrations.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-05-SUMMARY.md

<interfaces>
Implement the tracer's `DirectoryVerifier` and `DeviceCertificateIssuer` traits. `VerifiedComputerIdentity` contains object GUID/SID, DNS name, domain, and enabled state only after both DCs agree. `IssuedDeviceCredential` returns certificate chain/private key/serial/expiry once and never persists the private key server-side.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exact allowlist and dual-DC checks issue one device credential</name>
  <precondition>01-03 approved `ldap3`, `rcgen`, and `sha2`; DATABASE_URL and all `DLP_AD_*`/`DLP_DEVICE_CA_*` values are available for real integration.</precondition>
  <reversibility rating="costly">Per D-02/D-05, fingerprint composition and device authentication bind stored allowlist/credential identities and need coordinated migration if changed.</reversibility>
  <files>crates/dlp-server/src/enrollment.rs, crates/dlp-server/src/ad.rs, crates/dlp-server/src/pki.rs, migrations/202608070002_enrollment_authority.sql, tests/e2e/server_enrollment.rs</files>
  <behavior>
    - Test 1: exact digest + valid unused token + matching enabled computer from both DCs yields one active client credential.
    - Test 2: changed/missing/sentinel source, digest mismatch, disabled/missing computer, DC disagreement, LDAPS trust failure, or timeout yields no credential.
    - Test 3: racing token requests yield one success and one already-used denial.
    - Test 4: replacement repeats all checks and revokes the old serial in the same transaction that activates the new serial.
  </behavior>
  <action>Add forward-only tables and constraints for normalized fingerprint digests, hashed token/expiry/use, verified AD identity, devices, credentials, and revocation. Implement direct LDAPS connections to the two configured hosts using the organization CA, bounded timeouts/attributes, referral rejection, exact computer lookup, and byte-for-byte identity/state agreement. Implement development CA issuance with device-ID URI SAN, clientAuth EKU, random serial, bounded validity, mounted CA key, and no private-key persistence. Lock token/device state, compare exact digest, require both DC results, issue once, and commit atomically. Replacement performs the full checks and prior-serial revocation in one transaction before response.</action>
  <verify><automated>cargo test -p dlp-server --test server_enrollment authority -- --nocapture; sqlx migrate run --source migrations --database-url "$env:DATABASE_URL"; sqlx migrate info --source migrations --database-url "$env:DATABASE_URL"</automated></verify>
  <done>Only the exact pre-provisioned endpoint corroborated by both DCs receives one device credential, and replacement revokes the old serial before return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist authority and expose administrator provisioning commands</name>
  <files>crates/dlp-server/Cargo.toml, crates/dlp-server/src/repository.rs, crates/dlpctl/Cargo.toml, crates/dlpctl/src/main.rs, config/server.env.example</files>
  <behavior>
    - Test 1: `device allow` normalizes three exact sources, rejects invalid/sentinel input, sends/stores only the digest, and never accepts MAC authority.
    - Test 2: token creation uses OS randomness, stores only a hash/expiry, and displays plaintext exactly once.
    - Test 3: migration/repository constraints enforce unique token hash/serial and at most one active credential per device.
    - Test 4: raw serial/token/bind/CA/private-key markers are absent from database snapshots, JSON responses, and captured logs.
  </behavior>
  <action>After package approval, add exact dependencies/lock entries and implement repositories used by `EnrollmentService`. `dlpctl device allow` accepts administrator-supplied SMBIOS UUID, BIOS serial, and physical system-disk serial, applies the shared normalization, computes SHA-256 locally, and sends only the digest through authenticated admin transport. `enrollment-token create --ttl` creates cryptographically random plaintext once and stores only its hash. Document endpoint/secret paths in the env example without values. Record digests, stable error codes, correlation IDs, and enrollment outcomes only.</action>
  <verify><automated>cargo test -p dlp-server repository; cargo test -p dlpctl enrollment; cargo clippy -p dlp-server -p dlpctl --all-targets -- -D warnings</automated></verify>
  <done>Authority persistence/CLI are exact, transactional, migration-backed, and disclose no raw fingerprint or credential material.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Endpoint/admin → enrollment authority | Token and locally observed hardware identity are untrusted. |
| Server → two AD DCs | Directory server identity and replicated attributes require independent authentication/agreement. |
| Device CA → enrollment response/repository | Issuance and revocation determine device authority; private key must cross once only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13 | Spoofing | enrollment | high | mitigate | Single-use expiring token, exact preloaded digest, dual-DC agreement, transaction locks, no MAC authority. |
| T-01-14 | Spoofing/Tampering | AD lookup | high | mitigate | Direct LDAPS to configured hosts, trusted CA, referral rejection, bounded attributes, exact identity agreement. |
| T-01-15 | Elevation of privilege | certificate issuance | high | mitigate | Mounted CA secret, device SAN/clientAuth EKU/random serial/expiry, one-time private-key response, atomic replacement revocation. |
| T-01-16 | Information disclosure | records/logs/API | high | mitigate | Digests/codes only; negative marker scans for raw serials, tokens, bind/CA/issued private keys. |
| T-01-SC | Tampering | Cargo packages | high | mitigate | Exact 01-03-approved package/version/source entries and lockfile/slopcheck evidence. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Implementation evidence | Executable evidence |
|---|---|---|---|
| V2 Authentication | Token plus exact endpoint and directory identity precede credential issuance | `EnrollmentService` transaction | success/expiry/reuse/race/wrong-device tests |
| V3 Session Management | Replacement revokes prior device credential before new use | active credential/revocation constraints | replacement old-serial rejection test |
| V4 Access Control | Admin provisioning and device issuance are separated | authenticated CLI/admin repository seam | admin authorization negative tests completed in 01-07 |
| V5 Validation | Hardware/DC/certificate fields and lengths are normalized/bounded | shared normalization and LDAP attribute parser | malformed/sentinel/disagreement corpus |
| V9 Communications | Both DCs use authenticated LDAPS with explicit endpoints | `LdapDirectoryVerifier` trust config | real two-DC trust/name/timeout tests |

**Completion gate:** Any unresolved critical/high threat, failed ASVS evidence, raw-secret marker, or authority race/revocation gap blocks this plan's completion and summary.
</threat_model>

<verification>
Apply migrations, run the complete authority and concurrency suite, exercise both real DCs, verify replacement revocation, inspect dependency approval/lock, and scan DB/API/log captures for prohibited raw values.
</verification>

<success_criteria>
- D-02 through D-06 enrollment authority is exact, dual-DC, transactional, and revocation-aware.
- One token yields at most one active credential.
- Private credential/CA/fingerprint material is absent from authoritative records and diagnostics.
- All ASVS L1 evidence passes and no critical/high threat remains unresolved.
</success_criteria>

## Artifact Inventory

| Disposition | File | Key symbols / purpose |
|---|---|---|
| Modified | `Cargo.lock` | exact approved LDAP/PKI/hash dependency versions and sources |
| Modified | `crates/dlp-server/Cargo.toml` | approved LDAP/PKI/hash dependencies |
| New | `crates/dlp-server/src/enrollment.rs` | `EnrollmentService`, `EnrollmentError` |
| New | `crates/dlp-server/src/ad.rs` | `LdapDirectoryVerifier`, `VerifiedComputerIdentity` |
| New | `crates/dlp-server/src/pki.rs` | `RcgenDeviceCertificateIssuer`, `IssuedDeviceCredential` |
| Modified | `crates/dlp-server/src/repository.rs` | token/allowlist/device/credential transactions |
| Modified | `crates/dlpctl/Cargo.toml` | approved CLI hash/transport dependencies |
| Modified | `crates/dlpctl/src/main.rs` | `device allow`, `enrollment-token create` |
| New | `migrations/202608070002_enrollment_authority.sql` | allowlist/token/device/credential/revocation schema |
| New | `config/server.env.example` | dual-LDAPS/device-CA/PostgreSQL path contract |
| New | `tests/e2e/server_enrollment.rs` | authority, race, replacement, redaction tests |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-06-SUMMARY.md` when done.
</output>
