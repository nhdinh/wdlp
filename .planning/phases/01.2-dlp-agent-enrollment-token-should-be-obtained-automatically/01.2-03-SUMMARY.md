---
phase: 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically
plan: 03
subsystem: enrollment-authority-verification
tags: [powershell, postgres, mtls, trusted-provisioning, recovery, evidence]
requires:
  - phase: 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically
    provides: automatic enrollment runtime, authority repository, cleanup, and first-policy activation
provides:
  - Exact URL-last PostgreSQL authority-query adapter with independent stdout/stderr and row-shape gates
  - Live ordinary-refusal and replacement/revocation authority evidence
  - Leaf-only provisioning client identity validated against a required-mTLS loopback server
  - Deterministic post-mutation failure recovery with fixed redacted output
affects: [phase-02, endpoint-enrollment, lab-operations, verification, security-review]
actuals:
  tasks: 3
  commits: 3
  duration: 8h
tech-stack:
  added: [rcgen-dev]
  patterns:
    - ProcessStartInfo.ArgumentList preserves native argument boundaries and captures streams independently
    - Required-mTLS regression asserts the client transmits only its leaf certificate
    - Atomic evidence publication uses exact allowlists and secret-free fingerprints
    - Failure injection captures inherited native process handles and emits fixed outer recovery codes
key-files:
  created:
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-ordinary-authority-evidence.json
    - .planning/phases/01.2-dlp-agent-enrollment-token-should-be-obtained-automatically/01.2-03-replacement-authority-evidence.json
  modified:
    - tests/windows/Invoke-AgentServiceSmoke.ps1
    - crates/dlpctl/src/lib.rs
    - crates/dlpctl/src/main.rs
    - crates/dlpctl/Cargo.toml
    - Cargo.lock
    - scripts/lab/Invoke-Client01Runtime.ps1
key-decisions:
  - "The self-signed administrator CA remains a server-side trust anchor; dlpctl validates it but presents only the administrator leaf certificate as its client identity."
  - "PowerShell authority queries use ProcessStartInfo.ArgumentList and reject nonzero exit, any stderr, wrong row count, and malformed result shapes before returning data."
  - "Injected replacement recovery runs the nested runtime in a redirected child pwsh process so native build output cannot cross the four-code redaction boundary."
requirements-completed: [SRV-13, SRV-14, TST-05, AGT-01]
coverage:
  - id: D1
    description: "Ordinary missing-credential refusal leaves the four authority fields unchanged before finally recovery restores a healthy endpoint."
    requirement: TST-05
    verification:
      - kind: live-integration
        ref: "01.2-03-ordinary-authority-evidence.json"
        status: pass
    human_judgment: false
  - id: D2
    description: "Replacement activates a distinct successor, revokes the predecessor, preserves endpoint artifacts, and removes both token copies."
    requirement: SRV-14
    verification:
      - kind: live-integration
        ref: "01.2-03-replacement-authority-evidence.json"
        status: pass
    human_judgment: false
  - id: D3
    description: "Post-mutation failure remains nonzero after fresh recovery, dual cleanup, endpoint health checks, and pass-artifact hash preservation."
    requirement: SRV-13
    verification:
      - kind: live-failure-injection
        ref: "ReplacementRevocation -FailureInjection AfterReplacementMutation"
        status: pass
    human_judgment: false
completed: 2026-08-29
status: complete
---

# Phase 01.2 Plan 03: Authority Recovery Evidence Summary

**The disconnected authority adapter is repaired, both live authority transitions are proven, and injected post-mutation failure now recovers the endpoint with fixed redacted output.**

## Performance

- **Duration:** approximately 8 hours across the confirmed exclusive lab window
- **Completed:** 2026-08-28T23:55:21Z
- **Tasks:** 3
- **Commits:** 3

## Accomplishments

- Repaired the PostgreSQL adapter to preserve exact native argv boundaries and independently reject nonzero exit, stderr, invalid row counts, and malformed authority shapes.
- Fixed the live provisioning TLS failure by keeping the self-signed administrator CA out of the transmitted client chain and added a required-mTLS regression proving leaf-only presentation.
- Published secret-free ordinary and replacement evidence with exact schemas, round-trip UTC timestamps, 64-hex fingerprints, atomic publication, and no staging residue.
- Proved the injected post-mutation path exits nonzero after a fresh successor recovery, dual cleanup, Running/Automatic service restoration, Active policy version 1, and preservation of service/data/cache and the prior pass artifact.

## RED / GREEN / REFACTOR

- **RED:** the deterministic fake psql rejected the original disconnected argument construction; the corrected mTLS test rejected a client chain containing the self-signed administrator CA; the first injection run exposed native cargo output outside the fixed-code allowlist.
- **GREEN:** `AuthorityQueryAdapter`, the required-mTLS loopback test, both live evidence scenarios, and the four-code failure-injection gate passed.
- **REFACTOR:** native recovery execution in injection mode now uses redirected `ProcessStartInfo` handles, keeping credentials in the child environment and raw/native output inside the validation boundary.

## Task Commits

1. **Task 1: Repair and prove the authority-query adapter** - `9224233`
2. **Blocking live fix: Present a leaf-only provisioning identity** - `6b8a947`
3. **Task 3: Prove ordinary, replacement, and injected recovery transitions** - `093cb69`

## Live Evidence

- `01.2-03-ordinary-authority-evidence.json` - SHA-256 `c6e6e4c0847adfaba28da54f5e5d4a04bf0cca30494a749d8e1ab38a066e57d5`
- `01.2-03-replacement-authority-evidence.json` - SHA-256 `1c7e14ad2cb26b275e11c320cca71cb740db586b3de6fa70a68dd7dda1d12cce`
- Injected replacement exited `1`, emitted exactly `replacement_post_mutation_failure_injected`, `replacement_recovery_attempted`, `replacement_recovery_succeeded`, and `replacement_dual_cleanup_succeeded`, and left the replacement evidence hash unchanged.
- Final authority state contained one active credential; the endpoint was Running/Automatic with a SYSTEM-readable protected credential, Active policy version 1, zero token entries, and no staging residue.

## Verification

- `cargo test -p dlpctl` - 13 passed across 4 suites.
- `cargo clippy -p dlpctl --all-targets -- -D warnings` - passed.
- `cargo fmt -p dlpctl -- --check` - passed.
- `Invoke-AgentServiceSmoke.ps1 -Scenario AuthorityQueryAdapter` - passed.
- `Invoke-AgentServiceSmoke.ps1 -Scenario AuthorityEvidenceContract` - passed.
- `EnrollmentTokenOrchestration.Tests.ps1 -TestCase CoreFlow` - passed.
- Exact evidence allowlists, UTC strings (parsed with `ConvertFrom-Json -DateKind String`), fingerprints, protected-field exclusion, and staging cleanup - passed.
- `git diff --check` - passed.

## Deviations from Plan

### Auto-fixed Blocking Issues

**1. Provisioning client transmitted a self-signed root in its client chain**

- Live rustls provisioning failed before the client finished mTLS authentication.
- The administrator CA is now validated as configuration but excluded from the client identity; only the leaf and private key are passed to reqwest/rustls.
- The user explicitly authorized this HIGH-risk provisioning-client fix.

**2. Lab AD bind credential was stale**

- Fresh server stderr isolated `enrollment_directory_rejected: NotFound`; a direct LDAPS probe returned error 49 for the inherited bind password.
- The DPAPI-stored lab Administrator credential successfully bound to both controllers and found exactly one enabled `LAB-CLIENT01.lab.local` object.
- The corrected password was used only in transient process environments; the DPAPI credential file was deleted after live verification.

**3. Native child output bypassed injection-mode stream suppression**

- `Start-Process -NoNewWindow` inherited OS handles, so cargo output was visible even when PowerShell streams were redirected.
- Injection-mode recovery now launches a child `pwsh` with redirected OS stdout/stderr and discrete arguments, producing exactly the four allowed outer codes.

## GitNexus Analysis

- Pre-edit PowerShell symbol lookups for the new evidence/recovery functions returned `UNKNOWN`; direct definition/call-site/switch inventories and deterministic tests were used because the index does not resolve these PowerShell symbols.
- Pre-commit executor delta: **HIGH**, 6 indexed files, 7 symbols, 7 affected flows, centered on `ProvisioningClient::new` and its regression test.
- Compare to `master`: **CRITICAL**, 37 files, 41 symbols, 57 flows. This is the accumulated Phase 01.2 branch and was not downgraded or attributed solely to Plan 03.
- After the Rust commit, the remaining PowerShell/JSON delta reported no indexed changes; this was treated as `UNKNOWN`, not low risk.

## Checkpoint and Cleanup

- The operator supplied `exclusive-lab-window-confirmed` and explicitly authorized the high-risk provisioning client fix.
- LAB-DC01 and LAB-CLIENT01 scenarios ran sequentially with no parallel mutation.
- `%LOCALAPPDATA%\DlpLab\vm-admin.credential.xml` was removed after all live work; it can be recreated from a fresh credential prompt.

## Self-Check: PASSED

- Both evidence files exist and their recorded hashes match.
- Commits `9224233`, `6b8a947`, and `093cb69` exist.
- Final endpoint, authority, token-cleanup, policy, failure-injection, deterministic, Rust, and diff gates pass.

---
*Phase: 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically*
*Completed: 2026-08-29*
