---
context: phase
phase: 01-first-encrypted-drive-vertical-slice
task: 3
total_tasks: 3
status: paused
last_updated: 2026-08-11T15:49:01.084Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failure during Plan 01-13 execution.

- [ ] CONSTRAINT: AD/LDAPS runtime secrets — `DLP_AD_PRIMARY_LDAPS_URL`, `DLP_AD_SECONDARY_LDAPS_URL`, `DLP_AD_BASE_DN`, `DLP_AD_BIND_DN`, and `DLP_AD_CA_CERT_PEM` must be supplied by the runtime-only secret provider before Task 3 can run.
- [ ] CONSTRAINT: Domain-joined LAB-CLIENT01 reachability — LAB-CLIENT01 must be domain-joined and reachable by FQDN from LAB-DC01 over Kerberos WinRM-over-HTTPS for fingerprint collection.
- [ ] CONSTRAINT: Worktree merge gate — the worktree `worktree-agent-a6c91c34fcca46478` contains completed Tasks 1 and 2; do not merge to master until Task 3 verification passes or the plan is intentionally split.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming server runs on LAB-SERVER01 | The management server was initially probed on LAB-SERVER01, but user clarified it belongs on LAB-DC01. | blocking | Verify machine roles in `config/lab.phase1.example.yaml` and `config/lab.roles.example.json` before any deployment; assert `ExecutionMachine` matches the declared server role. |
| Missing AD/LDAPS runtime secrets | `Invoke-Dc01Server.ps1 -Scenario TrustedProvisioning` aborts at `Assert-RuntimeAdSecretsPresent` when any AD/LDAPS variable is absent. | blocking | Pre-flight all `DLP_AD_*` environment variables before dispatching Task 3; fail closed on missing values. |
| Merging unverified worktree | Tasks 1 and 2 are verified only on the worktree branch; merging before Task 3 completes would leave TST-05 unverified on master. | blocking | Keep worktree branch alive until Task 3 passes and post-merge tests succeed; only then run worktree cleanup/merge. |
| Committing secrets | VM admin, database, AD bind, PKI, and token material must never enter repository files or commit messages. | blocking | Read all sensitive values only from environment/session/runtime provider; redact in logs and evidence. |

<current_state>
Plan 01-13 is paused at Task 3 of 3. Tasks 1 and 2 are complete and verified on worktree branch `worktree-agent-a6c91c34fcca46478`. Task 3 (trusted LAB-CLIENT01 provisioning) is blocked only by missing AD/LDAPS runtime secrets. The management server is running on LAB-DC01 against native PostgreSQL on LAB-SERVER01; LAB-CLIENT01 can reach it via TLS readiness probes.
</current_state>

<completed_work>

Completed plans this session:
- 01-13 Task 1: Reconcile machine roles and serve one LAB-DC01 readiness path — verified.
- 01-13 Task 2: Prove LAB-SERVER01 PostgreSQL migration idempotency, concurrency, and readiness through LAB-DC01 — verified.

Key commits on worktree branch `worktree-agent-a6c91c34fcca46478`:
- `5296e1c` fix(01-13): correct topology so management server runs on LAB-DC01 against LAB-SERVER01 PostgreSQL
- `2abe8bf` fix(01-13): align role names with approved privilege manifests
- `62d7797` fix(01-13): complete Task 1 tracer with LAB-DC01 server and reachable readiness probe
- `bf3c941` fix(01-13): complete DC01 Postgres verification scenarios
- `92974eb` fix(01-13): make server enrollment tests self-contained for Task 2 verify
- `46cad90` docs(01-13): refresh requirement matrix with latest SRV-11/SRV-12 evidence IDs
- `f275ca1` docs(01-13): update cross-phase defect ledger
- `e114473` docs(01-13): update plan summary to blocked with Tasks 1 and 2 complete

Verification passing:
- `Invoke-Phase1EnvironmentReconcile.ps1 -Apply` on hungdinh-lt.
- `Invoke-Dc01Server.ps1 -Scenario Tracer` (LAB-DC01 server + LAB-CLIENT01 probe).
- `Invoke-Dc01Server.ps1 -Scenario All` (PostgresFresh, PostgresRepeat, MigrationFailure, ConcurrentStart, ReadinessConcurrency).
- `verify-phase1-evidence.ps1 -Scenario Dc01Tracer` and `Dc01Postgres`.
- `cargo test --locked -p dlp-server -p dlpctl`.

</completed_work>

<remaining_work>

- Plan 01-13 Task 3: Execute trusted LAB-CLIENT01 provisioning on LAB-DC01 before enrollment.
- Provide missing AD/LDAPS runtime secrets and confirm LAB-CLIENT01 domain join / Kerberos WinRM HTTPS.
- Run `Invoke-Dc01Server.ps1 -Scenario TrustedProvisioning` and verify evidence.
- Merge worktree to master and update STATE.md/ROADMAP.md after Task 3 passes.
- Remaining waves after 01-13: 01-14, 01-18, 01-19, 01-15, 01-20, 01-16, 01-21.

</remaining_work>

<decisions_made>

- Management server runs on LAB-DC01 (192.168.50.10:8443); PostgreSQL remains native on LAB-SERVER01 (192.168.50.12).
- `DLP_LISTEN_ADDRESS` configures the server bind address; default remains localhost for safety, lab overrides to `0.0.0.0:8443`.
- `Reset-DlpPostgres.py` resets the DLP database on LAB-SERVER01 via SSH/sudo as `dlpadmin` without storing the password.
- Server enrollment integration tests generate deterministic PKI fixtures at test time from PEM-content env vars.
- Worktree branch `worktree-agent-a6c91c34fcca46478` must not merge until Task 3 verification passes.

</decisions_made>

<blockers>

- **Missing AD/LDAPS runtime secrets:** `DLP_AD_PRIMARY_LDAPS_URL`, `DLP_AD_SECONDARY_LDAPS_URL`, `DLP_AD_BASE_DN`, `DLP_AD_BIND_DN`, and `DLP_AD_CA_CERT_PEM` are not in the runtime provider. Only `DLP_AD_BIND_PASSWORD` is present.
- **Unconfirmed LAB-CLIENT01 domain join / Kerberos WinRM HTTPS:** Task 3 requires dual-DC corroboration and FQDN-based Kerberos WinRM-over-HTTPS reachability.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — canonical structured resume state.
2. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-PLAN.md` — Task 3 details and fail-closed gates.
3. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md` — completed Tasks 1 and 2, remaining blocker.
4. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-23-SUMMARY.md` — trusted provisioning preflight source contract.
5. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md` — evidence and privilege approval contract.
6. `scripts/lab/Invoke-Dc01Server.ps1` — current orchestration implementation.
7. `.planning/STATE.md` — phase position and accumulated decisions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not run the management server on LAB-SERVER01; LAB-DC01 is the declared host.
- Do not proceed with Task 3 until all `DLP_AD_*` runtime secrets are present.
- Do not merge the worktree until Task 3 passes and post-merge tests succeed.
- Do not commit or log VM admin, database, AD bind, PKI, or token material.

## Infrastructure State

- Host: `hungdinh-lt`.
- LAB-DC01: Windows Server running `dlp-server` bound to `0.0.0.0:8443`, connected to LAB-SERVER01 PostgreSQL via runtime secrets.
- LAB-SERVER01: Ubuntu server with native PostgreSQL (login `dlpadmin`/`Pl225371`), reachable from LAB-DC01.
- LAB-DC02: Independent secondary AD authority (corroboration only).
- LAB-CLIENT01: Endpoint runtime; reaches LAB-DC01 readiness endpoints; domain-join and WinRM HTTPS status unconfirmed.
- Runtime secret provider: database/PKI/VM admin credentials present; AD/LDAPS variables absent.
- Active worktree: `C:\Users\nhdinh\dev\dleakprevention\.claude\worktrees\agent-a6c91c34fcca46478` on branch `worktree-agent-a6c91c34fcca46478`, unmerged.
- Background processes or async jobs: `dlp-server` process on LAB-DC01 started by Task 2 scenarios; no persistent daemon configured.

<context>
Plan 01-13 is paused at the Task 3 precondition checkpoint. Tasks 1 and 2 are verified and committed to the worktree branch. The only remaining work is to provide AD/LDAPS runtime secrets, confirm LAB-CLIENT01 domain/WinRM state, and run the TrustedProvisioning scenario. The next session should not need to re-run Tasks 1 or 2 unless the LAB-DC01 server state is lost.
</context>

<next_action>
Start with: load `DLP_AD_PRIMARY_LDAPS_URL`, `DLP_AD_SECONDARY_LDAPS_URL`, `DLP_AD_BASE_DN`, `DLP_AD_BIND_DN`, and `DLP_AD_CA_CERT_PEM` into the runtime secret provider; confirm LAB-CLIENT01 is domain-joined and reachable by FQDN from LAB-DC01 over Kerberos WinRM-over-HTTPS; then run `scripts/lab/Invoke-Dc01Server.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-DC01 -ProbeMachine LAB-CLIENT01 -SecondaryDcMachine LAB-DC02 -SecretProvider Runtime -Scenario TrustedProvisioning`.
</next_action>
