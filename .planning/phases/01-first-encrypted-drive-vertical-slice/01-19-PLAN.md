---
phase: 01-first-encrypted-drive-vertical-slice
plan: 19
type: execute
wave: 5
depends_on: [01-18]
files_modified:
  - crates/dlp-windows-service/src/main.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/fingerprint.rs
  - tests/windows/Invoke-AgentServiceSmoke.ps1
  - config/agent.toml.example
autonomous: true
requirements: [AGT-01, AGT-02, AGT-03, AGT-07]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-01 through D-06 and D-21/D-24, the real LAB-CLIENT01 service starts automatically without an interactive user, reloads its validated DPAPI credential and current/LKG cache, polls through device mTLS, and posts redacted health."
    - "The production fingerprint collector returns only the exact normalized SMBIOS UUID, BIOS serial, and physical OS-disk serial; missing, sentinel, or changed observations fail before enrollment and MAC addresses are never authoritative."
    - "Per D-33 through D-47, service/trust/ACL/file mutations remain within the separately approved 01-19 manifest digest and publish machine-correct immutable evidence with baseline, cleanup, staleness, artifact, clock, and redaction checks."
    - "No endpoint service, credential, runtime trust, or endpoint evidence is installed or accepted on hungdinh-lt."
  artifacts:
    - path: "crates/dlp-windows-service/src/service.rs"
      provides: "Automatic noninteractive SCM state machine and restart orchestration"
    - path: "crates/dlp-windows-service/src/fingerprint.rs"
      provides: "Windows-native exact version-1 fingerprint collector"
    - path: "tests/windows/Invoke-AgentServiceSmoke.ps1"
      provides: "LAB-CLIENT01-only service install, stop, force-kill, restart, fingerprint, and health evidence"
    - path: "config/agent.toml.example"
      provides: "Secret-free service, trust, cache, polling, and later mount settings"
  key_links:
    - from: "crates/dlp-windows-service/src/service.rs"
      to: "crates/dlp-agent-core/src/lib.rs"
      via: "SCM startup loads the Plan 01-14 credential and Plan 01-18 current/LKG state before authenticated polling and health"
      pattern: "EnrollmentCoordinator|ConfigurationCache|HealthSnapshot"
  flagged_assumptions:
    - { id: "EDGE-AGT-01-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-01; SCM automatic-start, noninteractive startup, stop, and restart evidence must come from LAB-CLIENT01." }
  prohibitions:
    - "flagged-unverified: MUST NOT run the endpoint service interactively, accept a caller-provided fingerprint, or report Running before credential/config recovery is complete."
    - "flagged-unverified: MUST NOT install or retain endpoint service, credential, runtime trust, mount, or endpoint evidence on hungdinh-lt."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Install and exercise the automatic noninteractive LAB-CLIENT01 service around the enrolled credential and signed-configuration cache.

Purpose: Isolate SCM lifecycle, Windows-native fingerprint collection, and endpoint service recovery from enrollment custody and configuration-cache implementation.
Output: Completed service entry/state machine, exact fingerprint collector, secret-free configuration, and focused LAB-CLIENT01 service evidence.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-18-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Preserve `EnrollmentCoordinator`, `AgentHttpClient`, `DpapiCredentialStore`, `ConfigurationCache`, `HealthSnapshot`, and the existing SCM dispatcher. The service composes these completed seams; session-change controls are registered here and consumed by Plan 01-15 without implementing the session host in this plan.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Install and exercise the automatic noninteractive service on LAB-CLIENT01</name>
  <precondition>Plans 01-14 and 01-18 are green; the exact 01-19 privilege-manifest section has a valid digest-bound approval; LAB-CLIENT01 can install and start the exact listed automatic service through the existing lab orchestration channel.</precondition>
  <files>crates/dlp-windows-service/src/main.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/fingerprint.rs, tests/windows/Invoke-AgentServiceSmoke.ps1, config/agent.toml.example</files>
  <read_first>
    - Every path in `<files>` plus `crates/dlp-windows-service/src/credential.rs`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-01 through D-06 and D-20 through D-24
    - Plan 01-14 and 01-18 summaries
    - Research Windows service lifecycle, fingerprint sources, and health contracts
  </read_first>
  <behavior>
    - Test 1: install/start sets SCM automatic start, transitions StartPending→Running, creates Tokio inside the service entry, and operates with no signed-in user.
    - Test 2: fingerprint collection returns only exact SMBIOS UUID, BIOS serial, and physical OS-disk serial; missing, sentinel, and changed disk observations fail before enrollment and no MAC is accepted.
    - Test 3: stop/shutdown is bounded and restart reloads DPAPI credential plus current/LKG before polling and health resume.
    - Test 4: all service, fingerprint, credential, and configuration errors emit stable codes only and preserve the last usable state.
  </behavior>
  <action>Complete the SCM entry and state machine around the Plan 01-14 enrollment/credential components and Plan 01-18 configuration cache: register Stop and Shutdown plus the session-change controls consumed by Plan 01-15, report accurate pending/running/stopped states, create and stop the Tokio runtime inside the service, and never require an interactive desktop. Replace PowerShell-based production fingerprint collection with isolated documented Windows API calls for SMBIOS/BIOS and system-volume-to-physical-disk identity, preserving the exact normalization/digest shared with trusted provisioning; retain an injected collector only for portable tests. Create a secret-free agent configuration example with server hostname, public root path, cache/data paths, polling/backoff bounds, service identity, and later mount settings; runtime-sensitive values resolve through the endpoint runtime provider, not command arguments or committed files. Per D-33 through D-36, validate the separate 01-19 approval digest, baseline the exact LAB-CLIENT01 service/trust/ACL/file state, apply only listed mutations idempotently, verify final state, and remove temporary artifacts after success or failure. Extend the smoke to install, query, stop, start, force-kill, and restart the service on LAB-CLIENT01 and assert no service, credential, or runtime appeared on hungdinh-lt. Per D-25 and D-37 through D-47, publish focused machine-correct attempts and preserve failures/reruns rather than folding them into source evidence.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, SCM reports automatic start and a noninteractive Running service; stop, shutdown, force termination, and restart finish boundedly and resume authenticated polling/health."
    - "On LAB-CLIENT01, the exact three hardware sources match the trusted LAB-DC01 provisioning digest, while missing, sentinel, and system-disk-change cases abort before enrollment."
    - "On LAB-CLIENT01, service restart reloads the validated DPAPI credential and exact current/LKG cache before mTLS polling; on hungdinh-lt no endpoint service or credential is present."
    - "On LAB-CLIENT01 and LAB-DC01, captured errors and health use stable redacted codes and retain the last usable credential/configuration on transient failure."
    - "The service install/restart baseline, approved 01-19 mutation digest, cleanup, versions, binary/config hashes, machine role, time offset, and raw-artifact hashes are present in schema-valid focused-Hyper-V evidence."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: cargo fmt --all -- --check; cargo clippy --locked -p dlp-agent-core -p dlp-windows-service --all-targets -- -D warnings; then inside LAB-CLIENT01 via hungdinh-lt orchestration: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario ServiceRestart</automated>
  </verify>
  <done>The real automatic LAB-CLIENT01 service owns enrollment recovery, DPAPI credential, signed current/LKG state, device-mTLS polling, and redacted health without any endpoint runtime on hungdinh-lt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SCM → endpoint trust components | Startup and restart ordering determines whether credential/config state is authenticated before network activity. |
| Windows hardware APIs → enrollment identity | Missing, sentinel, changed, or caller-supplied observations must fail before enrollment. |
| Privileged installer → LAB-CLIENT01 | Service/trust/ACL changes must match the approved machine-specific manifest and leave no host residue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-19-01 | Spoofing | fingerprint collector | high | mitigate | Documented Windows API sources, exact normalization, server-side confirmation, missing/sentinel rejection, and no agent-selected identity. |
| T-01-19-02 | Denial of service | SCM lifecycle | medium | mitigate | Accurate pending states, bounded stop/shutdown, last-usable state retention, and force-kill/restart checks. |
| T-01-19-03 | Information disclosure | diagnostics | high | mitigate | Stable redacted codes and marker scans across health, logs, evidence, and configuration. |
| T-01-19-04 | Elevation/Repudiation | privileged service changes | high | mitigate | Separate digest-bound 01-19 manifest, role guard, baseline/apply/verify/remove, cleanup, and immutable Plan 01-17 evidence. |
| T-01-19-SC | Tampering | dependency graph | high | mitigate | Preserve the approved Cargo.lock and reject any unaudited dependency change. |

## ASVS L1 Gate

Any unapproved service mutation, wrong-machine runtime, unauthenticated startup, fingerprint substitution, secret leakage, or invalid evidence attempt blocks the plan.
</threat_model>

<verification>
Run source checks on hungdinh-lt only and SCM/fingerprint/restart scenarios on LAB-CLIENT01 against LAB-DC01. Re-run the host role audit and validate each attempt through Plan 01-17.
</verification>

<success_criteria>
- LAB-CLIENT01 runs the exact automatic noninteractive service.
- Startup and restart authenticate credential and current/LKG state before polling.
- Fingerprint collection is exact, Windows-native, and fail-closed.
- All privileged changes and evidence are separately approved, role-correct, sanitized, and immutable.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| Extended path | `crates/dlp-windows-service/src/service.rs` | SCM lifecycle and component composition |
| Extended path | `crates/dlp-windows-service/src/fingerprint.rs` | Exact Windows-native hardware collector |
| Extended path | `tests/windows/Invoke-AgentServiceSmoke.ps1` | LAB-CLIENT01 service scenarios |
| New path | `config/agent.toml.example` | Secret-free endpoint configuration contract |
| Generated evidence | `evidence/phase1/attempts/<evidence-id>.json` and matrix pointers | Immutable SCM/fingerprint/restart attempts |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-19-SUMMARY.md` when done.
</output>
