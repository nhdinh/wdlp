---
phase: 01-first-encrypted-drive-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - rust-toolchain.toml
  - crates/dlp-domain/Cargo.toml
  - crates/dlp-domain/src/lib.rs
  - crates/dlp-protocol/Cargo.toml
  - crates/dlp-protocol/src/lib.rs
  - crates/dlp-policy/Cargo.toml
  - crates/dlp-policy/src/lib.rs
  - crates/dlp-crypto/Cargo.toml
  - crates/dlp-crypto/src/lib.rs
  - crates/dlp-storage/Cargo.toml
  - crates/dlp-storage/src/lib.rs
autonomous: true
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, CRY-02, TST-01, TST-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The first five portable workspace crates compile on Rust 1.97, forbid unsafe code, and expose versioned/redacted contracts without Windows dependencies."
    - "WRK-02 identifiers, policy inputs/actions/decisions, and structured errors have serialization, equality, and redaction tests."
    - "WRK-03 enrollment, configuration, health, and signed-envelope DTOs reject unsupported versions and arbitrary-map canonicalization."
    - "TST-01 covers matching, priority, equal-priority conflict resolution, defaults, empty input, and deterministic repetition without wiring Phase 2 enforcement."
    - "CRY-02/TST-02 strict Ed25519 verification covers valid, tampered, wrong-key, weak-key, truncated, schema, and key-id cases before activation is possible."
  artifacts:
    - path: "Cargo.toml"
      provides: "Portable workspace membership, pinned shared dependencies, and lint profiles"
    - path: "crates/dlp-domain/src/lib.rs"
      provides: "Typed IDs, policy values, decisions, and redacted errors"
      exports: ["DeviceId", "UserSid", "StoreId", "FileId", "BundleVersion", "PolicyInput", "EnforcementAction", "DecisionReason"]
    - path: "crates/dlp-protocol/src/lib.rs"
      provides: "Versioned wire DTO and canonical signed-envelope contracts"
      exports: ["EnrollmentRequestV1", "EnrollmentResponseV1", "ConfigurationEnvelopeV1", "SignedConfigurationV1", "HealthReportV1"]
    - path: "crates/dlp-policy/src/lib.rs"
      provides: "Deterministic policy evaluator required by TST-01"
      exports: ["PolicyEvaluator"]
    - path: "crates/dlp-crypto/src/lib.rs"
      provides: "Strict configuration signature interfaces and AEAD primitive boundary"
      exports: ["ConfigurationSigner", "ConfigurationVerifier", "RecordCipher", "CryptoError"]
    - path: "crates/dlp-storage/src/lib.rs"
      provides: "Transport-neutral encrypted-store contracts; no persisted format writer yet"
      exports: ["EncryptedStore", "StoreKey", "StoreKeyProvider", "StorageError", "ProtectedFileSystem"]
  key_links:
    - from: "crates/dlp-protocol/src/lib.rs"
      to: "crates/dlp-crypto/src/lib.rs"
      via: "ConfigurationEnvelopeV1 canonical bytes are the sole Ed25519 signing input"
      pattern: "ConfigurationEnvelopeV1|verify_strict"
    - from: "crates/dlp-policy/src/lib.rs"
      to: "crates/dlp-domain/src/lib.rs"
      via: "Evaluator consumes and returns only shared typed policy values"
      pattern: "PolicyInput|DecisionReason"
  flagged_assumptions:
    - { id: "PROBE-WRK-01", status: unresolved, statement: "The deterministic edge probe could not classify WRK-01; exact workspace membership remains a closed-list verification in 01-02." }
    - { id: "PROBE-WRK-02", status: unresolved, statement: "The deterministic edge probe could not classify WRK-02; explicit serialization and equality tests are required." }
    - { id: "PROBE-WRK-03", status: unresolved, statement: "The deterministic edge probe could not classify WRK-03; every wire/persisted DTO needs a version and unknown-version behavior." }
    - { id: "PROBE-WRK-04", status: unresolved, statement: "The deterministic edge probe could not classify WRK-04; workspace lint plus a later Windows-boundary audit are required." }
    - { id: "PROBE-CRY-02", status: unresolved, statement: "The deterministic edge probe could not classify CRY-02; signature, schema, key-id, hash, version, and replay cases must be named." }
    - { id: "PROBE-TST-01", status: unresolved, statement: "The deterministic edge probe could not classify TST-01; matching, priority, conflict, and default cases must all pass." }
    - { id: "PROBE-TST-02", status: unresolved, statement: "The deterministic edge probe could not classify TST-02; positive and negative signature fixtures must be non-vacuous." }
  prohibitions: []
---

<objective>
Establish the portable half of the Cargo workspace and the contracts every later slice consumes, without creating encrypted persisted data before the format decision.

Purpose: Give executors stable typed boundaries for domain, protocol, policy, signing, and storage while keeping Windows FFI and one-way storage bytes out of the first plan.
Output: Five compiling portable crates, pinned toolchain/dependency metadata, deterministic contract tests, and no persisted encrypted-store records.
</objective>

## Phase Goal

**As a** domain-authenticated Windows user, **I want to** enroll my endpoint and use a mounted protected drive, **so that** I can store and recover files while the backing store remains authenticated and encrypted across restarts.

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-PATTERNS.md
@.planning/docs/adrs/ADR-002-api-transport.md
@.planning/docs/adrs/ADR-005-policy-signing.md
@.planning/docs/adrs/ADR-006-key-hierarchy.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile one typed domain value through the workspace boundary</name>
  <files>Cargo.toml, Cargo.lock, rust-toolchain.toml, crates/dlp-domain/Cargo.toml, crates/dlp-domain/src/lib.rs</files>
  <behavior>
    - Test 1: typed IDs reject empty/malformed values, roundtrip deterministically, and do not expose raw sensitive values through Debug/Display.
    - Test 2: policy action/reason/error equality and serialization are stable across repeated runs.
    - Test 3: the crate fails its compile-fail safety fixture if portable unsafe code is introduced.
  </behavior>
  <action>Pin Rust 1.97 and create the workspace with the five portable members delivered here; 01-02 extends the same closed member list to all ten WRK-01 crates. Define typed IDs, policy inputs/actions/reasons, and structured redacted errors in `dlp-domain`. Put shared dependency versions and workspace lint policy at the root, require `#![forbid(unsafe_code)]` in each portable root, and keep all Win32/WinFsp types out of public portable interfaces.</action>
  <verify><automated>cargo fmt --all -- --check; cargo test -p dlp-domain; cargo clippy -p dlp-domain --all-targets -- -D warnings</automated></verify>
  <done>The root workspace and `dlp-domain` compile with typed/redacted contracts and enforced portable safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Versioned protocol and deterministic policy contracts</name>
  <files>crates/dlp-protocol/Cargo.toml, crates/dlp-protocol/src/lib.rs, crates/dlp-policy/Cargo.toml, crates/dlp-policy/src/lib.rs</files>
  <behavior>
    - Test 1: every DTO carries an explicit version and unknown versions fail with a typed error.
    - Test 2: fixed-field canonical bytes are byte-identical on repeat and arbitrary JSON maps are not accepted as signing input.
    - Test 3: priority, equal-priority restrictive ordering/stable-rule tie-break, default action, and empty input are deterministic.
  </behavior>
  <action>Define the `/api/v1` enrollment, configuration, health, error, and signed-envelope DTOs in `dlp-protocol`; version every wire shape and keep canonical signed payloads as fixed-field structs. Implement the TST-01 evaluator seam in `dlp-policy` using the shared domain types. This is the complete required Phase 1 policy-test surface, not drive enforcement, which remains assigned to Phase 2.</action>
  <verify><automated>cargo test -p dlp-protocol -p dlp-policy; cargo clippy -p dlp-protocol -p dlp-policy --all-targets -- -D warnings</automated></verify>
  <done>Versioned DTOs and the deterministic TST-01 policy contract compile and pass all named cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strict signing and storage interface contracts without persisted bytes</name>
  <files>crates/dlp-crypto/Cargo.toml, crates/dlp-crypto/src/lib.rs, crates/dlp-storage/Cargo.toml, crates/dlp-storage/src/lib.rs</files>
  <behavior>
    - Test 1: strict Ed25519 verification accepts the exact signed canonical bytes and rejects tamper, wrong key, weak key, truncation, key-id, and schema mismatch.
    - Test 2: storage interfaces require captured SID/store/file identities and expose explicit flush/close/integrity/no-space errors.
    - Test 3: no test or API can persist an encrypted record because the format writer is deliberately absent until 01-03 approval and 01-04 implementation.
  </behavior>
  <action>Implement `ConfigurationSigner`/`ConfigurationVerifier` using `ed25519-dalek::verify_strict` and define the audited `aes-gcm` primitive wrapper without an on-disk record encoder. Define `EncryptedStore`, `StoreKeyProvider`, `ProtectedFileSystem`, and structured errors as interfaces that carry D-11 through D-15 semantics. The absence of a persisted writer is intentional enforcement of the one-way-door ordering, not reduced scope; 01-04 implements the approved format in full.</action>
  <verify><automated>cargo test -p dlp-crypto -p dlp-storage; cargo clippy -p dlp-crypto -p dlp-storage --all-targets -- -D warnings</automated></verify>
  <done>Strict signed-byte verification and final storage interfaces exist, while no persisted encrypted record can be created before approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Serialized input → domain/protocol types | Untrusted bytes may attempt schema confusion, oversized values, or sensitive-value disclosure. |
| Canonical bytes → signature verifier | Tamper, weak-key, key-id, and alternate-serialization attacks target configuration trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | signed-envelope contracts | high | mitigate | Fixed-field canonical bytes, strict Ed25519 verification, explicit schema/key identifiers, and negative fixtures. |
| T-01-02 | Information disclosure | typed errors/formatting | high | mitigate | Secret-bearing values use redacted Debug/Display and marker-scan tests. |
| T-01-03 | Elevation of privilege | portable safety boundary | medium | mitigate | Forbid unsafe code and prohibit Windows dependencies/types in portable crates. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Implementation evidence | Executable evidence |
|---|---|---|---|
| V5 Validation | Reject malformed, oversized, and unsupported-version data before use | `dlp-domain`, `dlp-protocol` typed constructors/version gates | `cargo test -p dlp-domain -p dlp-protocol` |
| V6 Cryptography | Approved primitives and strict signature verification | `ConfigurationVerifier` uses `verify_strict`; no custom primitive | `cargo test -p dlp-crypto` negative fixtures |
| V8 Data Protection | Sensitive values are not exposed in diagnostics | redacted error/ID formatting | marker assertions in domain/crypto tests |
| V10 Malicious Code | Portable code has no unsafe/Windows execution seam | crate-level unsafe prohibition and dependency audit | workspace clippy plus compile-fail safety fixture |

**Completion gate:** Any unresolved critical/high threat, failed ASVS evidence command, or missing mitigation evidence blocks this plan's completion and summary.
</threat_model>

<verification>
Run formatting, portable crate tests, clippy, version/canonicalization negative cases, policy determinism cases, and strict signature fixtures. Confirm no storage record writer or Windows dependency exists in these crates.
</verification>

<success_criteria>
- The portable workspace compiles with stable typed/versioned contracts.
- TST-01 and TST-02 named cases pass.
- Strict signed-byte verification and redaction fail closed.
- No persisted encrypted data exists before the 01-03 decision checkpoint.
- All ASVS L1 mappings have passing evidence and no critical/high threat remains unresolved.
</success_criteria>

## Artifact Inventory

| Disposition | File | Key symbols / purpose |
|---|---|---|
| New | `Cargo.toml` | portable workspace members, shared dependencies, lint profiles |
| New | `Cargo.lock` | pinned audited dependency graph |
| New | `rust-toolchain.toml` | Rust 1.97 toolchain contract |
| New | `crates/dlp-domain/Cargo.toml` | domain manifest |
| New | `crates/dlp-domain/src/lib.rs` | typed IDs, policy values, structured errors |
| New | `crates/dlp-protocol/Cargo.toml` | protocol manifest |
| New | `crates/dlp-protocol/src/lib.rs` | versioned DTOs and signed envelope |
| New | `crates/dlp-policy/Cargo.toml` | policy manifest |
| New | `crates/dlp-policy/src/lib.rs` | `PolicyEvaluator` and TST-01 tests |
| New | `crates/dlp-crypto/Cargo.toml` | crypto manifest |
| New | `crates/dlp-crypto/src/lib.rs` | signer/verifier and primitive interfaces |
| New | `crates/dlp-storage/Cargo.toml` | storage manifest |
| New | `crates/dlp-storage/src/lib.rs` | storage/filesystem ports and errors |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-01-SUMMARY.md` when done.
</output>
