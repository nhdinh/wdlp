---
phase: 02-policy-enforcement-and-user-feedback
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified:
  - Cargo.lock
  - crates/dlpctl/Cargo.toml
  - crates/dlpctl/src/lib.rs
  - crates/dlpctl/src/main.rs
  - crates/dlpctl/tests/policy_cli.rs
autonomous: true
requirements: [SRV-02, SRV-05, SRV-07, POL-07]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "dlpctl creates/edits drafts, validates, inspects, publishes, sets default, assigns/clears device overrides, and shows deployment status per D-01."
    - "Publish output states that no assignment changed; set-default/assign output states next-poll activation per D-04."
    - "Local validation and server publication use the same Plan 02-01 compiler and reject require_justification per D-09."
    - "Auditor inspection/status succeeds while every mutation remains forbidden; outputs are bounded, versioned, and redacted."
  artifacts:
    - {path: "crates/dlpctl/src/main.rs", provides: "Strict Phase 2 policy command grammar"}
    - {path: "crates/dlpctl/src/lib.rs", provides: "Typed mTLS policy lifecycle client"}
    - {path: "crates/dlpctl/tests/policy_cli.rs", provides: "Grammar, output, role, and redaction tests"}
  key_links:
    - {from: "crates/dlpctl/src/lib.rs", to: "crates/dlp-server/src/routes.rs", via: "versioned JSON over existing administrator mTLS", pattern: "policy.*(draft|publish|assign|status)"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] The CLI must not add a web surface, auto-assign on publish, expose signing material, or accept business justification."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Expose the complete locked policy lifecycle through dlpctl without coupling reviewable publication to deployment.

Purpose: D-01 makes the CLI the authoring surface and D-04 requires users to see publication and assignment as distinct actions.
Output: Exact commands, typed mTLS client calls, versioned outputs, and role/redaction tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@crates/dlpctl/src/main.rs
@crates/dlpctl/src/lib.rs
@crates/dlp-server/src/routes.rs

<interfaces>
Reuse the strict `parse_command`/stable `CliError::code` pattern and existing administrator mTLS client builder. Add `dlp-policy = { path = "../dlp-policy", version = "=0.1.0" }` directly to dlpctl.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create, validate, publish, and explicitly assign one policy through dlpctl</name>
  <read_first>
    - 02-CONTEXT.md — D-01 through D-04 and D-09.
    - crates/dlpctl/src/main.rs — exact parser/exit-code convention.
    - crates/dlpctl/src/lib.rs — mTLS request builder.
    - crates/dlp-server/src/routes.rs — Plan 02 route DTOs and role guards.
  </read_first>
  <files>Cargo.lock, crates/dlpctl/Cargo.toml, crates/dlpctl/src/main.rs, crates/dlpctl/src/lib.rs, crates/dlpctl/tests/policy_cli.rs</files>
  <behavior>
    - draft-create, draft-edit, validate, inspect, publish, set-default, assign-device, and clear-device-assignment accept exact arguments and reject unknown/duplicate/trailing input.
    - Publishing reports immutable version metadata and explicitly reports that endpoint assignment did not change.
    - Assignment output identifies the selected policy and next-poll activation semantics.
    - Local and server validation reject unsupported action and identical valid input yields the same canonical result.
  </behavior>
  <action>Start RED with an integration test file that names the complete command grammar. Add the exact local `dlp-policy` dependency and refresh Cargo.lock. Keep parsing/formatting in main.rs and typed requests in lib.rs; reuse the server compiler schema, never create a second validator. Draft edit accepts draft identifiers only. Keep publish, set-default, and assign-device distinct per D-02/D-04 and emit versioned machine-readable plus concise human output. Before existing symbol edits run `rtk node .gitnexus/run.cjs impact &lt;symbol&gt; --direction upstream --repo .`; pause on HIGH/CRITICAL and text-confirm UNKNOWN. Before every commit run complete nonpartial `rtk node .gitnexus/run.cjs detect-changes --scope all --repo .`.</action>
  <verify><automated>rtk cargo test --locked -p dlpctl --test policy_cli -- --list | rtk rg "^policy_lifecycle_tracer: test$" &amp;&amp; rtk cargo test --locked -p dlpctl --test policy_cli policy_lifecycle_tracer -- --exact</automated></verify>
  <acceptance_criteria>The discoverability-gated tracer performs the full draft-to-explicit-assignment path and proves publish has no hidden deployment effect.</acceptance_criteria>
  <done>An administrator can safely author, review, publish, and separately deploy one immutable policy through dlpctl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add inspection and monotonic deployment-status UX for admins and auditors</name>
  <read_first>
    - crates/dlpctl/tests/policy_cli.rs — Task 1 grammar fixtures.
    - crates/dlp-server/src/routes.rs — status and inspection response contracts.
    - 02-CONTEXT.md — D-01 inspection scope and D-17-inspired redaction boundary.
  </read_first>
  <files>crates/dlpctl/src/main.rs, crates/dlpctl/src/lib.rs, crates/dlpctl/tests/policy_cli.rs</files>
  <behavior>
    - Admin and auditor can inspect draft/version validation findings and desired/issued/activated/error status.
    - Auditor mutation attempts return stable forbidden errors.
    - Status never moves activated version backward and output omits private keys, plaintext, matches, and raw parser details.
  </behavior>
  <action>Expand failures before implementation. Add bounded inspect and deployment-status commands using typed server DTOs. Render desired, issued, activated, and stable error states monotonically, with auditor reads allowed and mutations still denied. Keep output versioned and project metadata only. Reuse Task 1 GitNexus gates.</action>
  <verify><automated>rtk cargo test --locked -p dlpctl --test policy_cli &amp;&amp; rtk cargo clippy --locked -p dlpctl --all-targets -- -D warnings</automated></verify>
  <acceptance_criteria>Both roles can inspect safe lifecycle/status data, only admins mutate, and all output remains stable and redacted.</acceptance_criteria>
  <done>dlpctl is the complete Phase 2 administrator/auditor surface required by D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator input -> CLI parser | File paths, IDs, output mode, and subcommands are untrusted. |
| dlpctl -> server mTLS | Typed policy operations cross the authenticated network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03-01 | Elevation of Privilege | Auditor CLI mutations | high | mitigate | Server-side role enforcement plus exhaustive auditor mutation tests. |
| T-02-03-02 | Tampering | Publish/assignment command confusion | high | mitigate | Distinct grammar and response contracts with no implicit assignment. |
| T-02-03-03 | Information Disclosure | CLI output/errors | medium | mitigate | Versioned allowlisted output and stable error codes. |
| T-02-03-SC | Tampering | Package supply chain | low | accept | Only exact local dlp-policy is added; no external package. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Administrators can author policies for the DLP boundary | 02-03 | COVERED |
| REQ | SRV-02, SRV-05, SRV-07, POL-07 | Role-safe CLI lifecycle/status and validation | 02-03 | COVERED |
| RESEARCH | — | Strict parser, shared compiler, typed mTLS client | 02-03 | COVERED |
| CONTEXT | D-01, D-02, D-03, D-04, D-09 | Complete CLI surface and publish/deploy separation | 02-03 | COVERED |
</source_audit>

<verification>Run the discoverability-gated tracer, full CLI integration suite, clippy, and complete GitNexus change detection.</verification>
<success_criteria>Every D-01 command exists; D-02/D-04 separation is visible; validation is shared; outputs and roles are safe.</success_criteria>

## Artifacts this phase produces

- Updated dlpctl manifest, parser, and typed client
- `crates/dlpctl/tests/policy_cli.rs`

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-03-SUMMARY.md` with TDD commits, command grammar, output examples, and GitNexus evidence.</output>
