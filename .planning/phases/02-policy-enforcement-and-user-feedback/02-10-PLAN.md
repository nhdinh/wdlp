---
phase: 02-policy-enforcement-and-user-feedback
plan: 10
type: tdd
wave: 9
depends_on: [02-09]
files_modified:
  - crates/dlp-windows-service/Cargo.toml
  - crates/dlp-windows-service/src/bin/dlp-companion.rs
  - crates/dlp-windows-service/src/notification.rs
  - crates/dlp-windows-service/src/session.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/lib.rs
  - crates/dlp-windows-service/tests/companion_grants.rs
autonomous: true
requirements: [AGT-10, DRV-05, DRV-08, UI-01, UI-02, UI-03]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "One small companion runs per eligible interactive session under that user's token; session 0 never displays UI per D-19/UI-01."
    - "The built companion authenticates, renders native block/warn toasts with exactly D-17 fields, and relays only opaque ProceedOnce action IDs."
    - "Empty/invalid names use a generic label; UTF-8/UTF-16 conversion is bounded and never splits surrogate pairs."
    - "Two sessions have separate pipe/process/notification/grant state; sign-out cleans up and crash restarts with bounded backoff."
    - "Service/machine restart restores active/LKG and companions but invalidates memory-only grants/intents (AGT-10)."
  artifacts:
    - {path: "crates/dlp-windows-service/src/bin/dlp-companion.rs", provides: "Per-user renderer and opaque action relay"}
    - {path: "crates/dlp-windows-service/src/notification.rs", provides: "Native Windows notification registration/render/update adapter"}
    - {path: "crates/dlp-windows-service/src/session.rs", provides: "SID/session-bound companion lifecycle"}
  key_links:
    - {from: "crates/dlp-windows-service/src/session.rs", to: "crates/dlp-windows-service/src/bin/dlp-companion.rs", via: "CreateProcessAsUserW plus authenticated purpose pipe", pattern: "companion|CreateProcessAsUser"}
    - {from: "crates/dlp-windows-service/src/bin/dlp-companion.rs", to: "crates/dlp-windows-service/src/notification.rs", via: "allowlisted model rendered through native adapter", pattern: "Notification(Model|Sink)"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] The companion cannot evaluate policy, construct grants, display full paths/content/matches/SIDs/internal IDs, or share state across sessions."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Ship the native authenticated per-user companion and bind it to the existing Windows session/restart lifecycle.

Purpose: Service-side intents must end in an actual privacy-safe Windows notification without moving authority into session UI.
Output: Companion executable, native adapter/registration, lifecycle orchestration, and isolation/restart tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-RESEARCH.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@crates/dlp-windows-service/src/notification.rs
@crates/dlp-windows-service/src/session.rs
@crates/dlp-windows-drive/src/bin/dlp-drive-host.rs

<interfaces>
Reuse `EligibleSession`, `ActorLauncher`, authenticated pipe lifecycle, and drive-host argument/error conventions. Plan 02-09 supplies NotificationModel/Sink and opaque action DTOs; the companion renders/relays only.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render one native privacy-safe block toast in the authenticated user session</name>
  <read_first>
    - 02-CONTEXT.md — D-17 through D-19.
    - 02-RESEARCH.md — official Microsoft notification guidance and registration inventory.
    - 02-PATTERNS.md — corrected base-file-name projection and no in-repo adapter analog.
    - crates/dlp-windows-service/src/notification.rs — Plan 02-09 safe model/sink.
    - crates/dlp-windows-service/Cargo.toml — existing windows features.
  </read_first>
  <files>crates/dlp-windows-service/Cargo.toml, crates/dlp-windows-service/src/bin/dlp-companion.rs, crates/dlp-windows-service/src/notification.rs, crates/dlp-windows-service/src/lib.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - Exact top-level toast_projection test builds allowlisted block/warn models including base file name and excluding every prohibited field.
    - Built companion authenticates, registers deterministic app identity/activation, renders first toast, updates Tag+Group sequence, and relays opaque warn ID only.
    - Empty/invalid basename uses generic protected-item; bounded UTF conversion never splits surrogate pairs.
    - Registration/render/activation/IPC errors produce stable nonsecret exits and warn remains denied.
  </behavior>
  <action>Start RED with exact named projection/adapter tests. Use only required feature flags of the already locked official windows crate; add deterministic app identity/activation registration, Tag+Group update sequence, activation callback, and cleanup. Build dlp-companion with bounded arguments, stable nonsecret exit codes, authenticated handshake, render loop, and opaque ProceedOnce relay. Display the base file name plus operation, safe rule display name, stable reason, and remediation per D-17; never full path or other prohibited data. Apply exact-symbol GitNexus impact, HIGH/CRITICAL warning, UNKNOWN text confirmation, and complete nonpartial detect-changes before commits.</action>
  <verify><automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants -- --list | rtk rg "^toast_projection: test$" &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test companion_grants toast_projection -- --exact &amp;&amp; rtk cargo build --locked -p dlp-windows-service --bin dlp-companion</automated><human-check>At end of phase on LAB-CLIENT01, verify the visible toast contains exactly the D-17 fields and no prohibited data.</human-check></verify>
  <acceptance_criteria>The named projection test and built companion prove authenticated native rendering, exact allowlist/privacy, grouping update, opaque action relay, and stable failure behavior.</acceptance_criteria>
  <done>A blocked/warned operation can produce a native safe toast in the correct user session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Launch, isolate, stop, and recover companions with Windows sessions</name>
  <read_first>
    - 02-CONTEXT.md — D-16/D-19 authority and per-user process.
    - crates/dlp-windows-service/src/session.rs — EligibleSession/supervisor patterns.
    - crates/dlp-windows-service/src/service.rs — restart and grant invalidation.
    - crates/dlp-windows-service/tests/session_lifecycle.rs — session fakes and launcher patterns.
  </read_first>
  <files>crates/dlp-windows-service/src/session.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/bin/dlp-companion.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - Exact companion_lifecycle test starts one authenticated companion per eligible SID/session under that user token.
    - Two sessions cannot receive/act on each other's notification or grant state.
    - Sign-out stops/cleans; crash restarts with bounded backoff; service/machine restart restores active/LKG and companion but no grant.
    - Launch/auth/notification/IPC failure keeps warn denied and service healthy with redacted diagnostics.
  </behavior>
  <action>Write exact named lifecycle tests, then add a companion launcher beside the drive actor using CreateProcessAsUserW, unpredictable purpose pipe, captured SID/session/generation, and authenticated handshake before readiness. Never launch UI in session 0. Isolate stop/restart/backoff per session and call Plan 02-08 invalidation hooks on restart/session/policy/content change. Do not pass drive storage keys to the companion. Apply Task 1 GitNexus gates.</action>
  <verify><automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants -- --list | rtk rg "^companion_lifecycle: test$" &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test companion_grants companion_lifecycle -- --exact &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test session_lifecycle &amp;&amp; rtk cargo clippy --locked -p dlp-windows-service --all-targets -- -D warnings</automated></verify>
  <acceptance_criteria>The named lifecycle test proves one companion per authenticated session, cross-user isolation, deterministic cleanup/restart, LKG recovery, and grant invalidation.</acceptance_criteria>
  <done>The companion is safely integrated with per-user Windows session and service recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Session-0 service -> companion | Process token, pipe, expected identity, and safe model cross session boundary. |
| Companion -> notification platform | Allowlisted fields and opaque activation enter OS APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-10-01 | Spoofing | Launch/handshake | high | mitigate | Captured user token, unpredictable pipe, kernel SID/session authentication. |
| T-02-10-02 | Information Disclosure | Toast/activation | high | mitigate | Exact D-17 allowlist including base filename; opaque activation only. |
| T-02-10-03 | Elevation of Privilege | Multi-session action | high | mitigate | Service-owned exact grants and isolated session routing. |
| T-02-10-04 | Tampering | Restart/update state | medium | mitigate | Monotonic Tag+Group sequence and restart invalidation. |
| T-02-10-SC | Tampering | Windows bindings | low | accept | Existing locked official windows crate; feature flags only. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | User-facing feedback reaches the correct session | 02-10 | COVERED |
| REQ | AGT-10, DRV-05, DRV-08, UI-01, UI-02, UI-03 | Recovery, isolation, actionable authenticated toast | 02-10 | COVERED |
| RESEARCH | — | Official notification adapter and session lifecycle | 02-10 | COVERED |
| CONTEXT | D-16, D-17, D-18, D-19 | Opaque action, privacy, grouping, per-user service authority | 02-10 | COVERED |
</source_audit>

<verification>Run named projection/lifecycle list gates, build companion, full service tests, clippy, and GitNexus change detection.</verification>
<success_criteria>Native toasts are safe and authenticated; companions are per-user, isolated, recoverable, and never authoritative.</success_criteria>

## Artifacts this phase produces

- `dlp-companion` executable
- Native notification adapter/registration
- Session-bound launch/restart/isolation wiring

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-10-SUMMARY.md` with TDD commits, visible-field contract, lifecycle tests, and GitNexus evidence.</output>
