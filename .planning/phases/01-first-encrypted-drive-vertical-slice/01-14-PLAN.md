---
phase: 01-first-encrypted-drive-vertical-slice
plan: 14
type: execute
wave: 3
depends_on: [01-13, 01-17]
files_modified:
  - crates/dlp-protocol/src/lib.rs
  - crates/dlp-agent-core/src/lib.rs
  - crates/dlp-agent-core/src/enrollment.rs
  - crates/dlp-agent-core/src/client.rs
  - crates/dlp-agent-core/src/config_cache.rs
  - crates/dlp-agent-core/src/health.rs
  - crates/dlp-agent-core/tests/enrollment_activation.rs
  - crates/dlp-windows-service/src/main.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/fingerprint.rs
  - crates/dlp-windows-service/src/credential.rs
  - tests/windows/Invoke-AgentServiceSmoke.ps1
  - config/agent.toml.example
autonomous: true
requirements: [SRV-03, CRY-02, CRY-04, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, TST-02, TST-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-25 through D-27, enrollment, DPAPI, mTLS, configuration, SCM, and restart completion requires focused Hyper-V evidence from LAB-DC01/LAB-DC02/LAB-CLIENT01 in addition to portable tests; these machine-verifiable facts do not route to visual confirmation."
    - "Per D-30 and D-31, the development CA preserves production-shaped server/device roles, EKUs, identity binding, replacement and revocation, and the LAB-CLIENT01 virtual-system-disk fixture proves exact match plus identity-change rejection without being represented as physical-hardware attestation."
    - "Per D-33 through D-47, every 01-14 privileged mutation is digest-approved, baseline/cleanup/idempotence checked, role-allowlisted, and published as immutable sanitized evidence with time, environment, raw-artifact, staleness, and supersession validation."
    - "Per D-01 through D-06 and D-21/D-24, the installed service on LAB-CLIENT01 automatically enrolls through LAB-DC01, generates the ECDSA P-256 key locally, sends only the CSR and exact version-1 hardware observation, receives a constrained device certificate, and uses device mTLS without an interactive session."
    - "The LAB-CLIENT01 device private key, returned chain, serial, expiry, and token-until-consumed exist only in one machine-DPAPI blob with UI forbidden and a validated SYSTEM/service-SID-only ACL; hungdinh-lt and both DCs hold no endpoint credential blob."
    - "Missing, corrupt, wrong-machine, or ACL-invalid credentials on LAB-CLIENT01 cause a fresh key/CSR and complete replacement enrollment; the old serial is revoked atomically by LAB-DC01 before the replacement client proceeds."
    - "Configuration polling on LAB-CLIENT01 validates TLS identity, exact signed bytes, hash, signature, schema, key ID, audience, and strictly increasing version before an atomic current/LKG change, and all invalid/concurrent completions preserve the prior pointers."
    - "Service and configuration state survive service restart on LAB-CLIENT01 and report complete redacted health to LAB-DC01."
  artifacts:
    - path: "crates/dlp-agent-core/src/config_cache.rs"
      provides: "Immutable staged signed-bundle current/LKG cache"
      exports: ["ConfigurationCache", "CachePointers"]
    - path: "crates/dlp-agent-core/tests/enrollment_activation.rs"
      provides: "Enrollment, TLS, cache, replay, concurrency, and restart contracts"
    - path: "tests/windows/Invoke-AgentServiceSmoke.ps1"
      provides: "LAB-CLIENT01-only service install/enrollment/DPAPI/mTLS/config/restart evidence"
    - path: "config/agent.toml.example"
      provides: "Secret-free server, trust, cache, service, and endpoint-path configuration contract"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Current enrollment, DPAPI, mTLS, signed-configuration, SCM, and health evidence IDs"
  key_links:
    - from: "crates/dlp-windows-service/src/fingerprint.rs"
      to: "crates/dlp-agent-core/src/enrollment.rs"
      via: "the exact normalized version-1 observation/digest is placed in the versioned enrollment request and recomputed by LAB-DC01"
      pattern: "HardwareFingerprintSources|EnrollmentRequestV1"
    - from: "crates/dlp-agent-core/src/enrollment.rs"
      to: "crates/dlp-windows-service/src/credential.rs"
      via: "the endpoint-local key and validated returned identity are committed to DPAPI before the device-mTLS client is constructed"
      pattern: "EnrollmentCoordinator|DpapiCredentialStore"
    - from: "crates/dlp-agent-core/src/client.rs"
      to: "crates/dlp-agent-core/src/config_cache.rs"
      via: "validated device-mTLS response bytes are staged, strictly verified, and monotonically activated"
      pattern: "stage_verify_activate"
  flagged_assumptions:
    - { id: "EDGE-SRV-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify SRV-03; initial, expiry, reuse, wrong-device, replacement, and race evidence stays explicit." }
    - { id: "EDGE-CRY-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify CRY-02; signature, hash, schema, key, audience, version, and exact-byte cases stay explicit." }
    - { id: "EDGE-AGT-01-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-01; SCM automatic-start, noninteractive startup, stop, and restart evidence must come from LAB-CLIENT01." }
    - { id: "EDGE-AGT-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-02; DPAPI flags, owner/DACL, ordinary-user denial, tamper, and replacement evidence stays explicit." }
    - { id: "EDGE-AGT-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-03; wrong root/name, expiry, timeout, absent client certificate, and revoked certificate cases stay explicit." }
    - { id: "EDGE-AGT-05-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-05; two activations plus restart must expose exact current and LKG bytes." }
    - { id: "EDGE-AGT-06-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-06; every invalid or partial bundle class must prove selected pointers unchanged." }
    - { id: "EDGE-AGT-07-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-07; every health field and error-redaction behavior stays explicit." }
  edge_resolutions:
    - { id: "EDGE-CRY-04-IDEMPOTENCY", status: covered, statement: "Repeated protect/load/restart operations on LAB-CLIENT01 converge on one valid DPAPI credential without emitting plaintext." }
    - { id: "EDGE-CRY-04-CONCURRENCY", status: covered, statement: "Concurrent protect/load/replace operations on LAB-CLIENT01 atomically select one validated DPAPI blob and never expose an unprotected file." }
    - { id: "EDGE-AGT-04-CONCURRENCY", status: covered, statement: "Concurrent bundle completions on LAB-CLIENT01 cannot roll back, cross-link, or partially replace current/LKG." }
  prohibitions:
    - "flagged-unverified: MUST NOT silently downgrade an enrolled LAB-CLIENT01 agent to bearer authentication, proceed after DPAPI/ACL validation failure, or use a replacement certificate before LAB-DC01 has revoked the prior serial."
    - "flagged-unverified: MUST NOT describe the exact hardware allowlist as TPM-backed or hardware-rooted remote attestation; privileged-local spoofing remains an explicit residual risk."
    - "flagged-unverified: MUST NOT persist or emit raw serials, enrollment tokens, endpoint private keys, DPAPI plaintext, certificate-secret material, protected content, or sensitive paths in plans, commands, logs, evidence, or commits."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Finish the partially implemented 01-08 agent/service work and prove the complete automatic enrollment, protected credential, device-mTLS, signed configuration, current/LKG, health, and service-restart path on LAB-CLIENT01.

Purpose: Turn the preserved enrollment primitives and SCM skeleton into the real endpoint trust chain, using the PostgreSQL-backed LAB-DC01 service from 01-13 and no endpoint runtime on hungdinh-lt.
Output: Completed protocol/client/cache/DPAPI/service code plus LAB-CLIENT01 service evidence.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-06-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-07-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@scripts/evidence/Phase1.Evidence.psm1
@crates/dlp-agent-core/src/enrollment.rs
@crates/dlp-agent-core/src/client.rs
@crates/dlp-windows-service/src/credential.rs
@crates/dlp-windows-service/src/fingerprint.rs
@crates/dlp-windows-service/src/service.rs

<interfaces>
Preserve and complete `EnrollmentCoordinator`, `EnrollmentTransport`, `AgentHttpClient`, `HealthSnapshot`, `HardwareFingerprintSources`, `DpapiCredentialStore`, and the SCM dispatcher. Existing commits `55b0dba`, `09d0840`, `a3be015`, and `abc1023` are partial implementation history, not work to revert. Add missing behavior through these seams and the existing LAB-DC01 `/api/v1` routes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: LAB-CLIENT01 automatically enrolls and posts health with a DPAPI-held device credential</name>
  <precondition>Plans 01-13 and 01-17 are green; the exact 01-14 privilege-manifest section has a valid digest-bound approval; LAB-DC01 has a pre-provisioned LAB-CLIENT01 allowlist/token record and active server/issuer material available through runtime-secure injection; LAB-CLIENT01 trusts only the approved public Phase 1 root.</precondition>
  <reversibility rating="costly">Per D-02 and D-05, the fingerprint tuple and certificate/DPAPI credential contract are persisted server/endpoint identities; changing either later requires coordinated migration.</reversibility>
  <files>crates/dlp-protocol/src/lib.rs, crates/dlp-agent-core/src/enrollment.rs, crates/dlp-agent-core/src/client.rs, crates/dlp-windows-service/src/credential.rs, tests/windows/Invoke-AgentServiceSmoke.ps1</files>
  <read_first>
    - Every path in `<files>` plus `crates/dlp-windows-service/src/fingerprint.rs` and `crates/dlp-server/src/{enrollment,routes,tls,repository}.rs`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-01 through D-06 and D-21 through D-24
    - `01-06-SUMMARY.md`, `01-07-SUMMARY.md`, and `01-13-SUMMARY.md`
    - Research resolved PKI profile, DPAPI pitfall, and canonical fingerprint contract
  </read_first>
  <behavior>
    - Test 1: first startup sends exact v1 fingerprint observation/digest, device/computer identity, one-time token, and endpoint-signed ECDSA P-256 CSR but no private key; LAB-DC01 returns one constrained 30-day certificate identity.
    - Test 2: LAB-CLIENT01 validates root/hostname, certificate chain/profile/SAN/serial/expiry, commits key/chain/serial/metadata/token state to one UI-forbidden machine-DPAPI blob, erases the consumed token, builds device mTLS, and posts health.
    - Test 3: the DPAPI directory/file owner and DACL allow SYSTEM and the dedicated service SID only; an ordinary LAB-CLIENT01 user cannot list/read/decrypt the file.
    - Test 4: missing, corrupt, wrong-machine, or ACL-invalid blob generates a fresh key/CSR and completes atomic server-side old-serial revocation plus replacement before health resumes.
  </behavior>
  <action>Per D-01, D-02, D-03, D-04, D-05, and D-06, extend the versioned enrollment request/response to carry the exact fingerprint version/observation needed for server recomputation, signed CSR, domain computer identity, certificate profile metadata, and replacement prior-serial reference without exposing the private key or raw values in Debug/logs. Remove the current pre-enrollment `replacement_complete` ordering: one LAB-DC01 transaction must re-run token/domain/fingerprint checks, revoke the prior serial, activate the new serial, and only then return replacement success. Implement real rustls/reqwest bootstrap and device-mTLS requests with the installed public root, ordinary hostname validation, bounded body/timeouts, no dangerous verifier, and no bearer fallback after enrollment. Validate the returned leaf/chain/profile/audience before use. Complete `DpapiCredentialStore` with UI-forbidden machine DPAPI, same-directory atomic write/flush/replace, directory flush, bounded decode, owner/DACL creation and revalidation for SYSTEM plus the dedicated service SID, zeroization of private/token/plain buffers, and fail-closed load. Preserve the non-Windows test adapter only as an explicit injected unit-test backend; per D-29 and D-32 it cannot satisfy DPAPI, AD, WinRM, PKI, mTLS, service, or enrollment acceptance. Per D-30, install development public trust only on designated lab VMs and retain separate server/device roles, EKUs, identity binding, replacement, expiry, and application-layer revocation. Per D-31, prove the deterministic LAB-CLIENT01 system-disk fixture exact match and disk-identity change rejection and label it workflow evidence rather than physical-source robustness. Per D-33 through D-47, perform only approved mutations, capture baseline/cleanup/idempotence, and publish distinct immutable focused-Hyper-V attempts through Plan 01-17.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, initial service startup reaches one active LAB-DC01 device record and posts health using device mTLS without an interactive session or transmitted private key."
    - "On LAB-CLIENT01, the credential blob is machine-DPAPI protected with UI forbidden, atomically stored, owner/DACL validated, and denied to an ordinary test user; no corresponding blob exists on hungdinh-lt, LAB-DC01, or LAB-DC02."
    - "Across LAB-CLIENT01 and LAB-DC01, a corrupt/missing/wrong-machine/ACL-invalid credential results in a fresh key/CSR and the prior serial is rejected before the new health request succeeds."
    - "On LAB-CLIENT01, wrong root/name, invalid returned profile, timeout, absent client certificate, revoked serial, or attempted bearer fallback fails before protected agent data is returned."
    - "The initial, replacement, exact-match, disk-identity-change, dual-DC, DPAPI ACL, mTLS, and health attempts validate against the evidence schema with binding machines, environment fingerprints, artifact hashes, and no secret markers."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt source-only: cargo test --locked -p dlp-agent-core -p dlp-windows-service; then on hungdinh-lt orchestrating execution inside LAB-CLIENT01 against LAB-DC01: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario EnrollmentCredential</automated>
  </verify>
  <done>LAB-CLIENT01 completes initial and replacement enrollment with endpoint-local key custody, validated TLS/mTLS, prior-serial revocation, and a redacted health report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate signed configuration into a durable concurrent-safe current/LKG cache</name>
  <precondition>LAB-CLIENT01 has an active DPAPI credential from Task 1 and LAB-DC01 serves immutable audience-bound signed configurations.</precondition>
  <files>crates/dlp-agent-core/src/lib.rs, crates/dlp-agent-core/src/client.rs, crates/dlp-agent-core/src/config_cache.rs, crates/dlp-agent-core/src/health.rs, crates/dlp-agent-core/tests/enrollment_activation.rs</files>
  <read_first>
    - Every path in `<files>` plus `crates/dlp-crypto/src/lib.rs` and signed DTOs in `crates/dlp-protocol/src/lib.rs`
    - `.planning/docs/adrs/ADR-005-policy-signing.md`
    - `.planning/REQUIREMENTS.md` CRY-02 and AGT-03 through AGT-07
    - Research canonical signed-configuration and current/LKG patterns
  </read_first>
  <behavior>
    - Test 1: a higher valid exact-byte bundle is written to an immutable content-addressed staging path, durably flushed, verified, and atomically selected while the prior current becomes LKG.
    - Test 2: unsigned, tampered, wrong-key, unsupported-schema, hash-mismatched, wrong-audience, truncated, equal/lower-version, and interrupted downloads leave current/LKG byte-for-byte unchanged.
    - Test 3: concurrent poll completions select the greatest valid version and cannot cross-link current/LKG or activate a late lower response.
    - Test 4: restart validates current and LKG independently, ignores/quarantines unreferenced staging, and reports exact active bundle/policy state without sensitive diagnostics.
  </behavior>
  <action>Create `ConfigurationCache` as immutable content-addressed bundle files plus small authenticated/versioned pointer records. `AgentHttpClient` polls only through the Task 1 device-mTLS identity. Verify exact bytes with strict Ed25519 before parsing, then validate fixed hash, supported schema, signing key ID, device audience, and strictly increasing numeric version. Flush the bundle and parent directory before atomically replacing pointers. Use one activation mutex plus generation check so concurrent completion order cannot roll back. At startup, authenticate and resolve current/LKG independently, never infer state from filenames/timestamps, and clean unreferenced staging only after selected pointers are safe. Extend health with agent/service/config/drive state, active bundle/policy version, contact time, and stable redacted error code. Per D-25, D-27, D-37, D-40, D-41, D-43 through D-47, retain portable negative/concurrency attempts separately from LAB-CLIENT01 activation/restart attempts, enforce machine/tier/artifact/time/redaction checks, and update only the corresponding CRY/AGT/TST matrix rows.</action>
  <acceptance_criteria>
    - "On hungdinh-lt source tests, every signed-bundle negative and concurrent case proves active files/pointers remain unchanged or select only the greatest valid version."
    - "On LAB-CLIENT01, two successful activations followed by service restart preserve exact newest current and prior LKG bytes and report the active version to LAB-DC01."
    - "On LAB-CLIENT01, partial staging, invalid pointer targets, replay, wrong audience, and late lower responses never become active or alter LKG."
    - "On LAB-CLIENT01 and LAB-DC01, cache/health/log evidence contains no token, private key, raw fingerprint, certificate-secret material, protected content, or sensitive path."
    - "Each bundle class, concurrency race, two-activation sequence, and restart has an immutable schema-valid attempt with prior/supersession links where rerun."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: cargo test --locked -p dlp-agent-core --test enrollment_activation; cargo clippy --locked -p dlp-agent-core --all-targets -- -D warnings; then inside LAB-CLIENT01 via hungdinh-lt orchestration: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario ConfigurationCache</automated>
  </verify>
  <done>LAB-CLIENT01 activates only the newest valid audience-bound configuration and retains exact current/LKG state across concurrency, invalid inputs, and restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install and exercise the automatic noninteractive service on LAB-CLIENT01</name>
  <precondition>Tasks 1-2 are green, the approved 01-14 privilege-manifest digest remains unchanged, and LAB-CLIENT01 can install/start the exact listed automatic Windows service through the existing lab orchestration channel.</precondition>
  <files>crates/dlp-windows-service/src/main.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/fingerprint.rs, tests/windows/Invoke-AgentServiceSmoke.ps1, config/agent.toml.example</files>
  <read_first>
    - Every path in `<files>` after Tasks 1-2 plus `crates/dlp-windows-service/src/credential.rs`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-01 through D-06 and D-20 through D-24
    - Research Windows service lifecycle, fingerprint sources, and health contracts
    - `01-13-SUMMARY.md` role guards and LAB-DC01 deployment entry points
  </read_first>
  <behavior>
    - Test 1: install/start sets SCM automatic start, transitions StartPending→Running, creates Tokio inside the service entry, and operates with no signed-in user.
    - Test 2: fingerprint collection returns only exact SMBIOS UUID, BIOS serial, and physical OS-disk serial; missing/sentinel/changed disk observations fail before enrollment and no MAC is accepted.
    - Test 3: stop/shutdown is bounded and restart reloads DPAPI credential plus current/LKG before polling and health resume.
    - Test 4: all service/fingerprint/credential/config errors emit stable codes only and preserve the last usable state.
  </behavior>
  <action>Complete the SCM entry and state machine around the Task 1-2 components: register Stop/Shutdown now and the session-change controls consumed by 01-15, report accurate pending/running/stopped states, create/stop the Tokio runtime inside the service, and never require an interactive desktop. Replace PowerShell-based production fingerprint collection with isolated documented Windows API calls for SMBIOS/BIOS and system-volume-to-physical-disk identity, preserving the exact normalization/digest shared with trusted provisioning; retain an injected collector only for portable tests. Create a secret-free agent configuration example with server hostname, public root path, cache/data paths, polling/backoff bounds, service identity, and later mount settings; runtime-sensitive values resolve through the endpoint runtime provider, not command arguments or committed files. Per D-33 through D-36, validate the unchanged approval digest, baseline the exact LAB-CLIENT01 service/trust/ACL/file state, apply only listed mutations idempotently, verify final state, and remove temporary artifacts after success or failure. Extend the smoke to install, query, stop, start, force-kill, and restart the service on LAB-CLIENT01 and assert no service/credential/runtime appeared on hungdinh-lt. Per D-25 and D-37 through D-47, publish focused machine-correct attempts and preserve failures/reruns rather than folding them into source evidence.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, SCM reports automatic start and a noninteractive Running service; stop, shutdown, force termination, and restart finish boundedly and resume authenticated polling/health."
    - "On LAB-CLIENT01, the exact three hardware sources match the trusted LAB-DC01 provisioning digest, while missing/sentinel/system-disk-change cases abort before enrollment."
    - "On LAB-CLIENT01, service restart reloads the validated DPAPI credential and exact current/LKG cache before mTLS polling; on hungdinh-lt no endpoint service or credential is present."
    - "On LAB-CLIENT01 and LAB-DC01, captured errors and health use stable redacted codes and retain the last usable credential/configuration on transient failure."
    - "The service install/restart baseline, approved mutation digest, cleanup, versions, binary/config hashes, machine role, time offset, and raw-artifact hashes are present in schema-valid focused-Hyper-V evidence."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: cargo fmt --all -- --check; cargo clippy --locked -p dlp-agent-core -p dlp-windows-service --all-targets -- -D warnings; then inside LAB-CLIENT01 via hungdinh-lt orchestration: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario ServiceRestart</automated>
  </verify>
  <done>The real automatic LAB-CLIENT01 service owns enrollment, DPAPI credential, mTLS polling, current/LKG recovery, and redacted health without any endpoint runtime on hungdinh-lt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LAB-CLIENT01 hardware/service → LAB-DC01 enrollment | Local observations, token, CSR, and replacement metadata are untrusted until server authority checks succeed. |
| Endpoint-generated key + server response → DPAPI file | The private key must stay local and the returned identity/profile plus file ACL must validate before use. |
| LAB-DC01 signed bytes → LAB-CLIENT01 active cache | Tamper, truncation, replay, rollback, audience confusion, and concurrent completion target active state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Spoofing | enrollment identity | high | mitigate | Exact version-1 observations, LAB-DC01 allowlist and dual-DC checks, single-use token, signed endpoint CSR, and transaction-backed replacement. |
| T-01-14-02 | Information disclosure/Elevation | DPAPI credential | high | mitigate | UI-forbidden machine DPAPI, SYSTEM/service-SID owner/DACL, atomic file, ACL revalidation, zeroization, and ordinary-user denial on LAB-CLIENT01. |
| T-01-14-03 | Spoofing | TLS/mTLS client | high | mitigate | Installed public root, ordinary hostname validation, returned profile validation, active serial lookup, bounded transport, and no bearer fallback. |
| T-01-14-04 | Tampering | configuration cache | high | mitigate | Exact-byte strict signature/hash/schema/key/audience/version gates, immutable staging, durable pointer swap, activation mutex/generation, and restart validation. |
| T-01-14-05 | Information disclosure | diagnostics/evidence | high | mitigate | Stable codes/digests only and machine-specific marker scans for secrets, raw serials, paths, and protected content. |
| T-01-14-06 | Denial of service | service/polling | medium | mitigate | Bounded bodies/timeouts/backoff, SCM pending/stop states, last-usable state retention, and restart tests. |
| T-01-14-SC | Tampering | existing dependency graph | high | mitigate | Preserve the exact previously approved Cargo.lock sources/versions and run slopcheck before accepting any dependency change; no new package is authorized by this plan. |
| T-01-14-07 | Elevation/Repudiation | privileged endpoint changes and evidence | high | mitigate | Digest-bound 01-14 manifest, role guard, baseline/apply/verify/remove, cleanup, immutable Plan 01-17 attempts, staleness, artifact/time, and redaction gates. |

## ASVS L1 Gate

Mitigate every high-severity row before completion. Any unapproved mutation, bearer downgrade, transmitted/persisted plaintext secret, broad credential ACL, revoked-serial access, invalid cache activation, machine-provenance mismatch, or invalid evidence attempt blocks the plan.
</threat_model>

<verification>
Run source/unit checks on hungdinh-lt only. Run initial/replacement enrollment, DPAPI/ACL, mTLS, configuration polling, SCM, and restart scenarios inside LAB-CLIENT01 against LAB-DC01. Verify LAB-DC02 participates only through server-side directory corroboration and hungdinh-lt has no endpoint artifacts. Validate every focused result and current matrix pointer through Plan 01-17.
</verification>

<success_criteria>
- LAB-CLIENT01 enrolls automatically and protects its endpoint-local credential.
- Device mTLS and signed configuration activation fail closed and survive restart.
- LAB-DC01 revokes replaced serials and records redacted health.
- No endpoint runtime or endpoint evidence originates on hungdinh-lt.
- All high-severity threat mitigations have machine-specific passing evidence.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New/extended symbol | `EnrollmentRequestV1` / `EnrollmentResponseV1` fields | Versioned fingerprint, CSR, certificate profile, and replacement contract |
| Extended symbol | `EnrollmentCoordinator` | Initial/existing/replacement state machine with atomic server ordering |
| Extended symbol | `AgentHttpClient` | Real bootstrap TLS and device-mTLS transport |
| Extended symbol | `DpapiCredentialStore` | UI-forbidden machine DPAPI, atomic commit, and SYSTEM/service-SID ACL |
| New path | `crates/dlp-agent-core/src/config_cache.rs` | Durable `ConfigurationCache` and `CachePointers` |
| New path | `crates/dlp-agent-core/tests/enrollment_activation.rs` | Trust/cache/concurrency/restart tests |
| Extended symbol | `HealthSnapshot` | Complete redacted service/config/drive/contact report |
| New path | `tests/windows/Invoke-AgentServiceSmoke.ps1` | LAB-CLIENT01 endpoint trust/service scenario runner |
| New path | `config/agent.toml.example` | Secret-free endpoint configuration contract |
| New flags | `-CallerMachine`, `-ExecutionMachine`, `-ServerMachine`, `-SecretProvider`, `-Scenario` | Explicit endpoint/server provenance selectors |
| Generated evidence | `evidence/phase1/attempts/<evidence-id>.json` and requirement-matrix pointers | Immutable enrollment/DPAPI/mTLS/configuration/SCM attempts with D-37 through D-47 provenance |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md` when done.
</output>
