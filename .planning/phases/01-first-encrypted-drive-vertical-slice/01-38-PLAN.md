---
phase: 01-first-encrypted-drive-vertical-slice
plan: 38
type: execute
wave: 26
depends_on:
  - 01-37
files_modified:
  - evidence-private/phase1/d48-reviewer-policy.json
  - evidence-private/phase1/d48-reviewer-root.cer
  - evidence/phase1/independent-reviews/index.json
  - evidence/phase1/independent-reviews/generations
autonomous: false
gap_closure: true
requirements:
  - TST-08
estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 3
  confidence: low
user_setup:
  - service: phase1-d48-reviewer-pki
    why: "D-48 requires a verifier and station explicitly authorized by a separate policy and distinct from LAB\\dlp-reviewer and its archival signing certificate."
    dashboard_config:
      - task: "Select and authorize the D-48 verifier account, station, certificate subject/thumbprint, trust root, EKU, revocation endpoints, and procedure version."
        location: "Phase 1 lab PKI and access-control administration; do not change LAB-DC01/LAB-DC02 topology merely to fit the ceremony."
      - task: "Sign in interactively on the policy-authorized station as the distinct D-48 verifier with the approved CurrentUser private key available."
        location: "Station named by the newly approved D-48 policy."
must_haves:
  truths:
    - "The chosen D-48 identity and station are explicit policy decisions, not inferred or invented by the executor."
    - "The D-48 identity, certificate subject, and thumbprint are all distinct from LAB\\dlp-reviewer and E9407299128C7A1292E3B78F7F2E369CB71B67A5."
    - "The authorized verifier reviews the exact frozen artifact inventory and signs the displayed canonical commitment only after all D-49, accessibility, and retention checks."
    - "The ceremony appends one immutable generation and does not overwrite any prior review record or frozen artifact."
  artifacts:
    - path: evidence-private/phase1/d48-reviewer-policy.json
      provides: "Explicit public authorization for the distinct D-48 verifier and station"
    - path: evidence/phase1/independent-reviews/index.json
      provides: "Append-only pointer/index for signed D-48 generations"
  key_links:
    - from: D-48 policy-authorized interactive session
      to: signed review generation
      via: "observed Windows context, CurrentUser CMS key, and exact affirmation"
    - from: signed review generation
      to: frozen Phase 1 evidence
      via: "canonical full-set digest commitment"
---

<objective>
Authorize a truly distinct D-48 verifier and perform the authenticated phase-exit review ceremony against the frozen final evidence.

Purpose: Supply the human judgment and private-key operation that automation cannot perform without inventing authority.
Output: Separate D-48 public policy/trust inputs and one immutable signed review generation.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-37-SUMMARY.md
@scripts/add-independent-review.ps1
@scripts/verify-phase1.ps1
@evidence-private/phase1/reviewer-policy.json
@evidence/phase1/security-closure.yaml
@evidence/phase1/requirement-matrix.yaml
@tests/windows/results/phase1-evidence.json
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve the separate D-48 reviewer policy and station</name>
  <files></files>
  <decision>Approve the exact distinct D-48 verifier identity, station, certificate, trust, procedure, skew, and retention policy that will authorize the ceremony.</decision>
  <options>
    <option>Approve the presented candidate only if every public metadata field is correct and all three separation comparisons pass.</option>
    <option>Reject and provision a different candidate; no policy or ceremony artifact is published.</option>
  </options>
  <action>
    Present the observed candidate account, machine, certificate subject, normalized thumbprint, required key usage/EKU, trust root identity, revocation endpoints, domain/role, procedure version, maximum clock skew, and retention window. Mechanically compare identity, subject, and thumbprint with the archival policy and reject reuse. Preserve topology: LAB-DC01 remains management/trusted provisioning; LAB-DC02 remains the archival reviewer station for LAB\dlp-reviewer and secondary AD authority. The D-48 station comes only from this explicit policy decision and is not assumed to be either DC. The human must approve the exact public metadata before any review prompt or mutation. No private key is copied or exported. This pure decision checkpoint performs no repository mutation and creates no commit; staged `detect_changes` is therefore not applicable. Task 2 alone materializes and commits the approved public artifacts under its own scope gate.
  </action>
  <reversibility rating="costly">Changing reviewer authority after a signed review requires a new policy version and superseding review generation.</reversibility>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Evidence.Tests.ps1</automated>
    <human-check>The approver confirms the exact policy metadata and that the account, subject, and thumbprint are distinct from the archival signer.</human-check>
  </verify>
  <resume-signal>Type `policy approved` only after approving the exact displayed metadata; otherwise provide the corrected candidate metadata.</resume-signal>
  <done>The exact candidate metadata and custody/station choice are approved for materialization; no policy/root artifact is claimed yet.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Materialize and validate the approved public D-48 policy and root</name>
  <files>evidence-private/phase1/d48-reviewer-policy.json; evidence-private/phase1/d48-reviewer-root.cer</files>
  <precondition>The exact candidate metadata and station were approved in Task 1, and the public certificate chain is available without exporting the signer private key.</precondition>
  <action>Materialize a versioned D-48 reviewer policy containing exactly the approved identity, station, role, certificate subject/thumbprint, required EKU/key usage, trust-root identity, online revocation requirements/endpoints, procedure version, skew, domain, and retention contract. Copy/export only the public root certificate; preserve the CurrentUser private key on its authorized station. Validate schema, root digest, chain purpose, revocation configuration, exactly one reviewer, and identity/subject/thumbprint separation from the archival reviewer policy. Byte-compare materialized metadata with Task 1 approval; any difference returns to Task 1. Before committing, stage only these two files, run GitNexus staged `detect_changes`, and require zero code symbols/processes and no undeclared file.</action>
  <instructions>The human/PKI administrator supplies only approved public policy and certificate-chain material. The executor materializes and validates both files; no private key is exported, copied, or committed.</instructions>
  <reversibility rating="costly">A materialized authority policy is versioned; corrections require a newly approved version before signing.</reversibility>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Evidence.Tests.ps1</automated></verify>
  <verification>Policy validation proves exact approval equality, public-root identity, purpose/revocation contract, and separation from the archival signer.</verification>
  <resume-signal>Type `policy materialized` after both public files validate and the private key remains solely on the approved station.</resume-signal>
  <done>The approved public policy/root exist and validate; the ceremony does not assume missing artifacts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Review and sign the frozen D-48 commitment</name>
  <files>evidence/phase1/independent-reviews/index.json; evidence/phase1/independent-reviews/generations</files>
  <precondition>Plan 01-37 suites pass; the approved D-48 policy/root validate; the interactive observed context and CurrentUser certificate match exactly one policy entry; all frozen input hashes are inventoried and unchanged.</precondition>
  <action>
    The executor copies only reviewed public scripts, the validated Task 2 public trust/policy inputs, and the frozen evidence set to the policy-approved station. In the interactive authorized session, run the publisher without identity or machine overrides. The verifier must inspect the complete requirement matrix, provenance, current and superseded attempts, `01-SECURITY.md`, `01-REVIEW.md`, closure manifest, both reviewer policies/trust identities, every D-49 deviation disposition, artifact accessibility, retention/hold state, and displayed per-file hashes. Require explicit exact affirmation of the canonical commitment before CMS signing. Publish through the tested mutex/recovery protocol, return only the public signed generation/index, and prove every frozen artifact is byte-identical to its pre-ceremony inventory. Do not overwrite the existing `bundle.independent_review`, delete earlier attempts, alter matrix rows, export a private key, or manually edit signed JSON. Before committing the returned artifacts, stage only the declared index and new generation, run GitNexus staged `detect_changes`, and require no undeclared file, code symbol, or execution-flow impact.
  </action>
  <instructions>The executor stages the reviewed public inputs and frozen inventory. The human signs in interactively as the policy-authorized D-48 verifier, reviews every displayed section and hash, and enters the exact affirmative response only when the commitment is correct. No override, key export, or manual signed-artifact edit is permitted.</instructions>
  <reversibility rating="costly">The signed attempt is permanent audit history; correction requires an additive superseding generation.</reversibility>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH -D48TrustedRootPath $env:PHASE1_D48_TRUSTED_ROOT_PATH -D48ReviewerPolicyPath $env:PHASE1_D48_REVIEWER_POLICY_PATH</automated>
    <human-check>The verifier confirms the complete displayed commitment and signs from the policy-authorized interactive account/station.</human-check>
  </verify>
  <verification>The executor retrieves only public outputs, proves frozen byte identity, validates the CMS generation and append-only predecessor/index, then runs canonical FinalGate with both trust policies.</verification>
  <resume-signal>Type `D-48 ceremony complete` after the publisher completes without error and the public generation/index have been returned.</resume-signal>
  <done>One public CMS-signed D-48 generation is appended by the observed distinct verifier, all frozen bytes remain unchanged, and FinalGate validates it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authority decision -> D-48 policy | Human approval establishes who and where may sign. |
| Interactive signer -> CMS generation | Private-key custody remains on the approved station. |
| Ceremony transfer -> repository | Only public signed artifacts return; frozen bytes must match inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-38-01 | Spoofing | reviewer authorization | critical | mitigate | Explicit policy checkpoint and observed-context comparison. |
| T-01-38-02 | Elevation of privilege | signer reuse | critical | mitigate | Three-field separation from archival signer before ceremony. |
| T-01-38-03 | Tampering | ceremony transfer | high | mitigate | Pre/post byte inventory and public-only return. |
| T-01-38-04 | Repudiation | verifier judgment | high | mitigate | Exact canonical commitment affirmation and CMS signature. |
| T-01-38-05 | Information disclosure | private key/evidence | high | mitigate | No key export, sanitized public inputs, no unnecessary raw artifact transfer. |
</threat_model>

<verification>
The policy decision and ceremony are blocking. Completion requires canonical FinalGate to validate the returned public generation with both archival and D-48 trust inputs.
</verification>

<success_criteria>
- Authority and station are explicitly approved.
- All separation dimensions differ from the archival signer.
- The exact frozen set is reviewed and CMS signed.
- No prior record or frozen artifact is overwritten.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-38-SUMMARY.md` when done.
</output>
