---
phase: 02-policy-enforcement-and-user-feedback
plan: 06
type: tdd
wave: 5
depends_on: [02-04]
files_modified:
  - crates/dlp-protocol/src/lib.rs
  - crates/dlp-crypto/src/lib.rs
  - crates/dlp-agent-core/src/config_cache.rs
  - crates/dlp-agent-core/tests/policy_activation.rs
  - crates/dlp-server/tests/policy_distribution.rs
autonomous: true
requirements: [SRV-06, SRV-07, CRY-05, AGT-10]
estimate: {tokens: 35000, raw_tokens: 35000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Only an already-trusted key can authorize a unique new key ID; unknown/self-introduced/conflicting keys are rejected (CRY-05)."
    - "Old-key overlap accepts equality at both version and epoch bounds and rejects one step beyond either while preserving LKG."
    - "Concurrent/repeated transitions are idempotent and cannot cross-link key, payload, signature, or evaluator generations across restart."
    - "The policy_distribution integration target exercises publish/assign/poll/activate/evaluate/status through production components."
  artifacts:
    - {path: "crates/dlp-protocol/src/lib.rs", provides: "SigningKeyTransitionV1 canonical contract"}
    - {path: "crates/dlp-crypto/src/lib.rs", provides: "Bounded trusted verification keyring"}
    - {path: "crates/dlp-server/tests/policy_distribution.rs", provides: "Production distribution E2E target"}
  key_links:
    - {from: "crates/dlp-agent-core/src/config_cache.rs", to: "crates/dlp-crypto/src/lib.rs", via: "old trusted key authorizes atomic keyring/config transition", pattern: "key_transition|key_id"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Rotation cannot become trust-on-first-use or strand current/LKG when an endpoint misses overlap."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Add replay-safe server signing-key rotation and prove the complete server-to-endpoint policy distribution path.

Purpose: CRY-05 requires bounded established-trust transitions, while SRV-07 needs production-path evidence beyond injection-only activation tests.
Output: Key-transition protocol/keyring, atomic persistence, rotation edges, and policy_distribution integration target.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-RESEARCH.md
@.planning/docs/adrs/ADR-005-policy-signing.md
@crates/dlp-crypto/src/lib.rs
@crates/dlp-agent-core/src/config_cache.rs
@tests/e2e/server_enrollment.rs

<interfaces>
Plan 02-04 supplies ConfigurationBundleV2 and atomic policy activation. Extend that same transaction with a bounded keyring; `crates/dlp-server/tests/policy_distribution.rs` is a Cargo-auto-discovered integration target and requires no `[[test]]` entry.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Authorize one new signing key with the current trusted key</name>
  <reversibility rating="costly">The old-key-authorized rotation envelope and overlap edges become persistent trust contracts shared by server and endpoints.</reversibility>
  <read_first>
    - ADR-005-policy-signing.md — established-key authorization and missed-overlap recovery.
    - 02-RESEARCH.md — Pattern 5 and resolved rotation cross-reference for Plan 02-06 Task 1.
    - crates/dlp-crypto/src/lib.rs — current single-key verifier.
    - crates/dlp-agent-core/src/config_cache.rs — Plan 02-04 atomic activation.
  </read_first>
  <files>crates/dlp-protocol/src/lib.rs, crates/dlp-crypto/src/lib.rs, crates/dlp-agent-core/src/config_cache.rs, crates/dlp-agent-core/tests/policy_activation.rs</files>
  <behavior>
    - A top-level signing_key_rotation test accepts a current-key-signed transition to one unique new key ID.
    - Both keys verify inside overlap; equality at version/time bounds is accepted and one step beyond either retires old-key use.
    - Unknown/self-signed introduction, conflicting duplicate ID, empty keyring, replay, and expired transition reject without pointer changes.
    - Repeated equal transition is idempotent; concurrent activation chooses one coherent greatest generation across restart.
  </behavior>
  <action>Write the exact named rotation test first. Define canonical SigningKeyTransitionV1 with unique key ID/public bytes, monotonic not-after bundle version, and epoch-seconds not-after. Require signature by a currently trusted key before bounded keyring mutation; equality at both bounds is accepted and exceeding either rejects old-key use. Persist keyring transition atomically with configuration/current/LKG selection. Endpoints that miss overlap retain LKG and follow ADR-005 re-enrollment. Apply exact-symbol GitNexus impact, HIGH/CRITICAL warning, UNKNOWN text confirmation, and complete nonpartial detect-changes before commits.</action>
  <verify><automated>rtk cargo test --locked -p dlp-agent-core --test policy_activation -- --list | rtk rg "^signing_key_rotation: test$" &amp;&amp; rtk cargo test --locked -p dlp-agent-core --test policy_activation signing_key_rotation -- --exact</automated></verify>
  <acceptance_criteria>The named rotation test runs and proves established-trust introduction, exact overlap boundaries, replay/conflict rejection, atomic persistence, concurrency, and restart coherence.</acceptance_criteria>
  <done>Server verification keys can rotate without trust-on-first-use or loss of last-known-good enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove publish-to-decision distribution and monotonic deployment status</name>
  <read_first>
    - tests/e2e/server_enrollment.rs — production signed distribution test fixtures.
    - crates/dlp-agent-core/tests/policy_activation.rs — Task 1 rotation helpers.
    - crates/dlp-server/src/routes.rs — Plan 02-04 authenticated poll/status behavior.
  </read_first>
  <files>crates/dlp-server/tests/policy_distribution.rs, crates/dlp-agent-core/tests/policy_activation.rs, crates/dlp-protocol/src/lib.rs</files>
  <behavior>
    - Admin publish/assign -> authenticated device poll -> verify/activate -> evaluator decision reports desired, issued, and activated versions.
    - Invalid/older assignment retains LKG and reports stable failed version/status.
    - Device audience, canonical round-trip, digest, signature, key ID, and monotonic version are asserted on the real path.
  </behavior>
  <action>Create the policy_distribution integration target and exercise the real server repository/routes, signer, protocol, configuration cache, and Plan 02-01 evaluator. Do not replace the production path with injected bundle bytes. Include valid rotation and missed/expired overlap recovery signals. Run the target without a name filter so a missing target fails, then run relevant clippy and the Task 1 GitNexus gates.</action>
  <verify><automated>rtk cargo test --locked -p dlp-server --test policy_distribution &amp;&amp; rtk cargo test --locked -p dlp-agent-core --test policy_activation &amp;&amp; rtk cargo clippy --locked -p dlp-protocol -p dlp-crypto -p dlp-agent-core --all-targets -- -D warnings</automated></verify>
  <acceptance_criteria>The Cargo integration target exists and passes through production components with exact audience/signature/version/status assertions and LKG failure behavior.</acceptance_criteria>
  <done>Signed distribution and key rotation operate end to end with deterministic status and recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Trusted keyring -> transition | New verification authority can be introduced only by established authority. |
| Server poll -> endpoint activation | Signed assignment and status cross the device mTLS boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-06-01 | Spoofing | Key transition | high | mitigate | Current trusted signature, unique IDs, exact overlap, no self-introduction. |
| T-02-06-02 | Tampering | Key/config generation | high | mitigate | One atomic persisted transition with concurrency/restart tests. |
| T-02-06-03 | Repudiation | Deployment status | medium | mitigate | Device-authenticated monotonic desired/issued/activated/error records. |
| T-02-06-SC | Tampering | Package supply chain | low | accept | No package installation or dependency change. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Signed assigned policy reaches endpoint evaluator | 02-06 | COVERED |
| REQ | SRV-06, SRV-07, CRY-05, AGT-10 | Signed distribution, rotation, recovery | 02-06 | COVERED |
| RESEARCH | — | Old-key-authorized bounded rotation and production E2E | 02-06 | COVERED |
| CONTEXT | D-04, D-09, D-12 | Next-poll activation and validation before authority | 02-06 | COVERED |
</source_audit>

<verification>Run the named rotation list gate, complete activation suite, production policy_distribution target, clippy, and GitNexus change detection.</verification>
<success_criteria>Rotation is established-trust-only and exact at edges; distribution uses production components and preserves LKG/status on failure.</success_criteria>

## Artifacts this phase produces

- `SigningKeyTransitionV1` and bounded keyring
- Updated atomic configuration/key activation
- `crates/dlp-server/tests/policy_distribution.rs`

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-06-SUMMARY.md` with rotation boundaries, E2E evidence, TDD commits, and GitNexus results.</output>
