---
phase: 02-policy-enforcement-and-user-feedback
plan: 05
type: tdd
wave: 5
depends_on: [02-04]
files_modified:
  - crates/dlp-windows-service/src/pipe.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/session.rs
  - crates/dlp-windows-service/tests/session_lifecycle.rs
  - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs
autonomous: true
requirements: [SRV-07, POL-07, AGT-10]
estimate: {tokens: 35000, raw_tokens: 35000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The production service sends verified current/LKG to the built drive host before mount readiness and receives exact version/digest acknowledgement."
    - "A higher verified activation hot-swaps the evaluator used by the mounted DlpFileSystemContext without remount."
    - "Missing startup policy prevents mount exposure; stale/malformed/wrong-digest/unacknowledged update retains the prior evaluator."
    - "Process/ETW variation remains event enrichment only and never changes D-10 operation classification."
  artifacts:
    - {path: "crates/dlp-windows-service/src/pipe.rs", provides: "Versioned policy snapshot/update/ack frames"}
    - {path: "crates/dlp-windows-service/src/session.rs", provides: "Authenticated snapshot bootstrap and retained update channel"}
    - {path: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs", provides: "Strict snapshot consumer and atomic evaluator handle"}
  key_links:
    - {from: "crates/dlp-windows-service/src/service.rs", to: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs", via: "authenticated bootstrap/update frames and exact acknowledgements", pattern: "PolicySnapshot|policy_digest"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] The built drive host cannot expose a mount with missing or unverified policy state, and cannot silently discard control frames."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Deliver the verified current/LKG evaluator through the real service-to-drive-host process path and hot-update an existing mount safely.

Purpose: Cache-only activation is insufficient unless the actual mounted process installs and acknowledges that exact authority.
Output: Bounded snapshot protocol, service/session delivery, drive-host consumption, and built-process integration tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@crates/dlp-windows-service/src/service.rs
@crates/dlp-windows-service/src/session.rs
@crates/dlp-windows-service/src/pipe.rs
@crates/dlp-windows-drive/src/bin/dlp-drive-host.rs

<interfaces>
Extend the production path `run_scm_service -> SessionMonitor -> StorageBootstrap -> dlp-drive-host -> DlpFileSystemContext`. Plan 02-04 provides verified current/LKG materialization; this plan transports only that state.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bootstrap the built drive host with one verified policy before mount readiness</name>
  <read_first>
    - 02-CONTEXT.md — D-04, D-09, D-10, and D-12.
    - crates/dlp-windows-service/src/session.rs — authenticated StorageBootstrap lifecycle.
    - crates/dlp-windows-service/src/pipe.rs — bounded actor frames.
    - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs — current bootstrap and discarded control frames.
    - crates/dlp-windows-service/tests/session_lifecycle.rs — built-process harness.
  </read_first>
  <files>crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-service/src/session.rs, crates/dlp-windows-service/tests/session_lifecycle.rs, crates/dlp-windows-drive/src/bin/dlp-drive-host.rs</files>
  <behavior>
    - A top-level real_drive_host_policy_snapshot test builds/launches the host and requires exact current/LKG version/digest acknowledgement before ready.
    - Missing, malformed, wrong-digest, or over-budget bootstrap prevents mount exposure.
    - The host constructs DlpFileSystemContext with the materialized immutable evaluator.
  </behavior>
  <action>Write the exact top-level built-process test first. Define bounded versioned ActivePolicySnapshotV1 and acknowledgement frames carrying canonical policy input, detector ceilings, policy version, and digest. Extend StorageBootstrap and the authenticated actor handshake so ready is impossible before the host validates/materializes and acknowledges the exact identity. Replace discarded control input with strict tagged dispatch. Apply exact-symbol GitNexus impact, HIGH/CRITICAL warning, UNKNOWN text confirmation, and complete nonpartial detect-changes before each TDD commit.</action>
  <verify><automated>rtk cargo build --locked -p dlp-windows-drive --bin dlp-drive-host &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test session_lifecycle -- --list | rtk rg "^real_drive_host_policy_snapshot: test$" &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test session_lifecycle real_drive_host_policy_snapshot -- --exact</automated></verify>
  <acceptance_criteria>The named built-process test is discovered/executed and mount readiness is cryptographically tied to the exact verified current/LKG snapshot.</acceptance_criteria>
  <done>The real mounted drive host starts only with verified policy authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hot-update the mounted evaluator and retain prior authority on failure</name>
  <read_first>
    - crates/dlp-windows-service/src/service.rs — activation event source.
    - crates/dlp-windows-service/src/session.rs — retained authenticated actor channel.
    - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs — Task 1 dispatch/evaluator handle.
    - crates/dlp-windows-service/tests/session_lifecycle.rs — Task 1 built-process fixture.
  </read_first>
  <files>crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/session.rs, crates/dlp-windows-service/tests/session_lifecycle.rs, crates/dlp-windows-drive/src/bin/dlp-drive-host.rs</files>
  <behavior>
    - Higher-version verified update swaps the evaluator without remount and acknowledges exact version/digest.
    - Stale, malformed, wrong-digest, failed materialization, or acknowledgement timeout retains the prior evaluator.
    - Process/ETW changes do not alter identical file/operation decisions.
  </behavior>
  <action>Add PolicySnapshotUpdateV1 as a distinct tagged frame on the retained authenticated channel. The host validates version/digest/order, materializes, atomically swaps the evaluator shared by DlpFileSystemContext, then acknowledges; failure keeps prior authority. The service treats missing/mismatched acknowledgements as failed deployment status and never assumes activation. Preserve D-10 authorization independence from process/ETW enrichment. Reuse Task 1 GitNexus gates.</action>
  <verify><automated>rtk cargo test --locked -p dlp-windows-service --test session_lifecycle &amp;&amp; rtk cargo clippy --locked -p dlp-windows-service -p dlp-windows-drive --all-targets -- -D warnings</automated></verify>
  <acceptance_criteria>Hot update succeeds only with exact acknowledgement; every failure leaves the already-mounted evaluator unchanged and observable as failed status.</acceptance_criteria>
  <done>The production mount follows verified next-poll activation without remount or unsafe fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Service -> drive host | Verified policy authority crosses authenticated process IPC. |
| Drive host -> mounted callbacks | Atomic evaluator state authorizes plaintext and mutation boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05-01 | Tampering | Snapshot IPC | high | mitigate | Authenticated actor channel, bounded tagged frames, digest/order checks, exact ack. |
| T-02-05-02 | Elevation of Privilege | Mount readiness | high | mitigate | No ready/mount until verified current/LKG is installed. |
| T-02-05-03 | Tampering | Hot update | high | mitigate | Materialize then atomic swap; retain prior evaluator on all failures. |
| T-02-05-SC | Tampering | Package supply chain | low | accept | No package installation or dependency change. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Assigned policy reaches the real mounted boundary | 02-05 | COVERED |
| REQ | SRV-07, POL-07, AGT-10 | Distribution, invalid-policy safety, restart | 02-05 | COVERED |
| RESEARCH | — | Production process boundary and acknowledged hot update | 02-05 | COVERED |
| CONTEXT | D-04, D-09, D-10, D-12 | Next-poll activation and bounded authority | 02-05 | COVERED |
</source_audit>

<verification>Run the built-host named-test gate, hot-update tests, clippy, and complete GitNexus change detection.</verification>
<success_criteria>The production host starts with verified current/LKG, hot-updates exactly, and preserves prior authority on every failure.</success_criteria>

## Artifacts this phase produces

- Versioned snapshot/update/ack frames
- Updated service/session delivery
- Updated built drive-host consumer
- Expanded `session_lifecycle.rs`

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-05-SUMMARY.md` with built-process evidence, acknowledgement contracts, TDD commits, and GitNexus results.</output>
