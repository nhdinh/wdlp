---
phase: 01-first-encrypted-drive-vertical-slice
plan: 44
type: execute
wave: 32
depends_on:
  - 01-43
files_modified:
  - scripts/evidence/Phase1.Security.Tests.ps1
autonomous: true
gap_closure: true
requirements:
  - TST-08
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The corrupted-CMS FinalGate fixture passes its negative regression only when detached CMS verification reports signature_invalid."
    - "A copied-file digest mismatch or an unrelated PowerShell/fixture execution error cannot satisfy the corrupted-CMS branch."
    - "Focused FinalGate and complete All security runs exit zero on LAB-CLIENT01 with sanitized, binding-run provenance per D-21, D-24, D-37, D-41, and D-46."
  artifacts:
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "A specific corrupted-CMS oracle that distinguishes CMS rejection from unrelated copied-fixture failures."
  key_links:
    - from: scripts/evidence/Phase1.Security.Tests.ps1
      to: scripts/verify-phase1.ps1
      via: "The FinalGate focus mutates only the copied closure CMS signature, invokes the canonical verifier, and inspects its stable diagnostic."
      pattern: "signature_invalid"
    - from: scripts/evidence/Phase1.Security.Tests.ps1
      to: LAB-CLIENT01
      via: "Focused FinalGate and All runs execute on the binding endpoint and record sanitized provenance."
      pattern: "Focus.*(FinalGate|All)"
---

<objective>
Close P43-03, the sole remaining Phase 01 verification gap, by making the corrupted-CMS regression prove that CMS verification rejected the mutation rather than accepting an earlier copied-file or execution failure.

Purpose: TST-08 remains blocked because the Plan 01-43 negative oracle accepts a generic digest failure. This plan tightens that oracle, proves it through focused and full binding-endpoint runs, and leaves the authenticated Phase 1 evidence chain unchanged.
Output: One corrected PowerShell security assertion and sanitized LAB-CLIENT01 rerun provenance ready for Phase 01 re-verification.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-PATTERNS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-43-SUMMARY.md
@scripts/evidence/Phase1.Security.Tests.ps1
@scripts/verify-phase1.ps1
</context>

<gap_mapping>

| Gap ID | Source | Missing behavior | Closure tasks |
|---|---|---|---|
| P43-03 / WR-01 | `01-VERIFICATION.md` gap and `01-REVIEW.md` | The corrupted-CMS fixture must fail specifically with `signature_invalid`, never through a copied-file digest mismatch or unrelated execution failure | Tasks 1-2 |

</gap_mapping>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the corrupted-CMS oracle specific to detached-signature rejection</name>
  <read_first>
    - AGENTS.md — mandatory GitNexus impact and pre-commit change-detection rules.
    - C:/Users/nhdinh/.codex/RTK.md — PowerShell command guidance.
    - .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md — P43-03 gap statement and TST-08 blocker.
    - .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md — WR-01 false-positive analysis and expected oracle shape.
    - scripts/evidence/Phase1.Security.Tests.ps1 — current FinalGate branch and copied-closure mutation.
    - scripts/verify-phase1.ps1 — child verifier exit/report contract consumed by the test.
  </read_first>
  <files>scripts/evidence/Phase1.Security.Tests.ps1</files>
  <behavior>
    - P43-03A: Changing the latest copied closure `signature_cms_base64` byte makes FinalGate exit nonzero and emit `signature_invalid`.
    - P43-03B: The same branch fails its assertion if output contains `file digest mismatch`.
    - P43-03C: Missing files, trust/setup errors, command failures, PowerShell exceptions, or other unrelated execution diagnostics cannot satisfy the branch.
    - P43-03D: The unmodified authenticated repository FinalGate success assertions from 01-43 remain unchanged and passing.
  </behavior>
  <action>Before editing, locate every GitNexus-represented PowerShell symbol that contains or calls the FinalGate focus block and run upstream impact analysis for each exact symbol. Report direct callers, affected processes, and risk; warn and stop for user confirmation on HIGH or CRITICAL risk. Record an unindexed/UNKNOWN result only when GitNexus has no symbol for the script block. Run the current focused FinalGate test on LAB-CLIENT01 to preserve the starting behavior. In the copied-closure branch, retain the single-byte mutation and nonzero-exit assertion, normalize `$failedGate.Out + $failedGate.Err` into one diagnostic string, require that string to contain `signature_invalid`, and add explicit assertions that it contains neither `file digest mismatch` nor unrelated execution/setup failures. The allowed outcome must be the stable CMS diagnostic caused by the mutation; a nonzero child exit alone is insufficient. Keep the canonical successful-repository assertions and all signed closure, reviewer policy, trust root, independent-review, and generation artifacts byte-for-byte unchanged per D-41 and D-48.</action>
  <verify>
    <automated>On LAB-CLIENT01 with approved `PHASE1_TRUSTED_ROOT_PATH` and `PHASE1_REVIEWER_POLICY_PATH`: `rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus FinalGate` exits 0; inspection of the corrupted-CMS block shows an affirmative `signature_invalid` assertion and explicit rejection assertions for `file digest mismatch` and unrelated execution/setup errors.</automated>
  </verify>
  <acceptance_criteria>The corrupted-CMS branch can pass only after the copied signature mutation reaches CMS verification and produces `signature_invalid`; a copied-file digest failure or unrelated execution/setup failure makes the test fail, while the authenticated canonical FinalGate branch still passes unchanged.</acceptance_criteria>
  <done>P43-03's oracle is specific to the intended CMS failure and the focused regression exits zero on LAB-CLIENT01 without changing authenticated evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Capture binding focused/full proof and hand Phase 01 back to re-verification</name>
  <precondition>Execution is on LAB-CLIENT01 and the approved external paths in PHASE1_TRUSTED_ROOT_PATH and PHASE1_REVIEWER_POLICY_PATH exist and are readable without printing their contents.</precondition>
  <read_first>
    - .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md — D-21, D-24, D-25, D-37, D-41, D-46, and D-48 evidence constraints.
    - .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md — required focused FinalGate, All, and re-verification sequence.
    - .planning/phases/01-first-encrypted-drive-vertical-slice/01-43-SUMMARY.md — prior sanitized binding-run provenance format.
    - scripts/evidence/Phase1.Security.Tests.ps1 — corrected test suite and focus dispatch.
  </read_first>
  <files>scripts/evidence/Phase1.Security.Tests.ps1</files>
  <action>From clean PowerShell processes on LAB-CLIENT01, run `-Focus FinalGate` and then `-Focus All`; require both exits to be zero and confirm the corrupted-CMS branch actually executed rather than being skipped. Capture a sanitized binding-run record in `01-44-SUMMARY.md` containing UTC, commit/build identity, machine and endpoint role, exact commands, exits, environment fingerprint, and stable artifact references/hashes per D-21, D-24, D-25, D-37, D-41, and D-46. Do not include trust bytes, credentials, tokens, keys, protected plaintext, or unnecessary personal data. Before committing, run GitNexus change detection against `main` and require affected symbols/processes to match only the expected FinalGate security regression; also require the implementation diff to name only `scripts/evidence/Phase1.Security.Tests.ps1`, leaving all unrelated user-owned work untouched. Preserve prior failed/passing intervals and authenticated D-48 history. After the summary is complete, invoke the Phase 01 verification workflow so the verifier re-evaluates P43-03 and TST-08 from the corrected source and binding-run proof.</action>
  <verify>
    <automated>On LAB-CLIENT01: `rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus FinalGate; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus All` exits 0; `rtk git diff --check -- scripts/evidence/Phase1.Security.Tests.ps1` passes; GitNexus `detect_changes({scope: "compare", base_ref: "main"})` reports only the expected FinalGate test scope; and subsequent Phase 01 verification reports P43-03 verified and TST-08 satisfied.</automated>
  </verify>
  <acceptance_criteria>Focused FinalGate and full All runs both exit zero on LAB-CLIENT01, sanitized provenance binds the runs to the corrected commit, change detection finds no unrelated symbol/process impact, the source diff is limited to the security test, and Phase 01 re-verification has no remaining P43-03/TST-08 gap.</acceptance_criteria>
  <done>The binding endpoint proves both focused and full security regressions, the evidence is sanitized and append-only, and re-verification closes the sole Phase 01 gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Disposable copied repository -> canonical FinalGate | Attacker-controlled copied evidence must fail for the deliberately corrupted CMS signature, not an incidental fixture defect. |
| FinalGate child process -> PowerShell assertion | Exit status and diagnostic text cross a process boundary and must be attributed to the intended validation stage. |
| LAB-CLIENT01 -> published summary | Binding machine provenance crosses into repository evidence and must be complete, sanitized, and non-secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-44-01 | Tampering | Corrupted-CMS negative oracle | high | mitigate | Require `signature_invalid` and reject copied-file digest mismatch so only the deliberate CMS mutation satisfies P43-03. |
| T-01-44-02 | Repudiation | FinalGate child result | medium | mitigate | Couple nonzero exit to the stable CMS diagnostic and reject unrelated execution/setup errors. |
| T-01-44-03 | Spoofing | Binding endpoint evidence | medium | mitigate | Require LAB-CLIENT01 identity and role in sanitized provenance per D-21, D-24, and D-37. |
| T-01-44-04 | Information Disclosure | Trust inputs and run logs | high | mitigate | Record paths/hashes and allowlisted diagnostics only; never publish external trust bytes or secrets per D-46. |
| T-01-44-SC | Tampering | Package supply chain | low | accept | No package install or dependency-manifest change is permitted by this plan. |
</threat_model>

<source_audit>

| Source | ID | Gap-scoped item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | P43-03 | Phase 1 closes with trustworthy encrypted-drive security evidence | 01-44 | COVERED | Removes the sole false-positive path without changing runtime behavior. |
| REQ | TST-08 | Representative Windows security validation has a sound corrupted-CMS oracle | 01-44 | COVERED | Focused and All runs are binding to LAB-CLIENT01. |
| RESEARCH | Windows evidence constraint | Endpoint-bound checks run on the assigned Windows role | 01-44 | COVERED | Task 2 names LAB-CLIENT01 explicitly. |
| CONTEXT | D-21, D-24, D-25 | Machine-bound, layered verification | 01-44 | COVERED | Focused FinalGate precedes complete All. |
| CONTEXT | D-37, D-41, D-46, D-48 | Provenance, append-only failures, sanitization, independent integrity | 01-44 | COVERED | Summary is sanitized; signed evidence and prior intervals remain unchanged. |
| CONTEXT | Deferred Ideas | Items reserved beyond Phase 1 | — | EXCLUDED | No deferred idea is included. |
</source_audit>

<verification>
1. Validate this PLAN with the gap-closure frontmatter schema and plan-structure validator.
2. On LAB-CLIENT01, require focused FinalGate exit zero with a specific corrupted-CMS diagnostic oracle.
3. On LAB-CLIENT01, require the complete All security suite to exit zero.
4. Require sanitized binding-run provenance and GitNexus change detection limited to the intended FinalGate test scope.
5. Re-run Phase 01 verification and require P43-03 verified, TST-08 satisfied, and no remaining gaps.
</verification>

<success_criteria>
- The corrupted-CMS fixture requires `signature_invalid`.
- The corrupted-CMS branch rejects `file digest mismatch` and unrelated execution/setup failures.
- Focused FinalGate and complete All security runs exit zero on LAB-CLIENT01.
- Sanitized provenance binds both runs to the corrected commit without exposing trust material.
- Re-verification reports no remaining Phase 01 gap and TST-08 is satisfied.
</success_criteria>

## Artifacts this phase produces

No new production symbols are created.

- Modified: `scripts/evidence/Phase1.Security.Tests.ps1` — narrowed corrupted-CMS diagnostic oracle.
- Created by execution: `.planning/phases/01-first-encrypted-drive-vertical-slice/01-44-SUMMARY.md` — sanitized binding-run and re-verification handoff record.
- Planning artifact: `.planning/phases/01-first-encrypted-drive-vertical-slice/01-44-PLAN.md` — this executable gap-closure prompt.

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-44-SUMMARY.md` only after both LAB-CLIENT01 runs exit zero, change scope is clean, and re-verification closes P43-03/TST-08.
</output>
