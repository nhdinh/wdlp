---
phase: 02-policy-enforcement-and-user-feedback
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified:
  - Cargo.lock
  - crates/dlp-protocol/src/lib.rs
  - crates/dlp-agent-core/Cargo.toml
  - crates/dlp-agent-core/src/config_cache.rs
  - crates/dlp-agent-core/tests/policy_activation.rs
  - crates/dlp-server/src/repository.rs
  - crates/dlp-server/src/routes.rs
  - crates/dlp-server/tests/policy_lifecycle.rs
autonomous: true
requirements: [SRV-06, SRV-07, POL-07, AGT-10]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The server materializes an authenticated device's selected immutable policy into canonical signed bytes containing every SRV-06 field."
    - "The exact policy_bundle_contract test is extended, discovered, and executed; no Cargo filter may certify zero tests."
    - "The endpoint verifies schema, audience, digest, key ID, signature, monotonic version, ceilings, and action support before materialization and pointer swap."
    - "Invalid, older, partial, over-budget, unsupported, or wrong-audience input preserves current/LKG across restart per D-04, D-09, and AGT-10."
  artifacts:
    - {path: "crates/dlp-protocol/src/lib.rs", provides: "Canonical ConfigurationBundleV2 payload"}
    - {path: "crates/dlp-agent-core/src/config_cache.rs", provides: "Verified compile-before-activate current/LKG transaction"}
    - {path: "crates/dlp-agent-core/tests/policy_activation.rs", provides: "Activation, failure, concurrency, and restart tests"}
  key_links:
    - {from: "crates/dlp-server/src/routes.rs", to: "crates/dlp-protocol/src/lib.rs", via: "selected immutable version becomes canonical device-audience bytes", pattern: "ConfigurationBundleV2"}
    - {from: "crates/dlp-agent-core/src/config_cache.rs", to: "crates/dlp-policy/src/lib.rs", via: "verified payload materializes under endpoint ceilings before pointer swap", pattern: "compile|materialize"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] A failed bundle cannot replace current, LKG, or the materialized evaluator identity."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Turn selected immutable policy state into a canonical signed bundle and atomically materialize it as the endpoint's current/LKG evaluator.

Purpose: Assignment is not enforceable until the exact policy crosses the signed configuration boundary without weakening Phase 1 rollback guarantees.
Output: Bundle V2 contract, server materialization, endpoint activation, named bundle test, and restart/failure coverage.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/docs/adrs/ADR-005-policy-signing.md
@crates/dlp-protocol/src/lib.rs
@crates/dlp-agent-core/src/config_cache.rs
@crates/dlp-server/tests/policy_lifecycle.rs

<interfaces>
Preserve `ConfigurationEnvelopeV1` compatibility and `ConfigurationCache::stage_verify_activate`. Plan 02-02 already creates the top-level `policy_bundle_contract`; this plan owns extending that exact test for canonical signed bytes and explicitly re-running it.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Materialize one selected immutable policy into the canonical signed bundle</name>
  <reversibility rating="costly">The signed payload is a versioned persisted/wire contract used by server, agent, and later fleet phases.</reversibility>
  <read_first>
    - 02-CONTEXT.md — D-04 next-poll effect and D-09/D-12 validation.
    - ADR-005-policy-signing.md — canonical signing scope and key IDs.
    - crates/dlp-protocol/src/lib.rs — current canonical envelope.
    - crates/dlp-server/tests/policy_lifecycle.rs — Plan 02-02 policy_bundle_contract owner.
    - crates/dlp-server/src/repository.rs and routes.rs — selected policy/cursor source.
  </read_first>
  <files>crates/dlp-protocol/src/lib.rs, crates/dlp-server/src/repository.rs, crates/dlp-server/src/routes.rs, crates/dlp-server/tests/policy_lifecycle.rs</files>
  <behavior>
    - policy_bundle_contract remains a top-level discoverable test and now asserts exact canonical signed bytes and round-trip.
    - Bundle includes schema/policy versions and digest, settings, effective time, offline allowance, audience, bundle version, and signing key ID.
    - Publish alone yields no new selected bundle; explicit default/device assignment does.
    - Identical desired content does not mint another bundle version; concurrent polls serialize one authority.
  </behavior>
  <action>Extend the already-owned `policy_bundle_contract` test first; do not create a differently named substitute. Define bounded ConfigurationBundleV2 with fixed canonical field order, exact lengths, trailing-input rejection, device audience, policy identity/digest, detector definitions/ceilings, agent settings, effective time, seven-day offline allowance, monotonic bundle version, and signing key ID. In the row-locked device transaction, sign and persist immutable bytes only when desired canonical content changes. Apply exact-symbol GitNexus impact before edits, HIGH/CRITICAL warning, UNKNOWN text confirmation, and complete nonpartial detect-changes before each TDD commit.</action>
  <verify><automated>rtk cargo test --locked -p dlp-server --test policy_lifecycle -- --list | rtk rg "^policy_bundle_contract: test$" &amp;&amp; rtk cargo test --locked -p dlp-server --test policy_lifecycle policy_bundle_contract -- --exact</automated></verify>
  <acceptance_criteria>The exact named test is found and executed and proves one assignment produces one immutable canonical signed audience-bound bundle while publication alone does not.</acceptance_criteria>
  <done>The server can distribute an exact signed representation of the selected immutable policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize and atomically activate the bundle while preserving current/LKG</name>
  <read_first>
    - crates/dlp-agent-core/src/config_cache.rs — Phase 1 verify/stage/current/LKG flow.
    - crates/dlp-agent-core/Cargo.toml — crate-local exact-pin convention.
    - crates/dlp-policy/src/lib.rs — compiler and hard ceilings.
    - crates/dlp-protocol/src/lib.rs — Task 1 signed payload.
  </read_first>
  <files>Cargo.lock, crates/dlp-agent-core/Cargo.toml, crates/dlp-agent-core/src/config_cache.rs, crates/dlp-agent-core/tests/policy_activation.rs, crates/dlp-protocol/src/lib.rs</files>
  <behavior>
    - A top-level activation_tracer test verifies assigned bundle -> compile -> atomic current/LKG -> expected decision.
    - Publish without assignment leaves the selected evaluator unchanged.
    - Invalid/older/partial/over-budget/unsupported/wrong-audience bundles preserve current and LKG.
    - Restart reloads the selected compiled identity and removes incomplete staging before polling.
  </behavior>
  <action>Add `dlp-policy = { path = "../dlp-policy", version = "=0.1.0" }` to dlp-agent-core and refresh Cargo.lock. Write policy_activation RED tests, including exact top-level `activation_tracer`. Verify envelope, schema, audience, monotonic version, digest, key ID, and signature, then compile under immutable endpoint ceilings; only after successful materialization perform the existing atomic current/LKG transition. D-09 and resource failures retain prior pointers. Load the selected compiled identity during restart. Apply the same GitNexus gates as Task 1.</action>
  <verify><automated>rtk cargo test --locked -p dlp-agent-core --test policy_activation -- --list | rtk rg "^activation_tracer: test$" &amp;&amp; rtk cargo test --locked -p dlp-agent-core --test policy_activation activation_tracer -- --exact &amp;&amp; rtk cargo test --locked -p dlp-agent-core --test policy_activation</automated></verify>
  <acceptance_criteria>The named activation tracer runs, invalid cases preserve current/LKG, and restart recovers exactly the last verified materialized evaluator.</acceptance_criteria>
  <done>An assigned policy activates on the next successful poll without weakening rollback or restart safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PostgreSQL selection -> signer | Transactional desired state becomes immutable signed bytes. |
| Network bundle -> cache/compiler | Untrusted bytes cross signature, audience, replay, schema, and resource gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04-01 | Tampering | Bundle materialization | high | mitigate | Canonical bytes, digest/signature/audience/schema checks, and named contract test. |
| T-02-04-02 | Tampering | Atomic activation/restart | high | mitigate | Compile before current/LKG swap and preserve prior pointers on every failure. |
| T-02-04-03 | Denial of Service | Detector materialization | medium | mitigate | Bounded decode and Plan 02-01 hard ceilings before allocation/swap. |
| T-02-04-SC | Tampering | Package supply chain | low | accept | Only exact local dlp-policy is added. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Assigned policy safely reaches endpoint authority | 02-04 | COVERED |
| REQ | SRV-06, SRV-07, POL-07, AGT-10 | Bundle, distribution, unsupported action, recovery | 02-04 | COVERED |
| RESEARCH | — | Desired assignment -> canonical bundle -> compile-before-activate | 02-04 | COVERED |
| CONTEXT | D-04, D-09, D-12 | Next-poll effect and bounded validation | 02-04 | COVERED |
</source_audit>

<verification>Run the named-test list gates, focused tests, full activation suite, relevant clippy, and complete GitNexus change detection.</verification>
<success_criteria>The exact server contract and activation tracer cannot pass at zero tests; assigned policy activates atomically; invalid inputs preserve LKG.</success_criteria>

## Artifacts this phase produces

- `ConfigurationBundleV2` in dlp-protocol
- Updated server bundle materialization
- Updated agent cache/materialization and `policy_activation.rs`

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-04-SUMMARY.md` with named-test discovery, canonical bytes, TDD commits, and GitNexus evidence.</output>
