---
status: blocked
runtime_verification: not_run
timestamp_utc: 2026-08-14T07:40:46Z
plan: 01.3-03
---

# LAB debugger runtime verification blocked

The named, read-only `Test-DlpLogDebugLabPreflight` command was run before any
privilege manifest or debugger mutation. The lab is not currently available for
the manual lifecycle and layered network checks.

| Machine alias | Preflight result | Stable code |
| --- | --- | --- |
| LAB-CLIENT01 | failed | `PREFLIGHT_LAB_CLIENT01_ADMIN_UNREACHABLE` |
| LAB-DC01 | failed | `PREFLIGHT_LAB_DC01_PROBE_UNREACHABLE` |
| LAB-DC02 | failed | `PREFLIGHT_LAB_DC02_PROBE_UNREACHABLE` |

No runtime predicate is passed. No SCM, ACL, firewall, configuration, synthetic
fixture, or endpoint mutation was made.

## Passing source and runbook evidence

- `Test-DlpLogDebugRunbookSyntax.ps1` parsed every fenced PowerShell block.
- The configuration example contract check passed.
- `Test-DlpLogDebugLabPreflight` was invoked and failed closed as shown above.

## Remaining manual steps after lab recovery

1. Re-run the exact named preflight and require exit 0 with all three machine
   results passing.
2. Present the documented privilege manifest, record the normalized
   `DlpWindowsService` baseline, and follow the manual config, ACL, SCM, and
   program/port/source-scoped firewall lifecycle in the runbook.
3. Verify trusted raw tail, application rejection during the temporary two-probe
   window, firewall rejection after narrowing, bounded complete lines, and the
   malformed-config fail-closed behavior.
4. Remove only debugger-owned service, firewall, configuration, and fixture
   artifacts; then compare the normalized DLP baseline byte-for-byte.
