---
phase: 01-first-encrypted-drive-vertical-slice
plan: 02
type: execute
wave: 2
depends_on: [01-01, 01-03]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/dlp-agent-core/Cargo.toml
  - crates/dlp-agent-core/src/lib.rs
  - crates/dlp-server/Cargo.toml
  - crates/dlp-server/src/lib.rs
  - crates/dlp-server/src/main.rs
  - crates/dlp-windows-service/Cargo.toml
  - crates/dlp-windows-service/src/main.rs
  - crates/dlp-windows-drive/Cargo.toml
  - crates/dlp-windows-drive/src/lib.rs
  - crates/dlpctl/Cargo.toml
  - crates/dlpctl/src/main.rs
  - migrations/202608070001_walking_skeleton.sql
  - deploy/compose.yaml
autonomous: true
requirements: [WRK-01, WRK-04, SRV-01, SRV-11, AGT-01]
user_setup:
  - service: phase1-local-postgres
    why: "The later tracer and server integration use a real PostgreSQL database supplied by Compose."
    env_vars:
      - name: DATABASE_URL
        source: "Use the value documented beside the PostgreSQL service in deploy/compose.yaml."
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The workspace contains exactly all ten WRK-01 crates and `cargo metadata --no-deps` succeeds."
    - "Portable agent core depends only on domain/protocol/crypto/storage interfaces; Windows service and drive crates are the only future FFI boundaries."
    - "The server binary starts through a library-owned application seam, applies the forward-only migration before serving, and exposes process-only liveness without persisting encrypted user data."
    - "Docker Compose defines PostgreSQL and the server with health ordering, mounted secrets/config, durable database storage, and no automatic production seed data."
    - "No SUS/ASSUMED package governed by 01-03 is present in manifests or Cargo.lock."
  artifacts:
    - path: "Cargo.toml"
      provides: "Exact ten-member WRK-01 workspace"
      contains: "crates/dlp-windows-drive"
    - path: "crates/dlp-server/src/lib.rs"
      provides: "Server application/repository/migration composition seams"
      exports: ["ServerConfig", "ServerState", "build_app", "run_migrations"]
    - path: "crates/dlp-agent-core/src/lib.rs"
      provides: "Portable enrollment/configuration/cache ports"
      exports: ["EnrollmentPort", "ConfigurationActivator", "ActiveConfigurationSet", "HealthReporter"]
    - path: "crates/dlp-windows-drive/src/lib.rs"
      provides: "Replaceable Windows filesystem host interfaces"
      exports: ["MountHost", "MountedVolume"]
    - path: "migrations/202608070001_walking_skeleton.sql"
      provides: "Forward-only tracer repository and migration-ledger schema"
    - path: "deploy/compose.yaml"
      provides: "PostgreSQL/server development deployment contract"
  key_links:
    - from: "crates/dlp-server/src/main.rs"
      to: "crates/dlp-server/src/lib.rs"
      via: "binary loads configuration, runs migrations, and starts the library-owned app"
      pattern: "run_migrations|build_app"
    - from: "crates/dlp-agent-core/src/lib.rs"
      to: "crates/dlp-protocol/src/lib.rs"
      via: "portable state machines consume only versioned DTOs"
      pattern: "SignedConfigurationV1|HealthReportV1"
  flagged_assumptions: []
  prohibitions: []
---

<objective>
Complete the exact ten-crate workspace and establish executable server, agent, Windows, CLI, migration, and Compose boundaries without installing gated packages or writing encrypted stores.

Purpose: Supply coherent compile-ready prerequisites so the format-gated storage plan and later end-to-end tracer can focus on their real behavior rather than project scaffolding.
Output: All remaining crate boundaries, the first forward-only SQL migration, Compose deployment, and a clean package-boundary audit.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-PATTERNS.md
@.planning/docs/adrs/ADR-002-api-transport.md
@.planning/docs/adrs/ADR-003-database-migrations.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-01-SUMMARY.md

<interfaces>
Use `DeviceId`, `UserSid`, `ConfigurationEnvelopeV1`, `SignedConfigurationV1`, `ConfigurationVerifier`, `EncryptedStore`, `ProtectedFileSystem`, and their typed errors from 01-01. Server and Windows crates adapt these interfaces; they do not redefine them.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile server and agent entry seams through shared contracts</name>
  <files>crates/dlp-agent-core/Cargo.toml, crates/dlp-agent-core/src/lib.rs, crates/dlp-server/Cargo.toml, crates/dlp-server/src/lib.rs, crates/dlp-server/src/main.rs</files>
  <behavior>
    - Test 1: server state accepts repository, directory, certificate, signer, and clock ports and rejects missing production providers.
    - Test 2: agent core exposes enrollment, signed activation, current/LKG, and health ports without any Windows type.
    - Test 3: server liveness is process-only and migration failure prevents listener start.
  </behavior>
  <action>Create `dlp-agent-core` as a portable state-machine crate and `dlp-server` as an Axum/SQLx library plus thin binary using only packages marked OK in the research audit. Define final dependency-injection seams named `DirectoryVerifier`, `DeviceCertificateIssuer`, `ConfigurationSigner`, repositories, and clock. Allow test providers only behind test/debug compilation; production startup fails closed when a provider is absent. Add process-only `/health/live`; configuration/readiness/enrollment behavior is expanded in later plans.</action>
  <verify><automated>cargo test -p dlp-agent-core -p dlp-server; cargo clippy -p dlp-agent-core -p dlp-server --all-targets -- -D warnings</automated></verify>
  <done>Portable agent and server executable seams compile against shared contracts and fail closed on missing production dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate Windows service and filesystem boundaries</name>
  <files>crates/dlp-windows-service/Cargo.toml, crates/dlp-windows-service/src/main.rs, crates/dlp-windows-drive/Cargo.toml, crates/dlp-windows-drive/src/lib.rs</files>
  <action>Create compiling Windows service and drive crates with no `winfsp`, `windows`, `ldap3`, or other 01-03-gated dependency yet. `dlp-windows-service` exposes a thin SCM-entry composition boundary; `dlp-windows-drive` exports `MountHost`/`MountedVolume` adapters over `ProtectedFileSystem`. Document that all later unsafe blocks must remain in these two crates with local safety invariants. The crates compile on Windows while performing no mount and creating no persisted store.</action>
  <verify><automated>cargo check -p dlp-windows-service -p dlp-windows-drive; cargo tree -p dlp-windows-service -p dlp-windows-drive</automated></verify>
  <done>The two Windows crates are the only declared FFI boundary and contain no unapproved package or simulated WinFsp claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close workspace membership and provision migration/Compose/CLI prerequisites</name>
  <files>Cargo.toml, Cargo.lock, crates/dlpctl/Cargo.toml, crates/dlpctl/src/main.rs, migrations/202608070001_walking_skeleton.sql, deploy/compose.yaml</files>
  <behavior>
    - Test 1: workspace metadata reports exactly the ten WRK-01 members and no additional package.
    - Test 2: the migration applies to an empty PostgreSQL database and repeated migration application is idempotent.
    - Test 3: Compose waits for PostgreSQL health and server migration success; failed migration leaves the server unready/not listening.
  </behavior>
  <action>Extend root membership to the exact ten-crate closed list, refresh the lockfile using only OK-audited packages, and add a std-argument-based `dlpctl` entry seam. Create the first forward-only SQLx migration for tracer allowlist/token/configuration/health repository rows and migration constraints; it must not contain encrypted-drive records or automatic seed data. Create Compose PostgreSQL/server services with durable database volume, health ordering, environment examples/comments, mounted secret paths, and a local command that proves migration state.</action>
  <verify><automated>cargo metadata --no-deps; cargo check --workspace --locked; docker compose -f deploy/compose.yaml config; sqlx migrate run --source migrations --database-url "$env:DATABASE_URL"; sqlx migrate info --source migrations --database-url "$env:DATABASE_URL"</automated></verify>
  <done>The exact ten-crate workspace compiles, the real PostgreSQL migration is repeatable, and Compose supplies the later tracer without creating encrypted store data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Process configuration → server/service composition | Untrusted paths, endpoints, and missing providers can weaken startup security. |
| Server → PostgreSQL | Migration ordering and SQL state determine authoritative persistence. |
| Portable contracts → Windows crates | FFI/OS-specific behavior must not contaminate safe portable logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Tampering | migration/startup | high | mitigate | Forward-only SQLx migration ledger runs before bind; checksum/version mismatch fails startup. |
| T-01-05 | Elevation of privilege | provider composition | high | mitigate | Production server rejects fixture/missing providers and Windows FFI stays isolated. |
| T-01-06 | Information disclosure | Compose/config | high | mitigate | Secret paths are mounted references; examples contain no credential values or protected data. |
| T-01-SC | Tampering | Cargo packages | high | mitigate | Manifests/lock include only audit-OK packages; all SUS/ASSUMED names are blocked pending 01-03 human approval. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Implementation evidence | Executable evidence |
|---|---|---|---|
| V1 Architecture | Security-sensitive provider and FFI boundaries are explicit | server provider traits and two Windows crates | `cargo tree` boundary inspection |
| V4 Access Control | Production routes cannot run with fixture authority | startup provider validation | server negative startup tests |
| V8 Data Protection | Configuration and Compose contain secret references only | mounted-secret paths and redacted examples | secret-marker scan over config/deploy |
| V14 Configuration | Database schema changes are versioned and startup-gated | SQLx migration + Compose health dependency | `sqlx migrate info`; `docker compose config` |

**Completion gate:** Any unresolved critical/high threat, unapproved dependency, failed ASVS evidence command, or missing mitigation evidence blocks this plan's completion and summary.
</threat_model>

<verification>
Build/check the exact workspace, inspect dependency trees, apply the real migration twice, validate Compose, and scan all manifests/lock/config for gated package names and secret values.
</verification>

<success_criteria>
- All ten WRK-01 crates exist and compile.
- Server/agent/Windows/CLI boundaries consume the 01-01 contracts directly.
- Migration and Compose prerequisites are real, repeatable, and contain no encrypted user records.
- No gated package has been installed before 01-03 approval.
- All ASVS L1 evidence passes with no open critical/high threat.
</success_criteria>

## Artifact Inventory

| Disposition | File | Key symbols / purpose |
|---|---|---|
| Modified | `Cargo.toml` | closes exact ten-member workspace |
| Modified | `Cargo.lock` | pins only audit-OK dependency graph |
| New | `crates/dlp-agent-core/Cargo.toml` | agent-core manifest |
| New | `crates/dlp-agent-core/src/lib.rs` | enrollment/config/cache/health ports |
| New | `crates/dlp-server/Cargo.toml` | server manifest |
| New | `crates/dlp-server/src/lib.rs` | `ServerConfig`, `ServerState`, provider seams, liveness |
| New | `crates/dlp-server/src/main.rs` | thin migration-before-listen binary |
| New | `crates/dlp-windows-service/Cargo.toml` | Windows service boundary manifest |
| New | `crates/dlp-windows-service/src/main.rs` | SCM composition entry seam |
| New | `crates/dlp-windows-drive/Cargo.toml` | Windows drive boundary manifest without WinFsp dependency |
| New | `crates/dlp-windows-drive/src/lib.rs` | `MountHost`, `MountedVolume` |
| New | `crates/dlpctl/Cargo.toml` | CLI manifest |
| New | `crates/dlpctl/src/main.rs` | CLI entry/argument seam |
| New | `migrations/202608070001_walking_skeleton.sql` | forward-only tracer repository schema |
| New | `deploy/compose.yaml` | PostgreSQL/server deployment and health ordering |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-02-SUMMARY.md` when done.
</output>
