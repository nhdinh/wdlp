---
phase: 02-policy-enforcement-and-user-feedback
plan: 08
type: tdd
wave: 7
depends_on: [02-07]
files_modified:
  - crates/dlp-windows-service/src/grants.rs
  - crates/dlp-windows-service/src/pipe.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/lib.rs
  - crates/dlp-windows-service/tests/companion_grants.rs
autonomous: true
requirements: [POL-06, DRV-05, DRV-08, UI-02]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Warn denies the current attempt; authenticated Proceed once creates a 120-second memory-only grant bound to the exact D-16 tuple."
    - "Grant consumption is atomic compare-and-remove; replay, expiry, mismatch, concurrent double-use, restart, session end, policy/content change, and eviction deny."
    - "Companion sends only opaque challenge ID plus action; kernel PID/token SID/session is authoritative per D-19."
    - "Two users cannot create, receive, or consume each other's intent/grant state (DRV-05/UI-02)."
  artifacts:
    - {path: "crates/dlp-windows-service/src/grants.rs", provides: "Bounded authoritative intent/grant registry"}
    - {path: "crates/dlp-windows-service/src/pipe.rs", provides: "Authenticated companion request frames"}
    - {path: "crates/dlp-windows-service/tests/companion_grants.rs", provides: "Grant identity/TTL/replay/concurrency/invalidation tests"}
  key_links:
    - {from: "crates/dlp-windows-service/src/pipe.rs", to: "crates/dlp-windows-service/src/grants.rs", via: "kernel-authenticated SID/session resolves opaque service-owned intent", pattern: "challenge_id|consume"}
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Companion-supplied SID, path, operation, rule, policy, or content data cannot become authorization authority."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization's DLP boundary.

<objective>
Make Proceed once an exact, brief, service-authoritative capability behind authenticated per-user IPC.

Purpose: D-15/D-16 make warn feedback authorization-sensitive, so identity, binding, expiry, and consumption must not live in the UI.
Output: Bounded grant registry, companion protocol, lifecycle invalidation, and deterministic security tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@crates/dlp-windows-service/src/pipe.rs
@crates/dlp-windows-service/src/service.rs

<interfaces>
Reuse bounded frames, named-pipe DACL, client PID, impersonated token SID/session, and injected clock patterns. Plan 02-07 supplies the warn decision/attempt tuple; the service owns every authoritative field.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deny one warn and consume one exact authenticated Proceed-once grant</name>
  <reversibility rating="costly">D-16 exact binding becomes a cross-process authorization contract.</reversibility>
  <read_first>
    - 02-CONTEXT.md — D-15, D-16, and D-19.
    - 02-RESEARCH.md — opaque intent pattern and 120-second TTL recommendation.
    - crates/dlp-windows-service/src/pipe.rs — kernel identity/framing.
    - crates/dlp-windows-service/src/service.rs — authoritative lifecycle.
  </read_first>
  <files>crates/dlp-windows-service/src/grants.rs, crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - Exact top-level warn_grant_tracer denies initial warn and permits exactly one same-SID/session retry.
    - Grant binds SID, session, store generation, normalized file identity/path, operation, rule/action, policy version, and content/policy fingerprint.
    - Replay and each one-field mismatch deny; simultaneous consumers yield one success.
    - Spoofed identity, malformed/oversized frame, absent service, full registry, and expiry deny.
  </behavior>
  <action>Write exact named RED tests. Create one service-owned bounded registry using injected monotonic clock, 120-second TTL, 300-second hard maximum, memory-only state, opaque identifiers/fingerprints, and deterministic oldest-expiry/creation eviction that never authorizes absence. Atomically compare and remove the complete D-16 binding. Define a versioned companion request containing only challenge ID and ProceedOnce; authenticate PID and impersonated token SID/session before lookup and always revert impersonation. Run exact-symbol GitNexus impact, warn on HIGH/CRITICAL, text-confirm UNKNOWN, and complete nonpartial detect-changes before commits.</action>
  <verify><automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants -- --list | rtk rg "^warn_grant_tracer: test$" &amp;&amp; rtk cargo test --locked -p dlp-windows-service --test companion_grants warn_grant_tracer -- --exact</automated></verify>
  <acceptance_criteria>The discoverable tracer grants exactly one matching retry and denies every identity/binding/replay/expiry/concurrency/frame failure.</acceptance_criteria>
  <done>Proceed once exists only as a brief exact service capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound multi-session state and invalidate grants on every authority change</name>
  <read_first>
    - crates/dlp-windows-service/src/grants.rs — Task 1 registry.
    - crates/dlp-windows-service/src/service.rs — restart/config/session/content lifecycle.
    - crates/dlp-windows-service/tests/companion_grants.rs — Task 1 fixtures.
  </read_first>
  <files>crates/dlp-windows-service/src/grants.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/lib.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - Service restart, session end, policy activation, file/content generation change, and expiry invalidate relevant state.
    - Equal-looking notifications for different users/files/operations/rules/policies never share authorization.
    - Bounded eviction and concurrent insertion/consumption preserve isolation and fail closed.
  </behavior>
  <action>Expand RED lifecycle/concurrency matrices, then wire deterministic invalidation hooks from service restart, session teardown, policy activation, content generation, and expiry. Partition state by authenticated SID/session and keep dedupe keys separate from authorization keys. Export narrow tested types only. Apply Task 1 GitNexus gates.</action>
  <verify><automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants &amp;&amp; rtk cargo clippy --locked -p dlp-windows-service --all-targets -- -D warnings</automated></verify>
  <acceptance_criteria>All invalidation, bound, concurrency, and cross-user cases pass and absence never becomes authorization.</acceptance_criteria>
  <done>Grant authority remains isolated and fail-closed across all Phase 2 lifecycle transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Companion -> service pipe | Untrusted interactive action crosses kernel-authenticated SID/session boundary. |
| Service intent -> retry grant | UI intent becomes one exact authorization capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-08-01 | Spoofing | Companion identity | high | mitigate | DACL, PID, impersonated SID/session, no claimed identity. |
| T-02-08-02 | Elevation of Privilege | Grant | high | mitigate | Exact tuple, TTL, atomic one-use removal, lifecycle invalidation. |
| T-02-08-03 | Denial of Service | Registry/frames | medium | mitigate | Hard bounds, deterministic eviction, bounded frames, fail-closed absence. |
| T-02-08-SC | Tampering | Package supply chain | low | accept | No package install or dependency change. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Warn feedback cannot weaken DLP enforcement | 02-08 | COVERED |
| REQ | POL-06, DRV-05, DRV-08, UI-02 | Warn, isolation, clear denial, authenticated IPC | 02-08 | COVERED |
| RESEARCH | — | Opaque intent, exact atomic grant, bounded state | 02-08 | COVERED |
| CONTEXT | D-15, D-16, D-19 | Deny-first, exact one-use grant, service authority | 02-08 | COVERED |
</source_audit>

<verification>Run the named grant tracer, full grant matrices, clippy, and complete GitNexus change detection.</verification>
<success_criteria>Warn denies first; one exact authenticated retry succeeds; every mismatch/replay/lifecycle/concurrency failure denies.</success_criteria>

## Artifacts this phase produces

- `grants.rs` authoritative registry
- Versioned authenticated companion action frames
- `companion_grants.rs` grant security suite

<output>Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-08-SUMMARY.md` with TDD commits, binding matrix, and GitNexus evidence.</output>
