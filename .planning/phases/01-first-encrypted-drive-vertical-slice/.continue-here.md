---
context: phase
phase: 01-first-encrypted-drive-vertical-slice
plan: 24
task: 3
total_tasks: 3
status: halted_at_checkpoint
last_updated: 2026-08-19T03:30:00Z
---

# Continue Here — Plan 01-24 Task 3 (Secure Session-Host Lifecycle)

## BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] CONSTRAINT: Source implementation of `ActorPipe` and its integration into `SessionMonitor::session_logon` is complete and committed.
- [x] CONSTRAINT: `dlp-drive-host.rs` uses synchronous pipe I/O and safe `std::path::Path::try_exists` drive-letter selection.
- [x] CONSTRAINT: Release binaries for `dlp-windows-service` and `dlp-windows-drive` have been built successfully from the current source.
- [x] CONSTRAINT: All source tests and clippy checks pass on `hungdinh-lt`.
- [ ] CONSTRAINT: The exact `01-24` privilege manifest digest must be approved by an authenticated domain operator before any LAB-CLIENT01 mutation.
- [ ] CONSTRAINT: LAB-CLIENT01 must have the approved WinFsp 2.1 runtime, current binaries, `DlpWindowsService` installed as LocalSystem, and an eligible interactive user session (`WTSActive`).

**Do not proceed until the remaining boxes are checked.**

## Current State

Plan 01-24 Tasks 1–2 are complete. Task 3 is blocked at the privilege-manifest human checkpoint. No deployment, service restart, or smoke-test mutation has been performed on LAB-CLIENT01.

## Completed This Session

1. Wired `ActorPipe` into `SessionMonitor::session_logon` (`crates/dlp-windows-service/src/session.rs`):
   - Creates the named pipe before `CreateProcessAsUserW` using `PipeFactory`/`PipeBootstrap` seams.
   - Spawns a dedicated authentication thread calling `accept_and_authenticate`.
   - Waits up to 30 seconds for the child to connect and authenticate.
   - Stores the authenticated runtime (`ActorRuntime`) on success.
   - Terminates the child process on authentication failure.
   - Added `FakePipeFactory`/`FakePipeBootstrap` to keep source tests deterministic.
2. Updated `dlp-drive-host.rs` (`crates/dlp-windows-drive/src/bin/dlp-drive-host.rs`):
   - Removed `FILE_FLAG_OVERLAPPED` from `CreateFileW` for synchronous pipe I/O.
   - Implemented `select_drive_letter` with safe `std::path::Path::try_exists` probes.
   - Sends the selected drive letter back to the service via `DriveLetterReport`.
   - Keeps the authenticated pipe open in the control loop.
3. Verified build and tests:
   - `cargo build --locked --release -p dlp-windows-service -p dlp-windows-drive` succeeded.
   - `cargo test --locked -p dlp-windows-service --test session_lifecycle` passed (22 tests).
   - `cargo test --locked -p dlp-windows-service pipe::tests` passed (9 tests).
   - `cargo test --locked -p dlp-windows-drive --lib` passed.
   - `cargo test --locked -p dlp-windows-drive --bin dlp-drive-host drive_letter_selection` passed (3 tests).
   - `cargo clippy --locked -p dlp-windows-service -p dlp-windows-drive --all-targets -- -D warnings` passed.

## Work Remaining

1. **Human checkpoint (blocking):**
   - Review and approve the exact `01-24` privilege manifest digest in `config/lab.phase1.example.yaml` using an authenticated domain-operator identity.
   - Confirm LAB-CLIENT01 runtime availability (WinFsp 2.1, current binaries, LocalSystem service, interactive session).
2. **Deploy and verify (after checkpoint approval):**
   - Copy `target/release/dlp-windows-service.exe` and `target/release/dlp-drive-host.exe` to `LAB-CLIENT01` `C:\Program Files\DLP\`.
   - Verify SHA-256 hashes match.
   - Restart `DlpWindowsService`.
3. **Smoke test (after deployment):**
   - Run `tests/windows/Invoke-ServiceSessionSmoke.ps1 -Scenario SecureSessionHostLifecycle` locally on LAB-CLIENT01.
   - Publish sanitized evidence and update `evidence/phase1/requirement-matrix.yaml`.

## Blockers

- **Human:** Authenticated domain-operator approval of the exact `01-24` privilege manifest digest is required before any LAB-CLIENT01 mutation.
- **Infrastructure:** LAB-CLIENT01 must be reachable and have the approved WinFsp runtime, current binaries, LocalSystem service, and an eligible interactive session.

## Required Reading (in order)

1. `crates/dlp-windows-service/src/pipe.rs` — `PipeFactory`, `PipeBootstrap`, `WindowsPipeFactory`, `ActorPipe`, and `accept_and_authenticate`.
2. `crates/dlp-windows-service/src/session.rs` — `SessionMonitor::session_logon`, `ActorRuntime`, `HostProcessHandle`.
3. `crates/dlp-windows-drive/src/bin/dlp-drive-host.rs` — synchronous pipe client, drive-letter selection, and ack.
4. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-24-SUMMARY.md` — plan status and evidence tracking.

## Infrastructure State

- **hungdinh-lt:** Source workspace is clean; release binaries are built at `target/release/dlp-windows-service.exe` and `target/release/dlp-drive-host.exe`.
- **LAB-CLIENT01:** Awaiting approved manifest and runtime confirmation; no mutation has occurred in this session.
- **Staging directory:** `C:\dlp\tmp\smoke-run` contains smoke test and evidence helpers.

## Decisions Made

- The service creates the per-actor named pipe **before** launching the host and authenticates the child on a dedicated thread.
- Synchronous pipe I/O is sufficient for Phase 1; overlapped complexity is deferred.
- `std::path::Path::try_exists` is the safe drive-letter probe because the workspace forbids unsafe code.
- The host sends the real selected drive letter back to the service so the monitor's health snapshot is accurate.

## Next Action

Resolve the Task 3 human checkpoint. Once approved, deploy the release binaries to LAB-CLIENT01, restart `DlpWindowsService`, and run `Invoke-ServiceSessionSmoke.ps1 -Scenario SecureSessionHostLifecycle`.
