---
phase: 01-first-encrypted-drive-vertical-slice
plan: 29
type: execute
wave: 17
depends_on:
  - 01-28
files_modified:
  - evidence/phase1/security-closure.yaml
autonomous: false
gap_closure: true
requirements:
  - CRY-04
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "An authenticated independent verifier reviews the complete current payload of each of the seven reopened high threats."
    - "Each resulting attestation binds the exact current payload, actual reviewer identity, fresh UTC, environment/procedure provenance, and immutable predecessor link."
  artifacts:
    - path: evidence/phase1/security-closure.yaml
      provides: "Fresh append-only current-payload attestations for the seven reopened records"
  key_links:
    - from: scripts/add-security-closure-review.ps1
      to: evidence/phase1/security-closure.yaml
      via: "Authenticated reviewer confirmation and atomic append after payload-drift recheck"
---

<objective>
Obtain the fresh authenticated independent review that automation cannot truthfully manufacture for the seven Aug 24 rebound closure records.

Purpose: Satisfy D-37, D-40, D-41, D-44, and D-48 with real reviewer provenance before CRY-04 security completion is restored.
Output: Seven fresh, payload-bound, append-only independent-review attestations in the canonical closure manifest.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-28-SUMMARY.md
@evidence/phase1/security-closure.yaml
@scripts/add-security-closure-review.ps1
@scripts/verify-phase1-security.ps1
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Perform authenticated independent review of the seven current closure payloads</name>
  <action>
    On the approved authenticated lab/domain review station, use `scripts/add-security-closure-review.ps1` to review exactly T-01-15-03, T-01-16-02, T-01-16-03, T-01-18-SC, T-01-20-01, T-01-20-02, and T-01-20-05. The reviewer must be independent of the Plan 01-27 digest update and must inspect each displayed mitigation assertion, complete current implementation/artifact hash set, evidence attempt IDs, required machine roles, procedure version, and environment fingerprint before confirming. The command derives the reviewer identity and fresh UTC from the authenticated session, binds the current payload digest, checks for drift immediately before atomic append, and preserves predecessor attestations. If any mitigation or evidence is not acceptable, decline that record; leave it unsigned/open and report the specific issue rather than approving by default. Do not edit YAML review fields manually and do not reuse `lab/administrator` as a text label without an authenticated session.
  </action>
  <instructions>
    Plan 01-28 has already installed the fail-closed v2 verifier, reopened the seven records, and provided the authenticated append-only capture command. Sign in on the approved lab/domain review station using the independent verifier account, run the review command for the exact seven threat IDs, inspect every displayed payload, and confirm only records whose current mitigation and evidence you independently accept. Return to the executor after the command atomically appends the accepted attestations.
  </instructions>
  <verification>Run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md -RequireSignedOff`; it must accept all seven current payload bindings and all 19 closure targets.</verification>
  <resume-signal>Type "review complete" after the authenticated capture command succeeds, or identify every declined/blocked threat ID.</resume-signal>
  <done>All seven current payloads have fresh authenticated independent attestations, or the checkpoint remains blocked with the declined records explicitly open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human reviewer -> signed closure evidence | A real authenticated judgment crosses into security-completion evidence and cannot be synthesized by the executor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-29-01 | Spoofing | reviewer identity | high | mitigate | Capture identity from the authenticated domain session and reject caller-supplied names. |
| T-01-29-02 | Repudiation | seven review decisions | high | mitigate | Append fresh UTC, current payload digest, procedure/environment provenance, and predecessor link per record. |
| T-01-29-03 | Tampering | review-time artifact drift | high | mitigate | Recompute payload immediately before atomic publication and abort on any change. |
| T-01-29-04 | Information Disclosure | review material | high | mitigate | Review allowlisted assertions, hashes, and sanitized evidence only; existing redaction gates remain blocking. |
</threat_model>

<verification>
- The authenticated review command completes for exactly seven threat IDs.
- The v2 verifier accepts every new payload/attestation binding.
- `-RequireSignedOff` succeeds only if all seven were actually approved; otherwise the checkpoint remains blocked.
</verification>

<success_criteria>
- The reviewer identity is authenticated and independent of the digest-changing implementation work.
- Every accepted attestation has a fresh UTC and binds the exact complete current record payload.
- Prior attestations remain present and linked.
- Declined or drifted records remain unsigned/open.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-29-SUMMARY.md` when done.
</output>
