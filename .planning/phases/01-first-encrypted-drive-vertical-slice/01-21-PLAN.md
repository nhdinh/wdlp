---
phase: 01-first-encrypted-drive-vertical-slice
plan: 21
type: execute
wave: 9
depends_on: [01-16]
files_modified:
  - tests/windows/Invoke-Phase1Matrix.ps1
  - tests/windows/Invoke-AbruptLossHarness.ps1
  - tests/windows/results/phase1-evidence.json
  - tests/windows/results/phase1-evidence.sha256
  - scripts/verify-phase1.ps1
autonomous: true
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, SRV-01, SRV-03, SRV-11, SRV-12, CRY-01, CRY-02, CRY-04, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, DRV-01, DRV-02, DRV-03, DRV-04, DRV-06, DRV-07, DRV-09, TST-01, TST-02, TST-03, TST-05, TST-08]
user_setup:
  - service: phase1-abrupt-loss-and-independent-review
    why: "D-19 requires hungdinh-lt to hard-power LAB-CLIENT01 off, and D-48 requires an authenticated independent verifier to review the final sanitized matrix."
    dashboard_config:
      - task: "Ensure hungdinh-lt may turn LAB-CLIENT01 off through Hyper-V without an in-guest shutdown and an independent verifier is available for the final matrix review."
        location: "Existing Phase 1 Hyper-V lab and authenticated review workflow"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-19, clean service restart, Windows reboot, forced service termination during active write, and hungdinh-lt host-controlled abrupt LAB-CLIENT01 power loss are distinct cases that recover exact old-complete or new-complete data only."
    - "Abrupt loss is proved by `Stop-VM -TurnOff` after a LAB-CLIENT01 guest barrier confirms execution between durability steps; an in-guest graceful shutdown cannot satisfy the row."
    - "Per D-25 through D-32, every final row has its binding verification tier, machine, and real provider or permitted fixture scope; no lower tier or substitute satisfies a higher boundary."
    - "Per D-33 through D-36, all final elevated actions use the separately approved 01-21 manifest digest with baseline, strict role allowlist, pinned tools, idempotent apply/verify/remove, and cleanup."
    - "Per D-37 through D-47, all attempts, signed visual checklists, artifact references, staleness results, clock offsets, environment fingerprints, rerun links, and redaction outcomes validate through the Plan 01-17 schema."
    - "Per D-48 through D-50, an independent verifier reviews the complete requirement matrix, deviations, provenance, artifact integrity, and retention/hold state before Phase 1 can pass."
  artifacts:
    - path: "tests/windows/Invoke-AbruptLossHarness.ps1"
      provides: "hungdinh-lt host-controlled LAB-CLIENT01 hard-off and recovery orchestration"
    - path: "tests/windows/results/phase1-evidence.json"
      provides: "Sanitized final attempts, visual checklists, abrupt-loss results, and independent-review record"
    - path: "scripts/verify-phase1.ps1"
      provides: "Single Phase 1 requirements, decisions, edges, prohibitions, ASVS, provenance, and retention gate"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Authoritative current Phase 1 requirement, success-criterion, decision, tier, and independent-review index"
  key_links:
    - from: "tests/windows/Invoke-AbruptLossHarness.ps1"
      to: "tests/windows/results/phase1-evidence.json"
      via: "guest durability barrier, host hard-off command/time, reboot, authenticated recovery, and old/new complete hashes become one immutable machine-correct attempt"
      pattern: "Stop-VM|TurnOff|barrier|execution_machine"
    - from: "scripts/verify-phase1.ps1"
      to: "tests/windows/results/phase1-evidence.json"
      via: "all 30 requirements, seven success criteria, D-01 through D-50, edge/prohibition/ASVS rows, signed visual records, and independent review block final acceptance"
      pattern: "independent_review|matrix_digest|retention"
  prohibitions:
    - "flagged-unverified: MUST NOT treat a graceful in-guest shutdown, missing barrier, wrong-machine power command, or mixed/partial recovered file as abrupt-loss success."
    - "flagged-unverified: MUST NOT pass Phase 1 with a missing, stale, deviated, inaccessible, hash-mismatched, skewed, unsigned, unsanitized, unreviewed, or unretained required row."
    - "flagged-unverified: MUST NOT store protected content, raw paths or SIDs, raw serials, tokens, credential blobs, private keys, CA/database secrets, or command-line secret values in committed evidence."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Complete the D-19 failure matrix, hash-seal the sanitized Phase 1 evidence, and require an independent requirement-indexed exit review.

Purpose: Separate destructive recovery execution and final evidence/provenance sealing from the application/operation/size matrix so the phase exit gate has a narrow, auditable scope.
Output: Host-controlled abrupt-loss evidence, final requirement verifier, sealed matrix digest, independent-review record, and retention/hold validation.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-18-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-19-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-20-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-16-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@evidence/phase1/requirement-matrix.yaml
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Treat all implementation and Plan 01-16 application/operation/size evidence as final inputs. `phase1-evidence.json` is the sanitized attempt bundle governed by Plan 01-17; `verify-phase1.ps1` reads it and the authoritative requirement matrix but never embeds protected input bytes or secrets.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Execute restart, reboot, forced-termination, and host-controlled abrupt-loss recovery</name>
  <precondition>Plan 01-16 is green; the separate 01-21 privilege-manifest section has a valid digest-bound approval; hungdinh-lt can hard-power LAB-CLIENT01 off while the active-write barrier is signaled, and LAB-CLIENT01 can resume the evidence collector after boot.</precondition>
  <files>tests/windows/Invoke-Phase1Matrix.ps1, tests/windows/Invoke-AbruptLossHarness.ps1, tests/windows/results/phase1-evidence.json, tests/windows/results/phase1-evidence.sha256</files>
  <read_first>
    - Every path in `<files>` plus the completed Plan 01-16 evidence
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-12 through D-24 and D-33 through D-47
    - `.planning/ROADMAP.md` all Phase 1 success criteria and `.planning/REQUIREMENTS.md` Definition of Done
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md` failure and Hyper-V capabilities
  </read_first>
  <action>Run four distinct D-19 scenarios using a prior committed file and a distinguishable staged replacement: clean service restart inside LAB-CLIENT01; Windows reboot initiated inside LAB-CLIENT01 with registered post-boot continuation; forced LAB-CLIENT01 service/process termination at an injected active-write barrier; and hungdinh-lt `Stop-VM -TurnOff` of LAB-CLIENT01 only after a guest signal confirms execution between durability steps. For abrupt loss, capture host command and UTC, verify no in-guest graceful shutdown event, reboot LAB-CLIENT01, wait for service/session/drive recovery, compare old/new hashes, and scan for mixed, partial, or staging exposure. Per D-33 through D-36, enforce the separate 01-21 privilege manifest and allow hungdinh-lt to control only LAB-CLIENT01 power. Per D-37 through D-47, publish a distinct immutable attempt per failure class with machine role, build/procedure/environment/time, raw-artifact hashes, staleness, deviation, redaction, and prior-attempt links; preserve failures and hash-seal the sanitized bundle after every successful update.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, clean service restart, Windows reboot, and forced mid-write termination are distinct cases and recover only exact old-complete or new-complete hashes after authenticated remount."
    - "On hungdinh-lt, the abrupt-loss case records a host-side hard-off of LAB-CLIENT01 while the guest barrier is active; LAB-CLIENT01 recovery contains no graceful-shutdown substitute or mixed/partial file."
    - "Every D-19 attempt is machine-correct, immutable, schema-valid, artifact-accessible, current, sanitized, and linked to prior attempts when rerun."
    - "The separately approved 01-21 mutation digest, baseline, cleanup, Hyper-V target allowlist, clock offset, and environment fingerprint all validate."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt controlling only LAB-CLIENT01 power: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AbruptLossHarness.ps1 -CallerMachine hungdinh-lt -EndpointMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -RunAll</automated>
  </verify>
  <done>All four D-19 outcomes preserve one authenticated complete generation, and the abrupt-loss attempt proves a host-controlled hard-off rather than a graceful guest shutdown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal the complete requirement-indexed evidence and independent Phase 1 review</name>
  <precondition>Task 1 and all Plan 01-16 matrix rows have current schema-valid evidence; the authenticated independent verifier is available.</precondition>
  <files>tests/windows/results/phase1-evidence.json, tests/windows/results/phase1-evidence.sha256, scripts/verify-phase1.ps1</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `evidence/phase1/requirement-matrix.yaml` and `evidence/phase1/schema/evidence-manifest.schema.json`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-25 through D-50
    - `.planning/ROADMAP.md` goal and seven success criteria
    - `.planning/REQUIREMENTS.md` all 30 Phase 1 IDs and Definition of Done
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SOURCE-AUDIT.md` and `COVERAGE.md`
  </read_first>
  <behavior>
    - Test 1: all 30 requirement IDs, seven success criteria, D-01 through D-50, 36 edge dispositions, prohibitions, API capabilities, and high ASVS threats resolve to allowed current evidence or explicit non-passing state.
    - Test 2: wrong-machine, lower-tier substitute, missing, failed, stale, deviated, inaccessible, hash-mismatched, skewed, unsigned, unsanitized, unreviewed, or unretained required rows fail closed.
    - Test 3: the final sanitized bundle digest changes on any attempt, checklist, deviation, artifact, retention, or review mutation and is bound to the independent verifier's signed record.
  </behavior>
  <action>Implement `verify-phase1.ps1` to validate all 30 requirement IDs, seven success criteria, D-01 through D-50, the 36 SPEC-less edge dispositions, descriptor-less prohibition flags, every COVERAGE INTEGRATE/OPT-OUT row, ASVS L1 high-threat evidence, machine roles, verification tiers, privilege approvals, source/output/evidence hashes, staleness, raw-artifact access, clock policy, redaction, retention/holds, signed visual checklists, and independent review. Per D-41 and D-44 every rerun gets a new ID and link. Per D-48, an authenticated verifier independent of the individual visual-run operator reviews the complete requirement matrix, provenance, deviations, artifact integrity, and current/superseded attempts. Per D-49, deviations stay non-passing unless that verifier documents immateriality; otherwise rerun. Per D-50, raw artifacts remain through milestone audit plus the documented window and are securely deleted only after expiry without a hold. Fail closed on every invalid row, re-run the hungdinh-lt endpoint-clean audit, compute the final sanitized matrix digest, and bind the independent-review record to that digest.</action>
  <acceptance_criteria>
    - "On hungdinh-lt, `scripts/verify-phase1.ps1` accepts only complete machine-correct requirement, decision, edge, API, ASVS, privilege, and evidence coverage plus a clean developer-host audit."
    - "Across all machines, the sealed evidence contains no protected content, raw path/SID/serial, token, credential blob, private key, CA/database secret, or command-line secret value."
    - "The independent verifier signs the complete four-machine exit matrix digest after reviewing current and superseded attempts, deviations, artifact integrity, visual checklists, and retention/hold state."
    - "Missing, failed, stale, deviated, inaccessible, hash-mismatched, skewed, unsigned, unsanitized, unreviewed, unretained, wrong-machine, or substitute-promoted rows keep Phase 1 non-passing."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01</automated>
    <human-check>An authenticated verifier who did not attest the individual visual runs reviews the final sanitized four-machine matrix on hungdinh-lt, checks provenance, deviations, raw-artifact hashes, visual checklists, and retention, then signs the D-48 record with UTC and the final matrix digest; any material deviation is rerun.</human-check>
  </verify>
  <done>The D-01 through D-50 evidence is redacted and hash-sealed, and the final requirement gate, signed visual checklists, independent review, and retention contract pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Failure harness → recovery result | Graceful shutdown, timing errors, or incomplete hashes can falsely certify crash consistency. |
| Attempt bundle → phase acceptance | Wrong-machine, lower-tier, stale, deviated, inaccessible, or forged evidence can falsely satisfy a requirement. |
| Product/log files → sanitized evidence | Collection can leak credentials, identity data, paths, or protected content. |
| Independent review → phase exit | A reviewer must validate the exact final digest rather than an earlier or partial matrix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-21-01 | Tampering/Repudiation | abrupt-loss harness | high | mitigate | Guest durability barrier, hungdinh-lt host-side hard-off, no graceful guest event, post-boot hash/provenance capture. |
| T-01-21-02 | Spoofing/Repudiation | machine/tier provenance | high | mitigate | Role-guarded execution machine, verification-tier and substitute checks in `verify-phase1.ps1`. |
| T-01-21-03 | Information disclosure | evidence bundle | high | mitigate | Allowlisted schema, forbidden-field and non-vacuous marker scans, runtime-only secrets, and sealed digest. |
| T-01-21-04 | Repudiation/Tampering | independent review | high | mitigate | Authenticated independent identity, matrix digest, current/superseded attempts, deviation policy, artifact integrity, and retention/hold validation. |
| T-01-21-05 | Elevation | final mutations | high | mitigate | Separate digest-bound 01-21 privilege approval, baseline, strict machine allowlist, idempotent apply/verify/remove, cleanup, and pinned tooling. |

## ASVS L1 Gate

Any graceful-shutdown substitution, partial recovery, secret marker, invalid attempt, missing independent review, manifest drift, or unresolved required row blocks Phase 1.
</threat_model>

<verification>
Run D-19 endpoint failure cases on LAB-CLIENT01 with only host power control on hungdinh-lt, then run the complete evidence verifier on hungdinh-lt. Require the authenticated independent review of the exact sealed matrix digest.
</verification>

<success_criteria>
- Every D-19 failure recovers old-complete or new-complete data only.
- The final verifier covers all 30 requirements, seven success criteria, D-01 through D-50, edge/prohibition/API/ASVS rows, and privilege/evidence contracts.
- Sanitized evidence is hash-sealed, machine-correct, current, accessible, and independently reviewed.
- Retention, deletion, and hold state is auditable.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New path | `tests/windows/Invoke-AbruptLossHarness.ps1` | Host-controlled LAB-CLIENT01 hard-off/recovery runner |
| Generated path | `tests/windows/results/phase1-evidence.json` | Final attempts, checklists, abrupt-loss results, and independent review |
| Generated path | `tests/windows/results/phase1-evidence.sha256` | Sealed final matrix digest |
| New path | `scripts/verify-phase1.ps1` | Final requirements/decision/edge/API/ASVS/evidence gate |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-21-SUMMARY.md` when done.
</output>
