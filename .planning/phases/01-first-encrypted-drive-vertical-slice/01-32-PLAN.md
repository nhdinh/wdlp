---
phase: 01-first-encrypted-drive-vertical-slice
plan: 32
type: execute
wave: 20
depends_on:
  - 01-31
files_modified:
  - evidence/phase1/security-closure.yaml
autonomous: false
gap_closure: true
requirements:
  - CRY-04
user_setup:
  - service: windows-enterprise-pki
    why: "An independent reviewer must control a signing certificate whose public chain and policy are trusted outside the mutable closure manifest."
    dashboard_config:
      - task: "Issue or select a CurrentUser document/code-signing certificate for the independent LAB reviewer and provide only its public trust root/policy inputs to the verifier."
        location: "LAB enterprise CA / reviewer CurrentUser certificate store"
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "All current high-threat payloads have fresh signatures made by an independently authenticated reviewer-controlled key."
    - "The trusted verifier accepts signer identity, machine/domain role, procedure, and timestamp policy for every current record."
  artifacts:
    - path: evidence/phase1/security-closure.yaml
      provides: "Fresh cryptographically signed current-payload attestations"
  key_links:
    - from: authenticated independent reviewer certificate
      to: evidence/phase1/security-closure.yaml
      via: "Mandatory exact-payload capture from the approved LAB environment"
---

<objective>
Capture fresh independent signatures over every current Phase 1 security-closure payload using the hardened Plan 01-31 workflow.

Purpose: Replace the existing self-authenticated approvals without inventing reviewer provenance or exposing the reviewer private key.
Output: A closure manifest containing trusted current-payload signatures ready for deterministic final reconciliation.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-31-SUMMARY.md
@scripts/add-security-closure-review.ps1
@scripts/verify-phase1-security.ps1
@evidence/phase1/security-closure.yaml
</context>

## Artifacts this phase produces

- `evidence/phase1/security-closure.yaml`: fresh reviewer-key signatures over all current closure payloads.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Perform independent signed review of every current closure payload</name>
  <precondition>Plan 01-31 tests pass, and the independent reviewer has an approved CurrentUser signing certificate with private key plus separately trusted public-root/policy inputs</precondition>
  <files>evidence/phase1/security-closure.yaml</files>
  <read_first>scripts/add-security-closure-review.ps1; scripts/verify-phase1-security.ps1; evidence/phase1/security-closure.yaml; .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md decisions D-37 through D-49</read_first>
  <instructions>Sign in to the approved independent LAB reviewer session; select the approved CurrentUser signing certificate; run the hardened capture command for all 19 threat IDs with `-TrustedRootPath <approved-root-file>` and `-ReviewerPolicyPath <approved-policy-file>`; inspect the complete payload and digest shown for each record and type the required affirmative response only when accepted; then run the signed-off verifier with those same two paths. Do not export the private key or copy it into the repository.</instructions>
  <action>
    On the approved LAB machine, the independent reviewer runs the hardened capture command for the complete canonical 19-threat allowlist using their CurrentUser signing-certificate thumbprint plus `-TrustedRootPath <approved-root-file>` and `-ReviewerPolicyPath <approved-policy-file>`. The command must render and require affirmative acceptance of every complete payload and digest. Do not copy the private key, synthesize approval, reuse the earlier unkeyed attestations, bypass confirmation, or source either trust input from manifest content. If either external file is missing/invalid, any record is declined, any payload drifts, the certificate/trust/role/time policy fails, or publication conflicts, stop with that record unsigned and preserve the prior manifest. After capture, run the signed-off verifier with the exact same two external files and require all 19 records valid.
  </action>
  <acceptance_criteria>
    - Exactly the authenticated reviewer controls each new signature.
    - Every current payload is affirmatively reviewed and signed under the approved external trust policy.
    - Historical attestations remain append-only and no private key enters the repository.
  </acceptance_criteria>
  <verification>The capture command completes without declined, drift, trust-policy, or publication-conflict diagnostics; the signed-off verifier reports all 19 target records valid; the manifest diff contains only append-only signed attestations and predecessor links.</verification>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH -RequireSignedOff</automated></verify>
  <done>All 19 current closure payloads carry fresh trusted independent-review signatures and signed-off validation succeeds.</done>
  <resume-signal>Reply `signed review complete` with the capture and signed-off verifier result, or provide the exact failing diagnostic.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reviewer private key -> repository evidence | The private key stays in the reviewer CurrentUser store; only signatures and public certificate material are published. |
| Human approval -> current payload | Each signature must follow exact complete-payload display and affirmative acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-32-01 | Spoofing | independent reviewer | high | mitigate | Approved reviewer-controlled certificate, external trust policy, and domain/machine/role validation. |
| T-01-32-02 | Repudiation | approval | high | mitigate | Mandatory per-record confirmation and detached signature over exact payload/procedure/time context. |
| T-01-32-03 | Information Disclosure | private signing key | high | mitigate | Sign in CurrentUser store; never export or commit private material. |
</threat_model>

<verification>
- All 19 records pass signed-off verification with external trusted-root and reviewer-policy inputs.
- The manifest diff adds only append-only signed attestations and expected predecessor links.
</verification>

<success_criteria>
- Independent authenticated review is real human action, not executor-synthesized provenance.
- Every current payload is signed and historical audit history is preserved.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-32-SUMMARY.md` when done.
</output>
