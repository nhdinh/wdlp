---
phase: 02-policy-enforcement-and-user-feedback
plan: 05
type: tdd
wave: 5
depends_on:
  - 02-04
files_modified:
  - crates/dlp-windows-drive/src/filesystem.rs
  - crates/dlp-windows-service/src/grants.rs
  - crates/dlp-windows-service/src/notification.rs
  - crates/dlp-windows-service/src/pipe.rs
  - crates/dlp-windows-service/src/service.rs
  - crates/dlp-windows-service/src/lib.rs
  - crates/dlp-windows-service/tests/companion_grants.rs
autonomous: true
requirements:
  - POL-06
  - POL-09
  - DRV-05
  - DRV-08
  - UI-02
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Windows service receives every drive decision, records one redacted in-memory Phase 2 event synchronously, and routes block/warn feedback only to the decision user’s authenticated session; notification dedupe never changes event count (D-18, D-19)."
    - "[FLAGGED ASSUMPTION A-GRANT-STATE] Warn denies the current attempt; Proceed once creates a 120-second, memory-only, single-use grant bound to exact SID, session, store generation, normalized file identity/path, operation, matched rule/action, policy version, and content/policy fingerprint, so service restart intentionally invalidates it (D-15, D-16)."
    - "Grant consumption is one atomic compare-and-remove: repeat/replay, expiry, wrong user/session/path/operation/rule/policy/content/generation, service restart, session end, policy activation, or content change denies (warn idempotency/concurrency resolved)."
    - "Concurrent companion replies and file retries cannot consume one grant twice or cross-consume grants; deterministic bounded registry eviction never turns absence into authorization (UI-02 concurrency)."
    - "User A cannot route an event, notification intent, grant, or store operation to User B; repeated and concurrent cross-user attempts remain denied with independent events (DRV-05 idempotency/concurrency)."
    - "Companion frames are versioned, length-bounded, and contain only opaque challenge ID plus action; kernel pipe PID/token SID/session is authoritative and body/header identity claims are rejected (UI-02)."
  artifacts:
    - path: "crates/dlp-windows-service/src/grants.rs"
      provides: "Bounded service-authoritative intent/grant registry and atomic consume"
      exports: ["GrantRegistry", "GrantBinding", "NotificationIntent"]
    - path: "crates/dlp-windows-service/src/notification.rs"
      provides: "Privacy-safe notification projection and injected notifier port"
      exports: ["NotificationModel", "NotificationSink"]
    - path: "crates/dlp-windows-service/src/pipe.rs"
      provides: "Authenticated versioned companion request/response framing"
    - path: "crates/dlp-windows-service/tests/companion_grants.rs"
      provides: "Identity, replay, TTL, invalidation, concurrency, isolation, dedupe, and redaction tests"
  key_links:
    - from: "crates/dlp-windows-drive/src/filesystem.rs"
      to: "crates/dlp-windows-service/src/service.rs"
      via: "decision/event port completes before callback response and returns exact grant outcome"
      pattern: "EnforcementEvent|Grant"
    - from: "crates/dlp-windows-service/src/pipe.rs"
      to: "crates/dlp-windows-service/src/grants.rs"
      via: "kernel-authenticated SID/session resolves an opaque service-owned notification intent"
      pattern: "challenge_id|consume"
    - from: "crates/dlp-windows-service/src/service.rs"
      to: "crates/dlp-windows-service/src/notification.rs"
      via: "event-first privacy projection; presentation dedupe occurs only after event creation"
      pattern: "record.*notify|NotificationModel"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] A user-facing action must never authorize a different person, file, content generation, operation, rule, or policy version, even when notifications look identical."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization’s DLP boundary.

<objective>
Make the Windows service the authoritative decision-event, notification-intent, and Proceed-once grant boundary with exact per-user authenticated IPC.

Purpose: Warn feedback is security-sensitive authorization, so the companion must remain a renderer while the service owns identity, binding, expiry, consumption, invalidation, and evidence.
Output: Event transport, privacy projection, authenticated companion protocol, bounded grant registry, and deterministic multi-session/replay/concurrency tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-RESEARCH.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@crates/dlp-windows-service/src/pipe.rs
@crates/dlp-windows-service/src/service.rs
@crates/dlp-windows-service/src/session.rs
@crates/dlp-windows-drive/src/filesystem.rs

<interfaces>
Reuse `ActorPipe`, kernel PID/token SID/session verification, bounded frame helpers, `EligibleSession`, injected `Clock`, and fail-closed `run_service_loop`. Plan 04 supplies synchronous `EnforcementEvent` callback output; the service returns only authoritative grant state, never a UI assertion.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deny one warn, authenticate Proceed once, and allow one exact retry</name>
  <reversibility rating="costly">D-16 exact grant bindings become a cross-process authorization and evidence contract shared by drive, service, and companion.</reversibility>
  <read_first>
    - 02-CONTEXT.md — D-15 through D-19 warn, grant, privacy, dedupe, and service-authority decisions.
    - 02-RESEARCH.md — Pattern 6, grant registry recommendation, and companion identity threats.
    - crates/dlp-windows-service/src/pipe.rs — bounded framing and kernel identity verification.
    - crates/dlp-windows-service/src/service.rs — fail-closed loop and redacted diagnostic pattern.
    - crates/dlp-windows-drive/src/filesystem.rs — Plan 04 synchronous decision/event boundary.
  </read_first>
  <files>crates/dlp-windows-drive/src/filesystem.rs, crates/dlp-windows-service/src/grants.rs, crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - RED: warn returns access denied and creates one event plus one opaque intent; an authenticated same-SID/session Proceed once allows exactly the next identical retry.
    - RED: replay and every mismatched binding deny; two simultaneous consumes yield exactly one success.
    - RED: service restart/session end/policy or content generation change invalidates outstanding intents/grants.
    - RED: absent service, failed IPC, malformed/oversized frame, spoofed identity, full registry, or clock expiry denies safely.
  </behavior>
  <action>Write failing grant/IPC/drive tests before production code. Create a single service-owned bounded registry with injected monotonic clock; choose D-16 discretionary values as 120-second TTL, hard maximum 300 seconds, memory-only state, and deterministic oldest-expiry/creation eviction that never authorizes. Store opaque IDs/fingerprints only. Bind exact SID, session, store generation, normalized file identity/path, operation, rule/action, policy version, and content/policy fingerprint. Extend the versioned bounded pipe protocol so the companion sends only challenge ID plus ProceedOnce; authenticate PID and impersonated token SID/session before resolving the intent, always revert impersonation, then atomically compare-and-remove. Route the drive retry through service grant consumption before allowing a warn result. Invalidate on restart/session end/config activation/content generation. Run exact-symbol impact before editing pipe/service/filesystem symbols; HIGH/CRITICAL pauses, UNKNOWN requires `rtk rg` confirmation, stale index is a lower bound. Run full nonpartial detect-changes before each TDD commit.</action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants warn_grant_tracer --quiet &amp;&amp; rtk cargo test --locked -p dlp-windows-drive --test policy_enforcement warn --quiet</automated>
  </verify>
  <acceptance_criteria>One real warn path denies, authenticates an opaque response, allows one exact retry, and rejects replay/mismatch/expiry/concurrency/restart/failure paths without trusting companion-supplied identity or authorization data.</acceptance_criteria>
  <done>Proceed once works as a brief exact service capability, while warn remains fail-closed whenever identity or service state is uncertain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every decision event and privacy-safe deduplicated notification intent</name>
  <read_first>
    - 02-CONTEXT.md — D-14, D-17, D-18, D-19 event/feedback/privacy rules.
    - 02-RESEARCH.md — notification projection, event-before-dedupe, and A-EVENT recommendation.
    - crates/dlp-windows-service/src/service.rs — Task 1 authoritative registry and lifecycle invalidation.
    - crates/dlp-windows-service/src/pipe.rs — authenticated companion channel.
    - crates/dlp-domain/src/lib.rs — enforcement event fields from Plan 01.
  </read_first>
  <files>crates/dlp-windows-service/src/notification.rs, crates/dlp-windows-service/src/service.rs, crates/dlp-windows-service/src/lib.rs, crates/dlp-windows-service/tests/companion_grants.rs</files>
  <behavior>
    - RED: every allow/block/warn/allow_and_audit/inspection_failed attempt records one event before any notification decision.
    - RED: block/warn notification model contains base name, operation, safe display name, stable reason, concise remediation, count, and opaque action only for warn.
    - RED: first notification always emits; equal decision key inside 30 seconds updates count; at 30 seconds boundary contract is explicit; different user/file/operation/rule/action/policy does not group.
    - RED: event counts remain equal to attempts under concurrent dedupe; notification model never contains full path, SID, content, detector match, secret, internal file/rule ID, or raw error.
  </behavior>
  <action>Write event-order/privacy/dedupe failures first. Add an injected `NotificationSink` and pure redacted projection. Per D-17 use only base file name, operation, administrator-authored safe display name, stable reason, remediation, count, and opaque challenge. If base-name derivation is empty, use a generic protected-item label rather than path leakage. Choose D-18 30-second dedupe window with 60-second hard maximum; always emit the first item, treat exactly 30 seconds as outside the previous window, and update only presentation count for the exact decision key. Record the normalized event before dedupe or IPC, including inspection failures. Bound dedupe state and isolate by SID/session. Add exports without expanding service UI authority. Apply exact impact and complete detect-changes gates before edits/commits.</action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-windows-service --test companion_grants --quiet &amp;&amp; rtk cargo clippy --locked -p dlp-windows-service -p dlp-windows-drive --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>Every decision creates one event before feedback; first/repeated notifications obey exact 30-second grouping without event loss; all payload fields are privacy-safe; multi-session and concurrent routing cannot cross users.</acceptance_criteria>
  <done>The service is the sole authoritative Phase 2 event/grant boundary and produces safe notification intents for the per-user companion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Drive host -> Windows service | Decision/event and retry authorization cross a privileged process boundary. |
| Per-user companion -> service pipe | Untrusted interactive input must be bound to kernel-authenticated SID/session. |
| Service event -> notification projection | Rich enforcement metadata is minimized before reaching interactive UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05-01 | Spoofing | Companion pipe identity | high | mitigate | Purpose-limited DACL, kernel client PID, impersonated token SID/session, and no claimed identity fields. |
| T-02-05-02 | Elevation of Privilege | Proceed-once grant | high | mitigate | Exact full binding, 120-second TTL, atomic compare/remove, one-use, bounded registry, and lifecycle invalidation. |
| T-02-05-03 | Tampering | Event/dedupe ordering | medium | mitigate | Record event before presentation dedupe and test concurrent attempts versus event counts. |
| T-02-05-04 | Information Disclosure | Notification model/IPC | high | mitigate | Project allowlisted safe fields only; opaque challenge carries no identity/path/rule authorization facts. |
| T-02-05-05 | Denial of Service | Intent/grant/dedupe registries | medium | mitigate | Hard bounds, monotonic expiry, deterministic eviction, bounded frames, and fail-closed absence. |
| T-02-05-SC | Tampering | Package supply chain | low | accept | No package install or new external dependency is used. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Denied/warned operations create feedback without weakening enforcement | 02-05 | COVERED | Service owns event, intent, and grant. |
| REQ | POL-06, POL-09, DRV-05, DRV-08, UI-02 | Effects/reasons/isolation/errors/authenticated IPC | 02-05 | COVERED | Exact bindings and edge tests cover all. |
| RESEARCH | Opaque intents, atomic grants, privacy projection, dedupe | 02-05 | COVERED | TTL/window discretion is explicit. |
| CONTEXT | D-14..D-19 | Fail-closed feedback, Proceed once, privacy, grouping, service authority | 02-05 | COVERED | Both tasks cite every decision. |
| CONTEXT | Deferred | Business justification | — | EXCLUDED | Proceed once carries no justification field. |
</source_audit>

<verification>
1. Run focused warn tracer, full companion grant/event suite, drive warn tests, and clippy.
2. Prove one-success atomic consumption under concurrency and all exact mismatches.
3. Prove event count is independent of toast dedupe and payload is allowlisted/redacted.
4. Require exact impact and nonpartial detect-changes evidence before commits.
</verification>

<success_criteria>
- Warn denies first and one exact authenticated retry can proceed once.
- Replay, mismatch, concurrency, expiry, restart, and IPC failure remain denied.
- Every decision produces a redacted event before notification grouping.
- No companion input becomes authoritative identity or policy state.
</success_criteria>

## Artifacts this phase produces

- New `crates/dlp-windows-service/src/grants.rs`: `GrantRegistry`, `GrantBinding`, `NotificationIntent` and atomic state transitions.
- New `crates/dlp-windows-service/src/notification.rs`: `NotificationModel`, `NotificationSink`, safe projection and dedupe key.
- Modified `crates/dlp-windows-service/src/pipe.rs`: versioned authenticated companion frames.
- Modified `crates/dlp-windows-service/src/service.rs` and `src/lib.rs`: event-first routing, registry lifecycle/invalidation, exports.
- Modified `crates/dlp-windows-drive/src/filesystem.rs`: service grant check and synchronous event transport.
- New `crates/dlp-windows-service/tests/companion_grants.rs`: full security/edge/concurrency suite.

<output>
Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-05-SUMMARY.md` with TDD commits, exact binding/TTL/window contracts, redaction evidence, and GitNexus reports.
</output>
