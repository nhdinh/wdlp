---
phase: 01.2-dlp-agent-enrollment-token-should-be-obtained-automatically
verified: 2026-08-28T08:57:59Z
status: gaps_found
score: 7/9 must-haves verified
behavior_unverified: 2
overrides_applied: 0
gaps:
  - truth: "Live authority-transition acceptance tests execute their PostgreSQL queries and prove ordinary refusal plus force replacement/revocation."
    status: failed
    reason: "Invoke-AuthorityQuery passes DLP_DATABASE_URL before psql options. PostgreSQL 16 treats every following -tA/-v/-c/SQL argument as an ignored extra argument, so the smoke test compares warning text rather than authority rows and then fails with credential_serial_invalid."
    artifacts:
      - path: "tests/windows/Invoke-AgentServiceSmoke.ps1"
        issue: "Line 244 orders psql arguments as URL then options; OrdinaryMissingCredentialDoesNotRevoke failed live and ReplacementRevocation uses the same disconnected helper."
    missing:
      - "Place all psql options and -c SQL before DLP_DATABASE_URL, reject any warning/error output, and require exactly one correctly shaped result row."
      - "Rerun OrdinaryMissingCredentialDoesNotRevoke against LAB-DC01/LAB-CLIENT01 and prove the active serial plus token authority fields are unchanged."
      - "Rerun ReplacementRevocation and prove the predecessor is revoked, the successor is active, both token locations are clean, and service/data/cache are preserved."
behavior_unverified_items:
  - truth: "Only explicit -ForceReenrollment -Apply replaces credentials and revokes the predecessor while preserving service, data, and cache."
    test: "Run ReplacementRevocation after correcting Invoke-AuthorityQuery."
    expected: "The predecessor serial becomes revoked, a different successor is active, service/data/cache remain, and both endpoint token locations are empty."
    why_human: "The production path is present and the force recovery restored an active endpoint, but the current live assertion helper never executes its SQL, so predecessor/successor transitions were not observed reliably."
  - truth: "An ordinary run with a missing local credential refuses without mutating active server authority."
    test: "Run OrdinaryMissingCredentialDoesNotRevoke after correcting Invoke-AuthorityQuery."
    expected: "The run refuses with ForceReenrollment -Apply guidance; active_serial, token_digest, token_consumed_at, and token_expires_at are unchanged and the predecessor remains active; the scenario finally restores the endpoint."
    why_human: "Repository code rolls back before token generation, but the live test's snapshots are psql warning text rather than database rows."
---

# Phase 01.2: Automatic Endpoint Enrollment Token Verification Report

**Phase Goal:** As a lab operator, obtain the endpoint enrollment token automatically from trusted provisioning so DlpWindowsService can be installed and started without manually copying a short-lived secret.
**Verified:** 2026-08-28T08:57:59Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | TrustedProvisioning installs and starts DlpWindowsService without manual token copying. | ✓ VERIFIED | `Invoke-Client01Runtime.ps1` defaults `EnrollmentTokenProvider` to TrustedProvisioning (line 8), invokes LAB-DC01 provisioning and validates the returned token before endpoint writes (lines 1037-1147), and the current lab force-recovery path completed without a manual token. |
| 2 | Normal reruns reuse `device.dpapi`; only explicit `-ForceReenrollment -Apply` replaces credentials, preserving service/data/cache. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | A normal no-provider-override live run emitted `existing_credential_reused`, then reached Automatic/Running and Active policy v1. Preview/Apply guards and preservation checks exist at lines 1605-1623 and 331-384, but predecessor revocation is not proven because the authority query helper is disconnected. |
| 3 | Provisioning/install failures clean both token locations, preserve partial artifacts, and require a fresh token. | ✓ VERIFIED | The runtime calls `Remove-Client01EnrollmentToken` on success/failure and clears the local token in `finally` (lines 1558-1666). `InstallStartFailureCleanup`, `CleanupFailure`, and `FreshTokenRetry` each passed using the real cleanup adapter contract. |
| 4 | Normal output is coded/sparse and opt-in diagnostics redact protected values. | ✓ VERIFIED | `NormalOutput` and `DiagnosticRedaction` passed. Diagnostics expose bounded paths, lengths, counts, status, fingerprints, and error type; no token/private-key/password/full-certificate/blob dump was found. |
| 5 | The service obtains and activates a signed configuration and reports non-empty active version/state through an authenticated post-start poll without moving admin mTLS material to LAB-CLIENT01. | ✓ VERIFIED | Live normal ServiceInstall completed with `active_policy_version=1`, `active_policy_state=Active`, Automatic/Running, and `tracer_complete`. `poll_and_activate` wires authenticated polling to `stage_verify_activate`; `Wait-Client01ActivePolicy` requires a post-baseline authenticated success log plus a durable pointer. Admin material is consumed only in the LAB-DC01 script block. |
| 6 | Operator docs make TrustedProvisioning the normal path and Manual an explicit offline fallback. | ✓ VERIFIED | HYPERV startup, ENV-VARS, LAB setup, and scripts/lab README all show ordinary `-Scenario ServiceInstall` without a provider override and explicit `-EnrollmentTokenProvider Manual` only for offline fallback. |
| 7 | Documentation/static gates cover Active policy evidence, cleanup, fresh retries, Apply gating, redaction, and D-01 through D-12. | ✓ VERIFIED | `EnrollmentTokenOrchestration.Tests.ps1 -TestCase All` and `EnvironmentGuidance.Tests.ps1` passed; decision coverage reports 12/12 honored. |
| 8 | Docs and test fixtures contain no secret-bearing examples. | ✓ VERIFIED | Secret-negative gates passed across all four operator surfaces; examples use placeholders or lengths and no unredacted token/key/password/certificate/blob fixture was found. |
| 9 | An ordinary missing-local-credential run refuses without changing active authority. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `PgAuthorityRepository::provision` locks the authority row and rolls back when non-recovery provisioning sees an active serial, before token generation. The live scenario attempted this invariant but its SQL helper ignored every query and failed `credential_serial_invalid`. |

**Score:** 7/9 truths verified (2 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/lab/Invoke-Client01Runtime.ps1` | Automatic provisioning, force gate, dual cleanup, redacted diagnostics, Active-policy proof | ✓ VERIFIED | Substantive orchestration; current live normal/reuse path passed. |
| `tests/windows/Invoke-AgentServiceSmoke.ps1` | Live enrollment/replacement/authority/cleanup assertions | ✗ PARTIAL / DISCONNECTED | Most deterministic scenarios pass, but line 244 never executes the supplied PostgreSQL SQL on this psql version. |
| `tests/lab/EnrollmentTokenOrchestration.Tests.ps1` | Requirement- and decision-indexed static gates | ✓ VERIFIED | All test cases passed; comment-stripped, region-scoped assertions cover runtime/docs contracts. |
| `.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md` | Normal, force, recovery, cleanup, Active-policy workflow | ✓ VERIFIED | Substantive and synchronized with current switches/paths. |
| `.planning/docs/ENV-VARS.md` | Token optionality, cleanup, redaction, offline fallback | ✓ VERIFIED | EnvironmentGuidance suite passed. |
| `.planning/docs/LAB-SETUP-GUIDE.md` | First-time default workflow and safe verification | ✓ VERIFIED | Contains default ServiceInstall and both cleanup locations. |
| `scripts/lab/README.md` | Script-level operator contract | ✓ VERIFIED | Normal/default and explicit Manual fallback are documented. |
| `crates/dlp-server/src/repository.rs` | Transactional ordinary-authority refusal and force revocation | ✓ PRESENT + SUBSTANTIVE + WIRED | `provision` locks `enrollment_authority`, rolls back ordinary active authority, and revokes/clears the predecessor only for recovery. Live authority assertions remain blocked by the smoke helper. |
| `crates/dlp-agent-core/src/config_cache.rs` | Selected-bundle repair and corruption fail-closed | ✓ VERIFIED | Exact missing-bundle repair and corrupt-selected-bytes tests passed. |
| `crates/dlp-windows-service/src/service.rs` | Early token zeroization and authenticated configuration loop | ✓ VERIFIED | `startup` takes the token into `Zeroizing` as its first statement; all three exact early-failure tests passed. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Runtime orchestrator | LAB-DC01 trusted provisioning | LAB-DC01-only script block returns one validated token | ✓ WIRED | Admin CA/cert/key values are consumed inside the LAB-DC01 invocation; token validation occurs before endpoint install writes. |
| Runtime token | `agent.env` and SCM Environment | `Install-Client01RuntimeSecrets` then service startup | ✓ WIRED | Both managed copies receive the startup token; live post-run counts were zero in both locations. |
| Service environment | DPAPI credential | `load_service_config` → `ServiceContext::startup` → `EnrollmentCoordinator` | ✓ WIRED | Environment value is removed after one read, guarded by `Zeroizing`, and transferred only at enrollment. |
| Device mTLS client | signed cache pointer | `poll_and_activate` → `stage_verify_activate` | ✓ WIRED | GitNexus shows the service loop calling authenticated poll and verified atomic cache activation; live Active v1 proof passed. |
| Smoke authority scenarios | PostgreSQL authority rows | `Invoke-AuthorityQuery` / psql | ✗ NOT WIRED | URL precedes options; psql emits four “extra command-line argument ignored” warnings and executes no `-c` SQL. |
| Static/docs gates | runtime and smoke contracts | Current switch/scenario/path/evidence names | ✓ WIRED | Both static suites passed against current source and all canonical docs. |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `Invoke-Client01Runtime.ps1` | enrollment token | LAB-DC01 `Invoke-TrustedProvisioning.ps1` JSON → validation → endpoint env/SCM → service | Yes; current force recovery enrolled and cleaned both locations | ✓ FLOWING |
| `service.rs` / cache | active policy version/state | authenticated device poll → signature/hash/audience/version checks → durable pointer → post-start log/pointer observation | Yes; live v1/Active | ✓ FLOWING |
| `Invoke-AgentServiceSmoke.ps1` authority helpers | active serial/token authority/status | intended psql query | No SQL executes because arguments after URL are ignored | ✗ DISCONNECTED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Normal no-manual-copy ServiceInstall / credential reuse / Active policy | `Invoke-Client01Runtime.ps1 ... -Scenario ServiceInstall -Apply` (no provider/token override) | `existing_credential_reused`; Automatic/Running; v1/Active; tracer complete | ✓ PASS |
| Ordinary missing credential does not mutate authority and force-restores endpoint | `Invoke-AgentServiceSmoke.ps1 -Scenario OrdinaryMissingCredentialDoesNotRevoke` | Endpoint restored Automatic/Running and current authority is active when queried correctly; scenario exited `credential_serial_invalid` because helper SQL never ran | ✗ FAIL |
| Cache selected-byte repair | exact `restart_repairs_missing_selected_bundle_before_reporting_unchanged` | 1 passed | ✓ PASS |
| Corrupt selected bytes fail closed | exact `equal_selected_bundle_fails_closed_when_cached_bytes_are_corrupt` | 1 passed | ✓ PASS |
| Concurrent greatest-version activation | exact `concurrent_activations_select_greatest_version_without_cross_linking` | 1 passed | ✓ PASS |
| Early token guard across three startup failures | three exact `startup_guards_token_when_*` tests | 3 passed | ✓ PASS |
| Cleanup/retry/output/redaction contracts | six named PowerShell smoke scenarios | 6 passed | ✓ PASS |
| Static requirement/docs contract | EnrollmentTokenOrchestration All + EnvironmentGuidance All | both passed | ✓ PASS |

### Probe Execution

No `probe-*.sh` probe is declared by either plan or present under `scripts`; live PowerShell scenarios are recorded above as behavioral checks.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SRV-13 | 01.2-01, 01.2-02 | Automatic trusted-provisioning token handoff with no normal manual copy | ✓ SATISFIED | Default provider and live no-override ServiceInstall/force recovery evidence. |
| SRV-14 | 01.2-01, 01.2-02 | Preserve LAB-DC01 admin boundary, validate handoff, clean token, redact output | ✓ SATISFIED | Code wiring, live dual-location zero counts, deterministic cleanup/redaction tests, no secret examples. |
| TST-05 | 01.2-01, 01.2-02 | Integration tests through first policy activation | ⚠️ PARTIAL | Current live service reached authenticated v1/Active, but the added live authority-transition coverage is disconnected and fails. |
| AGT-01 | 01.2-01, 01.2-02 | Automatic Windows service without interactive session | ✓ SATISFIED | Live Automatic/Running service and SYSTEM-based credential lifecycle. |

No additional requirement mapped to Phase 01.2 is orphaned from the plans.

### Decision Coverage

All 12 trackable CONTEXT.md decisions are honored by shipped artifacts (`honored: 12`, `total: 12`, no missing decisions). This gate is non-blocking; the blocker is the live authority-test wiring defect.

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
|---|---|---:|---:|---:|---|---|
| `tests/windows/Invoke-AgentServiceSmoke.ps1` | SRV-13, SRV-14, TST-05, AGT-01 | Active | 0 | 0 | Intended behavioral; authority assertions receive warning text | 🛑 BLOCKER |
| `tests/lab/EnrollmentTokenOrchestration.Tests.ps1` | SRV-13, SRV-14, TST-05, AGT-01, D-01..D-12 | Active | 0 | 0 | Value/static contract | ✓ PASS |
| `crates/dlp-agent-core/src/config_cache.rs` and integration tests | TST-05 / cache safety | Active | 0 | 0 | Value + state transition | ✓ PASS |
| `crates/dlp-windows-service/src/service.rs` tests | SRV-14 / token custody | Active | 0 | 0 | Error-path state transition | ✓ PASS |

**Disabled tests on requirements:** 0.  
**Circular expected-value patterns:** 0.  
**Disconnected authority assertions:** 1 helper affecting two live scenarios — BLOCKER.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `tests/windows/Invoke-AgentServiceSmoke.ps1` | 244 | psql URL placed before `-tA -v ... -c SQL` | 🛑 Blocker | SQL is ignored; snapshots/status are not database evidence and the scenario fails. |
| `tests/lab/EnrollmentTokenOrchestration.Tests.ps1` and docs | various | `placeholder` terminology | ℹ️ Info | Intentional secret-safe placeholder validation/examples, not implementation debt. |

No unreferenced TBD/FIXME/XXX debt markers, skipped tests, or taint findings were found in the modified Rust authority/cache/service files. GitNexus reports CRITICAL aggregate compare scope (38 changed symbols, 68 affected processes); focused tests and the live path cover the central service/policy flows, while the authority-transition live gap remains open.

### Human Verification Required

No separate manual UX check is appropriate. This is an infrastructure/lab-automation phase. The two behavior-unverified authority invariants require corrected automated live scenarios, not subjective human judgment.

### Gaps Summary

#### Critical Gap: live authority tests do not execute SQL

- **Missing:** A working PostgreSQL query adapter in `Invoke-AgentServiceSmoke.ps1` and successful live ordinary-refusal plus replacement/revocation scenarios.
- **Impact:** The phase cannot prove that ordinary missing-credential recovery leaves authority untouched or that Apply-gated force recovery revokes exactly the predecessor and activates the successor. Existing equality assertions compare repeated psql warnings, not rows.
- **Observed:** `OrdinaryMissingCredentialDoesNotRevoke` failed `credential_serial_invalid`. A minimal correct-order `psql -tA -v ... -c SQL DATABASE_URL` query returned one real row; the current helper produced four ignored-argument warnings.
- **Lab preservation:** The scenario's `finally` recovery restored LAB-CLIENT01; final checks found DlpWindowsService Automatic/Running, an active authority, cache pointer present, and zero token entries in both `agent.env` and SCM Environment.

### Recommended Gap-Closure Plan

#### 01.2-03: Repair and rerun live authority-transition evidence

1. Fix `Invoke-AuthorityQuery` argument ordering and fail closed on stderr, unexpected row count, or invalid row shape.
2. Add a deterministic helper test proving the actual SQL is executed rather than accepting psql warnings.
3. Rerun `OrdinaryMissingCredentialDoesNotRevoke` and `ReplacementRevocation` live; record unchanged ordinary authority, predecessor revocation/successor activation, dual cleanup, Active policy, and preserved service/data/cache.

Estimated scope: small.

## Verification Metadata

**Verification approach:** Goal-backward, adversarial initial verification.  
**Must-haves source:** ROADMAP goal, both PLAN frontmatter blocks, and the post-review authority/cache/token-custody invariants explicitly requested by the orchestrator.  
**Automated/live checks:** 17 passed; 1 failed.  
**Human checks required:** 0 subjective checks; 2 automated authority invariants remain behavior-unverified behind the blocker.  
**GitNexus:** Current PDG index inspected; key contexts and critical compare scope reviewed; no taint findings in authority/cache/service files.  

---

_Verified: 2026-08-28T08:57:59Z_  
_Verifier: the agent (gsd-verifier)_
