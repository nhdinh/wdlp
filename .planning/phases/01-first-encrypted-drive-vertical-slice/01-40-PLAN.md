---
phase: 01-first-encrypted-drive-vertical-slice
plan: 40
type: execute
wave: 28
depends_on:
  - 01-39
files_modified:
  - scripts/add-security-closure-review.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
autonomous: true
gap_closure: true
requirements:
  - CRY-04
  - TST-08
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Publication is possible only when the observed Windows identity and COMPUTERNAME match exactly one policy reviewer before any prompt, lock acquisition, signing, or file mutation."
    - "The signed attestation records observed identity and machine values; policy-only spoofing, wrong-user, and wrong-machine attempts fail with byte-identical closure bytes and no temporary publication artifact."
    - "Any D-48 archival digest mismatch or legacy null-subject incompatibility remains fail-closed and explicitly reported; this gap plan does not reinterpret a derived operational note as authoritative evidence."
  artifacts:
    - path: scripts/add-security-closure-review.ps1
      provides: "Observed-context-bound, append-only security closure publisher"
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Behavioral publication identity and non-mutation regressions"
  key_links:
    - from: observed WindowsIdentity/COMPUTERNAME
      to: reviewer policy selection
      via: "case-insensitive exact comparison before Read-Host and before mutex/publication"
    - from: approved context
      to: CMS attestation
      via: "attestation fields are populated from observed values, never policy claims"
    - from: mismatch diagnostic
      to: canonical closure file
      via: "failure path performs no replace and removes staging files"
---

<objective>
Close CR-05/SEC-05 by binding the security-closure ceremony to the executing Windows reviewer and station, with executable wrong-context non-mutation evidence.

Purpose: D-22 authorizes one reviewer identity and machine; a certificate or policy file alone must not let another context publish a false attestation.
Output: Hardened publisher and focused behavioral regressions, while preserving all prior append-only evidence and bounding the later D-48 observations as non-authoritative.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-39-SUMMARY.md
@scripts/add-security-closure-review.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
</context>

## Source Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Trustworthy Phase 1 evidence | Tasks 1-2 close only the publication-context verification blocker. |
| REQ | CRY-04, TST-08 | Frontmatter and publication regressions cover secret-safe attestation and adversarial validation. |
| VERIFICATION | CR-05/SEC-05; D-48 digest/null-subject observations | Tasks 1-2 enforce observed context and explicitly bound later observations as non-authoritative. |
| CONTEXT | D-22, D-33-D-50 | Machine-role allowlist, privilege/evidence provenance, append-only mutation, and sanitized failure behavior are preserved. |

<tasks>

<task type="auto">
  <name>Task 1: Enforce observed reviewer identity and station before publication</name>
  <files>scripts/add-security-closure-review.ps1</files>
  <read_first>scripts/add-security-closure-review.ps1; evidence-private/phase1/reviewer-policy.json; .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md (D-22, D-33 through D-36, D-37 through D-49)</read_first>
  <action>Before modifying any function or script symbol, run GitNexus impact({target:"the symbol",direction:"upstream"}) and record the blast radius; warn and stop for HIGH or CRITICAL risk. Resolve exactly one policy reviewer from the selected certificate, capture [Security.Principal.WindowsIdentity]::GetCurrent().Name and COMPUTERNAME, and compare both observed values to policy with an explicit ordinal case-insensitive comparison before preview, Read-Host affirmation, mutex acquisition, re-read, signing, or any write. Populate reviewer_identity.name and environment_fingerprint.machine_identity from those observed values while retaining policy-controlled role/domain/procedure fields. Return stable reviewer_identity_mismatch or reviewer_machine_mismatch diagnostics and leave the canonical closure byte-identical, with no staging file, on every mismatch or later validation failure. Preserve the existing payload/predecessor TOCTOU checks and append-only replacement protocol; do not add identity or machine override switches. Record that the derived D-48 digest mismatch and legacy null-subject note remain operational observations requiring their own authenticated remediation or an explicit compatibility contract, not evidence that this task silently resolves. Before committing, run GitNexus detect_changes() and confirm only expected symbols/flows changed.</action>
  <acceptance_criteria>Observed identity and machine are captured before user consent; a policy claiming another user or station cannot reach signing or mutation; successful records contain the observed values; existing approved policy fields and append-only semantics remain unchanged.</acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Publication</automated>
  </verify>
  <done>Publisher enforces D-22 observed context and rejects all mismatches before mutation with stable diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Add wrong-context child-process and non-mutation regressions</name>
  <files>scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/evidence/Phase1.Security.Tests.ps1; scripts/add-security-closure-review.ps1; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md (CR-05/SEC-05 gap)</read_first>
  <action>Before modifying any test function or symbol, run GitNexus impact({target:"the symbol",direction:"upstream"}) and record the blast radius; warn and stop for HIGH or CRITICAL risk. Extend the publication focus suite with isolated child-process cases that use valid signing/trust inputs but policy identity different from the observed Windows identity and policy machine different from COMPUTERNAME. For each case capture a pre-run SHA-256 of the copied closure, run the real publisher without bypass switches, require a non-zero exit and the matching stable diagnostic, assert byte identity and absence of temporary replacement files, then clean all fixtures and certificates. Retain the positive observed-context case and assert the resulting reviewer identity and machine equal the child process observations. Keep tests deterministic on the named Windows test machine and do not mutate canonical evidence. Before committing, run GitNexus detect_changes() and confirm only expected symbols/flows changed.</action>
  <acceptance_criteria>Both wrong-user and wrong-station attacks execute the publisher, fail before prompt/mutation, produce stable diagnostics, and leave fixture bytes unchanged; the positive path records observed values; no test relies solely on source-text matching.</acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Publication</automated>
  </verify>
  <done>CR-05 regressions prove identity/station binding behaviorally and preserve the append-only audit file on rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Interactive Windows context -> reviewer policy | Untrusted process context attempts to claim an approved reviewer/station. |
| Publisher -> canonical closure | A failed or concurrent publication must not mutate signed history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-40-01 | Spoofing | add-security-closure-review.ps1 | critical | mitigate | Compare observed Windows identity and COMPUTERNAME to exactly one policy reviewer before prompt, lock, signing, and mutation. |
| T-01-40-02 | Tampering | closure publication | high | mitigate | Byte-preserving mismatch failures, mutex/re-read checks, and staging cleanup. |
| T-01-40-SC | Tampering | PowerShell/CMS trust inputs | high | mitigate | Use only existing approved dependencies; test trust-input mismatch and no-mutation behavior before any new install. |
</threat_model>

<verification>
Run the publication behavioral suite on the explicitly named Windows test machine, inspect stable diagnostics for both mismatch classes, and confirm canonical evidence remains untouched. The later D-48 archival digest mismatch and legacy null-subject compatibility observation remains bounded as non-authoritative until an authenticated follow-up or explicit verifier contract is executed.
</verification>

<success_criteria>
- CR-05/SEC-05 wrong-user and wrong-machine attempts fail before prompt/publication and preserve bytes.
- Successful publication records observed Windows identity and machine.
- No prior attestation or canonical evidence is rewritten.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-40-SUMMARY.md` when done.
</output>
