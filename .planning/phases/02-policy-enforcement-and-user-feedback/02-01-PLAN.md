---
phase: 02-policy-enforcement-and-user-feedback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Cargo.lock
  - crates/dlp-domain/src/lib.rs
  - crates/dlp-policy/Cargo.toml
  - crates/dlp-policy/src/lib.rs
  - crates/dlp-policy/tests/policy_v2.rs
  - crates/dlp-windows-drive/src/filesystem.rs
  - crates/dlp-windows-drive/tests/policy_enforcement.rs
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-03
  - POL-04
  - POL-05
  - POL-06
  - POL-07
  - POL-08
  - POL-09
  - POL-10
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An assigned extension-based block rule can be compiled, evaluated for a drive read/export, denied before any plaintext byte is copied, and represented as a synchronous enforcement event with a stable reason (tracer; D-05, D-07, D-10, D-11)."
    - "The same policy version and input produce byte-for-byte equivalent decisions in repeated and parallel evaluations, with no mutable global evaluator state (POL-01 unclassified; POL-02 concurrency; POL-04 idempotency/concurrency)."
    - "An empty rule list is valid only with an explicit default action; missing/null defaults and empty any_of lists are rejected; a single rule follows the same path as a multi-rule policy (POL-02 empty; D-06, D-07)."
    - "Policy strings are valid UTF-8 and comparisons use documented Unicode scalar/code-point semantics after field-specific path/extension normalization; invalid serialized text is rejected and grapheme truncation is never used (POL-02 encoding)."
    - "Name, extension, MIME/type, normalized path, owner, and checked-u64 size conditions use flat AND plus bounded any_of; size comparators define inclusive thresholds and distinguish N-1, N, and N+1 without rounding or overflow (POL-05 boundary/precision; D-06)."
    - "Regex, dictionary, authenticated full-file hash, and structured-identifier detectors scan only their configured prefix/resource budgets; reaching the prefix limit is success, while required detector failure returns inspection_failed for the affected rule (POL-03 unclassified; D-12, D-13, D-14)."
    - "[FLAGGED ASSUMPTION A-LIMITS] The research-recommended detector defaults/hard ceilings are operationally suitable; activation tests and bounded benchmarks must prove they remain below the declared memory/scan budgets before release."
    - "Read/write/import/export/copy/delete are representable, drive reads map to export, creates/writes map to import, and unavailable destination/owner/process context records a sorted input_unavailable observation without matching that rule (POL-04, POL-05; D-08, D-10)."
    - "All four runtime actions exist; require_justification is rejected at compilation; precedence is numeric priority, then block > warn > allow_and_audit > allow, then stable rule ID (POL-06, POL-07, POL-08; D-05, D-09)."
    - "Every matched, default, empty-rule, unavailable-input, conflict, and inspection-failure outcome carries one stable primary reason; equal/touching observations are deduplicated and sorted deterministically (POL-09 adjacency/empty/ordering)."
    - "The complete evaluator and detector suite runs in portable dlp-policy without Windows APIs (POL-10 unclassified)."
  artifacts:
    - path: "crates/dlp-domain/src/lib.rs"
      provides: "Versioned policy input, action, decision, observation, and enforcement-event vocabulary"
      exports: ["PolicyInput", "PolicyDecision", "DecisionReason", "DecisionObservation", "EnforcementEvent"]
    - path: "crates/dlp-policy/src/lib.rs"
      provides: "Bounded authoring validation, compilation, detector materialization, and deterministic evaluation"
      exports: ["PolicyDocumentV2", "CompiledPolicyV2", "DetectorCeilings", "PolicyEvaluator"]
    - path: "crates/dlp-policy/tests/policy_v2.rs"
      provides: "POL-01 through POL-10 table, boundary, concurrency, and detector tests"
    - path: "crates/dlp-windows-drive/tests/policy_enforcement.rs"
      provides: "Production tracer proof that read denial precedes plaintext exposure"
  key_links:
    - from: "crates/dlp-policy/src/lib.rs"
      to: "crates/dlp-domain/src/lib.rs"
      via: "compiler and evaluator consume the portable input/action/reason/event contracts"
      pattern: "Policy(Input|Decision)|Enforcement(Event|Action)"
    - from: "crates/dlp-windows-drive/src/filesystem.rs"
      to: "crates/dlp-policy/src/lib.rs"
      via: "immutable evaluator snapshot authorizes read/export before output-buffer copy"
      pattern: "evaluate.*Export|Export.*evaluate"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Phase 2 must not persist, transmit, log, or display inspected plaintext or detector-match excerpts; content exists only in bounded ephemeral inspection buffers."
---

## Phase Goal

**As an** authorized Windows user, **I want to** have protected-drive operations evaluated against assigned policy with clear feedback, **so that** sensitive files remain inside the organization’s DLP boundary.

<objective>
Create the portable deterministic policy contract and prove the first production enforcement slice from a compiled rule to a pre-plaintext drive denial and synchronous event.

Purpose: This is the tracer that proves the evaluator can sit on the existing encrypted-drive transaction boundary before server lifecycle, signed rollout, mutation coverage, and native feedback expand it.
Output: Versioned policy/decision/event types, bounded compiler/evaluator, exact edge semantics, approved detector dependencies, and a runnable read/export block tracer.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@C:/Users/nhdinh/.codex/RTK.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-CONTEXT.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-RESEARCH.md
@.planning/phases/02-policy-enforcement-and-user-feedback/02-PATTERNS.md
@.planning/docs/adrs/ADR-004-policy-expression.md
@.planning/docs/THREAT-MODEL.md
@crates/dlp-domain/src/lib.rs
@crates/dlp-policy/src/lib.rs
@crates/dlp-windows-drive/src/filesystem.rs

<interfaces>
Existing seams: `PolicyInput`, `EnforcementAction`, `DecisionReason`, `PolicyDecision`; `PolicyEvaluator::evaluate`; and `DlpFileSystemContext` read callbacks. Extend these rather than creating a parallel runtime. The drive must receive an immutable evaluator/event-sink port so later plans can swap signed snapshots and service transport without changing callback authorization order.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one compiled block rule at the read/export boundary</name>
  <read_first>
    - AGENTS.md — mandatory GitNexus impact, UNKNOWN confirmation, and detect-changes contract.
    - C:/Users/nhdinh/.codex/RTK.md — every shell command is prefixed with rtk.
    - 02-CONTEXT.md — D-05, D-07, D-10, D-11, D-14, and the no-silent-deployment boundary.
    - crates/dlp-domain/src/lib.rs — current PolicyInput, EnforcementAction, DecisionReason, and PolicyDecision contracts.
    - crates/dlp-policy/src/lib.rs — existing extension evaluator and deterministic ordering.
    - crates/dlp-windows-drive/src/filesystem.rs — current read path and output-buffer copy boundary.
  </read_first>
  <files>crates/dlp-domain/src/lib.rs, crates/dlp-policy/src/lib.rs, crates/dlp-windows-drive/src/filesystem.rs, crates/dlp-windows-drive/tests/policy_enforcement.rs</files>
  <behavior>
    - RED: an assigned compiled extension rule with action block makes the first read return access denied, copies zero bytes, and emits one event naming policy version, rule ID, block, export, and matched_rule.
    - RED: an explicit allow default permits a nonmatching read and returns the authenticated plaintext normally.
    - RED: two equal-priority matches resolve by D-05 restrictiveness and stable rule ID, independently of input rule order.
    - RED: every decision has a stable primary reason and sorted/deduplicated observations.
  </behavior>
  <action>Start with the failing integration tests, then extend the portable domain vocabulary and evaluator and inject a narrow immutable policy/evidence port into `DlpFileSystemContext`. Per D-10 classify the tested read as export; per D-11 call the evaluator after authenticated store read/inspection facts exist but before copying into the WinFsp buffer. A block or required inspection failure returns the existing centralized access-denied status path, copies zero bytes, and synchronously hands a redacted `EnforcementEvent` to the injected sink. Keep the tracer production-quality: exact typed errors, no global state, and no test-only authorization branch. Before each existing symbol edit, run `rtk node .gitnexus/run.cjs impact <exact-symbol> --direction upstream --repo .`; report callers/processes/risk, pause and warn on HIGH/CRITICAL, and for UNKNOWN confirm call sites with `rtk rg -n <symbol> crates tests` before proceeding. The GitNexus index is known to be one commit stale, so treat returned callers as a lower bound. Complete RED then GREEN then optional REFACTOR commits. Before each commit run `rtk node .gitnexus/run.cjs detect-changes --scope all --repo .`; partial/truncated output is unresolved and must be rerun.</action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-windows-drive --test policy_enforcement read_export_tracer --quiet</automated>
  </verify>
  <acceptance_criteria>The tracer proves a real compiled block rule denies the first read before plaintext exposure, emits exactly one stable reasoned event, allows the explicit-default nonmatch, and passes D-05 tie ordering; impact and complete change detection are recorded in the summary.</acceptance_criteria>
  <done>A user-visible protected-drive read can now be authoritatively allowed or denied by a deterministic portable policy snapshot, with decision evidence produced before the effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the compiler across metadata, content, actions, and edge contracts</name>
  <reversibility rating="costly">D-05 ordering and the canonical policy/decision shapes become shared signed, persisted, CLI, agent, and evidence contracts.</reversibility>
  <read_first>
    - 02-CONTEXT.md — D-05 through D-14 exact matching, unavailable-input, operation, bounds, and failure semantics.
    - 02-RESEARCH.md — approved regex/aho-corasick package audit, Pattern 1/2, and recommended ceilings.
    - ADR-004-policy-expression.md — declarative schema, compilation, and bounded detector constraints.
    - crates/dlp-policy/Cargo.toml — portable dependency placement.
    - crates/dlp-policy/src/lib.rs — tracer compiler/evaluator contracts from Task 1.
  </read_first>
  <files>Cargo.lock, crates/dlp-domain/src/lib.rs, crates/dlp-policy/Cargo.toml, crates/dlp-policy/src/lib.rs, crates/dlp-policy/tests/policy_v2.rs</files>
  <behavior>
    - RED: empty rules plus explicit default is valid; missing/null default, empty any_of, duplicate stable IDs, and require_justification are rejected.
    - RED: UTF-8 metadata normalization, all metadata fields, inclusive checked size bounds, AND/any_of semantics, and unavailable destination behavior match the frontmatter truths.
    - RED: regex, dictionary, hash, and structured-ID detectors honor configured defaults and hard ceilings; a prefix-boundary completion is successful; corruption/decoder/missing-digest/resource failure becomes inspection_failed.
    - RED: repeated and parallel calls produce the same decision bytes; input order, hash-map order, equal observations, and touching matches do not change reason/observation order.
  </behavior>
  <action>Write the full failing `policy_v2` table/boundary/concurrency suite before implementation. Implement `PolicyDocumentV2 -> CompiledPolicyV2` with strict versioned deserialization, unknown-field rejection, flat AND and bounded any_of per D-06, an explicit default per D-07, and activation-time rejection of D-09. Apply the research-selected ceilings as configurable defaults under immutable hard caps: 1 MiB/4 MiB content prefix, 4 KiB/16 KiB regex source, 32/64 nesting, 1 MiB/4 MiB regex automaton per rule, 10k/25k dictionary entries, 1 MiB/4 MiB dictionary source, and 8 MiB/16 MiB dictionary automaton per policy. Add exactly audited `regex = 1.13.1` and `aho-corasick = 1.1.5`; pin through Cargo.lock and do not add a wrapper crate. Define metadata encoding/comparison and inclusive size threshold semantics explicitly, preserve D-05 ordering, and separate primary reasons from sorted observations per D-08/D-14. Re-run exact-symbol impact before edits and full nonpartial GitNexus change detection before each RED/GREEN/REFACTOR commit using the Task 1 command contract.</action>
  <verify>
    <automated>rtk cargo test --locked -p dlp-policy --test policy_v2 --quiet; rtk cargo test --locked -p dlp-policy --quiet; rtk cargo clippy --locked -p dlp-policy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>POL-01 through POL-10 tests cover empty/null/single inputs, UTF-8/code-point behavior, repeated and parallel evaluation, inclusive size boundaries, bounded detectors, all actions/operations, unavailable inputs, stable precedence/reasons, and portable Windows-free execution; exact audited packages are locked.</acceptance_criteria>
  <done>The portable compiler/evaluator completely implements D-05 through D-14 and supplies stable contracts for server publication, endpoint activation, drive enforcement, and feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Author policy document -> compiler | Untrusted administrator-authored schema, patterns, dictionaries, sizes, and actions enter bounded portable code. |
| Authenticated file facts -> evaluator | Metadata/content/digest observations determine whether plaintext may cross the drive boundary. |
| Evaluator -> WinFsp output/event sink | Decision ordering must precede plaintext copy and event projection must remain redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01-01 | Denial of Service | Policy compiler/detectors | high | mitigate | Enforce server-compatible defaults plus immutable endpoint hard ceilings on counts, bytes, nesting, compiled memory, and scanned prefix before evaluator activation. |
| T-02-01-02 | Tampering | Rule ordering/default action | high | mitigate | Canonical compilation, explicit default, unique stable IDs, and D-05 sort keys covered by order/permutation tests. |
| T-02-01-03 | Information Disclosure | Read/export and event construction | high | mitigate | Decide before output-buffer copy; event types omit content and detector-match excerpts and tests assert zero bytes on denial. |
| T-02-01-04 | Elevation of Privilege | require_justification action | medium | mitigate | Reject during compilation and retain a runtime action type that cannot activate unsupported authorization semantics per D-09. |
| T-02-01-SC | Tampering | Cargo dependency install | high | mitigate | Install only regex 1.13.1 and aho-corasick 1.1.5 from the RESEARCH Package Legitimacy Audit, pin Cargo.lock, and verify reverse dependency scope. |
</threat_model>

<source_audit>

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Encrypted drive becomes a metadata/content policy boundary | 02-01 | COVERED | Tracer proves pre-plaintext denial; expansion completes evaluator. |
| REQ | POL-01..POL-10 | Deterministic portable policy engine and reasons | 02-01 | COVERED | Every requirement is named in truths/tests. |
| RESEARCH | Compiler, tri-state inputs, detector limits, package audit | 02-01 | COVERED | Approved dependencies and recommended ceilings are explicit. |
| CONTEXT | D-05..D-14 | Matching, defaults, unavailable inputs, timing, bounds, fail-closed inspection | 02-01 | COVERED | Actions and tests cite each decision. |
| CONTEXT | Deferred | Web UI, group rollout, kernel interception, business justification | — | EXCLUDED | No deferred capability is planned. |
</source_audit>

<verification>
1. Run the tracer test first and require block-before-copy plus exactly one event.
2. Run the complete portable policy suite and clippy with locked dependencies.
3. Confirm package versions and legitimacy match RESEARCH.md.
4. Require complete GitNexus change detection before every commit; partial/truncated is not clean.
</verification>

<success_criteria>
- The read/export tracer works end to end before expansion.
- All metadata, operation, destination, action, priority, reason, and detector contracts are deterministic and bounded.
- Unsupported justification and detector failures fail closed.
- Portable tests need no Windows API.
</success_criteria>

## Artifacts this phase produces

- Modified `crates/dlp-domain/src/lib.rs`: `PolicyInput`, `PolicyDecision`, `DecisionReason`, plus new `DecisionObservation` and `EnforcementEvent` contracts.
- Modified `crates/dlp-policy/src/lib.rs`: new `PolicyDocumentV2`, `CompiledPolicyV2`, `DetectorCeilings`, compiler/materializer, and expanded `PolicyEvaluator`.
- New `crates/dlp-policy/tests/policy_v2.rs`: deterministic edge/detector/contract suite.
- New `crates/dlp-windows-drive/tests/policy_enforcement.rs`: read/export enforcement tracer (expanded in Plan 04).
- Modified `crates/dlp-windows-drive/src/filesystem.rs`: immutable policy/event ports and read-before-copy hook.
- Modified `crates/dlp-policy/Cargo.toml` and `Cargo.lock`: audited detector dependencies.

<output>
Create `.planning/phases/02-policy-enforcement-and-user-feedback/02-01-SUMMARY.md` with RED/GREEN/REFACTOR commits, impact/change-detection evidence, package versions, and tracer results.
</output>
