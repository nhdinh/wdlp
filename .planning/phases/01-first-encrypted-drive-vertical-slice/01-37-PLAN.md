---
phase: 01-first-encrypted-drive-vertical-slice
plan: 37
type: execute
wave: 25
depends_on:
  - 01-35
files_modified:
  - scripts/add-independent-review.ps1
  - scripts/verify-phase1.ps1
  - scripts/evidence/Phase1.Evidence.psm1
  - scripts/evidence/Phase1.Evidence.Tests.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
autonomous: true
gap_closure: true
requirements:
  - CRY-04
  - TST-08
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-48 publication derives the verifier identity and station from the executing Windows context and a dedicated policy, never from free-form input."
    - "A D-48 verifier whose identity, certificate subject, or thumbprint matches the archival signer is rejected before prompt, signature, or mutation."
    - "One detached CMS signature commits canonically to the complete material frozen Phase 1 artifact set, trust identities, UTC, procedure version, D-49 dispositions, accessibility, and retention state; later derived operational logs are non-authoritative and outside that set."
    - "Independent-review history is append-only and a crash or concurrent publisher cannot expose a mixed matrix, bundle, digest, index, or attempt generation."
    - "FinalGate fails unless the latest D-48 generation is present, cryptographically valid, fully bound, separated, and current."
  artifacts:
    - path: scripts/add-independent-review.ps1
      provides: "Observed-context, certificate-backed, transactionally recoverable D-48 publisher"
    - path: scripts/evidence/Phase1.Evidence.psm1
      provides: "Canonical review commitment, append-only index, trust and frozen-set validation primitives"
    - path: scripts/verify-phase1.ps1
      provides: "Mandatory present-and-valid D-48 FinalGate predicate"
    - path: scripts/evidence/Phase1.Evidence.Tests.ps1
      provides: "Adversarial D-48 publication and verification regressions"
  key_links:
    - from: scripts/add-independent-review.ps1
      to: scripts/evidence/Phase1.Evidence.psm1
      via: "canonical commitment construction and recoverable generation publication"
    - from: scripts/verify-phase1.ps1
      to: evidence/phase1/independent-reviews/index.json
      via: "latest signed generation lookup and frozen-artifact digest validation"
    - from: D-48 verifier certificate
      to: evidence/phase1/security-closure.yaml
      via: "explicit separation from archival signer policy identity, subject, and thumbprint"
---

<objective>
Replace the self-asserted D-48 record with an observed-context, certificate-backed, append-only, transactionally recoverable review protocol and make that protocol a mandatory Phase 1 FinalGate.

Purpose: Close the feasibility gap discovered in 01-36 Task 3 without representing the currently frozen evidence as independently approved.
Output: Hardened publisher, canonical commitment/verifier primitives, mandatory FinalGate wiring, and behavioral regressions.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
Approved execution revision: when Plan 01-37 changes a script digest protected by the Plan 01-36 security closure, use the authenticated additive supersession mode in `add-security-closure-review.ps1`. It preserves the prior protected payload, attestations, CMS bytes, and history envelope under immutable `superseded_payload_history`, recomputes current implementation digests, requires the observed authorized LAB-DC02 signer and exact `YES`, and publishes a newly signed current attestation/history envelope. Manual manifest editing and verification weakening remain forbidden.

@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-35-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-36-PLAN.md
@scripts/add-independent-review.ps1
@scripts/verify-phase1.ps1
@scripts/evidence/Phase1.Evidence.psm1
@scripts/evidence/Phase1.Evidence.Tests.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
@evidence/phase1/requirement-matrix.yaml
@tests/windows/results/phase1-evidence.json
@evidence-private/phase1/reviewer-policy.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one authenticated D-48 generation end to end</name>
  <files>scripts/add-independent-review.ps1; scripts/evidence/Phase1.Evidence.psm1; scripts/evidence/Phase1.Evidence.Tests.ps1</files>
  <behavior>
    - An observed authorized Windows identity and CurrentUser certificate can sign one canonical frozen-set commitment and publish one append-only review generation.
    - A free-form name, wrong observed user, wrong machine, zero/multiple certificates, or policy mismatch fails before prompt or mutation.
    - Matching the archival signer by normalized identity, certificate subject, or thumbprint fails before prompt or mutation.
  </behavior>
  <action>
    Before editing each function, run GitNexus upstream impact analysis for the exact symbol and report direct callers, affected processes, and risk; stop and warn before any HIGH or CRITICAL edit. Replace `VerifierName` with mandatory paths to a distinct D-48 reviewer policy and public trust root plus an allowlisted CurrentUser signer selector. Capture `[WindowsIdentity]::GetCurrent().Name`, `COMPUTERNAME`, certificate subject, normalized thumbprint, EKU/key usage, and policy role once; compare those observations with exactly one authorized policy entry before preview, prompt, locking, signing, or filesystem mutation. Load the archival reviewer policy as a separate trust input and reject equality with its signer identity, subject, or thumbprint. Canonicalize a versioned commitment containing SHA-256 for `security-closure.yaml`, archival reviewer policy, D-48 reviewer policy, trusted roots, the pre-ceremony authoritative `01-SECURITY.md` and `01-REVIEW.md`, requirement matrix, final evidence bundle and digest files; include commit/build, all trust identities, UTC/clock offset, procedure version, complete D-49 deviation dispositions, raw-artifact accessibility, retention/hold state, and prior review generation digest per D-37 through D-50. The signed generation is the immutable authoritative D-48 closure. Later status notes must be explicitly derived, non-authoritative, outside the signed set, and forbidden from changing material review meaning. Sign the exact canonical bytes with detached CMS and publish an immutable generation plus append-only index entry; preserve all prior records. Start with a failing end-to-end fixture, then implement the production path. Do not import trust persistently, export private keys, accept identity/machine overrides, or mutate the currently frozen canonical evidence fixture during tests. Before committing, stage only the three declared Task 1 files, run GitNexus `detect_changes({scope: "staged"})`, and require no undeclared file, symbol, or flow outside D-48 publication/canonicalization/testing.
  </action>
  <reversibility rating="costly">Published signed review history may only be superseded by an additive generation; it cannot be erased.</reversibility>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Evidence.Tests.ps1</automated>
  </verify>
  <done>One policy-authorized, observed, separated certificate signer can publish a CMS-signed append-only D-48 generation over the full frozen set; all pre-mutation rejection cases leave bytes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify the complete D-48 trust and frozen-set contract</name>
  <files>scripts/evidence/Phase1.Evidence.psm1; scripts/verify-phase1.ps1; scripts/evidence/Phase1.Evidence.Tests.ps1</files>
  <behavior>
    - Verification checks CustomRootTrust chain, signature, signing purpose, revocation, policy authorization, clock, procedure, and archival-signer separation.
    - Mutation of any committed frozen artifact, policy/root identity, deviation disposition, accessibility/retention state, generation order, or signature fails with a stable diagnostic.
    - FinalGate requires both `independent_review_present` and `independent_review_valid`; warning-only or shape-only review records cannot pass.
  </behavior>
  <action>
    Run GitNexus upstream impact analysis on every function before editing, report callers/processes/risk, and stop with a warning on HIGH/CRITICAL. Replace literal/shape validation in `Test-Phase1VisualReview` for independent reviews with a dedicated signed-review verifier that decodes canonical bytes, validates detached CMS with process-scoped CustomRootTrust, required digital-signature key usage and EKU, online revocation, exact policy identity/machine/role/thumbprint/subject, UTC skew, procedure version, append-only predecessor/index order, and separation from all archival-envelope signer identity/subject/thumbprint values. Recompute every bound material digest and reject post-review drift, missing or inaccessible raw artifacts, retention/hold changes, unreviewed D-49 deviations, removed/overwritten/reordered history, unknown fields, or inconsistent bundle/index/generation identities. Treat only the signed generation/index as authoritative D-48 closure; derived post-ceremony operational notes are not inputs and cannot override signed meaning. Change FinalGate success to require present and valid D-48 review, and ensure it performs no digest-file writes before validation. Emit stable redacted diagnostics only. Add failing cases before implementation for missing/invalid CMS, untrusted/wrong-purpose/revoked signer, same signer, and every binding category per D-37 through D-50. Before committing, stage only the three declared Task 2 files, run GitNexus `detect_changes({scope: "staged"})`, and reject any undeclared file, symbol, or flow outside review verification/FinalGate/testing.
  </action>
  <reversibility rating="reversible">Fail-closed verifier and gate behavior can be reverted without migrating signed evidence, though doing so would reopen D-48.</reversibility>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Evidence.Tests.ps1</automated>
  </verify>
  <done>Only a currently valid, cryptographically trusted, separated D-48 generation over the exact frozen evidence can satisfy FinalGate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove atomicity, recovery, concurrency, and adversarial drift behavior</name>
  <files>scripts/add-independent-review.ps1; scripts/evidence/Phase1.Evidence.psm1; scripts/evidence/Phase1.Evidence.Tests.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <behavior>
    - Two concurrent publishers serialize and at most one commits against a given predecessor.
    - Injected crashes at every publication boundary recover to either the complete old generation or complete new generation, never a mixed state.
    - Spoofing, same signer, signature faults, each frozen hash mutation, overwrite/removal/reorder, and post-review drift are rejected behaviorally.
  </behavior>
  <action>
    Before each symbol edit, run GitNexus upstream impact analysis (use PDG mode where statement-level control/data dependence is relevant), report direct callers, affected processes, and risk to the user/orchestrator, and stop with a warning before proceeding when risk is HIGH or CRITICAL. Implement one canonical-path named mutex and a recoverable generation protocol using same-volume staging, write-through flushes, durable generation directory/index replacement, predecessor recheck under lock, and startup cleanup/recovery. Avoid a sequential multi-file state in which matrix serialization makes a previously calculated digest stale: immutable generation content is signed first and the single index pointer is the commit point. Extend the PowerShell behavioral harness to launch real child publishers behind barriers, inject process termination before every commit boundary, and assert byte/digest identity after recovery. Cover spoofed asserted identity, wrong observed context, archival-signer reuse by each prohibited field, missing/truncated/recomputed-invalid CMS, each frozen file changed independently, policy/root substitution, history overwrite/removal/reorder, simultaneous writers, crash residue, and drift after signing. Require stable failure codes and non-mutation for every rejection. Run the canonical FinalGate against a valid fixture and against each deterministically invalid copied fixture. Before each task commit, stage only declared files, run GitNexus `detect_changes({scope: "staged"})`, and investigate any unexpected symbol or execution-flow impact.
  </action>
  <reversibility rating="reversible">The recovery protocol changes code and test fixtures only; no real review ceremony occurs in this task.</reversibility>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Evidence.Tests.ps1; if ($LASTEXITCODE -eq 0) { powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 }</automated>
    <human-check>Before every commit, GitNexus staged detect_changes reports only declared evidence-publication, review-validation, and FinalGate flows.</human-check>
  </verify>
  <done>All specified adversarial cases run behaviorally, publication is recoverable and serialized, and the untouched repository remains fail-closed until a new ceremony is completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Windows session -> D-48 publisher | Observed account, station, and CurrentUser key cross into authenticated publication. |
| D-48 policy/root -> verifier | External public authorization and trust inputs constrain accepted signers. |
| Frozen evidence -> canonical commitment | All phase-exit artifacts, deviations, accessibility, and retention are digest-bound. |
| Staged generation -> append-only index | Concurrent/crashed publication must expose one complete generation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-37-01 | Spoofing | publisher identity/station | critical | mitigate | Observe Windows identity/machine/certificate and match a distinct explicit D-48 policy before mutation. |
| T-01-37-02 | Elevation of privilege | signer separation | critical | mitigate | Reject archival signer equality by identity, subject, or thumbprint. |
| T-01-37-03 | Tampering | frozen-set commitment | critical | mitigate | Canonical CMS commitment over every artifact and trust identity with drift recomputation. |
| T-01-37-04 | Repudiation | review history | high | mitigate | Immutable generations, predecessor-bound append-only index, UTC/procedure/deviation records. |
| T-01-37-05 | Denial of service/Tampering | publication transaction | high | mitigate | Named mutex, durable staging, atomic pointer commit, crash recovery, behavioral concurrency tests. |
| T-01-37-06 | Information disclosure | evidence publication | high | mitigate | Public certificate material only, redacted diagnostics, forbidden-content scan, no key export. |
| T-01-37-SC | Tampering | dependency graph | high | mitigate | No package install or dependency change is authorized. |
</threat_model>

<verification>
Run both Phase 1 evidence/security behavioral suites. The repository's canonical FinalGate is expected to remain blocked specifically on missing D-48 review until Plan 01-38.
</verification>

<success_criteria>
- Weak free-form review publication is removed.
- A distinct policy-authorized certificate signer can publish one signed append-only generation.
- Every trust, separation, binding, history, concurrency, crash, and drift regression is behavioral.
- FinalGate cannot pass without `independent_review_present=true` and `independent_review_valid=true`.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-37-SUMMARY.md` when done.
</output>
