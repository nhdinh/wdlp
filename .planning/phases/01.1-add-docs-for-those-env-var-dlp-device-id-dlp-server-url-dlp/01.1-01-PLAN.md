---
phase: 01.1-add-docs-for-those-env-var-dlp-device-id-dlp-server-url-dlp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/docs/ENV-VARS.md
  - .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
autonomous: true
requirements: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All 15 service-consumed variables receive equal purpose, acquisition, validation, persistence, and troubleshooting depth (D-01)."
    - "The guide distinguishes six required, one conditional, and eight defaulted values and reconciles 15 service reads with 10 runner-persisted values (D-02, D-04)."
    - "An operator can follow collect -> validate -> persist -> deploy -> verify from hungdinh-lt through LAB-CLIENT01 to LAB-DC01, led by Initialize-DlpEnvironment.ps1 with a manual fallback (D-05, D-06, D-07, D-08)."
    - "ENV-VARS.md stays canonical; other runbooks link to it and unrelated variable catalogs remain in their canonical guides (D-03, D-09)."
    - "Examples use concrete lab settings, non-runnable synthetic sensitive material, and accurate handling classes (D-10, D-11, D-12)."
    - "All persistence layers, their writers, the no-independent-edit rule, and both root-CA forms are explicit (D-13, D-14)."
    - "Every variable has a safe check; stable errors map to fixes; verification is bounded; exposure response is actionable (D-15, D-16, D-17, D-18)."
  artifacts:
    - path: ".planning/docs/ENV-VARS.md"
      provides: "Setup-first workflow, 15-variable catalog, 15-vs-10 reconciliation, redacted checks, troubleshooting"
    - path: ".planning/docs/HYPERV-DLP-STARTUP-GUIDE.md"
      provides: "Concise navigation to the canonical endpoint environment guide"
  key_links:
    - from: ".planning/docs/ENV-VARS.md"
      to: "crates/dlp-windows-service/src/service.rs"
      via: "one-to-one inventory of service reads"
      pattern: "DLP_DRIVE_HOST_BINARY_PATH"
    - from: ".planning/docs/ENV-VARS.md"
      to: "scripts/lab/Invoke-Client01Runtime.ps1"
      via: "15-consumed versus 10-persisted reconciliation"
      pattern: "agent\.env|Environment"
    - from: ".planning/docs/HYPERV-DLP-STARTUP-GUIDE.md"
      to: ".planning/docs/ENV-VARS.md"
      via: "relative canonical link"
      pattern: "ENV-VARS\.md"
---

<objective>
Replace the incomplete four-variable emphasis with a setup-first, source-reconciled operator guide for every endpoint-service environment variable, implementing D-01 through D-18.

Purpose: Operators must configure and troubleshoot the endpoint without exposing sensitive material or mistaking caller-only overrides for service-persisted settings.
Output: Restructured `.planning/docs/ENV-VARS.md` and a concise canonical link in `.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md`.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.1-add-docs-for-those-env-var-dlp-device-id-dlp-server-url-dlp/01.1-CONTEXT.md
@.planning/phases/01.1-add-docs-for-those-env-var-dlp-device-id-dlp-server-url-dlp/01.1-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/docs/ENV-VARS.md
@.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md
@.planning/docs/PEM-KEY-GUIDE.md
@.planning/docs/LAB-SETUP-GUIDE.md
@crates/dlp-windows-service/src/service.rs
@scripts/lab/Initialize-DlpEnvironment.ps1
@scripts/lab/Invoke-Client01Runtime.ps1
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Document one complete endpoint setup path from collection through redacted verification</name>
  <files>.planning/docs/ENV-VARS.md</files>
  <action>
Restructure the guide so the complete operator journey precedes the catalog (D-05). Lead with `Initialize-DlpEnvironment.ps1`: explain interactive prompts, protected env-file input, non-interactive use, the source of each endpoint value, and a manual PowerShell process-environment fallback (D-07). Use the concrete `hungdinh-lt` -> `LAB-CLIENT01` -> `LAB-DC01` topology and real Phase 1 hostnames, `https://LAB-DC01:8443`, paths, key ID, intervals, and drive letter, but visibly non-runnable synthetic markers for certificates, keys, tokens, passwords, and other sensitive material (D-06, D-10, D-11).

Create explicit collect, validate, persist, deploy, and verify stages, each with an observable expected result (D-08). Explain transient process input, runner-generated `C:\dlp\agent\agent.env`, installer-written registry `Environment`, and why generated copies must not be edited independently (D-13). Show trusted provisioning as the normal token path and manual/offline input as the alternative (D-02). Verification must safely report only names, shape, path access, fingerprints, and status while covering generated config, registry visibility, service startup, and redacted diagnostics—not successful enrollment or first configuration polling (D-15, D-17).

Introduce handling classes for cryptographic secrets, sensitive identifiers, public trust/configuration, and ordinary settings (D-12). Link to canonical PKI/lab guides instead of duplicating server, provisioning, AD, or orchestration catalogs (D-03). Preserve inline certificate PEM and absolute PEM path forms for `DLP_ROOT_CA_PEM`, including what the runner deploys and the service sees (D-14).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$p='.planning/docs/ENV-VARS.md'; if(-not(Test-Path $p)){throw 'missing ENV-VARS.md'}; $s=Get-Content -Raw $p; @('Initialize-DlpEnvironment.ps1','collect','validate','persist','deploy','verify','hungdinh-lt','LAB-CLIENT01','LAB-DC01','agent.env','DlpWindowsService','TrustedProvisioning','Manual','inline PEM','absolute') | ForEach-Object { if($s -notmatch [regex]::Escape($_)){throw ('missing journey evidence: '+$_)} }; if($s -notmatch 'registry.*Environment|Environment.*registry'){throw 'missing registry explanation'}; if($s -notmatch 'enrollment.*not required|not require.*enrollment'){throw 'verification boundary unclear'}"</automated>
  </verify>
  <done>A Phase 1 operator can execute one safe documented path from initializer input through SCM-visible persistence, service startup, and redacted diagnostics, with manual input and root-CA dual-form behavior covered per D-02, D-05 through D-15, and D-17.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the 15-variable catalog, persistence reconciliation, and recovery matrix</name>
  <files>.planning/docs/ENV-VARS.md, .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md</files>
  <action>
After the workflow, give exactly 15 endpoint-service entries equal procedural depth: purpose; source/create; actual representation/parsing; safe copy-paste redacted validation; persistence support; default/requiredness; likely error; fix (D-01, D-15). Group six required (`DLP_DEVICE_ID`, `DLP_SERVER_URL`, `DLP_ROOT_CA_PEM`, `DLP_DATA_DIRECTORY`, `DLP_CACHE_DIRECTORY`, `DLP_CONFIGURATION_PUBLIC_KEY_HEX`), one conditional (`DLP_AGENT_ENROLLMENT_TOKEN`), and eight defaulted (`DLP_CONFIGURATION_KEY_ID`, `DLP_POLL_INTERVAL_SECONDS`, `DLP_HEALTH_INTERVAL_SECONDS`, `DLP_START_TIMEOUT_SECONDS`, `DLP_STOP_TIMEOUT_SECONDS`, `DLP_PREFERRED_DRIVE_LETTER`, `DLP_SIGN_OUT_GRACE_SECONDS`, `DLP_DRIVE_HOST_BINARY_PATH`) (D-04). Separate permissive runtime duration/drive-letter behavior from stricter recommended operator checks.

Make every catalog entry mechanically parseable with this exact repeated schema: a standalone `<!-- env-var: DLP_NAME -->` marker followed by non-empty `Classification`, `Purpose`, `Source/Create`, `Representation/Parsing`, `Safe validation`, `Persistence`, `Default/Requiredness`, `Likely error`, and `Fix` labeled fields. `Classification` must be exactly `required`, `conditional`, or `defaulted`. `Persistence` must contain exactly one deployment disposition token: `runner-persisted` for the ten values written by the runner or `service-default-fallback` for the five omitted overrides. Keep prose and commands within the corresponding labeled field so every variable has the same auditable depth; do not satisfy the schema with references to another entry.

Add a reconciliation table with service requiredness/default, initializer support, runner input, env-file persistence, registry persistence, and deployed behavior (D-04, D-13). State that the service consumes 15 names but the runner persists 10: the six required values, key ID, conditional token, poll interval, and health interval. The five omitted lifecycle/session overrides—start timeout, stop timeout, preferred drive, sign-out grace, drive-host path—therefore use service defaults in the deployed SCM process. Do not imply caller-only settings reach the service. Do not invent a manual registry write procedure; document the current limitation/defaults.

Add a stable error-to-fix matrix for `runtime_secrets_missing`, `service_config_missing`, and `service_config_invalid` with likely variables, redacted checks, and actions (D-16). Add the D-18 response: stop copying, safely remove exposed artifacts, rotate affected material, resume only with redacted diagnostics. Apply D-10 through D-14 throughout. Keep the startup guide concise with a prominent relative `ENV-VARS.md` link; acquisition/persistence/validation/troubleshooting stays canonical in `ENV-VARS.md`, with unrelated catalogs linked rather than copied (D-03, D-09).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$e='.planning/docs/ENV-VARS.md'; $g='.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md'; $required=@('DLP_DEVICE_ID','DLP_SERVER_URL','DLP_ROOT_CA_PEM','DLP_DATA_DIRECTORY','DLP_CACHE_DIRECTORY','DLP_CONFIGURATION_PUBLIC_KEY_HEX'); $conditional=@('DLP_AGENT_ENROLLMENT_TOKEN'); $defaulted=@('DLP_CONFIGURATION_KEY_ID','DLP_POLL_INTERVAL_SECONDS','DLP_HEALTH_INTERVAL_SECONDS','DLP_START_TIMEOUT_SECONDS','DLP_STOP_TIMEOUT_SECONDS','DLP_PREFERRED_DRIVE_LETTER','DLP_SIGN_OUT_GRACE_SECONDS','DLP_DRIVE_HOST_BINARY_PATH'); $persisted=@('DLP_DEVICE_ID','DLP_SERVER_URL','DLP_ROOT_CA_PEM','DLP_DATA_DIRECTORY','DLP_CACHE_DIRECTORY','DLP_CONFIGURATION_PUBLIC_KEY_HEX','DLP_AGENT_ENROLLMENT_TOKEN','DLP_CONFIGURATION_KEY_ID','DLP_POLL_INTERVAL_SECONDS','DLP_HEALTH_INTERVAL_SECONDS'); $fallback=@('DLP_START_TIMEOUT_SECONDS','DLP_STOP_TIMEOUT_SECONDS','DLP_PREFERRED_DRIVE_LETTER','DLP_SIGN_OUT_GRACE_SECONDS','DLP_DRIVE_HOST_BINARY_PATH'); $expected=$required+$conditional+$defaulted; $s=Get-Content -Raw $e; $matches=[regex]::Matches($s,'(?ms)^&lt;!-- env-var: (DLP_[A-Z0-9_]+) --&gt;\s*$\s*(.*?)(?=^&lt;!-- env-var: |\z)'); if($matches.Count-ne15){throw('expected exactly 15 catalog entries, found '+$matches.Count)}; $entries=@{}; foreach($m in $matches){$name=$m.Groups[1].Value; if($entries.ContainsKey($name)){throw('duplicate entry '+$name)}; $body=$m.Groups[2].Value; $fields=@{}; foreach($label in @('Classification','Purpose','Source/Create','Representation/Parsing','Safe validation','Persistence','Default/Requiredness','Likely error','Fix')){$fm=[regex]::Match($body,'(?mi)^[-*]?\s*'+[regex]::Escape($label)+'\s*:\s*(.+)$'); if(-not$fm.Success-or[string]::IsNullOrWhiteSpace($fm.Groups[1].Value)){throw('missing '+$label+' for '+$name)}; $fields[$label]=$fm.Groups[1].Value.Trim()}; $entries[$name]=$fields}; if(@($entries.Keys|Where-Object{$_-notin$expected}).Count-or@($expected|Where-Object{$_-notin$entries.Keys}).Count){throw 'catalog names differ from exact 15-name inventory'}; foreach($pair in @(@($required,'required'),@($conditional,'conditional'),@($defaulted,'defaulted'))){foreach($name in $pair[0]){if($entries[$name]['Classification']-ne$pair[1]){throw('wrong classification for '+$name)}}}; if(@($entries.Values|Where-Object{$_['Classification']-eq'required'}).Count-ne6-or@($entries.Values|Where-Object{$_['Classification']-eq'conditional'}).Count-ne1-or@($entries.Values|Where-Object{$_['Classification']-eq'defaulted'}).Count-ne8){throw 'classification totals are not 6/1/8'}; foreach($name in $persisted){if($entries[$name]['Persistence']-notmatch'\brunner-persisted\b'-or$entries[$name]['Persistence']-match'\bservice-default-fallback\b'){throw('wrong persistence for '+$name)}}; foreach($name in $fallback){if($entries[$name]['Persistence']-notmatch'\bservice-default-fallback\b'-or$entries[$name]['Persistence']-match'\brunner-persisted\b'){throw('wrong fallback for '+$name)}}; @('runtime_secrets_missing','service_config_missing','service_config_invalid','stop copying','rotate','redacted')|%{if($s-notmatch[regex]::Escape($_)){throw('missing evidence '+$_)}}; if((Get-Content -Raw $g)-notmatch '\[[^\]]+\]\(ENV-VARS\.md\)'){throw 'missing canonical link'}"</automated>
  </verify>
  <done>All 15 variables have equal actionable coverage; the 15-versus-10 boundary and five default fallbacks are explicit; errors, safe checks, and exposure recovery are actionable; the startup guide links without duplicating (D-01 through D-04, D-09 through D-18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator process | Copy-paste examples become process input and must not expose runnable sensitive material. |
| hungdinh-lt -> LAB-CLIENT01 | The runner materializes only selected values into agent.env and registry Environment. |
| registry Environment -> service | SCM supplies persisted names; caller-only overrides are not active service settings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-01 | Information Disclosure | Examples/diagnostics | high | mitigate | Non-runnable sensitive markers, redacted checks, and D-18 recovery. |
| T-01.1-02 | Spoofing | Server URL/root CA | high | mitigate | Hostname-aware HTTPS, certificate/path checks, dual root forms, canonical PKI link. |
| T-01.1-03 | Tampering | agent.env/registry | medium | mitigate | Generated-copy ownership, no independent edits, name-level visibility checks. |
| T-01.1-04 | Tampering | Five omitted overrides | medium | mitigate | Explicit 15-vs-10 table and effective service defaults. |
| T-01.1-05 | Repudiation | Troubleshooting | low | accept | Docs cannot enforce operator behavior; stable errors and repeatable checks are proportionate. |
| T-01.1-SC | Tampering | package installs | high | mitigate | No package installation occurs. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Complete endpoint runtime-variable documentation and operator guidance | 01.1-01 | COVERED | Tasks 1-2.
REQ | — | Phase requirement IDs are TBD/null | 01.1-01 | COVERED | No ID invented; requirements is empty.
RESEARCH | — | 15 service reads, 10 persisted values, five service-default fallbacks | 01.1-01 | COVERED | Task 2 exact reconciliation.
RESEARCH | — | Actual parsers, root dual form, redaction, docs-only scope | 01.1-01 | COVERED | Tasks 1-2.
CONTEXT | D-01..D-04 | Coverage and reconciliation | 01.1-01 | COVERED | Task 2.
CONTEXT | D-05..D-09 | Setup journey and canonical topology | 01.1-01 | COVERED | Tasks 1-2.
CONTEXT | D-10..D-14 | Safe examples, handling, persistence, root forms | 01.1-01 | COVERED | Tasks 1-2.
CONTEXT | D-15..D-18 | Checks, errors, verification boundary, exposure | 01.1-01 | COVERED | Tasks 1-2.
</source_audit>

<verification>
- Run both PowerShell gates from the repository root; they print no sensitive values.
- Inspect `git diff -- .planning/docs/ENV-VARS.md .planning/docs/HYPERV-DLP-STARTUP-GUIDE.md`; only docs change.
- Before commit, run GitNexus `detect_changes({scope: "compare", base_ref: "main"})` and confirm no code symbols or flows changed.
- Confirm Markdown fences/tables are balanced and relative links resolve.
</verification>

<success_criteria>
1. Setup-first collect/validate/persist/deploy/verify journey precedes the catalog.
2. Exactly 15 service names have equal procedural depth: six required, one conditional, eight defaulted.
3. The guide states the runner persists 10 and its five omitted overrides use service defaults under SCM.
4. Examples/checks are redacted; handling classes and exposure response are accurate.
5. ENV-VARS.md remains canonical and startup guidance links without duplicating.
6. No source code, runtime script, API integration, schema, dependency, or package changes occur.
</success_criteria>

<output>
Create `.planning/phases/01.1-add-docs-for-those-env-var-dlp-device-id-dlp-server-url-dlp/01.1-01-SUMMARY.md` when done.
</output>
