---
context: phase
phase: 01-first-encrypted-drive-vertical-slice
plan: 25
task: 3
total_tasks: 3
status: uat_paused
last_updated: 2026-08-21T11:07:31Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: GSD tools missing — `.claude/gsd-core/bin/gsd-tools.cjs` is not installed. `/gsd-plan-phase`, `/gsd-execute-phase`, and other GSD orchestration commands cannot run until it is installed.
- [ ] CONSTRAINT: Context budget critical — this session is ending at ~75% context usage. The resuming agent must not attempt large planning/execution loops in the same turn; install tools and then resume in a fresh session.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Direct repo edits outside GSD workflow | Project instructions require starting file-changing work through `/gsd-quick`, `/gsd-debug`, or `/gsd-execute-phase`. | blocking | Always invoke the correct GSD entry point before Edit/Write. |
| Ignoring SC-04 dependency | Tests 4 (D-26/D-38) and 5 (D-48) cannot be completed until the WinFsp directory bug is fixed and re-verified. | blocking | Do not start visual checklist or independent review until SC-04 passes. |

<current_state>
Phase 01 UAT is in progress. SC-02 and SC-03 passed. SC-04 failed with a major WinFsp directory-enumeration bug: the drive mounts, but created folders/files do not display names or timestamps in PowerShell/Explorer. The backing store persists the entries correctly, so the bug is in `crates/dlp-windows-drive/src/filesystem.rs`.
</current_state>

<completed_work>

Completed Tasks:
- SC-02 verification: server starts, provisioning/enrollment endpoints work, DPAPI credential persisted.
- SC-03 verification: service enrollment, signed-config activation, tampered bundle rejection.
- Updated `01-UAT.md` with results.
- Diagnosed SC-04 root causes:
  - `read_directory` rejects query patterns other than `"*"`.
  - `FileInfo` timestamps are never populated.
  - No directory-change notifications are emitted.

</completed_work>

<remaining_work>

- Install GSD core tools (`npx -y @opengsd/gsd-core@latest --claude --local`).
- Create a gap-closure plan for the SC-04 WinFsp bug (via `/gsd-plan-phase 01 --gaps`).
- Implement fixes in `crates/dlp-windows-drive/src/filesystem.rs`:
  - Accept wildcard patterns in `read_directory`.
  - Populate `FileInfo` creation/access/write/change times.
  - Emit directory-change notifications after create/delete/write.
- Rebuild and redeploy to LAB-CLIENT01.
- Re-run SC-04 verification.
- Complete Test 4 (D-26/D-38 visual checklist).
- Complete Test 5 (D-48 independent review).

</remaining_work>

<decisions_made>

- SC-04 is a blocking major issue; D-26/D-38 and D-48 remain pending until it is resolved.
- The fix must go through GSD gap-closure planning rather than an ad-hoc edit.

</decisions_made>

<blockers>
- GSD core tools not installed, so automated planning/execution workflows cannot run.
- WinFsp `read_directory` pattern/timestamp/notification bug prevents folder/file names from appearing in PowerShell/Explorer.
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-UAT.md` — current verification status.
2. `.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md` — verification report and human verification list.
3. `crates/dlp-windows-drive/src/filesystem.rs` — WinFsp callback implementation with the bug.
4. `.planning/docs/HYPERV-DLP-STARTUP-GUIDE.md` — lab startup procedures.

## Infrastructure State

- No active background processes.
- Hyper-V lab expected to be cold; follow startup guide before re-verification.

<next_action>
Install GSD core tools with `npx -y @opengsd/gsd-core@latest --claude --local`, then resume with `/gsd-resume-work` or start the gap-closure plan with `/gsd-plan-phase 01 --gaps`.
</next_action>
