---
phase: 01-first-encrypted-drive-vertical-slice
plan: 28
type: execute
wave: 16
depends_on:
  - 01-27
files_modified:
  - scripts/verify-phase1-security.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
  - scripts/add-security-closure-review.ps1
  - evidence/phase1/security-closure.yaml
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
autonomous: true
gap_closure: true
requirements:
  - CRY-04
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A closure record whose protected payload changes cannot inherit an attestation over an older payload and pass -RequireSignedOff."
    - "The seven Aug 24 rebound records are explicitly unsigned/open until a new authenticated independent review is captured."
    - "A repository tool can create an append-only review attestation bound to the exact complete record payload without accepting a caller-supplied reviewer name."
  artifacts:
    - path: scripts/verify-phase1-security.ps1
      provides: "Versioned closure/attestation validation and signed-off fail-closed enforcement"
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Regression coverage for stale inherited review attestations and attestation tampering"
    - path: scripts/add-security-closure-review.ps1
      provides: "Authenticated independent-review capture for current closure payloads"
    - path: evidence/phase1/security-closure.yaml
      provides: "Versioned closure records with explicit unsigned state for the seven invalid inherited reviews"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      provides: "Truthful open security state while fresh review is pending"
  key_links:
    - from: scripts/verify-phase1-security.ps1
      to: evidence/phase1/security-closure.yaml
      via: "Recompute each protected record-payload digest and require the latest attestation to bind that digest"
    - from: scripts/add-security-closure-review.ps1
      to: scripts/evidence/Phase1.Evidence.psm1
      via: "Resolve and validate the authenticated Windows/domain identity using the established Phase 1 evidence contract"
    - from: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      to: scripts/verify-phase1-security.ps1
      via: "Seven reopened high threats keep signed-off mode failing until fresh attestations exist"
---

<objective>
Replace the mutable scalar-review model with versioned attestations bound to complete closure-record payloads, add the demonstrated stale-reseal regression, and reopen the seven records whose current bytes were never independently reviewed.

Purpose: Enforce D-37, D-40, D-41, D-44, and D-48 so CRY-04 cannot be declared security-complete using temporally impossible inherited reviews.
Output: A fail-closed v2 closure contract, regression-tested verifier, authenticated review-capture command, and truthful open security state awaiting fresh review.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-27-SUMMARY.md
@evidence/phase1/security-closure.yaml
@scripts/verify-phase1-security.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
@scripts/evidence/Phase1.Evidence.psm1
@scripts/add-independent-review.ps1
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make inherited review attestations fail closed end to end</name>
  <files>scripts/verify-phase1-security.ps1, scripts/evidence/Phase1.Security.Tests.ps1</files>
  <behavior>
    - Test 1: changing any protected field or implementation/artifact digest while retaining the prior attestation makes `-RequireSignedOff` fail.
    - Test 2: an attestation whose bound payload digest, threat ID, reviewer identity, review UTC, or prior-attestation link is changed fails verification.
    - Test 3: a record with valid current artifact hashes but no current-payload attestation passes pre-sign-off structural/hash validation and fails signed-off validation.
    - Test 4: an unchanged record with a valid current attestation continues to pass.
  </behavior>
  <action>
    Before editing, run GitNexus upstream impact for the PowerShell verifier and test entry points and report the blast radius; warn before proceeding if risk is HIGH or CRITICAL. Start with a failing tamper test that copies the canonical closure manifest, changes one protected record field or digest, preserves the previous review material, and proves `-RequireSignedOff` currently returns success. Upgrade the manifest contract to a versioned shape in which each record's complete security-relevant payload (threat ID, disposition, severity, mitigation assertion, implementation references, evidence attempt IDs, required roles, artifact references, procedure version, and environment fingerprint) has a deterministic SHA-256 identity and review attestations bind that identity. Define deterministic canonicalization in one verifier helper: fixed field order, stable list order, normalized UTF-8/LF serialization, and no locale-dependent conversions. Validate immutable predecessor linkage for appended attestations and reject duplicate, unknown, malformed, or payload-mismatched attestations. Keep pre-sign-off mode capable of reporting structurally valid unsigned records; make `-RequireSignedOff` require a latest valid attestation for every target. Do not infer freshness only from file modification time or Git commit time, and do not accept a new timestamp over an old payload digest. Extend the Pester suite with mutations covering payload reseal, attestation digest, identity, timestamp, predecessor link, and deletion. Preserve the exact 19-target allowlist and existing current-file hash checks.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1</automated>
  </verify>
  <done>The demonstrated Aug 24 digest-reseal attack and equivalent attestation mutations are rejected while unchanged valid records remain accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authenticated append-only closure review capture</name>
  <files>scripts/add-security-closure-review.ps1, scripts/evidence/Phase1.Security.Tests.ps1</files>
  <behavior>
    - Test 1: the command derives the reviewer from the authenticated Windows/domain session and refuses an unqualified, unavailable, or non-independent identity.
    - Test 2: the command reviews only explicitly selected threat IDs and binds each new attestation to the verifier-computed current payload digest.
    - Test 3: existing attestations remain byte-for-byte present and the new attestation links to the prior attestation digest.
    - Test 4: source/artifact drift between review display and publication aborts without modifying the canonical manifest.
  </behavior>
  <action>
    Implement a dedicated review-capture command following the authenticated identity and atomic-publication patterns in `add-independent-review.ps1` and `Phase1.Evidence.psm1`. The command must accept the closure path and an explicit threat-ID list, derive the current domain-qualified identity from the executing authenticated session rather than accept `-VerifierName`, display the complete current payload plus resolved file hashes and mitigation assertion for each selected record, require an explicit per-record confirmation, recompute the payload immediately before atomic publication, and abort on drift. Append a new UTC-stamped attestation with current payload digest, reviewer identity, procedure version, environment fingerprint, and previous-attestation digest; never edit or delete an earlier attestation. Reject execution on `hungdinh-lt` when the established independent-review contract requires the authenticated lab/domain context, per D-20/D-24/D-27/D-48. The command must support a non-mutating validation/dry-run path for automated tests. Do not capture secrets, protected plaintext, raw credential material, or caller-supplied identity strings.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1</automated>
  </verify>
  <done>A fresh independent reviewer can append a provenance-bound attestation for exact current records, and automation cannot forge the reviewer identity or race artifact drift.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate safely and reopen the seven unreviewed closures</name>
  <files>evidence/phase1/security-closure.yaml, .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md</files>
  <action>
    Migrate all 19 records to the v2 versioned payload/attestation shape without changing closure targets, mitigation assertions, implementation/artifact references, evidence attempts, required roles, or environment facts. Preserve each existing Aug 21 review as historical evidence bound only to the payload it actually reviewed when that prior payload is recoverable from repository history; otherwise retain it as an explicitly legacy/unbound historical item that cannot satisfy signed-off mode. For T-01-15-03, T-01-16-02, T-01-16-03, T-01-18-SC, T-01-20-01, T-01-20-02, and T-01-20-05, leave the current payload without a current attestation. Update `01-SECURITY.md` to `status: gaps_found`, set `threats_open: 7`, mark exactly those seven high threats open, and append an audit entry explaining that current hashes remain structurally verified but independent review is pending. Preserve prior audit/sign-off history and do not represent Plan 01-27's Aug 24 hash update as independent review. Run pre-sign-off validation successfully. For the signed-off negative check, use the verifier's structured JSON diagnostic mode: reserve exit code 3 for a completed validation that found unsigned/current-attestation violations, use a different nonzero exit for invocation, manifest parsing, or internal execution errors, and emit one diagnostic per affected record with `code: unsigned_current_attestation` and its `threat_id`. Capture stdout, stderr, and exit code; require parseable JSON, the validation-failed status, exit code 3, empty stderr, and an exact set equality against the seven IDs above. Reject duplicate IDs, any additional diagnostic ID or code, missing IDs, and any execution/parsing failure rather than treating arbitrary nonzero status as the expected result.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $stderrFile = [IO.Path]::GetTempFileName(); try { $stdout = (&amp; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md -RequireSignedOff -DiagnosticFormat Json 2&gt;$stderrFile | Out-String); $signedOffExit = $LASTEXITCODE; $stderr = Get-Content -Raw $stderrFile; if ($signedOffExit -ne 3) { Write-Error "Expected validation-failed exit 3, got $signedOffExit. stderr=$stderr"; exit 1 }; if (-not [string]::IsNullOrWhiteSpace($stderr)) { Write-Error "Unexpected verifier stderr: $stderr"; exit 1 }; try { $result = $stdout | ConvertFrom-Json -ErrorAction Stop } catch { Write-Error "Verifier JSON parse failed: $($_.Exception.Message)"; exit 1 }; if ($result.status -ne 'validation_failed') { Write-Error "Unexpected verifier status: $($result.status)"; exit 1 }; $expected = @('T-01-15-03','T-01-16-02','T-01-16-03','T-01-18-SC','T-01-20-01','T-01-20-02','T-01-20-05'); $diagnostics = @($result.diagnostics); if ($diagnostics.Count -ne 7 -or @($diagnostics | Where-Object { $_.code -ne 'unsigned_current_attestation' }).Count -ne 0) { Write-Error 'Expected exactly seven unsigned_current_attestation diagnostics'; exit 1 }; $actual = @($diagnostics | ForEach-Object { [string]$_.threat_id }); if (@($actual | Select-Object -Unique).Count -ne 7 -or @(Compare-Object ($expected | Sort-Object) ($actual | Sort-Object)).Count -ne 0) { Write-Error "Signed-off diagnostics did not name exactly the expected seven threat IDs: $($actual -join ', ')"; exit 1 }; exit 0 } finally { Remove-Item -LiteralPath $stderrFile -Force -ErrorAction SilentlyContinue }</automated>
  </verify>
  <done>The repository truthfully reports exactly the seven named high threats as open and preserves historical review material; pre-sign-off validation succeeds, while signed-off validation returns parseable structured output with exit code 3 and exactly one `unsigned_current_attestation` diagnostic for each named ID, with no duplicates, omissions, extra IDs/codes, stderr, parsing error, or unrelated verifier failure accepted as success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutable closure payload -> review attestation | Current hashes and mitigation claims must be bound to the exact payload an independent reviewer saw. |
| Authenticated Windows session -> reviewer identity | Caller-controlled text must not become evidence of an authenticated independent identity. |
| Historical v1 evidence -> v2 signed-off state | Legacy reviews must be preserved without being promoted to attestations over newer bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28-01 | Tampering | closure payload/attestation binding | high | mitigate | Deterministic complete-payload digest, append-only predecessor-linked attestations, and payload-reseal tamper tests. |
| T-01-28-02 | Spoofing | review-capture identity | high | mitigate | Derive and validate authenticated domain identity; forbid caller-supplied reviewer names and wrong-machine execution. |
| T-01-28-03 | Repudiation | historical review migration | high | mitigate | Preserve old attestations with their actual payload identity or explicitly mark them legacy/unbound; never rewrite review history. |
| T-01-28-04 | Information Disclosure | review display/publication | high | mitigate | Display only allowlisted hashes/assertions/provenance and retain existing redaction gates. |
| T-01-28-05 | Denial of Service | canonicalization drift | medium | mitigate | One deterministic canonicalization helper and regression fixtures for ordering/newline/encoding stability. |
</threat_model>

<verification>
- Pester includes a regression that reproduces the inherited-attestation false pass and now rejects it.
- Pre-sign-off mode validates all current hashes and v2 structure while signed-off mode names/fails the seven unsigned current records.
- The authenticated capture tool passes dry-run, identity, append-only, and race-detection tests.
- `01-SECURITY.md` and machine validation agree on exactly seven open blocking threats.
</verification>

<success_criteria>
- No changed closure payload can reuse an older attestation to pass `-RequireSignedOff`.
- Historical reviews are retained without being falsely rebound to current bytes.
- The seven invalid inherited reviews are explicitly reopened.
- Fresh review capture is authenticated, append-only, payload-bound, drift-safe, and redacted.
- No production Rust code, functional UAT evidence, or unrelated closure record is changed.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-28-SUMMARY.md` when done.
</output>
