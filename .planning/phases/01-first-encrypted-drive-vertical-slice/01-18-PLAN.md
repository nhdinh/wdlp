---
phase: 01-first-encrypted-drive-vertical-slice
plan: 18
type: execute
wave: 4
depends_on: [01-14]
files_modified:
  - crates/dlp-agent-core/src/lib.rs
  - crates/dlp-agent-core/src/client.rs
  - crates/dlp-agent-core/src/config_cache.rs
  - crates/dlp-agent-core/src/health.rs
  - crates/dlp-agent-core/tests/enrollment_activation.rs
autonomous: true
requirements: [CRY-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, TST-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-25 through D-27, signed-configuration and restart completion requires portable negative/concurrency checks plus focused LAB-CLIENT01 evidence; host-side source checks cannot satisfy the endpoint-runtime tier."
    - "Configuration polling on LAB-CLIENT01 validates TLS identity, exact signed bytes, hash, signature, schema, key ID, audience, and a strictly increasing numeric version before an atomic current/LKG change."
    - "Unsigned, tampered, wrong-key, unsupported-schema, hash-mismatched, wrong-audience, truncated, replayed, and concurrently completed bundles preserve the prior selected pointers."
    - "Per D-37 through D-47, every portable and focused attempt is immutable, machine-correct, sanitized, time-valid, artifact-verifiable, and linked to prior attempts without overwriting failures."
    - "Per D-33 through D-36, service-data cache activation and restart mutations are limited to the separately approved 01-18 manifest digest with baseline, idempotent apply/verify/remove, cleanup, and LAB-CLIENT01 role enforcement."
  artifacts:
    - path: "crates/dlp-agent-core/src/config_cache.rs"
      provides: "Immutable staged signed-bundle current/LKG cache"
      exports: ["ConfigurationCache", "CachePointers"]
    - path: "crates/dlp-agent-core/tests/enrollment_activation.rs"
      provides: "Signed-bundle validation, replay, concurrency, and restart contracts"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Current CRY-02 and AGT-03 through AGT-07 evidence IDs governed by Plan 01-17"
  key_links:
    - from: "crates/dlp-agent-core/src/client.rs"
      to: "crates/dlp-agent-core/src/config_cache.rs"
      via: "device-mTLS response bytes are staged, strictly verified, and monotonically activated"
      pattern: "stage_verify_activate"
  flagged_assumptions:
    - { id: "EDGE-CRY-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify CRY-02; signature, hash, schema, key, audience, version, and exact-byte cases stay explicit." }
    - { id: "EDGE-AGT-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-03; wrong root/name, expiry, timeout, absent client certificate, and revoked certificate cases stay explicit." }
    - { id: "EDGE-AGT-05-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-05; two activations plus restart must expose exact current and LKG bytes." }
    - { id: "EDGE-AGT-06-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-06; every invalid or partial bundle class must prove selected pointers unchanged." }
    - { id: "EDGE-AGT-07-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify AGT-07; every health field and error-redaction behavior stays explicit." }
  edge_resolutions:
    - { id: "EDGE-AGT-04-CONCURRENCY", status: covered, statement: "Concurrent bundle completions on LAB-CLIENT01 cannot roll back, cross-link, or partially replace current/LKG." }
  prohibitions:
    - "flagged-unverified: MUST NOT activate a bundle before exact-byte signature, hash, schema, key, audience, and monotonic-version validation succeeds."
    - "flagged-unverified: MUST NOT place tokens, private keys, raw fingerprints, certificate-secret material, protected content, or sensitive paths in cache, health, logs, evidence, or commits."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Activate signed configuration into a durable, concurrency-safe current/LKG cache on the already enrolled LAB-CLIENT01 endpoint.

Purpose: Isolate the signed-configuration lifecycle from enrollment credential custody and SCM installation so its monotonic activation and restart invariants can be implemented and verified independently.
Output: Completed configuration cache, health state, negative/concurrency tests, and focused LAB-CLIENT01 activation evidence.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Consume the enrolled `AgentHttpClient` and DPAPI-held device identity produced by Plan 01-14. Preserve `SignedConfigurationV1`, strict Ed25519 exact-byte verification, and `HealthSnapshot`; add `ConfigurationCache` and `CachePointers` without changing the enrollment/credential contract.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Activate signed configuration into a durable concurrent-safe current/LKG cache</name>
  <precondition>Plan 01-14 is green; the separate 01-18 privilege-manifest section has a valid digest-bound approval; LAB-CLIENT01 has an active DPAPI credential and LAB-DC01 serves immutable audience-bound signed configurations through device mTLS.</precondition>
  <files>crates/dlp-agent-core/src/lib.rs, crates/dlp-agent-core/src/client.rs, crates/dlp-agent-core/src/config_cache.rs, crates/dlp-agent-core/src/health.rs, crates/dlp-agent-core/tests/enrollment_activation.rs</files>
  <read_first>
    - Every path in `<files>` plus `crates/dlp-crypto/src/lib.rs` and signed DTOs in `crates/dlp-protocol/src/lib.rs`
    - `.planning/docs/adrs/ADR-005-policy-signing.md`
    - `.planning/REQUIREMENTS.md` CRY-02 and AGT-03 through AGT-07
    - Plan 01-14 summary and the research canonical signed-configuration/current-LKG patterns
  </read_first>
  <behavior>
    - Test 1: a higher valid exact-byte bundle is written to an immutable content-addressed staging path, durably flushed, verified, and atomically selected while the prior current becomes LKG.
    - Test 2: unsigned, tampered, wrong-key, unsupported-schema, hash-mismatched, wrong-audience, truncated, equal/lower-version, and interrupted downloads leave current/LKG byte-for-byte unchanged.
    - Test 3: concurrent poll completions select the greatest valid version and cannot cross-link current/LKG or activate a late lower response.
    - Test 4: restart validates current and LKG independently, ignores or quarantines unreferenced staging, and reports exact active bundle/policy state without sensitive diagnostics.
  </behavior>
  <action>Create `ConfigurationCache` as immutable content-addressed bundle files plus small authenticated/versioned pointer records. `AgentHttpClient` polls only through the Plan 01-14 device-mTLS identity. Verify exact bytes with strict Ed25519 before parsing, then validate fixed hash, supported schema, signing key ID, device audience, and strictly increasing numeric version. Flush the bundle and parent directory before atomically replacing pointers. Use one activation mutex plus generation check so concurrent completion order cannot roll back. At startup, authenticate and resolve current/LKG independently, never infer state from filenames or timestamps, and clean unreferenced staging only after selected pointers are safe. Extend health with agent/service/config/drive state, active bundle/policy version, contact time, and stable redacted error code. Per D-33 through D-36, validate the separate 01-18 approval digest before service-data mutation, capture baseline, apply/verify/remove idempotently, clean temporary state, and reject every non-LAB-CLIENT01 target. Per D-25, D-27, D-37, D-40, D-41, and D-43 through D-47, retain portable negative/concurrency attempts separately from LAB-CLIENT01 activation/restart attempts, enforce machine/tier/artifact/time/redaction checks, and update only the corresponding CRY/AGT/TST matrix rows.</action>
  <acceptance_criteria>
    - "On hungdinh-lt source tests, every signed-bundle negative and concurrent case proves active files/pointers remain unchanged or select only the greatest valid version."
    - "On LAB-CLIENT01, two successful activations followed by service-process restart preserve exact newest current and prior LKG bytes and report the active version to LAB-DC01."
    - "On LAB-CLIENT01, partial staging, invalid pointer targets, replay, wrong audience, and late lower responses never become active or alter LKG."
    - "On LAB-CLIENT01 and LAB-DC01, cache, health, log, and evidence output contains no token, private key, raw fingerprint, certificate-secret material, protected content, or sensitive path."
    - "Each bundle class, concurrency race, two-activation sequence, and restart has an immutable schema-valid attempt with prior/supersession links where rerun."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt: cargo test --locked -p dlp-agent-core --test enrollment_activation; cargo clippy --locked -p dlp-agent-core --all-targets -- -D warnings; then inside LAB-CLIENT01 via hungdinh-lt orchestration: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AgentServiceSmoke.ps1 -CallerMachine hungdinh-lt -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -SecretProvider Runtime -Scenario ConfigurationCache</automated>
  </verify>
  <done>LAB-CLIENT01 activates only the newest valid audience-bound configuration and retains exact current/LKG state across concurrency, invalid inputs, and restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LAB-DC01 signed bytes → LAB-CLIENT01 active cache | Tamper, truncation, replay, rollback, audience confusion, and concurrent completion target selected endpoint state. |
| Selected cache → health/evidence | Diagnostics and manifests must identify stable state without leaking protected material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-18-01 | Tampering | configuration cache | high | mitigate | Exact-byte strict signature/hash/schema/key/audience/version gates, immutable staging, durable pointer swap, activation mutex/generation, and restart validation. |
| T-01-18-02 | Spoofing | configuration transport | high | mitigate | Consume only Plan 01-14 device mTLS with ordinary server identity validation and no bearer fallback. |
| T-01-18-03 | Information disclosure | diagnostics/evidence | high | mitigate | Stable codes and digests only, machine-specific redaction scans, and Plan 01-17 publication gates. |
| T-01-18-04 | Elevation/Repudiation | service-data cache changes | high | mitigate | Separate digest-bound 01-18 manifest, LAB-CLIENT01 role guard, baseline/apply/verify/remove, cleanup, and immutable evidence. |
| T-01-18-SC | Tampering | existing dependency graph | high | mitigate | Preserve the approved Cargo.lock sources/versions and run slopcheck before accepting any dependency change; no new package is authorized. |

## ASVS L1 Gate

Any invalid activation, rollback, bearer downgrade, secret-bearing diagnostic, wrong-machine result, or invalid evidence attempt blocks the plan.
</threat_model>

<verification>
Run signed-bundle negative/concurrency source tests on hungdinh-lt and activation/restart scenarios only on LAB-CLIENT01 against LAB-DC01. Validate every result through the Plan 01-17 schema and matrix tier.
</verification>

<success_criteria>
- Exact-byte signed configuration activates monotonically through device mTLS.
- Current/LKG survives concurrency, invalid input, and restart without pointer corruption.
- Health and evidence are complete, redacted, immutable, and machine-correct.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New path | `crates/dlp-agent-core/src/config_cache.rs` | Durable `ConfigurationCache` and `CachePointers` |
| New path | `crates/dlp-agent-core/tests/enrollment_activation.rs` | Trust/cache/concurrency/restart tests |
| Extended symbol | `AgentHttpClient` | Device-mTLS configuration polling |
| Extended symbol | `HealthSnapshot` | Complete redacted service/config/drive/contact report |
| Generated evidence | `evidence/phase1/attempts/<evidence-id>.json` and matrix pointers | Immutable signed-configuration attempts |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-18-SUMMARY.md` when done.
</output>
