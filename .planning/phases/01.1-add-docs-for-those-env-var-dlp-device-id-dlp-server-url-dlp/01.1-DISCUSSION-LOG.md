# Phase 01.1: Endpoint Service Environment-Variable Documentation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-28
**Phase:** 01.1-endpoint-service-environment-variable-documentation
**Areas discussed:** Coverage boundary, Operator journey, Examples and secret handling, Validation and troubleshooting

---

## Coverage Boundary

| Decision | Options considered | Selected |
|---|---|---|
| Documentation depth | Four deep dives; all required; all required and optional | All required and optional |
| Conditional variables | Both automatic and manual; manual only; automatic only | Both automatic and manual |
| Non-endpoint variables | Cross-reference; summary appendix; full inclusion | Cross-reference |
| Authoritative inventory | Service plus deployment script; service only; deployment script only | Service plus deployment script |

**User's choice:** Give every endpoint-service variable equal procedural depth; explain both paths for conditional inputs; link rather than duplicate other variable families; reconcile source and deployment behavior.

---

## Operator Journey

| Decision | Options considered | Selected |
|---|---|---|
| Primary reading path | Setup-first; reference-first; per-variable only | Setup-first |
| Primary audience | Phase 1 lab; generic deployment; parallel workflows | Phase 1 lab |
| Main entry point | Guided initializer; manual PowerShell; deployment orchestrator | Guided initializer |
| Progress structure | Explicit checkpoints; continuous walkthrough; task recipes | Explicit checkpoints |

**User's choice:** Lead the Phase 1 Hyper-V lab operator through `Initialize-DlpEnvironment.ps1`, using collect → validate → persist → deploy → verify checkpoints before the reference catalog.

---

## Examples and Secret Handling

| Decision | Options considered | Selected |
|---|---|---|
| Concrete values | Real topology and synthetic secrets; synthetic everything; exact lab material | Real topology and synthetic secrets |
| Placeholder behavior | Visibly non-runnable; structurally valid fake; mixed | Visibly non-runnable |
| Classification | Separate classifications; treat all as secrets; private-only secrets | Separate classifications |
| Persistence detail | Explain all layers; operator input only; direct persistence commands | Explain all layers |

**User's choice:** Use real non-secret lab configuration with visibly non-runnable secret placeholders, distinguish data classifications, and explain PowerShell, file, and registry persistence ownership.

---

## Validation and Troubleshooting

| Decision | Options considered | Selected |
|---|---|---|
| Validation depth | Redacted copy-paste checks; prose formats; full diagnostic output | Redacted copy-paste checks |
| Failure organization | Error-to-fix matrix; per-variable; workflow-stage | Error-to-fix matrix |
| Verification boundary | Persistence plus running service; config only; full enrollment and polling | Persistence plus running service |
| Exposure response | Stop/remove/rotate/resume; reminder only; external link only | Stop/remove/rotate/resume |

**User's choice:** Give every variable safe validation commands, map stable errors to fixes, verify persistence and service health, and prescribe immediate containment and rotation after suspected exposure.

---

## Agent Discretion

- Exact headings, table columns, prose length, and command formatting may follow established documentation conventions.

## Deferred Ideas

None.
