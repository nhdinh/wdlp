---
phase: 01-first-encrypted-drive-vertical-slice
plan: 35
type: execute
wave: 23
depends_on:
  - 01-34
files_modified:
  - scripts/add-security-closure-review.ps1
  - scripts/verify-phase1-security.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
autonomous: true
gap_closure: true
requirements:
  - CRY-04
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A publisher can attest only when the executing Windows identity and machine exactly match the sole reviewer authorized by policy on D-22's LAB-DC01 trusted provisioning station."
    - "Every byte of every superseded CMS signature is committed by a currently trusted signed archival envelope, so historical signature corruption is detectable."
    - "A manifest-controlled reference cannot escape the repository through a junction, symbolic link, mount point, or other Windows reparse point."
    - "The security verifier restores every caller environment variable it temporarily changes or removes, including on failure, so nested verification preserves caller state (WR-02)."
  artifacts:
    - path: scripts/add-security-closure-review.ps1
      provides: "Observed-context authorization and archival-envelope publication"
    - path: scripts/verify-phase1-security.ps1
      provides: "Historical-signature-envelope and handle-resolved repository-containment validation"
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Wrong-user, wrong-machine, superseded-signature tamper, junction, and symbolic-link regressions"
  key_links:
    - from: scripts/add-security-closure-review.ps1
      to: reviewer policy
      via: "WindowsIdentity.GetCurrent().Name and COMPUTERNAME must match exactly one authorized reviewer before preview, prompt, or mutation"
    - from: evidence/phase1/security-closure.yaml
      to: scripts/verify-phase1-security.ps1
      via: "A trusted archival envelope commits the ordered attestation digests and SHA-256 digests of decoded historical CMS bytes"
    - from: scripts/verify-phase1-security.ps1
      to: referenced repository artifacts
      via: "Hashing uses the same opened file handle whose final resolved Windows path was proved repository-contained"
    - from: scripts/verify-phase1-security.ps1
      to: caller process environment
      via: "A pre-run snapshot is restored from finally after every success and failure path"
---

<objective>
Close CR-05 through CR-07 in the Phase 1 security-evidence boundary without weakening the already passing encrypted-drive slice.

Purpose: Bind publication to the real Windows execution context authorized by policy on D-22's LAB-DC01 trusted provisioning station, authenticate the complete append-only signature history, prevent filesystem-indirection escapes from the repository trust boundary, and close WR-02 caller-state corruption.
Output: Hardened publisher and verifier behavior plus deterministic adversarial regressions ready for a fresh trusted-reviewer ceremony.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-34-SUMMARY.md
@scripts/add-security-closure-review.ps1
@scripts/verify-phase1-security.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
@evidence/phase1/security-closure.yaml
</context>

## Artifacts this phase produces

- `scripts/add-security-closure-review.ps1`: observed Windows reviewer/station authorization and versioned archival-envelope publication.
- `scripts/verify-phase1-security.ps1`: trusted historical-signature envelope validation and handle-based canonical repository containment.
- `scripts/evidence/Phase1.Security.Tests.ps1`: behavioral regressions for the five attack variants named by CR-05 through CR-07.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind one complete review publication to the observed LAB-DC01 execution context</name>
  <files>scripts/add-security-closure-review.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/add-security-closure-review.ps1; scripts/evidence/Phase1.Security.Tests.ps1; evidence-private/phase1/reviewer-policy.json; .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md</read_first>
  <behavior>
    - A policy whose reviewer identity differs from `[Security.Principal.WindowsIdentity]::GetCurrent().Name` exits nonzero with `reviewer_identity_mismatch` before preview, `Read-Host`, signing, temporary-file creation, or manifest mutation.
    - A policy whose reviewer machine differs from `$env:COMPUTERNAME` exits nonzero with `reviewer_machine_mismatch` at the same pre-mutation gate.
    - A policy matching the observed identity, observed machine, selected CurrentUser certificate thumbprint, role, and domain-network identity completes one spawned-process publication and writes those observed identity/machine values into the attestation.
  </behavior>
  <action>
    Before changing `Get-CurrentUserSigner`, preview construction, or the publication loop, run GitNexus upstream impact analysis for every indexed symbol and report direct callers, affected processes, and risk; stop and warn before edits if any result is HIGH or CRITICAL. Add spawned-process RED regressions first, using an ephemeral CurrentUser test certificate and a copied closure manifest. Resolve the selected policy reviewer exactly once, require exactly one thumbprint match, obtain the executing identity from `WindowsIdentity.GetCurrent().Name`, and compare it and `$env:COMPUTERNAME` to the policy reviewer with Windows-appropriate case-insensitive equality before any preview, prompt, mutex acquisition, signature, or filesystem mutation. The approved policy and happy-path fixture must name LAB-DC01 as the station because D-22 assigns LAB-DC01 the trusted provisioning role; D-23 reserves LAB-DC02 for independent secondary-AD corroboration and does not authorize signing there. Do not use `$env:USERNAME`, certificate subject text, or policy-asserted values as observations. Build preview and attestation reviewer identity/machine from the captured observed values; keep policy values only for authorization metadata such as role and domain-network identity. Preserve exact `YES`, payload/predecessor drift detection, CurrentUser private-key custody, atomic replacement, and cleanup from Plan 01-34. This implements D-22, D-23, D-37, D-38, and D-41; D-48 phase-exit review remains a separate verifier action in Plan 01-36. Run GitNexus `detect_changes()` before committing and investigate any flow outside the publisher and its security tests.
  </action>
  <reversibility rating="reversible">The authorization gate and diagnostics can be reverted without changing persisted payload or application data.</reversibility>
  <acceptance_criteria>
    - Wrong-user and wrong-machine cases leave the copied manifest byte-identical and create no publication temporary files.
    - The happy-path fixture proves the attestation contains observed identity and machine values.
    - No code path reaches human affirmation or signing before observed-context authorization succeeds.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Publication</automated></verify>
  <done>SEC-05 is closed for one real end-to-end publication path and protected by non-mutation regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit the complete superseded CMS history with a trusted archival envelope</name>
  <files>scripts/add-security-closure-review.ps1; scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/add-security-closure-review.ps1; scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1; evidence/phase1/security-closure.yaml; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md</read_first>
  <behavior>
    - Signed-off verification rejects a record without the current versioned archival envelope using `historical_signature_envelope_missing`.
    - Flipping one decoded CMS byte in any superseded attestation, while leaving attestation digests and the latest CMS unchanged, yields `historical_signature_digest_mismatch`.
    - Reordering, adding, or removing historical attestations invalidates the archival envelope.
    - A fixture envelope signed by the approved observed-context reviewer over the exact ordered history passes and retains normal latest-attestation trust validation.
  </behavior>
  <action>
    Run GitNexus upstream impact analysis before changing `Body`, `Sign-Body`, `Test-SignedAttestation`, `Test-Record`, or new helpers, with the same HIGH/CRITICAL stop-and-warn gate. Add RED regressions that corrupt a non-latest `signature_cms_base64`, reorder history, and remove one history entry without touching the latest attestation. Introduce a versioned archival-envelope body per record containing the threat ID, payload digest, ordered entries of each attestation digest plus SHA-256 of the decoded CMS bytes, history count, latest attestation digest, reviewer identity, review UTC, procedure version, and environment fingerprint. Canonicalize it with the existing fixed JSON/UTF-8 contract and sign it with detached CMS only after the same observed-context authorization and exact `YES` affirmation used by Task 1. Keep the envelope outside the protected threat payload so adding it does not rewrite mitigation evidence; update the publisher atomically with the attestation and envelope in the same locked manifest generation. In signed-off mode, require one current envelope, verify its CMS with the existing approved signer certificate contract, recompute the ordered history commitment from every stored CMS byte sequence, and reject any mismatch before reporting valid. Continue validating the latest attestation under the full current certificate-purpose, chain, revocation, reviewer-context, and payload contract. Treat the evidence-format extension as an explicit additive schema migration accepted by both old fixture readers and the new signed-off gate until Plan 01-36 performs the ceremony; never silently synthesize an envelope in the verifier. This implements D-37, D-39, D-40, D-41, D-44, D-48, and D-50. Run GitNexus `detect_changes()` before committing.
  </action>
  <reversibility rating="costly">The additive signed evidence-format migration requires a trusted-reviewer ceremony to roll forward and must preserve all prior signatures if later replaced.</reversibility>
  <acceptance_criteria>
    - Every superseded CMS byte sequence contributes to a currently trusted detached signature.
    - Historical signature corruption, deletion, insertion, and reordering each fail with stable diagnostics.
    - Existing attestation entries and their signatures remain byte-for-byte preserved during the migration.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Verifier</automated></verify>
  <done>SEC-06 has executable proof and the publisher can create, but does not fabricate, the required trusted archival envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce handle-contained hashing and preserve caller environment state</name>
  <files>scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1; evidence/phase1/security-closure.yaml; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md</read_first>
  <precondition>Run on Windows where a temporary directory junction and an unprivileged symbolic link can be created and removed; if symbolic-link creation is disabled, enable Windows Developer Mode or run the focused test in an approved elevated session before claiming closure.</precondition>
  <behavior>
    - An in-repository directory junction whose child resolves to an external file is rejected with `reference_reparse_escape` before its bytes are accepted.
    - An in-repository symbolic link to an external file is rejected with the same stable diagnostic.
    - A normal in-repository file is hashed from the same opened handle whose final Windows path was canonicalized and proved to be below the repository root.
    - Replacing a path after resolution cannot redirect the already-opened handle to a different file for hashing.
    - A spawned caller with pre-existing trust-path environment values receives the exact original values after verifier success and after an injected verifier failure.
    - A spawned caller in which those variables were originally absent still observes them absent after either outcome.
  </behavior>
  <action>
    Run GitNexus upstream impact analysis before changing `Resolve-RepositoryReference`, `Get-HashFile`, `Test-Record`, the verifier entry/cleanup path, or new handle helpers, with the same HIGH/CRITICAL warning gate. Add real temporary junction and symbolic-link RED regressions that point from inside a copied repository fixture to distinct external files; assert the stable diagnostic and clean both links in `finally`. Replace lexical-only resolution with a Windows handle-based helper: reject rooted/lexically outside inputs as today, open the target without following a later path lookup, obtain the final normalized path from that exact handle using a narrowly scoped, documented Win32 `GetFinalPathNameByHandleW` boundary, strip the `\\?\` form safely, and require strict repository-root containment with a trailing separator and ordinal-ignore-case comparison. Reject any reparse-resolved outside target and compute SHA-256 from a `FileStream` over the same handle, not by reopening the original path. Preserve missing-file and digest-mismatch diagnostics without exposing external target contents or paths. Keep the interop definition local to this verifier script and clean up every handle deterministically. Close WR-02 in the same RED/GREEN cycle: snapshot both presence and value of every caller environment variable the verifier may set or remove, wrap all mutations and verification work in `try`/`finally`, and in `finally` restore original values or remove variables that were originally absent. Add spawned-process regressions for success and injected failure with both pre-populated and absent variables; assert exact post-run state without weakening the existing secret-cleanup behavior. This implements D-32, D-34, D-37, D-39, D-43, and D-46. Run GitNexus `detect_changes()` before committing.
  </action>
  <reversibility rating="reversible">The verifier-only handle resolver can be replaced without migrating checked-in evidence.</reversibility>
  <acceptance_criteria>
    - Both a real junction escape and a real symbolic-link escape are rejected behaviorally.
    - Accepted file bytes come from the already validated handle, closing the check/use race.
    - Ordinary contained references and all existing lexical escape regressions still behave as specified.
    - WR-02 regressions prove exact caller environment restoration after both success and failure, including original absence.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus Verifier</automated></verify>
  <done>SEC-07 is closed against Windows filesystem indirection and path replacement, and WR-02 is closed with deterministic caller-environment restoration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Windows session -> reviewer policy -> CMS publisher | Policy assertions cannot substitute for the observed executing user or station. |
| Append-only attestation history -> archival envelope | Corruption of any historical signature byte must invalidate signed-off status. |
| Manifest path -> Windows filesystem -> hash input | Junctions, symbolic links, mount points, and races cannot redirect trusted hashing outside the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-35-01 | Spoofing | add-security-closure-review.ps1 | high | mitigate | Compare observed Windows identity and machine to exactly one policy reviewer before prompt or mutation. |
| T-01-35-02 | Repudiation | security-closure attestation history | high | mitigate | Sign an ordered archival envelope committing every attestation digest and decoded CMS-byte digest. |
| T-01-35-03 | Tampering | historical CMS signatures | high | mitigate | Recompute the full history commitment and reject any changed, missing, inserted, or reordered entry. |
| T-01-35-04 | Elevation of Privilege | repository reference resolver | high | mitigate | Resolve and hash through one opened handle whose final Windows path remains repository-contained. |
| T-01-35-05 | Information Disclosure | verifier diagnostics | medium | mitigate | Emit stable codes without exposing external target paths or contents. |
| T-01-35-06 | Tampering | caller process environment | medium | mitigate | Snapshot environment presence and values, then restore them from finally on every exit path. |
</threat_model>

<verification>
- Focused Publication tests pass for observed-context authorization and byte-identical rejection.
- Focused Verifier tests pass for historical CMS tampering and real junction/symbolic-link escapes.
- Focused Verifier tests prove WR-02 caller environment restoration on success and failure.
- The implementation plan intentionally leaves canonical signed-off verification failing on a missing archival envelope until the LAB-DC01 ceremony governed by D-22 in Plan 01-36.
</verification>

<success_criteria>
- All three blocking implementation gaps and WR-02 from `01-VERIFICATION.md` have deterministic source and behavioral-test closure.
- No accepted Phase 1 functional evidence or encrypted-drive implementation is rewritten.
- A fresh approved reviewer can publish the required archive commitment without weakening existing trust checks.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-35-SUMMARY.md` when done.
</output>
