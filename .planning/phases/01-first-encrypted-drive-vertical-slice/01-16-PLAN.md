---
phase: 01-first-encrypted-drive-vertical-slice
plan: 16
type: execute
wave: 5
depends_on: [01-13, 01-14, 01-15, 01-17]
files_modified:
  - tests/e2e/phase1_vertical_slice.rs
  - tests/e2e/compose.rs
  - tests/windows/Invoke-Phase1Matrix.ps1
  - tests/windows/Invoke-AbruptLossHarness.ps1
  - tests/windows/fixtures/manifest.json
  - tests/windows/README.md
  - tests/windows/results/.gitkeep
  - tests/windows/results/phase1-evidence.json
  - tests/windows/results/phase1-evidence.sha256
  - scripts/verify-phase1.ps1
autonomous: true
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, SRV-01, SRV-03, SRV-11, SRV-12, CRY-01, CRY-02, CRY-04, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, DRV-01, DRV-02, DRV-03, DRV-04, DRV-06, DRV-07, DRV-09, TST-01, TST-02, TST-03, TST-05, TST-08]
user_setup:
  - service: phase1-office-and-loss-lab
    why: "D-16 through D-19 need Word/Excel in the LAB-CLIENT01 interactive session and host-controlled hard power-off from hungdinh-lt; evidence remains sanitized and secret-free."
    dashboard_config:
      - task: "Ensure Word and Excel are licensed/launchable for the eligible LAB-CLIENT01 user and hungdinh-lt may turn LAB-CLIENT01 off through Hyper-V without an in-guest shutdown."
        location: "Existing Phase 1 Hyper-V lab"
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-25 through D-28, the final gate requires all portable, focused Hyper-V, signed visual, and four-machine exit tiers assigned to Phase 1; no mapped requirement or success criterion is waived or satisfied by a lower tier."
    - "Per D-29 through D-32, every final row identifies its real provider or permitted fixture scope, and infrastructure/runtime rows reject SQLite, mocks, fakes, callback emulation, host-side endpoint runs, and graceful-shutdown substitutes."
    - "Per D-33 through D-36, all matrix-time elevated actions use the approved 01-16 manifest digest, captured baselines, strict role allowlists, pinned/integrity-checked tools, idempotent apply/verify/remove, and success/failure cleanup."
    - "Per D-37 through D-47, every matrix attempt, signed visual checklist, raw-artifact reference, staleness result, time offset, environment fingerprint, rerun/supersession link, and redaction outcome validates through the Plan 01-17 schema."
    - "Per D-48 through D-50, an independent verifier reviews the complete matrix, deviations, and artifact integrity; deviations remain non-passing by default, and raw-artifact retention/deletion/hold state is recorded."
    - "The complete production-provider path runs on the binding topology: trusted provisioning and PostgreSQL/server on LAB-DC01, independent secondary AD corroboration on LAB-DC02, endpoint service/DPAPI/mTLS/WinFsp/user session on LAB-CLIENT01, and orchestration/source-only checks on hungdinh-lt."
    - "The real LAB-CLIENT01 drive completes enrollment, signed activation, per-user mount, encrypted copy/flush/readback, health, service restart, and Windows reboot while LAB-DC01 uses PostgreSQL migrations and hungdinh-lt contains no endpoint runtime."
    - "D-16 has real LAB-CLIENT01 evidence for Explorer, PowerShell, Notepad, Microsoft Word, and Microsoft Excel with application/runtime/OS provenance."
    - "D-17 has real LAB-CLIENT01 evidence for create, copy in/out, open, edit, save, Save As, rename, move, delete, and directory operations."
    - "D-18 has real LAB-CLIENT01 evidence for 0 B, 1 B, 4 MiB - 1, 4 MiB, 4 MiB + 1, 100 MiB, and 1 GiB with authenticated hashes and non-vacuous backing plaintext scans."
    - "D-19 has distinct evidence for LAB-CLIENT01 service restart, Windows reboot, forced service termination during active write, and hungdinh-lt host-controlled abrupt VM power loss; each recovers old-complete or new-complete only."
    - { statement: "Visible Explorer, Notepad, Word, Excel, sign-in/sign-out, restart, and reboot behavior is confirmed in the LAB-CLIENT01 user session by sanitized machine-generated evidence plus end-of-phase human review.", verification: backstop }
    - { statement: "Abrupt loss is confirmed by a hungdinh-lt Hyper-V hard-off of LAB-CLIENT01 while the guest is between durability steps, not by an in-guest graceful shutdown.", verification: backstop }
  artifacts:
    - path: "tests/e2e/phase1_vertical_slice.rs"
      provides: "Production-provider enrollment-through-mounted-roundtrip integration test"
    - path: "tests/windows/Invoke-Phase1Matrix.ps1"
      provides: "Machine-role-aware application, operation, size, restart, and recovery matrix"
    - path: "tests/windows/Invoke-AbruptLossHarness.ps1"
      provides: "hungdinh-lt host-controlled LAB-CLIENT01 hard-off/boot/recovery orchestration"
    - path: "tests/windows/results/phase1-evidence.json"
      provides: "Sanitized case-level evidence with execution-machine provenance and hashes"
    - path: "scripts/verify-phase1.ps1"
      provides: "Single Phase 1 requirements/decisions/API/edge/prohibition/ASVS/evidence gate"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Authoritative current Phase 1 requirement, success-criterion, decision, tier, checklist, and independent-review index"
  key_links:
    - from: "tests/e2e/phase1_vertical_slice.rs"
      to: "deploy/compose.yaml"
      via: "production PostgreSQL/migration/server providers on LAB-DC01"
      pattern: "Postgres|EnrollmentRequestV1|SignedConfigurationV1"
    - from: "tests/windows/Invoke-Phase1Matrix.ps1"
      to: "tests/windows/results/phase1-evidence.json"
      via: "every D-01 through D-24, requirement, application, operation, size, and fault row records the actual execution machine and sanitized outcome"
      pattern: "execution_machine|requirement_or_decision"
    - from: "scripts/verify-phase1.ps1"
      to: "tests/windows/results/phase1-evidence.json"
      via: "schema, coverage, machine-role, hashes, redaction, backstop, and high-threat checks block acceptance"
      pattern: "LAB-CLIENT01|LAB-DC01|D-24|ASVS"
  flagged_assumptions:
    - { id: "EDGE-TST-01-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-01; matching, priority, equal-priority conflict, defaults, empty input, and deterministic repetition all remain named source evidence on hungdinh-lt." }
    - { id: "EDGE-TST-02-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-02; valid, tampered, wrong-key, weak-key, truncated, schema, key-ID, audience, hash, replay, and concurrency cases remain named evidence." }
    - { id: "EDGE-TST-03-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-03; nonce, AAD, key, corruption, recovery, disk-full, and non-vacuous plaintext searches remain named evidence." }
    - { id: "EDGE-TST-05-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-05; PostgreSQL, AD/WinRM/PKI/TLS production providers, enrollment, signed activation, and negative trust boundaries remain named evidence." }
    - { id: "EDGE-TST-08-UNCLASSIFIED", status: unresolved, flagged: true, statement: "The SPEC-less probe could not classify TST-08; real LAB-CLIENT01 OS/WinFsp/application versions and every required application/operation/size/fault row remain named evidence." }
  prohibitions:
    - "flagged-unverified: MUST NOT mark a PostgreSQL, AD, WinRM, PKI, TLS, DPAPI, WinFsp, service, user-session, Office, restart, reboot, corruption, or abrupt-loss case passed without case-specific evidence from its binding named machine."
    - "flagged-unverified: MUST NOT substitute SQLite, mocks, callback emulation, hungdinh-lt endpoint runs, graceful guest shutdown, or unsigned synthetic rows for the required production-provider and LAB-CLIENT01 evidence."
    - "flagged-unverified: MUST NOT store protected content, raw paths or SIDs, raw hardware serials, tokens, credential blobs, private keys, CA secrets, database credentials, or command-line secret values in the evidence bundle or commit history."
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Seal Phase 1 by running the production-provider vertical slice and complete D-16 through D-19 matrix on the binding machines, then hash-seal sanitized evidence and require one machine-aware acceptance gate.

Purpose: Prove the promised encrypted-drive outcome on LAB-CLIENT01 with LAB-DC01 PostgreSQL/server and LAB-DC02 corroboration, while preventing historical host/SQLite evidence from satisfying runtime requirements.
Output: Production E2E test, Windows/Office/size/operation/restart/abrupt-loss harnesses, sanitized evidence, and final verifier.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-13-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-14-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-01-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-04-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-09-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-10-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@evidence/phase1/requirement-matrix.yaml
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Treat the completed portable/server/storage/recovery/WinFsp contracts and the 01-13 through 01-15 deployment/agent/session contracts as final. The evidence schema contains `case_id`, `requirement_or_decision`, `execution_machine`, environment/version provenance, expected/actual outcome, stable code, source/output SHA-256, backing-marker result, timestamps, and artifact hashes—never protected input bytes or secrets.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the production-provider enrollment-to-restart encrypted roundtrip on the four-machine topology</name>
  <precondition>Plans 01-13 through 01-15 and 01-17 are green; the exact 01-16 privilege-manifest section has a valid digest-bound approval; LAB-DC01 server/PostgreSQL/provisioning, LAB-DC02 directory corroboration, LAB-CLIENT01 endpoint service/WinFsp/user session, and hungdinh-lt Hyper-V orchestration are available through runtime-secure injection.</precondition>
  <files>tests/e2e/phase1_vertical_slice.rs, tests/e2e/compose.rs, tests/windows/Invoke-Phase1Matrix.ps1, tests/windows/results/.gitkeep</files>
  <read_first>
    - Every path in `<files>` plus dependency summaries in `<context>`
    - `.planning/ROADMAP.md` Phase 1 goal and seven success criteria
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-01 through D-24
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md` every INTEGRATE/OPT-OUT capability
  </read_first>
  <behavior>
    - Test 1: LAB-DC01 applies PostgreSQL migrations, performs trusted provisioning through both DCs plus Kerberos WinRM-over-HTTPS to LAB-CLIENT01, stores digest-only authority, and creates one single-use token through runtime-secure handling.
    - Test 2: LAB-CLIENT01 generates the key/CSR, receives and DPAPI-protects the constrained device identity, uses per-request revocation-aware mTLS, activates signed configuration, posts health, and launches one user-session drive host.
    - Test 3: a unique-marker file copied/flushed/read through the LAB-CLIENT01 drive has equal source/output SHA-256 and no marker in backing/cache/log/evidence files.
    - Test 4: LAB-CLIENT01 service restart reloads credential/current/LKG/store, remounts in the active eligible session, and reads the same committed hash; hungdinh-lt remains endpoint-clean.
  </behavior>
  <action>Create a production-provider integration harness that refuses fixture directory, certificate, database, TLS, DPAPI, WinFsp, or session providers. From hungdinh-lt, orchestrate commands into their binding machines: LAB-DC01 runs Compose/PostgreSQL/migrations/server and trusted provisioning; LAB-DC01 and LAB-DC02 independently corroborate the computer; LAB-DC01 collects LAB-CLIENT01 hardware through Kerberos WinRM-over-HTTPS; LAB-CLIENT01 runs endpoint-generated CSR, DPAPI, mTLS, signed activation, service, user-session host, WinFsp, encrypted write/read, and service restart. Generate a random marker only inside LAB-CLIENT01, hash before/after, and scan all backing/cache/log/evidence paths non-vacuously. Add negative trust cases for wrong fingerprint, DC/CIM disagreement, reused/racing token, invalid CSR/profile, revoked prior serial, wrong server identity, bad signed bundle, forged host IPC, corrupt ciphertext, and wrong execution machine. Per D-33 through D-36, validate the exact 01-16 approval digest before any elevated matrix setup, capture baselines, apply only listed role-correct changes, verify/remove idempotently, and clean temporary state after success or failure. Per D-25, D-27, D-29 through D-32, and D-37 through D-47, publish one immutable schema-valid attempt per positive/negative case with tier, machine, build/procedure/environment/time, raw-artifact hashes, staleness, deviation, redaction, and prior-attempt links; capture only allowlisted sanitized IDs, digests, codes, versions, timestamps, and machine provenance.</action>
  <acceptance_criteria>
    - "On LAB-DC01 with LAB-DC02 corroboration, PostgreSQL migrations and production AD/WinRM/PKI/TLS providers complete provisioning/enrollment without fixture providers, raw serial persistence, or server-held device private key."
    - "On LAB-CLIENT01, the automatic service completes DPAPI credential custody, device mTLS, signed activation, user-session WinFsp mount, encrypted flush/readback, health, and service-restart remount."
    - "On LAB-CLIENT01, source/output hashes match and the non-vacuous marker/secret scan is clean across backing/cache/log/evidence; every negative trust case returns no unauthorized state or bytes."
    - "On hungdinh-lt, the role audit still reports developer/orchestration tooling only; its source tests and orchestration logs are not counted as endpoint evidence."
    - "Every positive and negative trust-chain case is a distinct schema-valid current or retained attempt, and no fixture, stale, deviated, inaccessible, hash-mismatched, skewed, unsanitized, or wrong-machine result passes."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt source-only: cargo test --locked --workspace; then on hungdinh-lt orchestrating LAB-DC01, LAB-DC02, and LAB-CLIENT01: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-Phase1Matrix.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -SecretProvider Runtime -Scenario VerticalSlice</automated>
  </verify>
  <done>The binding four-machine topology completes the genuine production enrollment→activation→mount→encrypted roundtrip→service restart path with negative trust boundaries and clean provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the complete LAB-CLIENT01 application, operation, and size matrix</name>
  <precondition>The Task 1 vertical slice is green and Explorer, PowerShell, Notepad, Word, and Excel are launchable for the eligible LAB-CLIENT01 user.</precondition>
  <files>tests/windows/Invoke-Phase1Matrix.ps1, tests/windows/fixtures/manifest.json, tests/windows/README.md, tests/windows/results/phase1-evidence.json, tests/windows/results/phase1-evidence.sha256</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-11 through D-18 and D-21/D-24
    - `.planning/docs/adrs/ADR-001-winfsp-framework.md` validation surface
    - `01-10-SUMMARY.md` callback contracts and `01-15-SUMMARY.md` session-host evidence
  </read_first>
  <action>Define a manifest that covers every named D-16/D-17/D-18 item without omission: Explorer, PowerShell, Notepad, Word, Excel; create, copy in/out, open, edit, save, Save As, rename, move, delete, directory create/enumerate; 0 B, 1 B, 4 MiB - 1, 4 MiB, 4 MiB + 1, 100 MiB, 1 GiB. Run all filesystem/application actions inside the eligible LAB-CLIENT01 user session. Automate PowerShell operations, Shell/Explorer observation, Word/Excel COM with alerts disabled, and deterministic Notepad interaction. Per D-26 and D-38, route only visible drive presence/absence, Explorer, Word/Excel behavior, mount-failure recovery, and restart recovery to an explicit signed checklist embedded in the sanitized evidence bundle with authenticated domain identity, UTC, machine, build, expected behavior, and pass/fail; screenshots are optional. For every case, close/reopen, compare hashes where applicable, perform a non-vacuous backing marker scan, and record LAB-CLIENT01 plus OS/WinFsp/agent/server/migration/application provenance. Record a non-applicable combination only with a specific rationale while still covering each required item. Per D-39 through D-47, commit only sanitized manifests/checklists, keep bulky or sensitive raw artifacts in controlled storage by immutable ID/hash, apply staleness/time/artifact/redaction gates, and preserve every failed/rerun attempt.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, evidence contains every D-16 application, D-17 operation, and D-18 size/chunk boundary at least once with no unexplained missing row."
    - "On LAB-CLIENT01, Word and Excel open/edit/save/Save As/reopen successfully and Explorer/Notepad visible behavior has an end-of-phase human outcome."
    - "On LAB-CLIENT01, every successful file case has source/output hash equality after close/reopen and a clean non-vacuous encrypted-backing marker scan."
    - "On hungdinh-lt, evidence schema/hash/redaction/provenance validation passes but no application/runtime row names hungdinh-lt as its execution machine."
    - "Every required visible row has a signed checklist record; every machine-verifiable row has automated evidence, and neither tier silently substitutes for the other."
  </acceptance_criteria>
  <verify>
    <automated>Inside LAB-CLIENT01 via hungdinh-lt orchestration: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-Phase1Matrix.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -Scenario ApplicationsOperationsSizes; then on hungdinh-lt: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -EvidenceOnly</automated>
    <human-check>On LAB-CLIENT01, the authenticated domain operator reviews the visible drive plus Explorer, Word, Excel, mount-failure, and restart rows and signs the D-38 checklist records with UTC, machine, build, expected behavior, and pass/fail; machine-verifiable Notepad/hash/status facts remain automated.</human-check>
  </verify>
  <done>The complete D-16/D-17/D-18 corpus has real LAB-CLIENT01 evidence, authenticated readback hashes, human-visible application outcomes, and no readable backing marker.</done>
</task>

<task type="auto">
  <name>Task 3: Execute restart, reboot, forced-termination, and host-controlled abrupt-loss recovery and seal Phase 1</name>
  <precondition>The approved 01-16 privilege-manifest digest remains unchanged; hungdinh-lt can hard-power LAB-CLIENT01 off through Hyper-V while an active write barrier is signaled, and LAB-CLIENT01 can resume the evidence collector after boot.</precondition>
  <files>tests/windows/Invoke-Phase1Matrix.ps1, tests/windows/Invoke-AbruptLossHarness.ps1, tests/windows/results/phase1-evidence.json, tests/windows/results/phase1-evidence.sha256, scripts/verify-phase1.ps1</files>
  <read_first>
    - Every path in `<files>` plus completed Task 1-2 evidence
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-12 through D-24
    - `.planning/ROADMAP.md` all Phase 1 success criteria and `.planning/REQUIREMENTS.md` Definition of Done
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md` complete capability matrix
  </read_first>
  <action>Run four distinct D-19 scenarios using a prior committed file and a distinguishable staged replacement: clean service restart inside LAB-CLIENT01; Windows reboot initiated inside LAB-CLIENT01 with registered post-boot continuation; forced LAB-CLIENT01 service/process termination at an injected active-write barrier; and hungdinh-lt `Stop-VM -TurnOff` of LAB-CLIENT01 only after a guest signal confirms execution between durability steps. For abrupt loss, capture host command/time and verify no in-guest graceful shutdown event; reboot LAB-CLIENT01, wait for service/session/drive recovery, then compare old/new hashes and scan for mixed/partial/staging exposure. Implement `verify-phase1.ps1` to validate all 30 requirement IDs, seven success criteria, D-01 through D-50, the 36 SPEC-less edge dispositions, descriptor-less prohibition flags, every COVERAGE INTEGRATE/OPT-OUT row, ASVS L1 high-threat evidence, machine roles, verification tiers, privilege approvals, source/output/evidence hashes, staleness, raw-artifact access, clock policy, redaction, retention/holds, signed visual checklists, and independent review. Per D-41/D-44 every rerun gets a new ID and link; per D-48 an authenticated verifier independent of the individual visual-run operator reviews the complete requirement matrix, provenance, deviations, and artifact integrity; per D-49 deviations stay non-passing unless that verifier documents immateriality, otherwise rerun; per D-50 raw artifacts remain through milestone audit plus the documented window and are securely deleted only after expiry without hold. Fail on any missing, wrong-machine, substitute, failed, stale, deviated, inaccessible, hash-mismatched, skewed, unsigned, unsanitized, unreviewed, or unretained row and re-run the hungdinh-lt endpoint-clean audit.</action>
  <acceptance_criteria>
    - "On LAB-CLIENT01, clean service restart, Windows reboot, and forced mid-write termination are distinct cases and recover only exact old-complete or new-complete hashes after authenticated remount."
    - "On hungdinh-lt, the abrupt-loss case records a host-side hard-off of LAB-CLIENT01 while the guest barrier is active; LAB-CLIENT01 recovery contains no graceful-shutdown substitution or mixed/partial file."
    - "On hungdinh-lt, `scripts/verify-phase1.ps1` accepts only complete machine-correct requirement/decision/edge/API/ASVS/evidence coverage and a clean developer-host audit."
    - "Across all machines, the sealed evidence contains no protected content, raw path/SID/serial, token, credential blob, private key, CA/database secret, or command-line secret value."
    - "The independent verifier signs the complete four-machine exit matrix digest after reviewing current and superseded attempts, deviations, artifact integrity, visual checklists, and retention/hold state."
  </acceptance_criteria>
  <verify>
    <automated>On hungdinh-lt controlling only LAB-CLIENT01 power: powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-AbruptLossHarness.ps1 -CallerMachine hungdinh-lt -EndpointMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -RunAll; then on hungdinh-lt: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01</automated>
    <human-check>An authenticated verifier who did not attest the individual visual runs reviews the final sanitized four-machine matrix on hungdinh-lt, checks provenance/deviations/raw-artifact hashes/visual checklists/retention, and signs the D-48 independent-review record with UTC and matrix digest; any material deviation is rerun.</human-check>
  </verify>
  <done>All D-19 outcomes preserve one authenticated complete generation, the D-01 through D-50 evidence is redacted and hash-sealed, and the final machine-aware verifier, signed visual checklists, independent review, and retention contract pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Four-machine orchestration → evidence rows | A correct behavior on the wrong machine is invalid evidence and can conceal unsafe deployment. |
| Production trust chain → mounted drive | Enrollment, AD/WinRM, PKI, TLS, DPAPI, signed config, session IPC, WinFsp, and store authentication must all connect without fixture substitution. |
| Failure harness → recovery result | Graceful shutdown, timing errors, or incomplete hashes can falsely certify crash consistency. |
| Product/log files → sanitized evidence | Evidence collection can leak credentials, identity data, paths, or protected content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16-01 | Spoofing/Repudiation | machine provenance | high | mitigate | Role-guarded commands and required `execution_machine`; wrong/missing machine fails `verify-phase1.ps1`. |
| T-01-16-02 | Spoofing/Elevation | production trust chain | high | mitigate | No fixture providers; negative fingerprint/DC/CSR/cert/revocation/TLS/bundle/IPC cases across binding machines. |
| T-01-16-03 | Tampering/Information disclosure | AEAD/store result | high | mitigate | Hash equality, unique markers, authenticate before copy, corruption denial, old/new complete invariants, non-vacuous scans. |
| T-01-16-04 | Tampering/Repudiation | abrupt-loss harness | high | mitigate | Guest durability barrier, hungdinh-lt host-side hard-off, no graceful guest event, post-boot hash/provenance capture. |
| T-01-16-05 | Information disclosure | evidence/logs | high | mitigate | Strict sanitized schema, forbidden-field/marker scan, runtime-only secrets, hash-sealed evidence, no protected payload bytes. |
| T-01-16-06 | Denial of service | large/application matrix | medium | mitigate | Bounded manifest, per-case timeout/cleanup, restartable evidence collector, deterministic retry, isolated source/copy-out cleanup. |
| T-01-16-SC | Tampering | dependency/runtime provenance | high | mitigate | Verify the previously approved Cargo.lock graph, WinFsp installer hash/signature, and no newly introduced unaudited package. |
| T-01-16-07 | Repudiation/Tampering | evidence and independent exit review | high | mitigate | Plan 01-17 schema, immutable attempts, tier/machine/substitute gates, staleness/artifact/time/redaction checks, signed visual records, deviation policy, matrix digest, independent review, and retention/hold validation. |
| T-01-16-08 | Elevation | final matrix mutations | high | mitigate | Exact digest-bound 01-16 privilege approval, captured baselines, strict machine allowlist, idempotent apply/verify/remove, cleanup, and pinned/integrity-checked tooling. |

## ASVS L1 Gate

Mitigate every high-severity row before phase completion. Unapproved mutation, wrong-machine evidence, fixture substitution, missing negative trust case, partial/mixed recovery, secret/plaintext marker, stale/deviated/inaccessible/skewed attempt, invalid hash, unsigned checklist, missing independent review, invalid retention state, or unresolved backstop blocks the phase.
</threat_model>

<verification>
Run portable/source tests on hungdinh-lt; PostgreSQL/server/provisioning tests on LAB-DC01 with LAB-DC02 corroboration; every endpoint/service/DPAPI/mTLS/WinFsp/application/restart/reboot operation on LAB-CLIENT01; and only Hyper-V abrupt power control plus final evidence verification on hungdinh-lt. Validate all Plan 01-17 matrix/provenance rules and require the authenticated independent exit review.
</verification>

<success_criteria>
- The production trust/data path passes on the binding four-machine topology.
- Every D-16 application, D-17 operation, D-18 size, and D-19 failure has real machine-correct evidence.
- PostgreSQL—not SQLite—backs deployment verification on LAB-DC01.
- Encrypted backing remains unreadable and failure never releases/mixes unauthenticated plaintext.
- The final verifier, high-threat gate, developer-host cleanup audit, and human backstops all pass.
</success_criteria>

## Artifacts this phase produces

| Kind | Path / symbol / flag | Purpose |
|---|---|---|
| New path | `tests/e2e/phase1_vertical_slice.rs` | Production-provider four-machine vertical-slice test |
| Extended path | `tests/e2e/compose.rs` | Reusable LAB-DC01 PostgreSQL/server lifecycle helper |
| New path | `tests/windows/Invoke-Phase1Matrix.ps1` | Vertical/app/operation/size/restart matrix runner |
| New path | `tests/windows/Invoke-AbruptLossHarness.ps1` | hungdinh-lt host-controlled LAB-CLIENT01 hard-off/recovery runner |
| New path | `tests/windows/fixtures/manifest.json` | Complete D-16/D-17/D-18/D-19 case manifest |
| New path | `tests/windows/README.md` | Machine prerequisites, commands, evidence schema, and redaction rules |
| New path | `tests/windows/results/.gitkeep` | Sanitized evidence directory anchor |
| Generated path | `tests/windows/results/phase1-evidence.json` | Case-level outcomes, hashes, provenance, and backstops |
| Generated path | `tests/windows/results/phase1-evidence.sha256` | Sealed evidence digest |
| New path | `scripts/verify-phase1.ps1` | Final requirements/decision/edge/API/ASVS/evidence gate |
| New flags | `-CallerMachine`, `-ServerMachine`, `-SecondaryDcMachine`, `-EndpointMachine`, `-SecretProvider`, `-Scenario`, `-EvidenceOnly`, `-RunAll` | Explicit machine/provenance and scenario controls |
| Generated contract | `tests/windows/results/phase1-evidence.json` visual checklist and independent-review records | Authenticated D-38 and D-48 attestations bound to the final matrix digest |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-16-SUMMARY.md` when done.
</output>
