---
phase: 01-first-encrypted-drive-vertical-slice
plan: 24
type: execute
wave: 10
depends_on: [01-20]
files_modified:
  - crates/dlp-windows-service/Cargo.toml
  - crates/dlp-windows-service/src/session.rs
  - crates/dlp-windows-service/src/pipe.rs
  - crates/dlp-windows-drive/src/bin/dlp-drive-host.rs
  - crates/dlp-windows-drive/src/filesystem.rs
  - crates/dlp-windows-service/tests/session_lifecycle.rs
  - tests/windows/Invoke-ServiceSessionSmoke.ps1
  - scripts/verify-phase1-evidence.ps1
  - config/lab.phase1.example.yaml
  - evidence/phase1/requirement-matrix.yaml
autonomous: false
requirements: [WRK-04, CRY-01, CRY-04, AGT-01, AGT-07, DRV-01, DRV-02, DRV-03, DRV-04, DRV-09, TST-03, TST-08]
user_setup:
  - service: phase1-session-host-privilege-approval
    why: "D-33 requires a new explicit approval because the existing eight digest-bound manifests do not authorize Plan 01-24 LAB-CLIENT01 mutations."
    dashboard_config:
      - task: "Review and approve the exact 01-24 LAB-CLIENT01 privilege manifest after its digest is generated; approval must use the authenticated domain-operator identity and cannot be auto-advanced."
        location: "Plan 01-17 privilege workflow and config/lab.phase1.example.yaml"
  - service: phase1-session-host-runtime
    why: "D-21, D-24, D-25, and D-27 require the process, pipe, DPAPI/store-key, WinFsp, drive-letter, sign-out, and restart checks to run locally on LAB-CLIENT01."
    dashboard_config:
      - task: "Ensure the approved WinFsp 2.1 runtime, DlpWindowsService, dlp-drive-host.exe, and one eligible interactive domain-user session are available on LAB-CLIENT01."
        location: "Existing Phase 1 Hyper-V lab"
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-07, an eligible LAB-CLIENT01 session logon causes the LocalSystem service to launch exactly one dlp-drive-host.exe with the captured WTS primary token in that same logon session; duplicate notifications do not launch a second host."
    - "Per D-08, the user-session host observes the actual drive namespace, mounts the centrally preferred free letter, or selects the next available letter without displacing an existing mapping."
    - "Per D-09, sign-out first rejects new opens, permits existing handles for the selected 30-second grace, then unmounts, terminates the host, closes actor IPC/process handles, and releases in-memory key material."
    - "Per D-10, launch, authentication, key, and mount failures leave no drive or broken mapping, emit only stable redacted diagnostics, and retry with the existing bounded backoff."
    - "The service creates a real per-actor Windows named pipe, authenticates the connected kernel client PID plus impersonated SID/session against the launched child and actor generation, and sends service-derived store identity/root plus the DPAPI-unwrapped random store key only after authentication succeeds."
    - "The host command line and environment contain no store key, store root, raw SID, or caller-selectable store identity; zero-key store access and unconditional pipe acceptance are absent from the production path."
    - "The host retains ownership of WinFspMountedVolume for the complete control-channel lifetime; pipe disconnect, service stop, host failure, and service restart converge to one clean unmount followed by at most one authenticated remount for each active session."
    - "Per D-21, D-24, D-25, and D-27, LAB-CLIENT01 real-runtime evidence proves launch, pipe rejection, nonzero key use, letter collision, encrypted roundtrip, drain, orphan cleanup, and restart; hungdinh-lt checks remain source-only."
    - "Per D-33 through D-47, LAB mutation starts only after a fresh digest-bound 01-24 approval and every result is role-correct, immutable, artifact-accessible, current, sanitized, and linked to prior blocked 01-16 attempts."
  artifacts:
    - path: "crates/dlp-windows-service/src/session.rs"
      provides: "WTS-token CreateProcessAsUserW launcher, per-session child ownership, retry, drain, and restart convergence"
    - path: "crates/dlp-windows-service/src/pipe.rs"
      provides: "Bounded per-actor named-pipe transport, kernel-backed caller authentication, versioned bootstrap, and control messages"
    - path: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      provides: "Authenticated bootstrap client, real-key encrypted-store open, actual drive selection, retained WinFsp volume, and control loop"
    - path: "crates/dlp-windows-drive/src/filesystem.rs"
      provides: "Drain admission gate and active-handle accounting for D-09"
    - path: "crates/dlp-windows-service/tests/session_lifecycle.rs"
      provides: "Deterministic launch/auth/bootstrap/replay/drain/retry/restart contracts"
    - path: "tests/windows/Invoke-ServiceSessionSmoke.ps1"
      provides: "Machine-guarded real LAB-CLIENT01 secure session-host lifecycle runner"
    - path: "config/lab.phase1.example.yaml"
      provides: "Exact reversible 01-24 LAB-CLIENT01 privilege manifest and digest-bound approval record"
    - path: "evidence/phase1/requirement-matrix.yaml"
      provides: "Current requirement-indexed evidence for the resolved session-host blocker"
  key_links:
    - from: "crates/dlp-windows-service/src/session.rs"
      to: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      via: "WTSQueryUserToken followed by CreateEnvironmentBlock and CreateProcessAsUserW with the configured absolute executable path and actor-only command arguments"
      pattern: "WTSQueryUserToken|CreateEnvironmentBlock|CreateProcessAsUserW"
    - from: "crates/dlp-windows-service/src/pipe.rs"
      to: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      via: "authenticated bounded bootstrap/control exchange keyed to child PID, token SID/session, and actor generation"
      pattern: "CreateNamedPipe|GetNamedPipeClientProcessId|ImpersonateNamedPipeClient|TokenSessionId"
    - from: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      to: "crates/dlp-windows-drive/src/filesystem.rs"
      via: "service-derived CapturedStoreIdentity and StoreKey construct the filesystem context; drain control closes its admission gate"
      pattern: "CapturedStoreIdentity|StoreKey|begin_drain"
    - from: "crates/dlp-windows-drive/src/bin/dlp-drive-host.rs"
      to: "crates/dlp-windows-drive/src/host.rs"
      via: "the selected user-session letter starts WinFsp and the returned WinFspMountedVolume remains owned until stop/drain/pipe EOF"
      pattern: "WinFspMountHost|WinFspMountedVolume|unmount"
    - from: "tests/windows/Invoke-ServiceSessionSmoke.ps1"
      to: "evidence/phase1/requirement-matrix.yaml"
      via: "Plan 01-17 schema validation and publication of real LAB-CLIENT01 attempts"
      pattern: "SecureSessionHostLifecycle|Publish-Phase1Evidence"
---

## Phase Goal

**As a** Windows user authorized by the organization, **I want to** mount a private protected drive and store and read files through it, **so that** its backing store contains no directly readable plaintext and committed data survives the required restarts and failures.

<objective>
Close the blocker recorded during Plan 01-16 by completing the production session-host launch, authenticated bootstrap/key handoff, mounted-volume lifetime, drive-letter, drain, cleanup, and restart path per D-07 through D-10.

Purpose: Make the real LAB-CLIENT01 protected drive exist through the service-controlled user-session path so Plan 01-16 can resume its production vertical slice and D-16 through D-18 matrix.
Output: Production Windows session-host lifecycle implementation, deterministic source tests, fresh privilege approval, and real-runtime evidence.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/COVERAGE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/deferred-items.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-20-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-16-PLAN.md
@config/lab.phase1.example.yaml
@evidence/phase1/schema/evidence-manifest.schema.json
@evidence/phase1/requirement-matrix.yaml
@scripts/evidence/Phase1.Evidence.psm1

<interfaces>
Current implementation seams to preserve while replacing the recorded compile-only paths:

- `SessionMonitor::session_logon(&mut self, session_id: u32) -> Result<MountActor, SessionError>` is called by the existing dedicated session thread in `service.rs`; keep duplicate logon idempotency and mount snapshots compatible.
- `WtsSessionTokenProvider::primary_token` already returns `PrimaryToken` plus `UserSid` derived by `WTSQueryUserToken` and `GetTokenInformation(TokenUser)`.
- `DpapiStoreKeyProvider::load_store_key(&CapturedStoreIdentity) -> Result<StoreKey, StorageError>` is the service-owned source of the real random per-store key.
- `StoragePipeServer::{bind, validate_request, decode_request, close}` currently has no Windows pipe handle or impersonation; replace this seam without trusting request identity fields.
- `DlpFileSystemContext::new(CapturedStoreIdentity, LocalEncryptedStore)` enforces matching store identity; preserve it and add a shared admission/handle-lifetime control without changing existing callback durability/status mappings.
- `WinFspMountHost::start(DlpFileSystemContext) -> Result<WinFspMountedVolume, MountError>` returns the owned volume whose `MountedVolume::unmount(self)` performs unmount and stop.
- `run_windows_service` already dispatches WTS logon/logoff and reconciles active sessions after restart; do not introduce a second SCM/session dispatcher.

GitNexus impact evidence: `SessionMonitor::session_logon` has MEDIUM upstream risk with five direct dependents in the current index; the aggregate monitor, pipe, mount-host, and filesystem-context nodes report LOW risk, but the index is five commits stale because local FTS repair failed. Treat direct source/test callers as authoritative and run the full named crate suites.
</interfaces>
</context>

## Source Coverage Audit

| Source | ID | Required item | Plan/task | Status |
|---|---|---|---|---|
| GOAL | Phase 1 | Authorized user receives the real encrypted drive and committed state survives service restart | 01-24 Tasks 1-3, then 01-16 | COVERED |
| REQ | WRK-04 | Keep unavoidable process/pipe/token FFI isolated and documented in Windows crates | Task 1 | COVERED |
| REQ | CRY-01, CRY-04 | Use the DPAPI-unwrapped random store key without plaintext persistence or secret-bearing argv/env/evidence | Tasks 1, 3 | COVERED |
| REQ | AGT-01, AGT-07 | Automatic service owns session launch/recovery and reports redacted live drive state | Tasks 1-3 | COVERED |
| REQ | DRV-01, DRV-03, DRV-04 | Service-captured SID/session selects exactly one encrypted store; pipe clients cannot select identity/store | Tasks 1-3 | COVERED |
| REQ | DRV-02 | User-session preferred/next-free WinFsp drive selection | Task 2 | COVERED |
| REQ | DRV-09 | Host/service stop and restart cleanly unmount and remount committed authenticated state | Tasks 2, 3 | COVERED |
| REQ | TST-03, TST-08 | Key/IPC negative tests plus real LAB-CLIENT01 WinFsp lifecycle evidence | Tasks 1-3 | COVERED |
| RESEARCH | Session launch | WTS primary token -> CreateProcessAsUserW in the captured interactive session | Task 1 | COVERED |
| RESEARCH | IPC/key custody | Per-actor named pipe validates SID/session/PID/generation before real-key/store bootstrap | Task 1 | COVERED |
| RESEARCH | Letter/lifecycle | Select inside the user namespace; 30-second drain; capped retry; host owns mounted volume | Task 2 | COVERED |
| CONTEXT | D-07, D-08 | Automatic eligible-user mount and preferred/next-free letter | Tasks 1-3 | COVERED |
| CONTEXT | D-09, D-10 | Reject-new-open drain, cleanup, absent-on-failure, retry, and diagnostic behavior | Tasks 2, 3 | COVERED |
| CONTEXT | D-20, D-21, D-24 | hungdinh-lt source-only; all endpoint runtime proof on LAB-CLIENT01 | Task 3 | COVERED |
| CONTEXT | D-25, D-26, D-27, D-32 | Focused Hyper-V plus narrowly scoped visual evidence; no substitute runtime | Task 3 | COVERED |
| CONTEXT | D-33 through D-36 | Fresh exact privilege manifest, baseline, idempotent apply/remove, cleanup, and role guard | Task 3 | COVERED |
| CONTEXT | D-37 through D-47 | Immutable schema-valid provenance, staleness, time, redaction, artifact, and supersession rules | Task 3 | COVERED |
| CONTEXT | Deferred Ideas | No CONTEXT.md deferred idea applies | n/a | EXCLUDED |
| BLOCKER | deferred-items.md | Missing launch, authenticated IPC/key handoff, and real user-session mount lifecycle | Tasks 1-3 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Launch one authenticated session host through a real service-owned bootstrap</name>
  <files>crates/dlp-windows-service/Cargo.toml, crates/dlp-windows-service/src/session.rs, crates/dlp-windows-service/src/pipe.rs, crates/dlp-windows-drive/src/bin/dlp-drive-host.rs, crates/dlp-windows-service/tests/session_lifecycle.rs</files>
  <read_first>
    - Every path in `<files>` before modification
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md` Known Stubs and Threat Flags
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/deferred-items.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md` resolved per-session lifecycle and Security Domain sections
    - `crates/dlp-windows-service/src/service.rs` existing session-thread and active-session reconciliation calls
    - `crates/dlp-storage/src/lib.rs` exported `CapturedStoreIdentity`, `StoreKey`, and `LocalEncryptedStore` contracts
  </read_first>
  <behavior>
    - Test 1: one eligible logon uses the WTS primary token to create exactly one child in the requested session; duplicate logon is idempotent and a launch failure records Failed without a mount.
    - Test 2: the service creates the actor pipe before launch, and wrong protocol, wrong child PID, wrong impersonated SID, wrong token session, stale generation, second connection, oversized frame, and timeout are denied before any identity/root/key bytes are sent.
    - Test 3: the authenticated child receives a service-derived identity/root and the nonzero key returned by `DpapiStoreKeyProvider`; argv and environment contain only the executable, pipe endpoint, session ID, and actor generation.
    - Test 4: the child opens `LocalEncryptedStore` with that bootstrap, mounts the free preferred letter, reports Mounted, and retains the returned volume while the authenticated control channel remains open.
  </behavior>
  <action>Write the failing launcher, transport, and bootstrap tests first, using injected `SessionHostLauncher`/pipe seams on hungdinh-lt and Windows-only tests for kernel identity APIs. Add the `Win32_System_Environment` feature to the existing pinned `windows = 0.62.2` dependency; add no package. In `session.rs`, keep `WTSQueryUserToken` as the sole production identity source and implement a Windows launcher that validates the configured host path is an absolute regular file at the approved install location, prepares the target-user Unicode environment with `CreateEnvironmentBlock`, supplies the executable explicitly to `CreateProcessAsUserW`, uses `winsta0\default`, disables handle inheritance, and retains the returned process handle/PID. Do not invoke a shell or pass SID, store ID/root, key, token, certificate, or secret through argv/environment. In `pipe.rs`, replace the directory-only bind with one unpredictable per-actor `\\.\pipe\...` instance created before launch, `FILE_FLAG_FIRST_PIPE_INSTANCE`, a DACL limited to LocalSystem and the captured user, bounded length-framed messages, one accepted bootstrap, and a retained control connection. Authenticate after a successful client read by checking `GetNamedPipeClientProcessId` against the retained child PID, impersonating the client, reading `TokenUser` plus `TokenSessionId`, always calling `RevertToSelf`, and matching SID/session/generation; fail closed on every API or comparison error. Only then load the real DPAPI store key and send protocol version, service-derived identity/store root, preferred letter, generation, and key. Zeroize transient server/client frame and key buffers after constructing `StoreKey`; never format them with Debug. In `dlp-drive-host.rs`, remove caller-supplied identity/root inputs, connect with identification/impersonation SQOS, validate the bounded bootstrap, open `LocalEncryptedStore` with the received nonzero key and identity, start WinFsp, acknowledge the chosen letter, retain `WinFspMountedVolume`, and block in the control loop rather than returning from `main`. Preserve the existing stable diagnostic-code style and no second host for the same `(session_id, SID)` tuple per D-07.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command { if ($env:COMPUTERNAME -ne 'hungdinh-lt') { throw 'Task 1 source tests must run on hungdinh-lt' }; cargo test --locked -p dlp-windows-service --test session_lifecycle session_host_; if ($LASTEXITCODE -ne 0) { throw 'session-host tracer tests failed' }; cargo test --locked -p dlp-windows-service pipe::tests; if ($LASTEXITCODE -ne 0) { throw 'pipe tests failed' }; cargo build --locked -p dlp-windows-service -p dlp-windows-drive --all-targets; if ($LASTEXITCODE -ne 0) { throw 'session-host binaries failed to build' } }</automated>
  </verify>
  <done>A source-tested logon reaches a real CreateProcessAsUserW child, kernel-authenticated actor pipe, service-derived nonzero store bootstrap, preferred-letter WinFsp mount, and retained control loop with no secret-bearing process inputs per D-07.</done>
  <reversibility rating="costly">The versioned service/host bootstrap and control contract spans two deployed executables; changing it requires a coordinated binary rollout, so retain explicit version rejection and compatible diagnostics.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete drive collision, drain, cleanup, retry, and restart convergence</name>
  <files>crates/dlp-windows-service/src/session.rs, crates/dlp-windows-drive/src/bin/dlp-drive-host.rs, crates/dlp-windows-drive/src/filesystem.rs, crates/dlp-windows-service/tests/session_lifecycle.rs</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `crates/dlp-windows-drive/src/host.rs` ownership of `WinFspMountedVolume`
    - `crates/dlp-windows-drive/tests/mounted_smoke.rs` current real-mount/restart contracts
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-08 through D-10 and D-19
  </read_first>
  <behavior>
    - Test 1: the host reads the actual `GetLogicalDrives` mask in its own logon session, chooses preferred then the next free C-Z candidate, leaves occupied mappings untouched, and reports a bounded failure when none is free.
    - Test 2: BeginDrain atomically rejects later open/create callbacks while already-open handles remain counted; zero handles unmount immediately, otherwise unmount occurs at the 30-second deadline.
    - Test 3: pipe EOF, service stop, host crash, failed mount, authentication failure, and drain timeout close process/pipe handles, unmount any owned volume, zeroize transient key material, and leave no actor marked Mounted.
    - Test 4: a failed launch or mount follows the existing 1-to-300-second capped retry; success resets it; service restart reconciles an active session to one new child while an old disconnected child exits instead of becoming an orphan.
  </behavior>
  <action>Add an `Arc`-backed admission/handle counter to `DlpFileSystemContext` while preserving `new` for existing callers; the host-specific constructor shares the admission control with the control loop. `open` and `create` must check admission before store access, successful file/directory handles increment the count, and `close` decrements exactly once. In the user-session host, call `GetLogicalDrives` there—not in LocalSystem—normalize the configured preferred letter, order C-Z as preferred then subsequent candidates with wraparound, and mount only a candidate absent from the returned bitmask; if a candidate becomes occupied before mount, fail the attempt cleanly and let the bounded actor retry recompute the mask. Keep the mounted-volume value alive across all control reads. On BeginDrain, close admission, wait for the active-handle count until zero or the configured 30-second deadline, unmount exactly once, acknowledge the outcome, and exit. On pipe EOF or explicit Stop, unmount and exit without waiting indefinitely. In `SessionMonitor`, retain child/pipe runtime ownership separate from cloneable health snapshots, observe child exit, clear Mounted state, close all handles, and schedule retry only while the captured session remains eligible. Logoff cancels retry, sends BeginDrain, waits the configured bound, then terminates only the retained child if it did not exit; `stop_all` applies the same cleanup to every actor. Startup reconciliation must tolerate an old host closing after its service pipe disappears and create at most one authenticated replacement. Emit stable redacted failure codes only, with no SID, pipe name, store path, key, command line, or plaintext in health/log output, satisfying D-08 through D-10.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command { if ($env:COMPUTERNAME -ne 'hungdinh-lt') { throw 'Task 2 source tests must run on hungdinh-lt' }; cargo test --locked -p dlp-windows-service --test session_lifecycle; if ($LASTEXITCODE -ne 0) { throw 'session lifecycle tests failed' }; cargo test --locked -p dlp-windows-drive --lib; if ($LASTEXITCODE -ne 0) { throw 'drive admission tests failed' }; cargo clippy --locked -p dlp-windows-service -p dlp-windows-drive --all-targets -- -D warnings; if ($LASTEXITCODE -ne 0) { throw 'session-host clippy failed' } }</automated>
  </verify>
  <done>Actual user-session letter occupancy, reject-new-open drain, grace-period unmount, orphan-free cleanup, bounded retry, and one-host restart reconciliation are deterministic and source-tested per D-08 through D-10.</done>
  <reversibility rating="reversible">Admission, retry, and cleanup state remain internal to the two Windows crates and can be changed without persisted-format or server-protocol migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the secure session-host lifecycle on real LAB-CLIENT01</name>
  <precondition>The new exact 01-24 LAB-CLIENT01 privilege manifest has a schema-valid digest-bound approval from an authenticated domain operator; the approved WinFsp runtime, current binaries, automatic LocalSystem service, and eligible interactive user are available locally on LAB-CLIENT01.</precondition>
  <files>tests/windows/Invoke-ServiceSessionSmoke.ps1, scripts/verify-phase1-evidence.ps1, config/lab.phase1.example.yaml, evidence/phase1/requirement-matrix.yaml</files>
  <read_first>
    - Every path in `<files>` before modification
    - `scripts/evidence/Phase1.Evidence.psm1` validation/publication functions
    - `evidence/phase1/schema/evidence-manifest.schema.json`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md` evidence and privilege contracts
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-16-PLAN.md` Task 1 precondition and production roundtrip expectations
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/deferred-items.md` recorded blocker
  </read_first>
  <behavior>
    - Test 1: the real automatic service launches one child whose token session matches the eligible WTS session, whose image equals the approved host binary, and whose command line contains only non-secret actor rendezvous fields.
    - Test 2: same-user wrong-PID, LocalSystem/wrong-SID, stale-generation, replay, oversized, and malformed pipe probes receive no bootstrap/key/store data; the launched child completes the authenticated nonzero-key handshake.
    - Test 3: preferred-free and preferred-occupied runs mount the correct user-visible letter, keep the occupied mapping unchanged, roundtrip an unpredictable marker, and find no marker/key/bootstrap bytes in backing, argv, logs, or evidence.
    - Test 4: actual sign-out plus sign-in, forced host termination, service stop/start, and service restart leave no orphan pipe/process/mount and recover exactly one authenticated drive with the prior committed hash.
  </behavior>
  <action>Before any mutation, add the exact 01-24 manifest to `config/lab.phase1.example.yaml`, compute its approval digest through the Plan 01-17 tooling, and stop at a blocking human decision until an authenticated domain operator approves that exact digest. The manifest is LAB-CLIENT01-only and lists: query WTS/process/token state; start/restart the existing DlpWindowsService; launch/terminate only the approved dlp-drive-host.exe; create/remove only per-actor DLP named pipes and DACLs; mount/unmount only the selected DLP WinFsp letter; temporarily create/remove one `subst` mapping on the preferred test letter; stage/resume one real sign-out/sign-in check; and publish sanitized evidence. It declares no install, trust, hosts, domain, network, database, or reboot change; expected final state is the pre-existing automatic service running, exactly one host/mount for the active eligible session, no test mapping, pipe, task, or plaintext artifact, and no reboot required. Extend `Invoke-ServiceSessionSmoke.ps1` with `SecureSessionHostLifecycle`, guarded so endpoint actions execute only when both the declared and actual computer are LAB-CLIENT01. Capture baseline service/process/mount/pipe/mapping state; run free-letter mount and encrypted hash roundtrip; exercise wrong-PID, wrong-token identity, stale-generation, replay, malformed, and oversized probes without exposing bootstrap bytes; occupy the preferred letter, recycle the actor, prove next-free selection and occupant preservation; remove the occupant and prove preferred-letter recovery; force-kill the host and restart the service; stage a RunOnce or scheduled continuation around a real user sign-out/sign-in; and prove exactly one host/mount plus the prior committed hash after each recovery. Assert the host process session/token, executable hash, parent service PID, no secret-bearing argv, real nonzero-key store behavior, control-loop lifetime, reject-new-open/drain outcome, child/pipe handle cleanup, and no plaintext marker in backing/log/evidence paths. Use `try/finally` cleanup after success or failure. Publish separate immutable attempts for positive and negative cases with the Plan 01-17 module, link the prior 01-16 blocker, and update only current DRV-01/02/03/04/09, CRY-01/04, AGT-01/07, TST-03/08 rows after schema, machine, artifact, staleness, clock, deviation, redaction, and digest validation. Extend `verify-phase1-evidence.ps1` with a `SecureSessionHostLifecycle` scenario that rejects source-only, skipped, wrong-machine, stale, inaccessible, or secret-bearing attempts and proves the old source-only stub patterns are gone.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command { if ($env:COMPUTERNAME -ne 'LAB-CLIENT01') { throw 'SecureSessionHostLifecycle must execute locally on LAB-CLIENT01' }; powershell -NoProfile -ExecutionPolicy Bypass -File tests/windows/Invoke-ServiceSessionSmoke.ps1 -CallerMachine LAB-CLIENT01 -ExecutionMachine LAB-CLIENT01 -ServerMachine LAB-DC01 -Scenario SecureSessionHostLifecycle; if ($LASTEXITCODE -ne 0) { throw 'real secure session-host lifecycle failed' } }; powershell -NoProfile -ExecutionPolicy Bypass -Command { if ($env:COMPUTERNAME -ne 'hungdinh-lt') { throw 'evidence publication check must run on hungdinh-lt after sanitized artifacts are synchronized' }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario SecureSessionHostLifecycle; if ($LASTEXITCODE -ne 0) { throw 'secure session-host evidence verification failed' } }</automated>
    <human-check>On LAB-CLIENT01, the authenticated domain operator signs the D-26 checklist confirming the protected drive is visible only in the eligible user session, the occupied preferred letter is not displaced, the drive disappears at sign-out, and exactly one healthy drive returns after sign-in/service restart. The checklist must reference the automated attempt IDs and reveal no path, SID, key, or protected content.</human-check>
  </verify>
  <done>A freshly approved, real LAB-CLIENT01 run resolves the 01-16 blocker with authenticated child launch, real-key encrypted mount, safe letter fallback, sign-out cleanup, host/service restart recovery, negative pipe isolation, and current sanitized evidence per D-07 through D-10 and D-20 through D-47.</done>
  <reversibility rating="reversible">All privileged test mutations are LAB-CLIENT01-local and have explicit remove/restore steps; intended persistent state is only the already-approved running service and healthy per-session mount.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LocalSystem service -> user-session child | A privileged service crosses into an unprivileged interactive token and must launch only the approved image/session with no secret-bearing process inputs. |
| Named-pipe client -> service key/store authority | Any local process can attempt connection; no identity, store, root, or key data may cross until kernel-backed client identity matches the retained actor. |
| Service bootstrap -> host memory -> encrypted store | The real store key is exposed only in authenticated volatile memory and must not reach disk, argv, environment, logs, evidence, or unrelated processes. |
| User-session namespace -> WinFsp mount | Drive letters are per-logon-session; selection and collision handling must occur in the host's actual session. |
| WTS/SCM lifecycle -> process/pipe/mount cleanup | Logoff, service stop, host crash, timeout, and restart must not leave an orphan process, pipe, key, or mount. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-24-01 | Spoofing/Elevation | actor named pipe | critical | mitigate | First-instance per-actor pipe, restrictive DACL, client PID from the pipe handle, retained child PID/handle comparison, impersonated TokenUser/TokenSessionId comparison, actor generation, one accepted bootstrap, and fail-closed RevertToSelf handling. |
| T-01-24-02 | Information disclosure | key/store bootstrap | critical | mitigate | Send service-derived identity/root and DPAPI-unwrapped random key only after T-01-24-01 succeeds; bounded frames, transient zeroization, no Debug, and secret scans of argv/env/log/evidence/backing outputs. |
| T-01-24-03 | Elevation | identity/store selection | high | mitigate | Derive SID/session from WTS token and StoreId/root/key inside the service; host request contains no caller-selected SID/store/root fields and every mismatch terminates the child. |
| T-01-24-04 | Tampering/Replay | bootstrap/control protocol | high | mitigate | Explicit protocol version, actor generation, unpredictable per-actor endpoint, single-use bootstrap, stale/replay rejection, retained authenticated control connection, and incompatible-version denial. |
| T-01-24-05 | Elevation | CreateProcessAsUserW | high | mitigate | Absolute approved executable via lpApplicationName, mutable bounded command line, target-user Unicode environment, no shell/handle inheritance, captured primary token, retained process handle, and stable failure diagnostics. |
| T-01-24-06 | Denial of service | pipe connect/mount/retry | medium | mitigate | Bounded connection/frame/ready timeouts, no drive on failure, occupied-letter preservation, 1-to-300-second capped retry, and retry cancellation on logoff. |
| T-01-24-07 | Tampering/Denial of service | drain and mounted-volume ownership | high | mitigate | Shared admission gate, exact active-handle count, 30-second deadline, one owned WinFspMountedVolume, exactly-once unmount, child termination only after timeout, and state cleared before retry. |
| T-01-24-08 | Repudiation/Elevation | privileged LAB execution/evidence | high | mitigate | Fresh digest-bound 01-24 approval, LAB-CLIENT01 guard, baseline, exact mutation allowlist, try/finally cleanup, immutable attempts, artifact hashes, time/skew, staleness, redaction, and signed visible checklist. |

## ASVS L1 Gate

Any secret-bearing process input, unauthenticated bootstrap, caller-selected store identity, stale/replayed generation, wrong-session host, displaced drive mapping, unbounded drain, orphan process/pipe/mount, skipped LAB runtime, unapproved mutation, or invalid evidence blocks Plan 01-24 and keeps Plan 01-16 blocked.
</threat_model>

<verification>
Run launcher/pipe/drain/retry source suites and clippy on hungdinh-lt. After the fresh non-auto-approvable D-33 checkpoint, deploy and run the complete lifecycle locally on LAB-CLIENT01, publish through Plan 01-17, synchronize sanitized artifacts, and validate them on hungdinh-lt. Re-run the real lifecycle after service restart and actual sign-out/sign-in; no mocked, skipped, remote, or developer-host runtime result can satisfy the plan.
</verification>

<success_criteria>
- One eligible LAB-CLIENT01 session produces one same-session host and a real encrypted WinFsp drive through CreateProcessAsUserW.
- Only the retained child receives the real service-derived identity/root/key bootstrap; all forged, stale, replayed, malformed, and oversized pipe clients fail before secret or storage access.
- The mounted volume lives until authenticated drain/stop/EOF and converges to clean unmount, retry, and one-host remount behavior.
- Preferred/next-free drive selection uses the actual user namespace and never displaces an occupied letter.
- Fresh approved runtime evidence resolves `deferred-items.md` and makes the 01-16 Task 1 precondition true without altering its partial commits.
</success_criteria>

## Privilege Manifest / Blocking Approval Contract

The executor must materialize this exact list as the `01-24` manifest in `config/lab.phase1.example.yaml`, compute its digest through Plan 01-17, and obtain a fresh authenticated-domain-operator approval before Task 3 mutates LAB-CLIENT01. The prior approval set covers only Plans 01-13, 01-14, 01-18, 01-19, 01-15, 01-20, 01-16, and 01-21; it does not authorize this plan.

| Field | Required value |
|---|---|
| Machine/role | LAB-CLIENT01 / endpoint-runtime only |
| Baseline | DlpWindowsService state/start mode; eligible WTS sessions; dlp-drive-host PIDs/session IDs/image hashes; DLP pipe names; logical/mapped drives; WinFsp mounts; test scheduled tasks/RunOnce entries |
| Allowed mutation | Start/restart existing DlpWindowsService; launch/terminate approved dlp-drive-host via service; create/remove per-actor DLP pipe/DACL; mount/unmount DLP WinFsp letter; create/remove one temporary preferred-letter `subst`; stage/remove one sign-out continuation; write sanitized evidence |
| Forbidden mutation | Package/runtime install; certificate/trust/hosts/domain/network/database changes; non-DLP process termination; unrelated drive mappings; reboot |
| Expected final state | Existing automatic service running; one approved host/mount for the active eligible session; no temporary mapping/task/RunOnce entry; no orphan pipe/process/mount; no plaintext or secret artifact |
| Cleanup | `try/finally` removes test mapping and continuation; stops test child; closes test pipes/handles; unmounts test volume; restores baseline service state on failure before a clean approved restart |
| Reboot | None |
| Resume signal | Explicit approval of the generated 01-24 manifest digest by authenticated domain identity; auto-advance is ignored |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-24-SUMMARY.md` when done.
</output>
