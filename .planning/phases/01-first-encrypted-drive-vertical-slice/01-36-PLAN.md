---
phase: 01-first-encrypted-drive-vertical-slice
plan: 36
type: execute
wave: 24
depends_on:
  - 01-35
files_modified:
  - evidence/phase1/security-closure.yaml
  - evidence/phase1/requirement-matrix.yaml
  - tests/windows/results/phase1-evidence.json
  - tests/windows/results/phase1-evidence.sha256
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
autonomous: false
gap_closure: true
requirements:
  - CRY-04
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low
user_setup:
  - service: phase1-reviewer-pki
    why: "The additive archival envelope must be affirmed and signed by the policy-authorized reviewer on D-22's LAB-DC01 trusted provisioning station."
    env_vars:
      - name: PHASE1_TRUSTED_ROOT_PATH
        source: "Public reviewer root at evidence-private/phase1/reviewer-root.cer on hungdinh-lt."
      - name: PHASE1_REVIEWER_POLICY_PATH
        source: "Approved public reviewer policy at evidence-private/phase1/reviewer-policy.json on hungdinh-lt and LAB-DC01; its station must be LAB-DC01 per D-22."
    dashboard_config:
      - task: "Sign in interactively on LAB-DC01 as the policy-authorized archival-envelope signer and confirm the approved CurrentUser signing certificate and private key are present; do not export the private key."
        location: "LAB-DC01 trusted provisioning and signing station under D-22"
      - task: "Arrange a fresh authenticated D-48 phase-exit verifier who is not the archival-envelope signer and who will review only after the ceremony, gates, manifest, matrix, and audit records are final."
        location: "Authenticated independent-review workflow; verifier identity must differ from the Task 1 signer identity"
must_haves:
  truths:
    - "The authorized observed LAB reviewer on LAB-DC01 affirms and signs the exact archival commitment for all 19 current Phase 1 closure records per D-22; LAB-DC02 remains only the D-23 secondary AD authority."
    - "The full security regression suite, signed-off verifier, and canonical FinalGate pass against the same manifest and reviewer-policy identities."
    - "Append-only security and review records preserve every failed and superseded interval while recording closure of CR-05 through CR-07."
    - "After every ceremony and gate artifact is final, a D-48 verifier distinct from the archival-envelope signer reviews the complete current matrix, provenance, deviations, artifact integrity, manifest/policy identities, and signs the exact final evidence digest before Phase 1 is declared complete."
  artifacts:
    - path: evidence/phase1/security-closure.yaml
      provides: "Observed-context signed archival envelopes over all historical CMS bytes"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      provides: "Append-only security closure evidence"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
      provides: "Append-only resolution of CR-05 through CR-07"
    - path: tests/windows/results/phase1-evidence.json
      provides: "Fresh signed D-48 independent-review record bound to the final matrix digest and current manifest/policy identities"
    - path: tests/windows/results/phase1-evidence.sha256
      provides: "Digest sealing the final post-ceremony evidence reviewed under D-48"
  key_links:
    - from: LAB-DC01 reviewer session
      to: evidence/phase1/security-closure.yaml
      via: "Exact-YES affirmation and CMS signing after observed identity/machine authorization"
    - from: scripts/verify-phase1-security.ps1
      to: scripts/verify-phase1.ps1
      via: "Canonical FinalGate consumes signed-off validation and reports the identical manifest/policy identities"
    - from: independent D-48 verifier
      to: tests/windows/results/phase1-evidence.json
      via: "A distinct authenticated identity signs the final matrix digest only after reviewing all current and superseded evidence, deviations, provenance, integrity, and trust identities"
---

<objective>
Complete the trusted-reviewer ceremony on D-22's LAB-DC01 station, reconcile the hardened security boundary, and require a fresh separated D-48 phase-exit review before completion.

Purpose: Turn Plan 01-35's fail-closed implementation into authenticated, auditable closure evidence without erasing prior history.
Output: A signed archival-envelope migration for all closure records, passing canonical gates, append-only CR-05 through CR-07 resolutions, and a fresh independent-review signature over the final evidence digest.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-34-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-35-SUMMARY.md
@scripts/add-security-closure-review.ps1
@scripts/verify-phase1-security.ps1
@scripts/evidence/Phase1.Security.Tests.ps1
@scripts/verify-phase1.ps1
@evidence/phase1/security-closure.yaml
</context>

## Artifacts this phase produces

- `evidence/phase1/security-closure.yaml`: append-only current archival envelopes signed from the observed authorized LAB-DC01 context governed by D-22.
- `01-SECURITY.md`: a dated security-gate entry with exact regression, manifest, policy, signer, and FinalGate identities.
- `01-REVIEW.md`: append-only evidence-backed resolutions for CR-05, CR-06, and CR-07.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Perform the observed-context archival-envelope ceremony on LAB-DC01</name>
  <files>evidence/phase1/security-closure.yaml</files>
  <read_first>scripts/add-security-closure-review.ps1; scripts/verify-phase1-security.ps1; scripts/evidence/Phase1.Security.Tests.ps1; evidence/phase1/security-closure.yaml; evidence-private/phase1/reviewer-policy.json; .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md</read_first>
  <precondition>Plan 01-35 focused Publication and Verifier suites pass; on LAB-DC01 the interactive identity matches the sole reviewer in the approved policy, COMPUTERNAME is LAB-DC01, and the approved CurrentUser signing certificate has its private key.</precondition>
  <action>
    First run the full security suite on hungdinh-lt with the declared public trust paths and require only the expected `historical_signature_envelope_missing` signed-off result before ceremony; any other failure returns to Plan 01-35. Copy the reviewed publisher script, closure manifest, and public reviewer policy to the approved LAB-DC01 working directory without copying external raw evidence or any private key. In the interactive policy-authorized reviewer session, derive the signer thumbprint from the approved CurrentUser certificate store, fail if zero or multiple eligible certificates match policy, and run `scripts/add-security-closure-review.ps1` once for the exact 19 allowlisted threat IDs. The script must observe the policy-authorized identity and LAB-DC01 itself, display each complete history commitment, require exact `YES`, and publish all archival envelopes in one mutex-protected atomic replacement. D-22 is the governing station decision; D-23 reserves LAB-DC02 for secondary-AD corroboration and forbids treating it as the provisioning/signing workflow host. Do not pass asserted identity/machine overrides, do not export the reviewer private key, do not import persistent trust on hungdinh-lt, and do not delete or rewrite any historical attestation. Return the updated public-only closure manifest to hungdinh-lt, then compare record counts, attestation counts, and all pre-ceremony attestation bytes to prove only the additive envelope fields changed, per D-20, D-22, D-23, D-36, D-37, D-41, D-44, D-46, and D-50. This signer does not perform the D-48 phase-exit review in Task 3. Immediately before committing Task 1, stage only `evidence/phase1/security-closure.yaml`, run GitNexus `detect_changes({scope: "staged"})`, and require its report to contain no changed file outside that declared file and no affected execution flow outside archival-envelope evidence publication/verification; investigate and remove unexpected scope before committing.
  </action>
  <reversibility rating="costly">The ceremony adds authenticated audit history; rollback must preserve the signed attempt and publish a superseding envelope rather than deleting it.</reversibility>
  <acceptance_criteria>
    - Every current closure record contains one new envelope signed by the policy-approved certificate from the observed authorized identity on LAB-DC01.
    - All pre-existing records, attestations, detached signatures, and predecessor links are unchanged.
    - No private key, token, protected content, or unnecessary personal data leaves LAB-DC01 or enters committed evidence.
  </acceptance_criteria>
  <instructions>
    The executor prepares the reviewed script, copied manifest, public policy, exact 19-ID command, and pre-ceremony byte inventory. The human signs in interactively to LAB-DC01 as the policy-authorized signer, confirms the certificate prompt identifies the approved CurrentUser certificate, and enters exact `YES` for each displayed archival commitment. No identity override, private-key export, or manual manifest edit is permitted.
  </instructions>
  <verification>The executor retrieves the public-only result, compares every pre-existing attestation byte against the inventory, and runs signed-off verification with the external public trust inputs.</verification>
  <resume-signal>Type `ceremony complete` after all 19 exact-YES affirmations finish without error.</resume-signal>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH -RequireSignedOff -DiagnosticFormat Json</automated>
    <human-check>Before the Task 1 commit, GitNexus `detect_changes({scope: "staged"})` reports only `evidence/phase1/security-closure.yaml` and only archival-envelope evidence publication/verification flow impact.</human-check>
  </verify>
  <done>All 19 historical-signature commitments are authenticated by the observed authorized reviewer context and signed-off verification returns valid.</done>
</task>

<task type="auto">
  <name>Task 2: Run canonical gates and append CR-05 through CR-07 closure evidence</name>
  <files>.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md</files>
  <read_first>scripts/evidence/Phase1.Security.Tests.ps1; scripts/verify-phase1-security.ps1; scripts/verify-phase1.ps1; evidence/phase1/security-closure.yaml; .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-35-SUMMARY.md</read_first>
  <action>
    On hungdinh-lt, run the complete Phase 1 security suite, direct signed-off verifier, and canonical `scripts/verify-phase1.ps1` FinalGate with `CallerMachine=hungdinh-lt`, `ServerMachine=LAB-DC01`, `SecondaryDcMachine=LAB-DC02`, `EndpointMachine=LAB-CLIENT01`, and the same literal public trust paths. Require explicit passing cases for wrong user, wrong machine, superseded CMS byte flip, history removal/reordering, junction escape, symbolic-link escape, and path-swap/handle stability. Confirm the direct verifier and FinalGate report identical current manifest and reviewer-policy identities. Append a dated section to `01-SECURITY.md` containing the exact commands, regression names/counts, signer thumbprint, observed reviewer identity/machine, procedure version, manifest digest, policy identity, and gate results. Append CR-05 through CR-07 resolutions to `01-REVIEW.md` with the same identities and remediation commits. Preserve the Aug 25 failed interval and every earlier failed/superseded entry; if any gate or identity comparison fails, keep the findings open and record the failure instead of claiming completion, per D-37 through D-50. Run GitNexus `detect_changes()` before committing and confirm only the ceremony manifest plus append-only audit documents are changed.
  </action>
  <reversibility rating="reversible">Audit reconciliation is append-only; a later correction can supersede this entry without destroying history.</reversibility>
  <acceptance_criteria>
    - The full security suite and canonical FinalGate pass after the ceremony.
    - Direct verifier and FinalGate manifest/policy identities are identical.
    - `01-SECURITY.md` and `01-REVIEW.md` preserve prior history and cite exact evidence for all three resolved gaps.
    - GitNexus change detection reports only the expected manifest and audit-document scope before commit.
  </acceptance_criteria>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH -RequireSignedOff; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH</automated></verify>
  <done>CR-05 through CR-07 are append-only resolved and Phase 1 canonical gates pass against the authenticated current evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Obtain fresh separated D-48 review of the final Phase 1 evidence</name>
  <files>evidence/phase1/requirement-matrix.yaml; tests/windows/results/phase1-evidence.json; tests/windows/results/phase1-evidence.sha256; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md</files>
  <read_first>evidence/phase1/requirement-matrix.yaml; evidence/phase1/security-closure.yaml; tests/windows/results/phase1-evidence.json; tests/windows/results/phase1-evidence.sha256; .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md; .planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md; scripts/verify-phase1.ps1</read_first>
  <precondition>Tasks 1 and 2 are final; the security suite, direct signed-off verifier, and canonical FinalGate pass with identical current manifest and reviewer-policy identities, and an authenticated verifier whose identity differs from Task 1's archival-envelope signer is available.</precondition>
  <action>
    Freeze and hash the post-ceremony requirement matrix, sanitized evidence bundle, security closure manifest, reviewer policy, `01-SECURITY.md`, and `01-REVIEW.md`; any subsequent change invalidates this review under D-40 and requires rerunning Task 2 plus this checkpoint. Present the complete current requirement/success-criterion matrix, all current and superseded attempts, provenance and environment fingerprints, procedure versions, deviations and D-49 dispositions, raw-artifact references and hashes, retention/hold state, artifact-access results, CR-05 through CR-07 remediation, and the exact current manifest/policy identities to an authenticated independent verifier. Before review, mechanically prove the verifier identity is not the Task 1 archival-envelope signer identity or certificate subject/thumbprint; equality blocks completion. The verifier must inspect artifact integrity and accessibility, decide whether every deviation is immaterial or requires rerun, and sign a new D-48 record bound to the exact final matrix/evidence digest, manifest digest, policy identity, UTC time, and verifier identity. Append the record without overwriting the earlier phase-exit review or any failed/superseded evidence; update the requirement-matrix review index and evidence digest atomically. Only after the signed record is present rerun canonical FinalGate and require it to validate the fresh independent review against the same frozen identities, per D-37 through D-50. Immediately before committing Task 3, stage only `evidence/phase1/requirement-matrix.yaml`, `tests/windows/results/phase1-evidence.json`, `tests/windows/results/phase1-evidence.sha256`, and `.planning/phases/01-first-encrypted-drive-vertical-slice/01-REVIEW.md`; run GitNexus `detect_changes({scope: "staged"})` and require its report to contain no changed file outside those four declared files and no affected execution flow outside requirement-matrix review indexing, independent-review evidence publication/validation, evidence-digest sealing, and append-only review auditing. Investigate and remove unexpected scope before committing.
  </action>
  <reversibility rating="costly">The signed phase-exit record is append-only audit history; corrections must supersede it and repeat review rather than remove it.</reversibility>
  <acceptance_criteria>
    - The D-48 verifier is authenticated and demonstrably distinct from the Task 1 archival-envelope signer.
    - The review covers the complete final requirement matrix, provenance, current and superseded attempts, deviations, artifact integrity/accessibility, retention/holds, and current manifest/policy identities.
    - A new signed D-48 record binds the exact post-ceremony matrix/evidence digest and UTC time; prior review and failure history remain intact.
    - Any post-review change or material deviation makes FinalGate non-passing until gates and independent review are repeated.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1.ps1 -CallerMachine hungdinh-lt -ServerMachine LAB-DC01 -SecondaryDcMachine LAB-DC02 -EndpointMachine LAB-CLIENT01 -TrustedRootPath $env:PHASE1_TRUSTED_ROOT_PATH -ReviewerPolicyPath $env:PHASE1_REVIEWER_POLICY_PATH</automated>
    <human-check>The authenticated D-48 verifier confirms they did not sign the Task 1 archival envelopes, reviews the complete frozen evidence set, resolves deviations under D-49, and signs the exact final digest; the executor verifies the signature and identity separation before accepting Phase 1. Before the Task 3 commit, GitNexus `detect_changes({scope: "staged"})` reports only the four declared Task 3 files and only requirement-matrix review indexing, independent-review evidence publication/validation, evidence-digest sealing, and append-only review-audit flow impact.</human-check>
  </verify>
  <what-built>Tasks 1 and 2 produced the final archival envelopes, passing security and canonical gates, append-only closure records, and a frozen post-change requirement/evidence digest awaiting independent phase-exit review.</what-built>
  <how-to-verify>Authenticate as the designated D-48 verifier, confirm the verifier identity and certificate differ from the Task 1 signer, review the complete frozen evidence set named in the action, sign the exact digest, and confirm the executor's canonical FinalGate accepts the new record without altering prior history.</how-to-verify>
  <resume-signal>Type `independent review complete` only after the distinct verifier's signed D-48 record is appended and canonical FinalGate passes.</resume-signal>
  <done>A fresh, separated, digest-bound D-48 independent review is appended and canonical FinalGate accepts it; only then may Phase 1 be declared complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LAB-DC01 interactive reviewer -> checked-in closure manifest | Only the observed policy-authorized context on D-22's trusted station may create the archival envelopes. |
| Public ceremony artifact -> hungdinh-lt verifier | Returned evidence must contain signatures and public metadata only. |
| Security subgate -> canonical FinalGate -> audit status | Completion must bind to identical current manifest and reviewer-policy identities. |
| Archival-envelope signer -> D-48 phase-exit verifier | Separate authenticated identities must sign the ceremony and independently review the frozen final evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-36-01 | Spoofing | reviewer ceremony | high | mitigate | Publisher enforces the policy-authorized reviewer and observed LAB-DC01 station before affirmation or signing. |
| T-01-36-02 | Tampering | ceremony round trip | high | mitigate | Compare all pre-existing attestation bytes and accept only additive signed envelopes. |
| T-01-36-03 | Information Disclosure | public evidence transfer | high | mitigate | Transfer only public policy/certificate/signature metadata and scan before commit. |
| T-01-36-04 | Repudiation | closure audit | high | mitigate | Append exact commands, identities, digests, signer provenance, results, and remediation commits. |
| T-01-36-05 | Elevation of Privilege | FinalGate status | high | mitigate | Require the signed-off subgate and identical trust identities before completion. |
| T-01-36-06 | Repudiation | D-48 phase-exit review | high | mitigate | Require an authenticated verifier distinct from the archival signer to sign the frozen final matrix/evidence digest after complete review. |
</threat_model>

<verification>
- All five new adversarial classes execute behaviorally and pass.
- Direct signed-off verification and canonical FinalGate pass with identical trust identities.
- Ceremony diff proves existing append-only history was preserved.
- A fresh D-48 signature from a verifier distinct from the ceremony signer binds the final post-gate matrix/evidence digest and passes FinalGate.
</verification>

<success_criteria>
- SEC-05, SEC-06, and SEC-07 are verified and auditable.
- CRY-04 is no longer blocked by a defeatable Phase 1 evidence contract.
- Phase 1 can be re-verified without changing any accepted functional-drive evidence.
- Phase 1 is not declared complete until the fresh post-change D-48 independent review passes with separation of duties.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-36-SUMMARY.md` when done.
</output>
