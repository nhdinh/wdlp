---
phase: 01-first-encrypted-drive-vertical-slice
plan: 43
type: execute
wave: 31
depends_on:
  - 01-42
files_modified:
  - scripts/evidence/Phase1.Security.Tests.ps1
autonomous: true
gap_closure: true
requirements:
  - TST-08
requirements_addressed:
  - TST-08
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The requirement-linked Phase 1 FinalGate regression passes against the final authenticated repository state on LAB-CLIENT01 per D-21, D-24, and D-25."
    - "The canonical FinalGate assertion requires exit 0, 34/34 overall checks (`Checks run: 34` and `Passed: 34`), zero failures and warnings, and the published requirement, success-criterion, decision, and privilege-manifest counters."
    - "A copied closure with a corrupted CMS signature still makes FinalGate fail closed with a stable security diagnostic; closing the post-ceremony expectation cannot weaken D-41 or D-48 evidence integrity."
  artifacts:
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Post-ceremony canonical FinalGate success assertions plus the preserved tampered-copy failure regression."
  key_links:
    - from: scripts/evidence/Phase1.Security.Tests.ps1
      to: scripts/verify-phase1.ps1
      via: "Invoke-FinalGate executes the canonical verifier for both the tampered repository copy and final authenticated repository."
      pattern: "Invoke-FinalGate.*verify-phase1\\.ps1"
    - from: scripts/verify-phase1.ps1
      to: evidence/phase1/independent-reviews/index.json
      via: "FinalGate validates the authenticated D-48 generation before reporting canonical counters per D-48."
      pattern: "independent.review"
---

<objective>
Close verification gap V-01: make the committed Phase 1 acceptance/security regression describe and prove the authenticated post-ceremony repository state without weakening the tampered-copy fail-closed path.

Purpose: TST-08 remains blocked solely because the focused FinalGate test still expects the pre-ceremony T-01-21-04 digest failure after Plan 01-42 published generation 000002. This plan aligns the regression with the immutable final state and proves it on the binding endpoint role required by D-21, D-24, and D-25.
Output: Updated `scripts/evidence/Phase1.Security.Tests.ps1` with passing final-state assertions and captured passing focused/full security regression runs.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-UAT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-42-SUMMARY.md
@scripts/evidence/Phase1.Security.Tests.ps1
@scripts/verify-phase1.ps1
</context>

<gap_mapping>

| Gap ID | Source | Failed truth | Closure task |
|---|---|---|---|
| V-01 | `01-VERIFICATION.md` frontmatter `gaps[0]`, Required Artifacts, Test Quality Audit | Phase 1 acceptance/security regressions pass against the final authenticated repository state | Tasks 1-2 |

`01-UAT.md` is complete with 5/5 passing tests and contributes no unresolved gap. Its stale prose under `## Gaps` describes tests 4 and 5 as ready for attestation even though their result fields are already `pass`; it does not authorize implementation work in this gap-closure plan.
</gap_mapping>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace the stale pre-ceremony FinalGate expectation with final-state assertions</name>
  <files>scripts/evidence/Phase1.Security.Tests.ps1</files>
  <behavior>
    - Test V-01A: The unmodified authenticated repository FinalGate exits 0 on LAB-CLIENT01.
    - Test V-01B: FinalGate output reports 34 checks run and 34 passed, zero failed checks, zero warnings, 30/30 requirements, 7/7 success criteria, 50/50 decisions, and 9/9 privilege manifests.
    - Test V-01C: The copied repository whose latest closure CMS byte is changed still exits nonzero and reports a signature or digest diagnostic.
  </behavior>
  <action>Before editing, identify whether the targeted FinalGate test block is represented by a GitNexus function, class, or method symbol. For every represented symbol that will change, call GitNexus upstream `impact` on the exact symbol and record/report its direct callers, affected execution processes, and risk level. If any result is HIGH or CRITICAL, warn the user and stop for explicit confirmation before changing bytes, as required by `AGENTS.md`; an absent PowerShell symbol may be recorded as unindexed/UNKNOWN but does not waive checking any symbol that GitNexus does represent. Then run the existing `-Focus FinalGate` case on LAB-CLIENT01 and retain its red failure as the TDD starting point for V-01. In the `FinalGate` focus block, preserve the existing copied-repository mutation and its nonzero/security-diagnostic assertions. Replace only the canonical repository's stale pre-ceremony expectations: require `$successfulGate.Exit -eq 0`; assert `Checks run : 34` and `Passed : 34` together as the canonical overall 34/34 result; require `Failed : 0` and `Warnings : 0`; and assert the canonical report fields for `Requirements pass : 30/30`, `Success criteria : 7/7`, `Decisions : 50/50`, and `Privilege manifests: 9/9`. Match every field by its actual label with flexible whitespace around the colon and values so cosmetic alignment changes do not invalidate the behavior check. Remove the obsolete expectation for the T-01-21-04 digest mismatch from this successful path because Plan 01-42's additive authenticated closure and generation 000002 supersede that observed pre-ceremony state per D-41 and D-48. Do not modify signed closure, policy, independent-review, or generation artifacts.</action>
  <verify>
    <automated>On LAB-CLIENT01 with `PHASE1_TRUSTED_ROOT_PATH` and `PHASE1_REVIEWER_POLICY_PATH` set to the approved external trust inputs: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus FinalGate` exits 0 after its canonical branch asserts `Checks run` is 34 and `Passed` is 34 using whitespace-robust label matching, alongside all other declared counters.</automated>
  </verify>
  <done>GitNexus upstream impact evidence is recorded for every represented symbol edited, including direct callers, affected processes, and risk, with no unconfirmed HIGH/CRITICAL result; V-01's focused regression is green against the final authenticated repository, explicitly proves the overall 34/34 checks result plus every other canonical counter requested by verification, and continues to reject the tampered copy while preserving D-41 failure evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the corrected expectation through the complete Phase 1 security suite</name>
  <precondition>The command runs on LAB-CLIENT01, and the approved external trust paths named by PHASE1_TRUSTED_ROOT_PATH and PHASE1_REVIEWER_POLICY_PATH exist and are readable without displaying their contents.</precondition>
  <files>scripts/evidence/Phase1.Security.Tests.ps1</files>
  <action>Run the focused FinalGate regression again from a clean PowerShell process, then run the complete `All` focus so the post-ceremony expectation is exercised alongside publication, verifier, historical signer, containment, and cleanup regressions. Confirm the successful canonical run is not being skipped due to missing private trust inputs and that the tampered-copy branch executes before the successful branch. Record machine role, UTC, commit/build identity, commands, exit codes, and sanitized output references in the plan summary in accordance with D-24, D-37, D-41, and D-46. Before any commit, call GitNexus `detect_changes({scope: "compare", base_ref: "main"})`; require its report to contain only the expected `scripts/evidence/Phase1.Security.Tests.ps1` FinalGate regression scope and expected FinalGate/security-test execution flows, and investigate or stop on any unrelated affected symbol or process. Also run the normal Git diff scope checks and require the implementation diff to contain only `scripts/evidence/Phase1.Security.Tests.ps1`; staged or unstaged unrelated user-owned work must remain untouched and excluded from this plan's commit. Do not publish a replacement D-48 generation: this task changes only a test expectation and validates the already authenticated final artifacts.</action>
  <verify>
    <automated>On LAB-CLIENT01 with approved trust inputs: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus FinalGate; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1 -Focus All` exits 0; before commit, `git diff --check -- scripts/evidence/Phase1.Security.Tests.ps1` passes, `git diff --name-only -- scripts/evidence/Phase1.Security.Tests.ps1` names only that file, and the recorded GitNexus `detect_changes({scope: "compare", base_ref: "main"})` result reports only its expected FinalGate regression symbols/processes.</automated>
  </verify>
  <done>Both focused and complete security regressions exit 0 on the binding endpoint; their sanitized record identifies LAB-CLIENT01; normal Git diff scope is limited to `scripts/evidence/Phase1.Security.Tests.ps1`; pre-commit GitNexus compare-to-main change detection reports only the expected FinalGate regression scope and flows; and no authenticated evidence, private trust artifact, unrelated symbol, or unrelated process changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test harness to canonical FinalGate | The regression launches the production acceptance verifier and interprets its process exit and report output. |
| Repository copy to authenticated evidence | A disposable tampered copy must remain outside the canonical evidence path and must not mutate authenticated repository bytes. |
| LAB-CLIENT01 process to external reviewer trust | Private policy/root inputs are machine-bound trust material and must be consumed without being copied into output or Git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-43-01 | Tampering | `Phase1.Security.Tests.ps1` canonical success assertion | high | mitigate | Require exit 0 plus all canonical counters; retain the independent mutated-copy failure assertion so a weakened verifier cannot satisfy V-01. |
| T-01-43-02 | Spoofing | LAB-CLIENT01 test execution | medium | mitigate | Record and check the binding machine role per D-21 and D-24; host-only execution cannot close the gap. |
| T-01-43-03 | Information Disclosure | External trust paths and test output | high | mitigate | Reference paths only, retain sanitization checks, and exclude private policy/root bytes from summaries and commits per D-46. |
| T-01-43-04 | Repudiation | Gap-closure test result | medium | mitigate | Record UTC, commit/build identity, target role, exact commands, exits, and sanitized artifact references per D-37 and D-41. |
| T-01-43-SC | Tampering | Package supply chain | low | accept | This plan installs no npm, pip, or cargo package and changes no dependency manifest. |
</threat_model>

<source_audit>

| Source | ID | Gap-scoped item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Complete Phase 1 acceptance/security evidence | 01-43 | COVERED | Closes the sole test-quality blocker; all 12 observable truths already verify. |
| REQ | TST-08 | Early representative Windows/WinFsp validation remains backed by passing final regressions | 01-43 | COVERED | Verification names TST-08 as the sole blocked requirement. |
| RESEARCH | — | Binding Windows verification must execute on its assigned machine role | 01-43 | COVERED | Task verification is explicitly LAB-CLIENT01-bound. |
| CONTEXT | D-21, D-24, D-25 | Endpoint target, role-bound evidence, layered gates | 01-43 | COVERED | Task 1 and Task 2 require the binding endpoint and focused/full gates. |
| CONTEXT | D-37, D-41, D-46, D-48 | Provenance, preserved failures, sanitization, independent review | 01-43 | COVERED | Summary evidence is sanitized; tampered failure remains; authenticated D-48 artifacts are unchanged. |
| UAT | — | No unresolved UAT failures | — | EXCLUDED | `status: complete`, 5 passed, 0 issues/pending/blocked. |
</source_audit>

<verification>
1. Validate PLAN structure and gap-closure frontmatter with the GSD plan validators.
2. On LAB-CLIENT01, run `Phase1.Security.Tests.ps1 -Focus FinalGate` and require exit 0.
3. On LAB-CLIENT01, run `Phase1.Security.Tests.ps1 -Focus All` and require exit 0.
4. Confirm the diff is limited to `scripts/evidence/Phase1.Security.Tests.ps1` and contains no changes under `evidence/phase1`, `evidence-private/phase1`, policy files, or dependency manifests.
</verification>

<success_criteria>
- Verification gap V-01 is directly covered by Tasks 1-2.
- The focused FinalGate regression requires the post-ceremony canonical success state and all published counters.
- The tampered-copy FinalGate regression still fails closed.
- Focused and complete security suites exit 0 on LAB-CLIENT01 with approved external trust inputs.
- TST-08 has no remaining test-quality blocker.
</success_criteria>

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-43-SUMMARY.md` when done.
</output>
