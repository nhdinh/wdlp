---
phase: 01-first-encrypted-drive-vertical-slice
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-04]
files_modified:
  - crates/dlp-server/src/lib.rs
  - crates/dlp-server/src/main.rs
  - crates/dlp-agent-core/src/lib.rs
  - crates/dlpctl/src/main.rs
  - tests/e2e/walking_skeleton.rs
autonomous: true
requirements: [SRV-01, SRV-03, SRV-11, CRY-01, CRY-02, AGT-04, AGT-05, AGT-06, DRV-04, TST-02, TST-03]
user_setup:
  - service: phase1-postgres
    why: "The portable walking-skeleton tracer crosses a real PostgreSQL migration and repository."
    env_vars:
      - name: DATABASE_URL
        source: "A reachable Phase 1 PostgreSQL database; 01-07 later supplies the project Docker Compose deployment."
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One `dlpctl phase1-smoke` command crosses a real PostgreSQL write/read, final `/api/v1` contracts, Ed25519 signed bytes, agent verification/current-LKG activation, approved encrypted store write/flush, and authenticated readback."
    - "The backing directory contains neither source bytes nor plaintext sensitive metadata, and the tracer exits nonzero if its non-vacuous marker scan fails."
    - "Concurrent configuration deliveries serialize activation so only the highest valid version becomes current and the prior current becomes LKG."
    - "Invalid/unsigned/tampered/truncated/wrong-key/schema/replayed bundle bytes never alter current/LKG, and encrypted-record authentication failure returns no plaintext."
    - "This is the production-quality architectural tracer consumed by later enrollment/Windows plans; fixture authority is dependency-injected only for the unimplemented external AD/PKI edges."
  artifacts:
    - path: "crates/dlp-server/src/lib.rs"
      provides: "Final router/repository/signer composition for the tracer"
      exports: ["build_app", "ServerState", "DirectoryVerifier", "DeviceCertificateIssuer"]
    - path: "crates/dlp-agent-core/src/lib.rs"
      provides: "Verified configuration activation and current/LKG state machine"
      exports: ["ConfigurationActivator", "ActiveConfigurationSet"]
    - path: "crates/dlpctl/src/main.rs"
      provides: "`phase1-smoke` end-to-end entry point"
    - path: "tests/e2e/walking_skeleton.rs"
      provides: "PostgreSQL/API/signature/activation/encrypted-store tracer and negative cases"
  key_links:
    - from: "crates/dlpctl/src/main.rs"
      to: "crates/dlp-server/src/lib.rs"
      via: "phase1-smoke calls the bound versioned HTTP router backed by a real PgPool"
      pattern: "phase1-smoke|/api/v1/"
    - from: "crates/dlp-agent-core/src/lib.rs"
      to: "crates/dlp-crypto/src/lib.rs"
      via: "strict signed-byte verification occurs before atomic activation"
      pattern: "verify_strict|stage_verify_activate"
    - from: "tests/e2e/walking_skeleton.rs"
      to: "crates/dlp-storage/src/store.rs"
      via: "tracer writes/flushes/reads through the approved EncryptedStore"
      pattern: "EncryptedStore|flush"
  flagged_assumptions:
    - { id: "PROBE-AGT-05", status: unresolved, statement: "The deterministic edge probe could not classify AGT-05; two successful activations plus restart must expose current and LKG." }
    - { id: "PROBE-AGT-06", status: unresolved, statement: "The deterministic edge probe could not classify AGT-06; every invalid-bundle class must prove active pointers unchanged." }
  prohibitions:
    - id: PROH-01
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT label the tracer or storage path encrypted when file content, sensitive metadata, signing material, or endpoint secrets are persisted in directly readable form."
---

<objective>
Deliver the genuine production-quality server-to-signed-configuration-to-encrypted-store tracer after workspace, package, and persisted-format prerequisites are resolved.

Purpose: Prove the complete portable trust/data architecture early enough to expose integration mistakes before AD, DPAPI, WinFsp, and session expansion.
Output: One runnable `phase1-smoke` path plus negative, replay, concurrency, durability, and plaintext-leak assertions.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md
@.planning/docs/adrs/ADR-002-api-transport.md
@.planning/docs/adrs/ADR-003-database-migrations.md
@.planning/docs/adrs/ADR-005-policy-signing.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-02-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-04-SUMMARY.md

<interfaces>
Use the final versioned DTO/signature contracts from 01-01, server/agent composition and real migration-before-bind contract from 01-02, and exact `dlp-store/aes256gcm-4m/v1` `EncryptedStore` implementation from 01-04. Test-only `FixtureDirectoryVerifier` and CSR-based `FixtureDeviceCertificateIssuer` implement the same production traits and cannot be selected by release startup; the project Compose deployment is completed after the authenticated server surface in 01-07.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One real PostgreSQL/API/signed-activation/encrypted-file path</name>
  <precondition>01-03 approvals are recorded, `DATABASE_URL` is set, and the database accepts a read-only connection plus the 01-02 migration ledger.</precondition>
  <reversibility rating="costly">The versioned route/DTO composition becomes a downstream server/agent contract, but versioning and provider traits avoid a persisted-data migration.</reversibility>
  <files>crates/dlp-server/src/lib.rs, crates/dlp-server/src/main.rs, crates/dlp-agent-core/src/lib.rs, crates/dlpctl/src/main.rs, tests/e2e/walking_skeleton.rs</files>
  <read_first>
    - Every path in `<files>` plus the interfaces and migrations recorded by `01-02-SUMMARY.md` and `01-04-SUMMARY.md`
    - `.planning/docs/adrs/ADR-002-api-transport.md`, `ADR-003-database-migrations.md`, and `ADR-005-policy-signing.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-RESEARCH.md` canonical signing and generation-commit patterns
  </read_first>
  <behavior>
    - Test 1: authenticated admin calls create one allowlist/token row, injected directory/issuer providers authorize one enrollment, and the token is transactionally consumed.
    - Test 2: the enrolled client downloads canonical signed configuration, verifies signature/hash/schema/key/version, and atomically selects it as current.
    - Test 3: the active configuration drives one `ProtectedFileSystem` create/write/flush/close/open/read against the approved encrypted store; output hash equals input hash.
    - Test 4: PostgreSQL and store reopen retain authoritative rows, current/LKG pointers, and the committed authenticated file.
  </behavior>
  <action>Wire `phase1-smoke` through the final Axum router on an actual bound local listener and real PgPool/migration ledger. Admin bootstrap authentication, versioned enrollment/configuration endpoints, immutable signed bytes, and agent activation use the final production traits; only AD and certificate issuance are injected fixtures, and release startup rejects them. Model certificate issuance through a locally generated test key plus CSR so the tracer exercises the same no-server-private-key contract that 01-06 implements. After activation, obtain the configured store identity/key fixture, execute one create/write/flush/close/open/read through `ProtectedFileSystem`, restart repository/cache/store objects, and verify SQL rows, current/LKG, and content hash. Generate a unique source marker and recursively scan backing/cache/log output after proving the control scan finds it. Per D-11/D-12, flush/close completes only after the 01-04 durable commit.</action>
  <acceptance_criteria>
    - One command uses a bound `/api/v1` listener, real PgPool/ledger, exact signed bytes, current/LKG activation, and approved encrypted-store commit/readback.
    - The certificate fixture accepts a CSR and returns only certificate chain/identity metadata; release startup cannot select either fixture provider.
    - Reopened repository/cache/store objects retain authoritative rows, selected configurations, and the authenticated content hash.
    - The non-vacuous marker scan detects its control marker and finds none in backing, cache, log, or evidence output.
  </acceptance_criteria>
  <verify><automated>cargo test -p dlpctl --test walking_skeleton tracer_happy_path -- --nocapture; cargo run -p dlpctl -- phase1-smoke --database-url "$env:DATABASE_URL"</automated></verify>
  <done>The runnable tracer crosses real PostgreSQL/HTTP/signature/activation/encrypted persistence and returns authenticated bytes after restart with a clean non-vacuous plaintext scan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden tracer replay, concurrency, invalid-bundle, and record-integrity behavior</name>
  <files>crates/dlp-server/src/lib.rs, crates/dlp-agent-core/src/lib.rs, crates/dlpctl/src/main.rs, tests/e2e/walking_skeleton.rs</files>
  <read_first>
    - Every path in `<files>` after Task 1
    - `.planning/REQUIREMENTS.md` SRV-03, AGT-04 through AGT-06, TST-02, and TST-03
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md` D-12 through D-15
  </read_first>
  <behavior>
    - Test 1: racing single-use token requests yield one success; duplicate token/device state cannot issue two active tracer credentials.
    - Test 2: racing verified bundle deliveries choose the greatest allowed version deterministically and preserve the former current as LKG.
    - Test 3: invalid signature/key/hash/schema/audience/version/truncation/replay leaves current/LKG byte-for-byte unchanged.
    - Test 4: nonce duplication or AAD/tag/record substitution prevents publication/read and returns zero plaintext.
  </behavior>
  <action>Add the named concurrency and negative cases using synchronization barriers, fresh repositories/caches between restart cases, and stable redacted errors. Keep request, response, bundle, path, record, and test-output sizes bounded. The final tracer route contracts remain `/api/v1`; later server plans replace only external fixture providers and expand middleware/readiness, not the proven route→signer→activator→store path.</action>
  <acceptance_criteria>
    - Racing token use creates at most one accepted enrollment state and racing activation cannot select a lower completed version.
    - Every named invalid bundle preserves current/LKG byte-for-byte and returns a stable redacted failure.
    - Record nonce/AAD/tag corruption prevents publication or read and copies zero plaintext bytes.
    - All request, response, bundle, path, and record bounds have explicit negative fixtures.
  </acceptance_criteria>
  <verify><automated>cargo test -p dlpctl --test walking_skeleton -- --nocapture; cargo test -p dlp-agent-core; cargo test -p dlp-storage --test roundtrip; cargo clippy -p dlp-server -p dlp-agent-core -p dlpctl --all-targets -- -D warnings</automated></verify>
  <done>Tracer replay/races/tamper/integrity cases fail closed without unauthorized state, active-pointer change, or plaintext release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI/HTTP client → versioned server router → PostgreSQL | Untrusted JSON and bootstrap authentication cross into authoritative state. |
| Signed network bytes → agent activation cache | Tamper, truncation, replay, rollback, and concurrency target active configuration. |
| Filesystem port → encrypted backing directory | Untrusted names/content become persisted records and later plaintext. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08 | Spoofing/Tampering | bootstrap/enrollment tracer | high | mitigate | Authenticated admin bootstrap, expiring single-use token, transaction locking, final authority traits. |
| T-01-09 | Tampering | signed activation | high | mitigate | Strict signature/hash/schema/key/audience/version verification, immutable staging, atomic current/LKG, replay/concurrency tests. |
| T-01-10 | Tampering/Information disclosure | encrypted store | high | mitigate | Approved AEAD format, nonce/AAD checks, durable publication, authenticate before read, marker scan. |
| T-01-11 | Information disclosure | diagnostics/artifacts | high | mitigate | Stable codes/digests only and non-vacuous scans over backing/cache/log/test outputs. |
| T-01-12 | Denial of service | HTTP/file inputs | medium | mitigate | Bound bodies, records, paths, timeouts, and concurrency with typed no-partial-state errors. |

## ASVS L1 Control and Evidence Map

| ASVS L1 area | Applicable control | Implementation evidence | Executable evidence |
|---|---|---|---|
| V2 Authentication | Bootstrap mutation is authenticated and enrollment token is expiring/single-use | admin middleware and locked token repository | token race/expiry/reuse tests |
| V5 Validation | Versioned JSON, bundle, and record inputs are bounded/typed | Axum extractors, DTO versions, record parser | negative body/schema/record fixtures |
| V6 Cryptography | Strict Ed25519 and approved AEAD format gate all activation/read | signer/verifier and `EncryptedStore` | signature/replay/nonce/AAD tests |
| V8 Data Protection | Backing/cache/log artifacts contain no protected plaintext or secret | encryption + redaction + scan | non-vacuous marker/secret scan |
| V9 Communications | HTTP path and later TLS composition use the final versioned router | bound server and final route contracts | E2E HTTP tracer; production TLS completed in 01-07 |
| V12 Files and Resources | Flush/close publication and authenticated reopen preserve complete data | storage commit protocol | restart, corruption, and no-partial-state tests |

**Completion gate:** Any unresolved critical/high threat, failed ASVS evidence command, plaintext marker, or missing negative-boundary evidence blocks this plan's completion and summary.
</threat_model>

<verification>
Run the real PostgreSQL/HTTP tracer, all signed-bundle and encrypted-record negative/race/restart cases, workspace clippy, and non-vacuous backing/cache/log/test marker scans.
</verification>

<success_criteria>
- One command proves the real portable trust/data path end to end.
- Invalid/replayed/racing trust inputs cannot alter active state.
- Encrypted record corruption cannot release plaintext or publish partial state.
- Fixture authority is impossible in release startup.
- All ASVS L1 evidence passes and no critical/high threat remains unresolved.
</success_criteria>

## Artifact Inventory

| Disposition | File | Key symbols / purpose |
|---|---|---|
| Modified | `crates/dlp-server/src/lib.rs` | final tracer router, repositories, provider composition, signed configuration endpoint |
| Modified | `crates/dlp-server/src/main.rs` | real migration/listener startup for tracer |
| Modified | `crates/dlp-agent-core/src/lib.rs` | `ConfigurationActivator`, `ActiveConfigurationSet`, current/LKG persistence |
| Modified | `crates/dlpctl/src/main.rs` | `phase1-smoke` E2E command |
| New | `tests/e2e/walking_skeleton.rs` | happy-path, restart, race, invalid-bundle, integrity, and marker evidence |

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-05-SUMMARY.md` when done.
</output>
