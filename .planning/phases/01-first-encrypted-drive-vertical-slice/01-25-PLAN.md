---
phase: 01-first-encrypted-drive-vertical-slice
plan: 25
type: execute
wave: 13
depends_on: [01-21]
files_modified:
  - evidence/phase1/security-closure.yaml
  - scripts/verify-phase1-security.ps1
  - scripts/evidence/Phase1.Security.Tests.ps1
  - .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
autonomous: true
gap_closure: true
requirements: [WRK-01, WRK-02, WRK-03, WRK-04, SRV-01, SRV-03, SRV-11, SRV-12, CRY-01, CRY-02, CRY-04, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, DRV-01, DRV-02, DRV-03, DRV-04, DRV-06, DRV-07, DRV-09, TST-01, TST-02, TST-03, TST-05, TST-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every one of the 19 high-severity threats currently marked open in 01-SECURITY.md has a machine-checkable closure record tied to the implemented mitigation and immutable Phase 1 evidence, or remains open and fails the gate."
    - "The security closure verifier rejects missing threat IDs, stale or inaccessible evidence, hash mismatches, non-passing attempts, wrong-machine substitutes, secret-bearing artifacts, and unsupported accepted-risk declarations."
    - "Phase 01 security frontmatter reports status complete and threats_open 0 only after the closure verifier and the security audit both pass."
  artifacts:
    - path: evidence/phase1/security-closure.yaml
      provides: "Allowlisted closure records for all 19 blocking threat IDs"
    - path: scripts/verify-phase1-security.ps1
      provides: "Fail-closed validation of the threat-to-mitigation-to-evidence chain"
    - path: scripts/evidence/Phase1.Security.Tests.ps1
      provides: "Isolated Pester coverage for canonical and tampered closure manifests/registers"
    - path: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      provides: "Current signed-off threat register and audit trail"
  key_links:
    - from: scripts/verify-phase1-security.ps1
      to: evidence/phase1/security-closure.yaml
      via: "parses the allowlisted closure manifest and enforces the immutable closure-target set in both pre-sign-off and signed-off states"
    - from: evidence/phase1/security-closure.yaml
      to: evidence/phase1/requirement-matrix.yaml
      via: "references immutable attempt IDs and content hashes already sealed by the Phase 1 evidence pipeline"
    - from: .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
      to: scripts/verify-phase1-security.ps1
      via: "frontmatter and threat statuses may advance only after verifier success"
---

<objective>
Close the sole actionable Phase 01 verification gap: the governance security gate still reports 19 open high-severity threats despite completed implementation and sealed runtime evidence.

Purpose: Convert the existing mitigation and evidence record into a deterministic, auditable security sign-off without weakening D-20 through D-50 or treating portable substitutes as lab evidence.
Output: A fail-closed security closure manifest/verifier and an updated Phase 01 security audit with zero blocking threats only when proof is complete.
</objective>

<execution_context>
@C:/Users/nhdinh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-CONTEXT.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-21-SUMMARY.md
@.planning/phases/01-first-encrypted-drive-vertical-slice/01-24-SUMMARY.md
@evidence/phase1/requirement-matrix.yaml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one blocking threat through the security-closure chain</name>
  <files>evidence/phase1/security-closure.yaml, scripts/verify-phase1-security.ps1, scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md` threat register, accepted-risk log, audit trail, and sign-off
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-VERIFICATION.md` Gaps Summary and human-verification routing
    - `evidence/phase1/requirement-matrix.yaml` current immutable attempt references
    - `scripts/verify-phase1-evidence.ps1` evidence validation, staleness, machine-role, redaction, and digest gates
    - `scripts/evidence/Phase1.Evidence.psm1` schema and artifact-integrity helpers
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-15-SUMMARY.md` session identity mitigation implementation
  </read_first>
  <behavior>
    - Test 1: a closure record for T-01-15-01 passes only when it names the implemented mitigation, a current passing attempt, the required LAB-CLIENT01 role, and accessible raw artifacts whose hashes match.
    - Test 2: changing the threat ID, attempt status, machine role, procedure/build/environment fingerprint, artifact path, or artifact hash causes a nonzero verifier exit.
    - Test 3: a closure record containing a token, private key, protected plaintext, raw SID, or unnecessary sensitive path causes a nonzero verifier exit.
  </behavior>
  <action>Create the production-quality vertical tracer for T-01-15-01. Define a versioned YAML closure format with only allowlisted fields: an immutable `closure_targets` list, threat ID, disposition, mitigation assertion, implementation references, evidence attempt IDs, required machine roles, artifact references and SHA-256 values, review identity, UTC review time, and the sealed requirement-matrix digest. Seed `closure_targets` from the 19 blocking IDs recorded by the evidence-sealed Phase 1 security audit; never derive this stable set from later mutable open/closed status. Implement `scripts/verify-phase1-security.ps1` to parse the file and initially validate T-01-15-01 end to end. Create `scripts/evidence/Phase1.Security.Tests.ps1` using Pester and temporary copies of the manifest and SECURITY register so negative cases never modify canonical evidence. Reuse the existing Phase 1 evidence module and its artifact-access, hash, staleness, clock, deviation, wrong-machine, and redaction semantics rather than inventing a parallel trust model. The verifier must fail closed when evidence is skipped, blocked, superseded without a current successor, inaccessible, stale, hash-mismatched, or produced by a substitute machine. Do not change any threat status or security frontmatter in this task.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -ThreatId T-01-15-01</automated>
  </verify>
  <acceptance_criteria>T-01-15-01 has one valid, reproducible source-to-runtime proof chain and every tampered negative fixture is rejected before any security status can change.</acceptance_criteria>
  <done>The security-closure tracer validates one real blocking threat end to end using the same immutable evidence authority as FinalGate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover the complete 19-threat blocking set</name>
  <files>evidence/phase1/security-closure.yaml, scripts/verify-phase1-security.ps1, scripts/evidence/Phase1.Security.Tests.ps1</files>
  <read_first>
    - Every open high-severity row in `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-16-SUMMARY.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-17-SUMMARY.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-18-SUMMARY.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-20-SUMMARY.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-21-SUMMARY.md`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-24-SUMMARY.md`
    - `tests/windows/results/phase1-evidence.json` and `tests/windows/results/phase1-evidence.sha256`
  </read_first>
  <behavior>
    - Test 1: the manifest contains exactly T-01-15-01/02/03/04/06, T-01-16-01/02/03/04/06, T-01-18-SC, T-01-20-01/02/05, and T-01-21-01/02/03/04/05 with no duplicate or unknown blocking ID.
    - Test 2: session/IPC/store-key closures require LAB-CLIENT01 runtime evidence; provenance/matrix closures require their declared hungdinh-lt, LAB-DC01, LAB-SERVER01, or LAB-CLIENT01 tier and cannot be satisfied by source tests alone.
    - Test 3: the dependency-graph closure proves Cargo.lock still matches the approved package set; privilege closures prove the exact digest-bound approvals, baseline, apply/verify/remove behavior, cleanup, and final state for their plan manifests.
    - Test 4: the manifest's immutable closure_targets and records contain exactly the same 19 IDs before and after register status transitions; in pre-sign-off mode every target has verified evidence, and in signed-off mode every target is closed with verified evidence.
    - Test 5: either mode fails if any target or record is absent, duplicated, or unknown, or if the SECURITY register contains an unmanifested open threat at or above the configured high threshold.
    - Test 6: temporary-fixture tampering of the target set, threat ID, attempt status, machine role, procedure/build/environment fingerprint, artifact path/hash/accessibility/staleness, review identity/time, matrix digest, accepted-risk disposition, or secret-bearing fields produces a nonzero verifier exit without changing canonical artifacts.
  </behavior>
  <action>Expand the tracer to the immutable 19-ID `closure_targets` set. For each row, cite the concrete implementation and test/evidence artifacts that fulfill its existing mitigation; do not rewrite the mitigation after the fact. Require runtime-tier proof for identity, IPC, DPAPI/store key, sign-out/restart, WinFsp corruption/disk-full, abrupt loss, and privilege mutations per D-21 through D-35. Require immutable publication, matrix digest, independent-review, retention, and artifact-integrity proof per D-37 through D-50. For T-01-18-SC, verify the locked dependency graph against the recorded approved allowlist and reject any unreviewed package. Extend the verifier with explicit pre-sign-off and `-RequireSignedOff` modes. Both modes require records to equal `closure_targets`, validate every referenced evidence ID through the existing matrix, reject any unmanifested blocking open, and emit stable per-threat results plus `19/19 closure records valid`; signed-off mode additionally requires every target row closed and the computed blocking-open count to be zero. Expand the Pester artifact to enumerate every negative behavior using fresh temporary manifest/register copies, assert nonzero exit for each mutation, and assert the canonical files' hashes are unchanged after the suite. A mitigation lacking current evidence remains a hard failure; do not convert it to accepted risk. Keep the two medium rows marked non-blocking outside the closure set while still checking that their severity/status classification is unchanged.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario FinalGate</automated>
  </verify>
  <acceptance_criteria>All and only the 19 currently blocking threats have current, role-correct, hash-valid, sanitized closure proof; deletion or corruption of any one record makes verification fail.</acceptance_criteria>
  <done>The complete security closure manifest passes with 19/19 blocking threat records and the existing FinalGate remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-audit and sign off Phase 01 security</name>
  <files>.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md</files>
  <read_first>
    - `evidence/phase1/security-closure.yaml` after Task 2 passes
    - Output from `scripts/verify-phase1-security.ps1`
    - `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md`
    - `C:/Users/nhdinh/.codex/gsd-core/references/security-asvs-levels.md`
  </read_first>
  <action>First run the closure verifier in pre-sign-off mode and retain its passing result. Run the Phase 01 security audit against the current implementation, immutable evidence matrix, and validated closure manifest. Change each immutable closure target from open to closed only when its corresponding verifier result passes; preserve its severity, disposition, mitigation text, and target membership. Leave the two medium non-blocking rows accurately classified unless the audit finds direct closure proof. Update the frontmatter timestamp, set `threats_open` to the computed count, append a new audit-trail row with total/closed/open/accepted counts and reviewer identity, and complete the sign-off checklist. Set `status: complete` only when the computed blocking count is zero. Do not add accepted risk merely to clear the gate. Then run the Pester tampering suite, the closure verifier in signed-off mode, and Phase 1 FinalGate so the document cannot claim completion after evidence drift and the same stable target set is proven on both sides of the status transition.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/evidence/Phase1.Security.Tests.ps1; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-security.ps1 -ClosurePath evidence/phase1/security-closure.yaml -SecurityPath .planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md -RequireSignedOff; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase1-evidence.ps1 -ExecutionMachine hungdinh-lt -Scenario FinalGate</automated>
  </verify>
  <acceptance_criteria>`01-SECURITY.md` reports `status: complete` and `threats_open: 0`, its register/audit totals are internally consistent, and both independent gates pass against unchanged evidence.</acceptance_criteria>
  <done>The sole governance blocker named by 01-VERIFICATION.md is closed without waiving or downgrading any high-severity threat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing implementation/evidence -> closure manifest | A textual claim must not replace machine-role-correct, immutable proof of the mitigation. |
| Controlled raw artifacts -> committed security record | Inaccessible, stale, altered, or sensitive artifacts must invalidate closure rather than be silently trusted. |
| Closure verifier -> SECURITY.md sign-off | Security status must not advance unless the exact current blocking set validates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-25-01 | Spoofing/Repudiation | threat closure provenance | high | mitigate | Bind each closure to immutable evidence IDs, target roles, build/environment/procedure fingerprints, artifact hashes, UTC, and reviewer identity. |
| T-01-25-02 | Tampering | closure manifest/security register | high | mitigate | Exact-set comparison, duplicate/unknown rejection, digest validation, and recomputed register/frontmatter counts. |
| T-01-25-03 | Information disclosure | committed closure artifacts | high | mitigate | Allowlisted fields plus existing forbidden-marker and secret scans before acceptance. |
| T-01-25-04 | Elevation | governance gate | high | mitigate | No status transition, severity downgrade, or accepted-risk conversion without passing proof and an auditable reviewer record. |
</threat_model>

<verification>
Run the Pester security-verifier suite first, then the focused verifier in pre-sign-off mode, perform the Phase 01 security audit, rerun the suite and verifier in signed-off mode, and finally run the existing Phase 1 FinalGate. The Pester suite must tamper temporary copies across every enumerated closure dimension and prove canonical manifest/register hashes remain unchanged.
</verification>

<success_criteria>
- The exact 19 blocking threat IDs each have a verified mitigation-to-evidence chain.
- Missing, stale, wrong-machine, inaccessible, hash-mismatched, deviated, or sensitive evidence prevents sign-off.
- `01-SECURITY.md` reaches `status: complete` and `threats_open: 0` only after all gates pass.
- Phase 1 FinalGate remains passing and no prior evidence attempt or audit history is overwritten.
</success_criteria>

## Artifacts this phase produces

- `evidence/phase1/security-closure.yaml` — new versioned closure manifest; no production code symbols.
- `scripts/verify-phase1-security.ps1` — new security closure verifier entry point; no Rust symbols.
- Updated `.planning/phases/01-first-encrypted-drive-vertical-slice/01-SECURITY.md` threat statuses, frontmatter, audit row, and sign-off.

<output>
Create `.planning/phases/01-first-encrypted-drive-vertical-slice/01-25-SUMMARY.md` when done.
</output>
